---
title: Code Review 2026-09-25 Agent Reports
---

# Code Review 2026-09-25: Agent Reports

The four agent reports behind [code-review-2026-09-25.md](./code-review-2026-09-25.md), preserved verbatim. Each agent wrote its report to disk before returning. The orchestrator re-probed every claim it graded on; where an orchestrator number differs, the main report says so. These files are the raw evidence and are not edited for style, so they may carry markers the main report avoids.



<!-- ===== agent: src-ts ===== -->

# Agent report: src-ts (Architecture + Code Quality, TypeScript/Astro half)

Worktree `code-review-2026-09-25` at ceaca4c0. Scope: `src/`, `workers/`, `scripts/`, `.claude/hooks`, `.claude/bin`, `tests/` as structure. Line counts are LOGICAL (ESLint `max-lines` with `skipBlankLines` + `skipComments`) unless marked raw. Scratch instruments live in the scratchpad dir as `src-ts-*.sh` / `src-ts-*.mjs`; every command below is runnable from the worktree root.

Instruments used throughout:

- `S1` = `bash scratchpad/src-ts-eslint-ignored.sh`: the repo's structural rules (max-lines 500, max-lines-per-function 75, complexity 15, max-depth 4, max-params 5) run with `--no-ignore` over `scripts/` and `.claude/hooks/`, the two trees `eslint.config.js` ignores.
- `S2` = `bash scratchpad/src-ts-nearceiling.sh`: the repo's full ESLint config over `src workers` with `max-lines` lowered to 449, so every file at 450+ logical lines prints its count.
- `S3` = `node scratchpad/src-ts-edges.mjs`: relative-import walk over 403 non-test `src/` files (1,083 edges), `.js` specifiers mapped to `.ts`, type-only imports classified separately; prints db/ outbound edges, stripe->db edges, the Operator split, top-level mutual deps, and file-level value-import cycles (DFS).
- `S4` = `node scratchpad/src-ts-dupes.mjs src` (and `workers scripts`): TypeScript-compiler AST census of top-level `function` declarations and `const x = () =>` across non-test files, bodies hashed after whitespace normalisation.
- `S5` = `bash scratchpad/src-ts-capture.sh`: `captureError` numerator/denominator over `git ls-files src` non-test `.ts/.tsx/.astro`.
- `S6` = `bash scratchpad/src-ts-types.sh`: `any`, `@ts-*`, `eslint-disable`, `JSON.parse`, JSON-then-`as` census over 671 non-test files in `src workers scripts`.
- `S7` = `bash scratchpad/src-ts-rawsql.sh` and `bash scratchpad/src-ts-sqlspread.sh`: `.prepare(` in route files and in `src/lib` outside `db/`, at 1da55373 and ceaca4c0.

Orchestrator baseline cited, not re-run: `npx tsc --noEmit` exit 0; `npm run lint` 0 problems; `npx vitest run` green. Re-run by me: `npm run deadcode`, `npx vitest run tests/api-error-vocabulary.test.ts` (7 passed).

---

### 1. Architecture

**Carried findings.**

- Near-500 cluster in `src/` (09-10 A3): **CHANGED, nearly closed.** S2 prints one non-test, non-exempt `src/` file at 450+: `src/pages/operator.astro` 498 (grew 482 -> 520 raw in #2788/#2791). The six files at 494-499 on 09-10 (`service-finalize.ts`, the settings `index.astro`, `stripe-subscription-handler.ts`, `db/quotes.ts`, `sow-template.tsx`, `types.ts`) are all below 450 except `types.ts`, now 504 under an explicit, reasoned `max-lines: off` block at `eslint.config.js:297-308` (1 exported function, 1 inline predicate; `grep -cE '^export (function|const ... = \()' types.ts` = 1). The raw sizes the orchestrator named (`quotes.ts` 678, `customer-yaml-editor.ts` 676) are under 450 logical. Decomposed by #2771 (`git log -S"max-warnings 0" -- package.json` -> 8eac02fb).
- `heartbeat.ts` as domain layer: **CLOSED.** `wc -l` 151; `grep -cE '^(export )?(async )?function '` = 0; zero `.prepare(`. Parsers moved to `src/lib/operator/heartbeat-parsers.ts` (424 raw), D1 to `src/lib/db/fleet-status.ts` (182 raw).
- `booking/reserve.ts` as domain layer: **CLOSED at the seam that mattered.** 289 raw lines, 8 functions, zero `.prepare(`; the multi-table commit is `src/lib/booking/commit.ts` (200) and calendar sync is `src/lib/booking/calendar-sync`. What stays is input validation and orchestration, which is route work.
- Dead admin OAuth callback + middleware exemption: **CLOSED.** `ls src/pages/api/oauth` -> no such directory; `grep -n oauth src/middleware.ts` -> no hits; `grep -rn api/oauth src tests` -> two comment mentions recording the deletion (`src/lib/oauth/providers.ts:8`, portal `callback.ts:11`).
- `src/lib/db/` upward edges (integrations->booking/encryption, quotes->sow/store, engagements->follow-ups/scheduler, milestones->stripe/client): **CLOSED.** S3 lists six outbound value edges from `db/`, all to leaves: `security/encryption.ts`, `pricing/card-fee.ts`, `entities/slug.ts`, `entities/recompute.ts` (whose only import is `../api/helpers`). None of the four named edges survive. Done in #2751 (db98f09e).
- Stripe->db fee-rule back-edge: **CLOSED.** The rule lives in `src/lib/pricing/card-fee.ts`, which has zero imports (`grep -n '^import' src/lib/pricing/card-fee.ts` empty); `db/invoices.ts:16`, `stripe/subscriptions.ts:42`, `portal/billing.ts:15` all import it downward.
- Operator split back-edge `entitlement-compiler.ts -> config-governance`: **CLOSED.** S3: `src/lib/portal/operator -> src/lib/operator` 31 edges (24 value), back-edges `src/lib/operator -> src/lib/portal` = 0. `entitlement-compiler.ts:54-56` now imports from `./vertical-floors`.
- Non-atomic price+rail write in `operator-price.ts`: **CLOSED.** Route line 53 calls `setOperatorPriceAndPaymentMethod` (`src/lib/db/services.ts:256-287`), one `UPDATE ... SET recurring_price = ?, payment_method = COALESCE(?, payment_method)`; route also now calls `captureError` (line 57).
- `fleet-alerts` missing from verify.yml dry-run: **CLOSED.** `.github/workflows/verify.yml:104` `cd workers/fleet-alerts && npm ci && npx wrangler deploy --dry-run`.
- Stale NOTE in `tests/operator-module-size.test.ts`: **CLOSED.** Lines 345-363 now branch on `/_load_sibling_module|_load_skill_helpers/` and only emit the "stages this file ALONE" NOTE for skills without a sibling loader.
- Hook registration invariant (09-10 A15/CQ15): **CLOSED.** `tests/hooks-registered.test.ts` asserts every `.claude/hooks/*.{mjs,sh}` is in `settings.json` or in a reasoned `EXEMPT` map (only `trap-recurrence.mjs`), prunes stale exemptions, and floors the scan at 10. My own loop (`grep -c <basename> .claude/settings.json` per file) agrees: 10 of 11 registered, the exempt one is the CLI.
- Raw SQL in route files: **CHANGED, 16 -> 15.** S7: `git grep -lE '\.prepare\(' 1da55373 -- src/pages | wc -l` = 16; at HEAD 15, and every one is `old` (no file added in the window carries SQL). The only page added in the window is `src/pages/case-studies/personal-injury-law-firm.astro` (`git diff --name-only --diff-filter=A 1da55373..ceaca4c0 -- src/pages`).

**New findings.**

1. **[MEDIUM, NEW] ESLint ignores `scripts/**` and `.claude/**` wholesale, and the window put its two largest new TS/JS modules there, both over the 500 ceiling.** `eslint.config.js:315` (`'**/.claude/**'`, added 2026-05-06 to skip worktrees under `.claude/worktrees`, per `git blame -L 315,323`) and `:323` (`'scripts/**'`). S1 over those trees: 23 violations of the repo's own error-tier ceilings, including `scripts/ci-reconcile-obligations.ts` **598 logical lines**, its `main` at **249 lines / complexity 50** (`:533`) and `probeEvidence` complexity 18 (`:360`); `.claude/hooks/lib/register.mjs` **627 logical lines**, `cmdAdd` 82 lines / complexity 16 (`:481`); `.claude/hooks/lib/board.mjs:83` `main` complexity 35; `.claude/hooks/trap-recurrence.mjs:168` complexity 26; `.claude/hooks/memory-audit.mjs:154` 112 lines. Both new modules are live: `.github/workflows/obligation-reconcile.yml` runs the reconciler on a schedule, and `.claude/bin/register` execs `register.mjs` (tests: `tests/obligation-reconcile.test.ts`, `tests/register-{cli,libs,deliver}.test.ts`). The `.claude/**` glob was meant for worktrees and also exempts every enforcement hook (`worktree-guard.mjs`, `engagement-guard.mjs`) from lint; `.mjs` under a dot-directory is also outside `tsc` (`tsconfig.json` include `**/*` skips dot-dirs). `register.mjs` is additionally misfiled: it is a Captain CLI library, not a hook, yet sits in `.claude/hooks/lib/`. Recommendation: narrow the ignore to `'**/.claude/worktrees/**'`, drop `'scripts/**'` in favour of a scripts block that relaxes only the type-aware rules, split `ci-reconcile-obligations.ts` `main` along its own four documented phases (import / probe / census / escalate), and move `register.mjs` beside `scripts/lib/register-view.mjs`.

2. **[MEDIUM, NEW] The obligation register has two write implementations of one table, one parameterised and one string-built.** `src/lib/db/obligations.ts:238-275` `upsertObligation` binds 22 `?` placeholders; `.claude/hooks/lib/register.mjs:381-413` rebuilds the same `INSERT ... ON CONFLICT(customer_slug, kind, stable_key) DO UPDATE SET` from 17 columns via a hand escaper `sqlLiteral` (`replaceAll("'", "''")`) and ships it to `wrangler d1 execute --command`; `:421`, `:808`, `:813` interpolate more values the same way. The column lists already differ (the CLI omits `evidence_class`, `evidence_surface`, `evidence_locator`, `recur_rule`, `recur_anchor`). The reconciler's own header (`scripts/ci-reconcile-obligations.ts:20-27`) names this exact shape as the defect it was rewritten to avoid, and `scripts/lib/wrangler-d1.ts` exists to give a CLI the real module. `grep -n "upsertObligation\|parity" tests/register-*.test.ts` finds no parity test; `tests/register-cli.test.ts:293` asserts only a `toContain` on the SQL string. The escaper is correct for SQLite string literals today, so this is a drift and injection-surface finding, not an exploit. Recommendation: have the CLI call `upsertObligation` through `scripts/lib/wrangler-d1.ts`, as the reconciler does, and delete `buildUpsertSql`/`sqlLiteral`.

3. **[MEDIUM, NEW] `src/lib/db/` is not the data layer: more files outside it write SQL than inside it, and the count rose in the window.** S7 (`src-ts-sqlspread.sh`): at 1da55373, 58 `src/lib` files outside `db/` held `.prepare(` (203 call sites) against 22 inside; at ceaca4c0, 61 outside (208 sites) against 25 inside. By directory (`git ls-files src/lib | grep -v '\.test\.' | grep -v '^src/lib/db/' | xargs grep -l '\.prepare(' | sed 's#/[^/]*$##' | sort | uniq -c`): `admin` 12, `portal/operator` 10, `portal` 8, `booking` 7, `auth` 7, `webhooks` 5, `sow` 4, `operator/mcp` 3, `operator` 3, `stripe` 1, `entities` 1. The 09-10 fix for the `db -> stripe` edge moved `milestones` logic to `src/lib/stripe/milestone-invoicing.ts`, carrying 4 `.prepare(` calls into the Stripe layer (`grep -c` = 4). The "db calls nothing above it" invariant now holds, but the boundary it protects is one of eleven places SQL lives. Recommendation: decide the rule in writing (either `db/` owns all SQL, or each domain owns its tables), then pin it with an import-restriction or a `.prepare(` allowlist test; the choice matters more than the direction.

4. **[LOW, NEW] One file-level import cycle, introduced in the window, and no rule that would stop the next.** S3: `sections-scope.ts -> sections-staff-send-as.ts -> sections-scope.ts` (value imports: `sections-scope.ts:9` imports `checkStaffSendAs`; `sections-staff-send-as.ts:3` imports `canonRosterAddress`). Introduced by b5c21a1d (#2858, 2026-09-22). `grep -n no-cycle eslint.config.js` is empty although `eslint-plugin-import-x` is already loaded. ES modules tolerate this because both uses are inside functions, so it is a hazard, not a bug. Recommendation: move `canonRosterAddress` to `customer-yaml/helpers.ts`; enable `import-x/no-cycle` at error.

5. **[LOW, NEW] `src/lib/admin` and `src/lib/portal` depend on each other by value.** S3 prints `src/lib/admin <-> src/lib/portal`. `admin/governance.ts:18-23` imports `getVerticalFloor`, `restrictiveness`, `isCeiling` from `portal/operator/config-governance`; `admin/people-view.ts:17` imports `isClientRole`; `portal/operator/pause-control.ts:26-30` imports `clearStopOnMachine` from `admin/sticky-stop-clear`. The governance accessors already have a neutral home in `src/lib/operator/vertical-floors.ts`, which `config-governance` re-exports. Recommendation: point `admin/governance.ts` at `operator/vertical-floors`, and move `sticky-stop-clear` under `src/lib/operator/` since both surfaces call it.

6. **[LOW, NEW] `workers/fleet-alerts/src/index.ts` is 485 logical lines and holds six concerns.** S2 line `485 workers/fleet-alerts/src/index.ts`. Its functions (`grep -nE '^(export )?(async )?function ' ...`): threshold readers, `parseConnectorEntry`/`parseConnectorsMap`, `evaluateConditions`/`connectorConditions`, D1 alert state (`getAlertState`, `markOpen`, `markResolved`), `sendTransitionEmail`, `pingAlerterHealthcheck`, and `runOnce`, while a sibling `conditions.ts` already exists. This is the pager; the next feature lands it over the ceiling. Recommendation: move `evaluateConditions`/`connectorConditions` into `conditions.ts` and the alert-state trio into its own module before the next addition.

7. **[POSITIVE, verified] Every `src/` boundary finding carried from 09-10 closed at the structure, not by trimming.** Instruments above: zero back-edges in the Operator split (S3), the four db upward edges gone (S3), heartbeat 0 functions and 0 SQL, reserve's commit in `src/lib/booking/commit.ts`, the dead callback and its middleware clause deleted, fee rule in a zero-import leaf, price and rail in one statement, fleet-alerts in the verify build, registration pinned by test. What would have shown this false: any of those greps returning the 09-10 line.

8. **[POSITIVE, verified] No new route file carries SQL or a business rule.** S7: all 15 SQL-bearing route files predate the window; the largest window change to one (`events.ts` +50/-11, `git diff 1da55373..ceaca4c0 -- src/pages/api/events.ts`) is a per-IP rate limit and a cookie-first session id, i.e. a security fix, plus the move to catalog error codes.

**Not checked:** `.astro` frontmatter logic outside `src/pages/api` beyond line counts; `src/scripts/` (browser bundles) layering; import edges into `src/pages` from `src/lib` (route-to-lib direction assumed); `workers/cost-*` internal structure; whether `scripts/lib/wrangler-d1.ts` is a faithful D1 shape.

**Suggested grade: B-.** Rubric B is "1-2 slightly large files or one unclear boundary"; in scope there are two non-exempt files over 500 (598, 627, both invisible to the gate) and one unclear boundary (SQL ownership), with the new cycle and admin/portal pair as lows. Not B: the two oversized modules exist because the gate does not look at their trees, and the SQL boundary is wider than the one the last wave fixed. Not C+: the C clause "mixed concerns in route handlers" is now substantially false (no new route SQL, both named domain routes extracted), and every carried `src/` boundary item closed.

---

### 3. Code Quality

**Carried findings.**

- Byte-identical duplication (`stripeHeaders`, `machineBaseUrl`, `escapeHtml`, `jsonError`, `redirectWithStatus`): **CHANGED, 4 of 5 closed.** S4 "carried names": `machineBaseUrl` 1 def (`src/lib/operator/machine-url.ts`), `escapeHtml` 1 (`src/lib/api/helpers.ts`), `jsonError` 0, `redirectWithStatus` 0 (renamed per destination, e.g. `redirectToSettings`), `trimString`/`isRecord`/`isValidEmail` 1 each. **Open:** `stripeHeaders` 3 identical bodies plus 3 `STRIPE_API_BASE` constants (`src/lib/stripe/checkout.ts:43,50`, `subscriptions.ts:45,62`, `client.ts:14,16`).
- `no-restricted-syntax` guard scope: **CHANGED, widened.** `eslint.config.js:176-212` (src) and `:217-265` (api) now forbid redeclaring `jsonResponse`, `escapeHtml`, `trimString`, `isRecord`, `isValidEmail`, `machineBaseUrl`, `jsonError`, plus hand-built `{ error: }` bodies in `jsonResponse` and `new Response(JSON.stringify(...))` in API routes. Still not covered: `stripeHeaders` and the new duplicate groups in finding 2 below.
- `captureError` reach (5 of 174): **CHANGED, 14 of 180; money path still dark.** S5: 180 non-test `src` files with `catch`; 14 call `captureError(` (13 of them catch-bearing); `git grep -l captureError 1da55373 -- src | grep -v '\.test\.' | wc -l` = 5. Webhook/money files with a `catch` and no `captureError`: 22, including `src/pages/api/webhooks/stripe.ts`, `src/lib/webhooks/stripe-handler.ts`, `stripe-subscription-handler.ts`, `stripe-subscription-lifecycle.ts`, `operator-checkout-handler.ts`, `src/lib/stripe/checkout.ts`, `milestone-invoicing.ts`, `src/lib/sow/service-finalize.ts`, `pages/api/webhooks/signwell.ts`. With `captureError`: `subscription-billing.ts`, `webhooks/resend.ts`, `operator-price.ts`. Middleware-level `withSentryRequestHandler` (`src/lib/observability/sentry.ts:32`) sees only uncaught throws; a handler that catches and returns 500 is invisible to it. 187 `console.error(` call sites.
- Two error vocabularies under `error`: **CHANGED, closed in routes, open in `src/lib`.** Routes: `grep -rhoE "errorResponse\(\s*[0-9]+,\s*'[^']*'" src/pages` yields 23 distinct first arguments, all snake_case codes, zero sentences; `tests/api-error-vocabulary.test.ts` 7 passed; lint forbids hand-built bodies in `src/pages/api/**`. `src/lib` is outside that rule and still hand-builds 8 bodies (`git ls-files src | grep -v '\.test\.' | xargs grep -nE "JSON\.stringify\(\s*\{\s*error\s*:"`): `src/lib/auth/admin-session.ts:66` returns `{ error: 'Unauthorized' }` (catalog code is `unauthorized`, `src/lib/api/errors.ts:43`) and is the 401 body for every route using `requireAdminSession` (`grep -rn requireAdminSession src/pages/api | wc -l` = 99); six webhook/SOW sites return `'INTERNAL_ERROR'` (catalog: `internal_error`, `errors.ts:72`).
- knip false "unused dependency" warnings: **OPEN.** `npm run deadcode` prints `@astrojs/cloudflare package.json:29:6` and `@astrojs/sitemap package.json:30:6`. `knip.jsonc:53` lists `astro.config.mjs!` as an entry but the `project` array (`:88-90`) omits it.
- Lint ceiling / twelve `JSON.parse` sites: **CLOSED.** `package.json:17` `"lint": "eslint . --max-warnings 0"`; orchestrator: 0 problems. `grep -nE 'JSON\.parse' src/lib/db/quotes.ts` is empty; parsing goes through `parseLineItems` (`src/lib/db/quote-content.ts:40-50`, `const parsed: unknown`, `Array.isArray`, `filter(isLineItem)`). Residual: the comment at `eslint.config.js:56-64` still says the cap is `--max-warnings 13` and that "13 is the number both see" (LOW, finding 6).
- Type discipline (`any`, ts-ignore): **holds.** S6: the only `: any`/`any[]` hits in 671 non-test files are 8 prose lines (each read); zero `@ts-ignore`/`@ts-expect-error`/`@ts-nocheck`; 4 `eslint-disable` directives in production code.

**New findings.**

1. **[MEDIUM, NEW] Two money-path modules bypass the validated line-item parser with a bare cast, and 19 sites cast parsed JSON straight to a type.** `src/lib/stripe/milestone-invoicing.ts:84` `JSON.parse(quote.line_items) as { estimated_hours: number }[]`, then `:88` `lineItems[milestoneIndex].estimated_hours * quote.rate` is an invoice amount; a row whose `estimated_hours` is absent yields `NaN` into a Stripe invoice. `src/lib/sow/service-finalize.ts:278-279` does the same for the SOW. Both carry the pattern from the 09-10 tree (`git show 1da55373:src/lib/db/milestones.ts | grep -n "JSON.parse(quote.line_items)"` -> line 301), so the move preserved it. S6 counts 19 `JSON.parse(...) as T` / `(await x.json()) as T` sites across non-test `src`/`scripts`; the lint cap reached zero partly because `as T` on an `any` does not trip `no-unsafe-assignment`. Several are post-HMAC signed payloads followed by field checks (`booking/signed-link.ts:125-140`), which is defensible; the money-path two and `pages/api/webhooks/sentry.ts:81` are not. The project standard (CLAUDE.md coding-standards row) is "Parse external inputs (never cast)". Recommendation: call `parseLineItems` in both money modules; add `@typescript-eslint/no-unsafe-type-assertion` (or a `no-restricted-syntax` selector on `TSAsExpression > CallExpression[callee.property.name='parse']`) at warn with a ratchet.

2. **[MEDIUM, NEW] Three modules each hand-roll the signed-token codec: `base64UrlEncode`, `base64UrlDecode`, `importSigningKey`.** S4: `base64UrlEncode` 4 defs (3 identical), `base64UrlDecode` 3 identical, `importSigningKey` 3 variants; files `src/lib/oauth/state.ts:149-181`, `src/lib/booking/signed-link.ts:150-176`, `src/lib/assessment/session.ts:220-252`, plus `booking/tokens.ts:58`. All predate the window (`git grep -nE "function (base64UrlEncode|importSigningKey)" 1da55373 -- src`) and no prior review names them. All three verify with `crypto.subtle.verify` (`state.ts:127`, `signed-link.ts:114`, `session.ts:144`), so there is no drift today; the hazard is that the next fix to one (a key-length check, a `kid`) lands in one of three. S4 over `src`: 48 names defined 2+ times, 21 with identical bodies, 28 redundant identical copies in total; the rest are small (`ok`/`serverError` x3 in `src/lib/webhooks/`, with `operator-checkout-handler.ts:60` importing from `stripe-subscription-shared.ts` yet redefining both at `:62,:69`; `redirectToSettings` x4 with 2 bodies; `formatCurrency` x2; `renderMarkdown` x2; `portalCallbackUrl` x2). `workers/` + `scripts/`: 2 identical groups, both inside separate worker packages that cannot share code. Recommendation: one `src/lib/security/signed-payload.ts` (codec + key import + sign/verify), `stripeHeaders` into `stripe/client.ts`, delete the two webhook-local `ok`/`serverError`, and add the codec names to the existing guard.

3. **[MEDIUM, carried-sharpened] The Stripe webhook and invoicing path reports caught failures only to `console.error`.** Numbers under carried findings (S5: 22 webhook/money files with `catch` and no `captureError`). The one route fixed in the window (`operator-price.ts:57`) is the model. Recommendation: adopt in the 22 listed files, starting with `webhooks/stripe.ts` and `stripe-handler.ts`, where a caught 500 is otherwise seen only as a Stripe retry.

4. **[LOW, NEW] `scripts/migrate-to-entities.ts` is dead and cannot run.** It reads `lead_signals` and `parking_lot` (`grep -oE "FROM (clients|lead_signals|parking_lot|assessments)\b"` -> one each), dropped by `migrations/0080_drop_standalone_lead_gen_tables.sql:37` and `migrations/0011_booking_tables.sql:117`. It backfills migration 0008; last touched 2026-07-14 (f79bd5f7). It is invisible to knip because `knip.jsonc:43` marks `scripts/*.{ts,mjs}!` as entries, and to lint because of `scripts/**`; S1 reports its `migrateToEntities` at 288 lines / complexity 53. Recommendation: delete it; the migration comment at `0008_create_entities_context.sql:8` can cite the commit.

5. **[LOW, NEW] The governance restrictiveness table is mirrored under a justification that no longer holds.** `src/lib/operator/customer-yaml/sections-persona-skills.ts:155-170` keeps `CEILING_RESTRICTIVENESS` and a private `restrictiveness`, "Local mirror of the frozen governance table (src/lib/operator/vertical-floors.ts) — the validator layer stays free of portal imports." `vertical-floors.ts:23-41` is not a portal module any more (it moved to `src/lib/operator/` in the window), so importing it costs nothing. Values are identical today. Recommendation: import `restrictiveness` from `../vertical-floors`.

6. **[LOW, NEW] Stale ratchet comment and three reasonless `eslint-disable` directives.** `eslint.config.js:56-64` documents `--max-warnings 13` and "13 is the number both see"; `package.json:17` is `--max-warnings 0`. S6: `src/env.d.ts:1`, `src/lib/marketing/attribution.ts:53`, `src/lib/portal/operator/rbac-audit.ts:162` disable a rule with no `-- reason`. Recommendation: rewrite the comment to state the zero cap; add `eslint-comments/require-description` or a one-line reason to each.

7. **[POSITIVE, verified] The error-body contract for routes is now enforced by three layers.** A catalog (`src/lib/api/errors.ts`), a membership test (`tests/api-error-vocabulary.test.ts`, 7 passed), and lint selectors that reject a hand-built `error` property in `jsonResponse` and `new Response(JSON.stringify(...))` in `src/pages/api/**` (`eslint.config.js:252-263`). All 23 literal codes in route calls are snake_case. What would show it false: any sentence-case first argument in the `errorResponse` grep above.

8. **[POSITIVE, verified] The lint cap reached zero by fixing boundaries, and the type floor held.** `--max-warnings 0` with 0 warnings (orchestrator); the 09-10 twelve `JSON.parse` sites in `quotes.ts` were replaced by a validated parser, not casts (`quote-content.ts:40-50`); zero real `any` and zero `@ts-*` suppressions (S6); zero empty `catch {}` bodies; the eight `.catch(() => null)` sites each convert the failure to an explicit result or error flag on the next line (read: `oauth/store.ts:104-112`, `portal/index.astro:44-46`).

**Not checked:** exception handling inside `.astro` components beyond grep; `workers/*` `captureError` equivalents (workers have no Sentry import to adopt); whether each of the 19 JSON casts is followed by a field check (read 3 of 19); duplication inside `.astro` frontmatter (S4 walks `.ts/.tsx` only); `src/scripts/` browser bundles beyond the census.

**Suggested grade: B-.** Rubric C is "inconsistent error handling OR notable DRY OR 3+ dead exports"; B is "1-2 minor". Error handling is consistent in shape and the route vocabulary is now enforced, but observability on the Stripe path is absent and `src/lib` still emits non-catalog codes, and the DRY census (28 identical copies, including a three-way security codec) is more than minor. Not B: the money-path cast and the unmonitored webhook catches are on the path that bills clients. Not C+: four of five carried duplicates, the lint cap, the parse sites and the route vocabulary all closed at the gate layer, and dead exports remain at zero per knip.



<!-- ===== agent: operator-py ===== -->

# Agent report: operator-py (Architecture + Code Quality, Python half of `operator/`)

Commit ceaca4c0. Worktree `/Users/scottdurgan/dev/ss-console/.claude/worktrees/code-review-2026-09-25`. Read-only. Python tooling from a scratch venv (`opy-venv`) with the CI pins: ruff 0.15.17, pyright 1.1.406, pytest, pyyaml, cryptography. All scripts named below are in the scratchpad as `operator-py-*.{sh,py}`; every number comes from one of them or from a command quoted inline.

Tree size: `git ls-files '*.py' | wc -l` = 543 under `operator/`; 324 non-test modules by the graph script's test filter (`operator-py-graph.py`), 326 by a looser grep filter.

## Measurement conventions

- **Logical lines** are the module-size ratchet's own counter (`tests/operator-module-size.test.ts:126 countLogicalLines`), reproduced exactly in `operator-py-tscount.py`. It matches every baselined value it was checked against. Raw `wc -l` is labelled "raw".
- **Complexity**: `ruff check --isolated --select C901 --config 'lint.mccabe.max-complexity=15'`, excluding tests (`operator-py-3.sh`). It finds 27 functions, the same 27 the function-size baseline holds.

---

### 1. Architecture

**Carried findings.**

| 09-10 item | Verdict | Instrument |
|---|---|---|
| A1 broker `handle()` 313 lines, McCabe 51, positional auth | **CLOSED** | `workspace_broker/server.py` is 322 raw / 180 logical lines. `handle` at `:278-282` is one line: `return dispatch(...)`. `workspace_broker/verbs.py` holds `VERBS: tuple[Verb, ...]`; every `Verb` declares an `auth` frozenset validated in `__post_init__`. `dispatch` resolves the peer class before calling any handler. The C901 run lists no `server.py` function. `workspace_broker/tests/test_verb_table.py` drives each verb with a wrong peer; it passed with the others: 179 passed. `tests/broker-verb-registry.test.ts` also passed. The change landed in #2746 (d62cfd9f). |
| A2 no root Python packaging; 33 `sys.path` shims, 130 E402 | **CHANGED, mostly closed** | `operator/pyproject.toml` exists, packages `adapter, workspace_broker, bin, rehearsal, safety_substrate`, and the substrate job runs `pip install -e ./operator` (`operator-substrate.yml:97`). Non-test `sys.path.insert/append`: **10** (`operator-py-1.sh`). Non-test `noqa: E402`: **4** (`git grep -n "noqa: *E402" -- '*.py' ':!**/tests/**' ':!**/test_*.py'`). Tree-wide E402 annotations: 46. `safety_substrate/` exists and `operator/safety-substrate` does not; the only surviving mentions are in archived `.stitch/audits/backlog-snapshot-*.json`. There are 49 `bin.lib` import lines, and one bare-name straggler (new finding 6). |
| A4 skill helper duplication, 15 bodies / 176 lines; sibling loader 2 of 6 | **CHANGED, mostly closed** | The shared bodies moved to `templates/skill_helpers.py`. It is vendored byte-identical into six skills and discovered by glob in `tests/test_skill_helpers_sync.py:38-39` with a floor of 6 (`:95`). `_load_skill_helpers` is present in all 6 (AST census). Residual: 17 AST-identical top-level def groups outside whole-file vendors, 407 redundant lines (`operator-py-dup.py`), and most of those are contract-pinned (details in new finding 8). |
| 09-04 probe F7: "16 vendored symbols vs expected 20" | **WRONG (the probe's threshold)** | `claims-2026-09-04.probe.sh:63` counts `"copies"` rows in `contracts/pre-run-shared-symbols.json` and expects at least 20. The contract's own `_comment` explains the drop. The library bodies left `pre_run.py` for `skill_helpers.py` on 09-11 and are gated there instead. `medical-chronology-maintainer/pre_run.py` was deleted on 09-17. A smaller contract here means less duplication, not less gating. `npx vitest run tests/operator-vendored-symbols.test.ts` passes. |
| A5 hand-tuple sync gates; `broker_writer.py` twins ungated (probe I5: 4-line diff) | **CLOSED** | `operator/tests/vendored_sync.py` provides `discover_copies` (glob `skills/*/<name>`) and `discover_stamps` (a function-body hash match against the template). `test_broker_writer_sync.py:35`, `test_case_alert_routing_sync.py:30`, `test_escalation_ledger_sync.py:58` and `test_pre_run_gate.py` all use them. `test_vendored_sync.py` proves the detectors on planted trees. The broker_writer twins are now md5-identical: they appear as a 2-copy group in `operator-py-dup.py`. |
| A9 `adapter/inbound_envelope.py`, `resolve_skill_pins.py` zero importers | **CLOSED** | Both deleted in #2772 (d8f00be3), with their fixtures and test. `git grep` finds only a retirement note in `docs/specs/operator/inbound-trust-boundary.md:3` and comment mentions of the overlay's own function in `scripts/validate-customer-yaml.ts`. |
| A10 `adapter/evidence/packet.py` 1,128 logical; `establishment_store.py` 1,415 | **CHANGED** | `packet.py` is now 456 logical and has left the baseline: #2769 split out `packet_render.py` and `packet_sources.py`. `establishment_store.py` is 598 logical (from 1,415); c02f91fc split the four proposal lifecycles into collaborators. Both come from the baseline diff in `operator-py-base.py`. |
| A11 shell scripts outside every ceiling | **CHANGED** | Shell is now ratcheted: the second half of `tests/operator-module-size.test.ts` with `contracts/operator-shell-size.json`. Two scripts are baselined: `templates/entrypoint.sh` 619 and `bin/provision-customer.sh` 559 logical (1,322 and 1,288 raw). `bootstrap.sh` (925 raw) and `boot-smoke-test.sh` (705 raw) are under 500 logical, since the test passes. shellcheck runs in substrate at `operator-substrate.yml:139-145`: `--severity=warning` over `git ls-files 'operator/**/*.sh' 'operator/*.sh'` minus the generated env-arrays file. That is 21 files, and a local run exits 0. At `--severity=info` there are 8 notes: SC1091 ×5, SC2016 ×2, SC2329 ×1. |

**New and residual findings.**

1. **[MEDIUM, carried, reshaped] 16 non-test modules sit over the 500 logical-line ceiling; all are baselined, and six more sit within 44 lines of it, three of them new this window.**
   - **Instrument.** `contracts/operator-module-size.json` lists 16 modules totalling 10,751 lines (12,761 at 1da55373). The largest are `templates/drafting/drafting_gate_check.py` 1,135, `connectors/smokeball/smokeball_connector/server.py` 1,001, `skills/lien-ledger-tracker/pre_run.py` 880 and `skills/deadline-miss-escalator/pre_run.py` 723. `operator-py-tscount.py` finds the near-ceiling cluster: `runners/medchron/medchron/driver.py` 493, `workspace_broker/send_as_acts.py` 489, `workspace_broker/medchron_ledger.py` 486, `bin/reconcile-outcomes.py` 472, `runners/medchron/medchron/audit/run.py` 464, `adapter/evidence/packet.py` 456.
   - **Entries this window.** `runners/medchron/medchron/daemon.py` (537, now 520) and `llm.py` (510) entered through the formatter commit #2744. `seed_data.py` grew 515 to 590 in the same commit. `safety_substrate/sticky_stop.py` 521 re-entered under its new path. For the four files checked, #2744 is AST-identical before and after (`operator-py-fmt.py`: `ast.dump` equal), so the growth is line-wrapping, not code.
   - **Why it matters.** The rubric's "3+ files over 500" clause is true for the Python half. The ratchet has held with no growth from 2026-09-14 to HEAD (`operator-py-ratchet-hist.py`). The two largest modules are single files on purpose: `drafting_gate_check.py`'s header says it is stdlib-only and runs under `execute_code` with no install step, and `record_check.py` / `spec_leak_check.py` load it by path.
   - **Recommendation.** Split `smokeball_connector/server.py`, which has 64 defs and a 168-line `render_docx_template`, along its tool families. Split the two large tracker `pre_run.py` files along `decide`/`run_once`, now that the sibling loader makes that legal. Watch the three new near-ceiling modules before they cross.

2. **[MEDIUM, NEW] A live decommission's step 7, the compliance evidence packet, always runs a stub that writes a list of expected files. The pipeline reports that step as executed, and the live-run refusal does not know the stub exists.**
   - **Instrument.** `bin/lib/decommission.py:665` defaults `archiver` to `InMemoryComplianceArchiver`. Its docstring (`:415-421`) says it "writes a minimal compliance-packet stub", and it returns `{"archive_path": ..., "stub": True}` (`:458-460`).
   - **The CLI never replaces it.** In `bin/lib/decommission_cli.py`, `grep -n "archiver"` has no hits and the only "compliance" hit is the `--archive-root` help text at `:196`. Both are passed through as defaults.
   - **The refusal does not list it.** `unwired_destructive_backends()` (`:682-712`) lists the audit preserver, R2, Vectorize, AgentMail, Fly and observability, but not the archiver.
   - **The step is marked executed.** The step runner sets `SKIPPED` only on a `skipped` key (`decommission.py:914`), and the stub returns `stub`, not `skipped`, so the status is `EXECUTED`.
   - **The real generator exists.** It is `bin/lib/evidence.py` / `adapter/evidence/`, and it is not wired to step 7.
   - **Why it matters.** This is the #1123 class the refusal was built to end: a `--live` run exits OK while reporting a step it did not perform. Here the missing piece is the offboarding evidence, not a deletion. The module docstring (`:79-80`) also still marks AgentMail and Fly "(stubbed)", which has been false since the real backends shipped.
   - **Recommendation.** Wire step 7 to the evidence builder, reading the seam-pulled sqlite the preserver writes; the docstring at `:73-75` already names it as that builder's `--read-db` input. Until then, add `"compliance_archiver"` to `unwired_destructive_backends()`, and have the stub return `skipped: True` so the manifest says what happened.

3. **[MEDIUM, NEW] The verb-table refactor removed type checking from every broker handler.**
   - **Instrument.** `verbs.py:51`: `Handler = Callable[[Any, str, dict[str, Any], int, "int | None"], dict[str, Any]]`. `grep -rn "broker: Any" workspace_broker --include='*.py' | grep -v /tests/ | wc -l` returns **53** across 10 modules: `send_as_acts.py` 16, `job_verbs.py` 12, `audit_verbs.py` 7, `verbs.py` 6, and others. No `Protocol` names the broker's shape. At 1da55373 the same bodies were `Broker` methods: `git show 1da55373:operator/workspace_broker/server.py | grep -c "self\.\(ledger\|operations\|job_ledger\|medchron\|credential_path\)"` returns 35 attribute reads, all visible to pyright.
   - **Why it matters.** A typo'd or renamed broker attribute in any verb is now invisible to the pyright ratchet, because `Any` produces no errors. On the send-as lane that means an attribute error reaches the socket as a runtime error reply. The authorization structure improved and the type checking of the same code regressed, and no gate registers the second change.
   - **Recommendation.** Add a `BrokerContext` `Protocol` in `verbs.py` naming the attributes the handlers read (`ledger`, `job_ledger`, `medchron`, `operations`, `credential_path`, `customer_path`, the transports), and type the `Handler` alias and the 53 parameters with it.

4. **[POSITIVE, verified] No import cycles, and the layer direction is clean.**
   - **Instrument.** `operator-py-graph.py` walks 324 non-test modules with local-directory-first resolution. It finds 404 top-level edges, 44 deferred edges, and **0** strongly connected components larger than one.
   - **Four mutual pairs exist, each broken by a deferred import.** `send_verify`/`send_attribution` and `smokeball extract`/`vision` are carried from 09-10. `medchron driver.py`/`rehearsal.py` is new: `rehearsal.py:121,217` does a function-local `from . import driver`, commented "lazy: driver imports this module". `establishment_lifecycle.py`/`establishment_store.py` is new: `establishment_lifecycle.py:37-38` imports under `TYPE_CHECKING`.
   - **Cross-root edges** all point the expected way: `bin -> adapter` 6, `bin -> connectors` 6, `bin -> workspace_broker` 3, `safety_substrate -> adapter` 2, `bin -> runners` 2, `templates -> connectors` 2, `customers -> connectors` 1, `runners -> connectors` 1. There are **zero** `adapter -> workspace_broker`, `workspace_broker -> bin`, `workspace_broker -> adapter` or `skills -> bin` edges.
   - **Residual.** The two new mutual pairs are genuine mutual dependencies, papered over by where the import sits.

5. **[POSITIVE, verified] Every large new module this window is reached from a real path.**
   - **The broker lane.** `send_as_acts.py` is imported by `verbs.py:48`. `medchron_ledger.py` by `medchron_verbs.py:41`. `msgraph_ops.py` by `server.py:40`, `transmit_verbs.py:24-25` and `send_as_acts.py:54`.
   - **The medchron daemon.** `entrypoint.sh:684-697` runs `python -m medchron.daemon` under a respawn loop (`|| true`, then log, then sleep 5, then loop). `boot-smoke-test.sh:636-637` asserts the daemon's tick file is less than 90 s old and that no child is running, and `Dockerfile:1453` import-checks the daemon at build time.
   - **Decommission.** `decommission_cli.py` runs from `bin/decommission-customer.sh:91` as `-m bin.lib.decommission_cli`.
   - **The drafting gate.** `drafting_gate_check.py` is loaded by path from `connectors/smokeball/smokeball_connector/record_check.py:63` and `bin/spec_leak_check.py:132-143`.
   - **The voice transform.** `adapter/voice/transform.py` is an overlay twin, enrolled in `contracts/overlay-pairs.json`, and consumed via `adapter/voice/__init__.py:50` by the `bin/voice-*` CLIs. `python3 bin/verify-overlay-pairs.py` prints `PASS: all 10 overlay runtime file(s) match their pinned overlaySha256`.
   - **Responsibilities** are coherent per module. `send_as_acts.py` runs pure helpers, then `SendAsStore`, then `propose`/`decide`/`decide_link`/`match_reply`, with a header stating the ADR 0089 invariants. `drafting_gate_check.py` has 17 gate functions behind one exit contract (0/1/2) and fails closed on malformed input.

6. **[LOW, NEW] The pyproject "still shimmed" list undercounts the shims that remain.**
   - **Instrument.** The `operator/pyproject.toml` header names only the connector path in `voice-fetch-corpus.py` and `voice-survey-corpus.py`, plus the `pilot-smokeball` seed/l2 scripts. `operator-py-1.sh` finds 10 non-test mutations. Four are not in the list: `bin/medchron-backfill-covered.py:90` (puts `runners/medchron` on the path), `bin/seed-staging-matter.py:55` (`/app/connectors/smokeball`, function-local), and `voice-fetch-corpus.py:87` and `voice-survey-corpus.py:106`, which also insert the operator root that Stage 2 says was removed.
   - **One bare-name straggler.** `bin/lib/send_attribution.py:56` still does `from send_verify import ...` under `TYPE_CHECKING`. pyright resolves it through its same-directory fallback and reports no diagnostic, so the import is harmless, but it contradicts Stage 3's "bin.lib.* everywhere".
   - **Recommendation.** Make it `from .send_verify import ...`. Add the four shims to the header list with their reasons: two are the same separate-distributable case already documented.

7. **[LOW, NEW] The module-size counter skips every triple-quoted span, not only docstrings.**
   - **Instrument.** `tests/operator-module-size.test.ts:131-153` drops everything between `"""`/`'''` delimiters wherever they appear. A docstring-only AST count (`operator-py-new.py`) gives `adapter/voice/transform.py` 744 lines against the ratchet's 669, and `send_as_acts.py` 509 against 489.
   - **Why it matters.** Triple-quoted SQL, prompts and templates are code the reader must hold, and the ratchet does not charge for them. The header says "docstring bodies excluded", which is narrower than what the counter does.
   - **Recommendation.** Either document the broader rule in the header, or restrict the exclusion to a string that is the first statement of a module, class or function.

8. **[LOW, NEW] The two tracker skills carry same-named, different-content modules with ungated identical helpers inside them.**
   - **Instrument.** The same name holds largely different code in `client-verification-tracker/` and `deadline-miss-escalator/`: `blind_wake.py` differs on 137 lines, `dispatch_envelope.py` on 490, `render.py` on 469 (`diff ... | grep -c '^[<>]'`).
   - **Identical helpers.** Inside them, `_load_sibling` (15 lines), `_load_yaml` (14), `_write_envelope` (14) and `canonical_body_sha256` (7) are AST-identical, and no contract or sync test names them (`operator-py-dup.py`). The two `blind_wake.py` copies name the same concept `_plan_counts` (`client-verification-tracker/blind_wake.py:171`) and `plan_counts` (`deadline-miss-escalator/blind_wake.py:112`).
   - **Recommendation.** Move the four helpers into `skill_helpers.py`, which both skills already vendor and gate, and settle on one name.

9. **[LOW, NEW] Two stale comments describe gates as they were before this window.**
   - `tests/operator-module-size.test.ts:16` says `operator-substrate.yml` "is NOT a required status check". The ruleset makes `substrate` required, per orchestrator preamble item 5.
   - `decommission.py:79-80` still says "(stubbed)" (finding 2).
   - The skill NOTE that 09-10 low 13 flagged is **fixed**: `:345-367` now branches on `_load_sibling_module|_load_skill_helpers`.

**Not checked:** runtime behaviour on a seat, meaning whether the daemon ticks or a live decommission runs; the overlay repo beyond the ten pinned hashes; `connectors/*` and `runners/medchron` test suites (the orchestrator runs substrate); the correctness of the send-as authorization logic (security agent); `customers/*/` seed-script structure beyond sizes.

**Suggested grade: C+ (Python half), up in substance from the half that earned C+ on 09-10.** Rubric: "C: 3+ files >500 OR unclear domain boundaries". The first clause is literally true: 16 baselined modules. The boundary clauses are now false: zero cycles, a clean layer direction, the broker gate a declared table, and zero-importer modules gone. Not B: sixteen modules over the ceiling is not "1-2 slightly large files", and the window added three near-ceiling modules plus a stub on a destructive live path. Not C: six of the eight carried items closed or substantially closed at the layer that matters, and every gate that holds the remaining debt is a ratchet that can only tighten, which has held for eleven days.

---

### 3. Code Quality

**Carried findings.**

| 09-10 item | Verdict | Instrument |
|---|---|---|
| CQ6 two thirds of `noqa` inert (398; 262 inert) | **CLOSED** | `ruff.toml` selects `E4, E7, E9, S102, BLE001, F401, RUF100, S310, S603, S608`. `ruff check .` prints `All checks passed!`, and so does `ruff check --extend-select RUF100 .`, so **zero** annotations are inert. Note: `--select RUF100` *alone* reports 461, which is an artifact: it replaces the selection, so every other suppression reads as unused. Total annotations: **461** (BLE001 260, S603 53, S310 47, E402 46, F401 31, S608 19, E731 4, S102 2). Bare `# noqa`: 0. `python3 bin/noqa-reasons.py` prints "every noqa in the tree names its rule and carries a reason", and it runs in substrate (`operator-substrate.yml:119-122`). `ruff check --isolated --select BLE001` over non-test code: 0 uncovered. |
| CQ7 no type checker, formatter, function or complexity ceilings | **CLOSED** | **pyright**: `pyproject.toml [tool.pyright]` in basic mode, run at `operator-substrate.yml:128-134`. My run gives 104 errors in 44 files, and `bin/pyright-ratchet.py` prints "holding at 104 error(s) across 44 file(s)". The ratchet fails on growth *and* on shrink-without-regenerate (`compare()`, `:57-68`); an unparseable report raises. Baseline history: 111/45 at adoption, 104/44 now; the only entry added after adoption is the `sticky_stop` rename. **Formatter**: `ruff format --check .` reports 528 files already formatted. The 11 overlay twins are excluded with the reason stated at `ruff.toml [format]`. **Function size and complexity**: `bin/tests/test_function_size_ratchet.py` gives 5 passed. The baseline holds **52** length entries (ceiling 75) and **27** complexity entries (ceiling 15). The test fails on growth and on shrink (`:72-74`), and `test_the_counter_can_fail` plants an 87-line function and a twisty one. |
| CQ9 shellcheck never runs | **CLOSED** | See A11: `--severity=warning`, 21 files, exit 0, inside the required check. |
| CQ10 six-skill `except: pass` swallow | **CLOSED** | Every "observability never gates the wake" handler now reports the failure. `templates/skill_helpers.py:321-322` and its six vendored copies call `warn_observability_failure(exc, ...)`. `blind_wake.py` in both skills writes `sys.stderr` at each site (`client-verification-tracker/blind_wake.py:97-98,129-130,227`; `deadline-miss-escalator/blind_wake.py:63,95,184`). Non-test `S110`/`S112` sites fell from 24 to **19** (residual, finding 5). |
| CQ12 `S310` justified at the callee; `_plan_counts` name collision | **CLOSED (both)** | `decommission_backends.py:95-96` refuses any non-`https://` URL before `urlopen`, and both `noqa: S310` reasons at `:98,103` now cite that check. The collision is gone: `git grep "def _plan_counts\|def plan_counts"` finds `plan_counts_total`/`plan_counts_capped` in `skill_helpers.py`, and `_capped_plan_counts` pinned in the contract. A small naming residual remains in `blind_wake` (A-finding 8). |
| CQ14 effectively zero dead Python modules | **HOLDS (re-derived, wider method)** | `operator-py-graph.py` looked for non-test modules with no importer and no mention in any non-test, non-doc tracked file. It found 6: `bin/gmail-sa-smoke.py`, `bin/killtest-msgraph-send.py`, `bin/retire-output-class-spec.py`, `bin/voice-corpus-extract.py`, `customers/pilot-smokeball/seed/delete_memo.py` and `set_client_contact_email.py`. All six are hand-run CLIs whose docstrings state their purpose and the issue that created them (`operator-py-dead.sh`). None is library code without a consumer. `killtest-msgraph-send.py` also has a test. |
| 09-10 Security 3 edges (in `decommission`, re-probed here as requested) | **CLOSED (all three)** | **`--allow-unwired`**: `_allow_unwired_refusal` (`decommission_cli.py:218-240`) refuses unless a `--customers-root` outside `operator/customers` is given, and it runs before anything else and returns 5 (`:280-283`). **`_fly_authenticated`**: `decommission_backends.py:507-517` returns `bool(env.get("FLY_API_TOKEN"))` only. **`_row_deleted`**: computed from a read-back, `obs["table_present"] and obs["rows_remaining"] == 0` (`:487`). |

**New and residual findings.**

1. **[MEDIUM, NEW] 53 `broker: Any` handler parameters sit where there used to be type-checked `self` access.** See Architecture finding 3. It is counted here too because it is a type-discipline regression the pyright ratchet structurally cannot see.

2. **[LOW, NEW] Every ratchet baseline regenerates from one environment variable, and nothing compares the new baseline with the base branch's.**
   - **Instrument.** `UPDATE_OPERATOR_MODULE_SIZE_BASELINE=1`, `UPDATE_OPERATOR_FUNCTION_SIZE_BASELINE=1` and `pyright-ratchet.py --update` each rewrite their contract unconditionally. `operator-py-ratchet-hist.py` walked every commit touching the four contracts since 1da55373. Every entry that entered or grew traces to one of two causes:
     - moves: #2769 packet split, c02f91fc establishment split, 2eb3a5cb `safety_substrate` rename;
     - the formatter commit #2744: 17 function entries and 3 modules, proven AST-identical for the files checked.
   - **Why it matters.** The discipline held, but only because of review. A PR that regenerates a baseline upward shows as a JSON diff that is easy to wave through.
   - **Recommendation.** Add a substrate step that loads each contract at the merge-base and fails when any value rises or any key is added, unless the PR carries a named label, mirroring `scope-deferred`.

3. **[LOW] Complexity and length debt is baselined and concentrated.**
   - **Complexity.** The worst function is `runners/medchron/medchron/stages/strip.py::_run` at 31. Next are six copies of `validate_append` at 27, which is one function vendored six times, then `billing_chart.py::run` at 25 (C901 list, `operator-py-3.sh`). Nine of the 27 complexity entries are medchron stage `run` functions.
   - **Length.** The longest are `customers/pilot-smokeball/seed/seed_data.py::build_documents` at 381 lines and `adapter/evidence/packet_render.py::_readme_text` at 197.
   - **Recommendation.** Medchron stages are the concentration. One pattern, a stage `run` that validates, loops and writes, recurs nine times and should be split once, with a shared stage frame.

4. **[LOW] 104 pyright errors are frozen, 14 of them in one file.** `templates/drafting/drafting_gate_check.py` carries 14, `bin/rehearse-card.py` 6 and `runners/medchron/medchron/decisions.py` 6 (`contracts/operator-pyright-baseline.json`). There are 68 warnings, all `reportMissingImports` for optional or third-party packages and separately-installed connector packages. None is a broken import of this project's own packages (classification in the pyright step above). Recommendation: start with the drafting gate, since it runs under `execute_code` on seats with no install step, where a type error surfaces only at run time.

5. **[LOW] 19 non-test `try/except/pass` or `continue` sites remain, and `ruff.toml`'s own table understates them.**
   - **Instrument.** `ruff check --isolated --select S110,S112` over non-test code finds 16 `S110` and 3 `S112` sites (`operator-py-3.sh`). The `ruff.toml` comment still lists "S110 14 / 14", dated 2026-08-24.
   - **The class.** Most are cleanup paths. One is behavioural: `skills/deadline-miss-escalator/pre_run.py:925-926` swallows a failed degraded-suppress audit write and falls through to the stripped wake. The comment explains the fall-through, but nothing records the write failure itself. Others sit on seat-side ledgers (`templates/drafting/ledger.py:139,195`, `runners/medchron/medchron/ledger.py:83`) and on `workspace_broker/send_as_links.py:219`.
   - **Recommendation.** Refresh the `ruff.toml` table. Put a `warn_observability_failure` call in the escalator's fall-through and on the two ledger sites.

6. **[POSITIVE, verified] The vendored-copy system is now discovered, not listed, and it proves its detectors.**
   - **Instrument.** md5 over non-test Python (`operator-py-dup.py`) finds 5 identical-file groups with 25 copies:
     - `pre_run.py` stamp ×8 (7 skills + `templates/pre_run_gate.py`);
     - `skill_helpers.py` ×7;
     - `escalation_ledger.py` ×6;
     - `broker_writer.py` ×2;
     - `routing.py` ×2.
   - **Every group is gated by glob with a floor**, through `operator/tests/vendored_sync.py`. `tests/test_vendored_sync.py` plants temporary trees to prove a missing, drifted or edited stamp fails.
   - **The count rose** from 3 groups / 14 copies on 09-10 because more duplication moved into gated whole-file vendors, which is the intended direction.
   - **Scope of the contract.** The remaining `pre_run.py` symbols are pinned by `contracts/pre-run-shared-symbols.json`, 16 symbols hashed over docstring-stripped `ast.dump`.

7. **[POSITIVE, verified] Every gate the 09-10 review asked for now runs inside a required check and fails in both directions.** These are ruff with the three security rules, RUF100, noqa-reasons, `ruff format --check`, the pyright ratchet, the function-size ratchet, shellcheck, and the module-size and shell-size ratchets. Each was run here: ruff clean, 528 formatted, 104/44 holding, 5 passed, shellcheck exit 0, vitest 6 passed. Each was read for its failure mode. A ratchet that silently passes on an empty census is guarded against in each case: pyright's shrink check, `test_the_counter_can_fail`, `toBeGreaterThan(20)` in the vendored-symbols census, and the floors in `vendored_sync`.

**Not checked:** the full substrate pytest run (the orchestrator runs it; I ran 179 targeted tests, all passing); `except` handlers that log but may still mask behaviour; `Any` usage outside `workspace_broker` (a tree-wide `Any` census was not taken); docstring accuracy beyond the sampled modules; the connectors' `_sdk` conformance suite.

**Suggested grade: B− (Python half).** Rubric: "B: 1-2 minor"; "C: notable DRY OR inconsistent error handling OR 3+ dead exports"; "D: swallowed errors OR dead modules". Both 09-10 HIGHs are closed at the gate layer: inert suppressions are now zero, and a type checker, a formatter and function ceilings are all live. The swallow class now reports, the dead-module census is clean, and DRY is gated rather than notable. Not B: the verb-table refactor opened a 53-parameter `Any` gap that the new type gate cannot see, 104 pyright errors and 19 swallow sites remain frozen, and the baselines rely on review not to be regenerated upward. Not C: none of the C clauses holds on current evidence.



<!-- ===== agent: sec-test ===== -->

## Code review 2026-09-25: Dimensions 2 (Security) and 4 (Testing)

**Agent:** sec-test. **Commit:** `ceaca4c0` (`git rev-parse HEAD` → `ceaca4c0f44e41e737c6238f5fe3b8467691e9f1`), worktree `.claude/worktrees/code-review-2026-09-25`. **Prior:** `docs/reviews/code-review-2026-09-10.md` (Security C, Testing C+).
**Overlay pin used for cross-repo reads:** `ARG OVERLAY_REF="a15f0971717e94863e09b8774cd4cb3b6af67c77"` at `operator/templates/Dockerfile:983`; source fetched with `gh api repos/venturecrane/hermes-smd-overlay/tarball/a15f0971...` into the scratchpad (never the `~/dev/hermes-smd-overlay` working copy).
**Suites:** local vitest (orchestrator `ba0ipgace.output`): 364 files, 5,385 passed / 5 skipped / 7 todo, 0 failed. CI vitest on HEAD (`gh run view 36154740893 --log`): 5,375 passed / **15 skipped** / 7 todo. Local pytest (orchestrator `bfikxbrsu.output`): 2,873 passed. CI substrate pytest on HEAD (`gh run view 36154740981 --log`): 2,871 passed, 2 skipped; medchron runner venv 305 passed.

Helper scripts (all read-only) are in the scratchpad as `sec-test-*.sh` / `sec-test-*.py`.

---

### 2. Security

**Carried findings.**

| 09-10 finding | Verdict | Instrument |
|---|---|---|
| S1 shared `MACHINE_HEARTBEAT_KEY`, unbound `X-Tenant-Slug` | **CLOSED** | `src/lib/auth/machine-key.ts:1-28,61-100`: one D1 join `customer_configs LEFT JOIN machine_credentials` on the slug, per-row salt, `key_hash` = HMAC-SHA256(salt, key), `prev_*` dual-key rotation, sentinel HMAC on unknown slug and on a slug with no credential row (fails closed; the fleet fallback is documented as removed 2026-09-14). Cross-language vector pinned both sides: `tests/machine-key-auth.test.ts:77-89` and `operator/bin/tests/test_machine_credential.py:33`. `npx vitest run tests/machine-key-auth.test.ts tests/machine-credentials.test.ts ...` → pass. |
| S2 portal enumerator gap (19 API + 31 page) | **CLOSED** | `tests/portal-routes-bind-tenant.test.ts` walks both roots, asserts a call site or import of the five-helper vocabulary (incl. `validateStateOrReject` for the OAuth callback), EXEMPT holds the 410 + five redirect tombstones with reasons, prune test, and a self-test that a comment mention does not match. Route counts unchanged: `git ls-files 'src/pages/api/portal/**' \| wc -l` → 19, `'src/pages/portal/**'` → 31. Passes. |
| S2 residual: admin template substring match | **CLOSED** | `tests/admin-routes-require-session.test.ts:26-27,48-50`: regex `requireAdminSession\s*\(` or named import; self-test that `// gated by requireAdminSession upstream` does not match. Residual (both files): an `import type { helper }` or an unused import satisfies the regex; ESLint unused-import rules make the second unlikely. Not graded. |
| S3 decommission `--allow-unwired --live` | **CLOSED** | `operator/bin/lib/decommission_cli.py:218-237` refuses unless `--customers-root` is explicit and resolves outside the repo's `operator/customers`; `:272-276` requires `--confirm-slug` equal to the slug for any `--live`. Tests `test_decommission.py:539,555`. |
| S3 `_fly_authenticated` trusting `fly auth whoami` | **CLOSED** | `decommission_backends.py:507-517` returns `bool(env.get("FLY_API_TOKEN"))` only; tests `test_decommission_backends.py:462,472`. |
| S3 unconditional `_row_deleted: True` | **CLOSED** | `decommission_backends.py:487`: `obs["table_present"] and obs["rows_remaining"] == 0`, from an observed post-delete count; tests `:326,:346,:372`. |
| S4 gitleaks `--no-git` on a public repo | **CLOSED** | `.github/workflows/security.yml:200` runs `./gitleaks git --verbose --config .gitleaks.toml .` under `fetch-depth: 0` (`:122`). My own run, `gitleaks git --redact` (8.30.0): 3,667 commits scanned, **no leaks found**. `.gitleaksignore` holds 8 fingerprints, each commit-pinned and annotated (escalation-ledger hex fixture ×4, a test-env assignment ×2, an empty `.dev.vars.example` value ×2); all three read as placeholders. HEAD check run `Secret Detection: success`. |
| `/api/events` no `rateLimitByIp` | **CLOSED** | `src/pages/api/events.ts:5,157`: per-IP KV bucket precedes the session bucket at `:168`. |
| `createRemoteJWKSet` per request | **CLOSED** | `src/lib/operator/mcp/token-validation.ts:77-85`: one JWKS per issuer in a module-level map, reasoned against coding-standards rule 10. |
| JIT MCP identity = Clerk subject as `localUserId` | **CLOSED** | `src/lib/operator/mcp/customer-resolution.ts:127,137-151`: `localUserIdForSubject` maps to local `users.id` when the entity has a row; one rule for granted and JIT paths. |
| `assessment/llm.ts` bearer `!==` | **CLOSED** | `src/pages/api/assessment/llm.ts:34,95` `constantTimeEqual`. |
| nosemgrep justification gate `//` only | **CHANGED** (see finding 3) | `security.yml:91` now matches `(//\|#)`, but the regex passes every rule-id-only suppression whose id is longer than 19 characters. |
| broker `str(exc)` to socket peer | **CLOSED** | `server.py:300-301` → `request_errors.py:29-34`: only `PermissionError`/`ValueError` keep their message; everything else logs broker-side and replies `internal_error`. |
| OAuth callback re-resolving access at store time | **CLOSED** | `src/pages/portal/products/operator/oauth/[connector]/callback.ts:230` reviewer match, `:234` `resolveOperatorAccess(...)`, `:297` `store()`. |
| CSRF on `SameSite=Lax` alone | **CLOSED** | `src/lib/security/cross-site.ts:47-60` (`Sec-Fetch-Site: cross-site` or `Origin` mismatch → refuse) wired at `src/middleware.ts:239-242` after auth; `tests/middleware-cross-site.test.ts` drives the real middleware on both hosts (7 cases incl. unauthenticated cross-site → 401 and webhook exemption). |
| Sentry non-finite timestamp | **CLOSED** | `src/pages/api/webhooks/sentry.ts:68-71` rejects `!Number.isFinite` with 401. |
| LOW: booking limiter allows when KV undefined | **CLOSED** | `src/lib/booking/rate-limit.ts:42,56` fails closed. |
| LOW: raw upstream body logged | **CLOSED** | `src/lib/db/integrations.ts:255-260` logs status and byte length only. |
| LOW: client email logged on invitation failure | **CLOSED** | `invitations.ts:149` logs `customer_id`, not email. |
| LOW: unescaped `Content-Disposition` filename | **CLOSED** | `transcript.ts:40` → `contentDispositionAttachment()` (`:46`). |
| LOW: `ss_sid` without HttpOnly | **OPEN, by design** | `events.ts:37-42`: client script reads it; carries no authority. Not counted. |
| LOW (Testing T1 residual): `substrate` required context has no `integration_id` pin | **OPEN** | `gh api .../rulesets/15555219` → `[{"context":"Security Summary"},{"context":"substrate"}]`, no `integration_id`. Counted under Testing. |

Seventeen of eighteen carried items are closed at the mechanism, each with a test. This is the largest single-window security improvement across the last four reviews.

**New findings.**

1. **[MEDIUM, NEW] The control that makes ADR 0089 approve buttons safe ("the agent cannot read the approver's mailbox") is enforced for only the first approver, and the new staff-mailbox-read feature has no rule keeping approvers out of its read set.**
   Chain read end to end: proposal (`send_as_acts.py:336-422`, gateway-only verb `verbs.py:152`, gates run on the proposing session in the overlay's `plugins/hermes-smd-trust/send_as.py:188-245`) → approval email (`send_as_links.py:152-209`: signed link naming row/verb/expiry, HMAC key at `/var/lib/smd-workspace-broker/send_as_link.key` 0600, `no_sent_copy` so no Sent Items copy) → emailed decision (overlay `plugins/hermes-smd-establishment/__init__.py:4125-4150`: `decided_by` is the verified inbound origin's sender, never a model argument; broker `send_as_acts.py:470-494`: approver-only send/change, admin cancel, Sent Items forgery probe that refuses on failure) or button (overlay `shared/approve_page.py:5-7`: GET renders, POST decides; broker `decide_link` `:627-662`, GATEWAY_OR_AGENT at `verbs.py:160`) → send (`_apply_send` `:565-604`: digest, `from == approver`, approver still on roster, atomic consume, no retry). Every gate I could name is present and tested (`workspace_broker/tests/test_send_as.py`, 33 tests incl. tampered/foreign token, expired link, admin cannot send, forged origin `:309-314`, Bcc stripped, digest over rendered HTML).
   The gap: amendment 5a says the link is safe because "the only mailbox holding the link is the approver's" and "a boot probe fails a send-as seat whose read app can open that mailbox." (a) `operator/templates/msgraph-read-app-cannot-read-staff-probe.py:162` probes `mailbox = staff[0]` only; a second `staff_send_as` entry is never asked about, and the test file (`bin/tests/test_msgraph_read_app_cannot_read_staff_probe.py`, 7 tests) has no multi-approver case. (b) #2896/#2899/#2910 widened the same read app's ApplicationAccessPolicy to named staff mailboxes and added `list_staff_messages`/`read_staff_message`; `connectors/msgraph-mail/msgraph_mail_connector/staff_mailboxes.py:92-113` `authorize()` refuses the Operator's own mailbox but not a `scope.staff_send_as` address. Today the sets are disjoint (my `sec-test-overlap.py` over every customer.yaml: ashton-price send_as=1, reads=3, overlap=0; smd-staging send_as=1, reads=0). One authored line adding the approver to `staff_mailbox_reads` (plus the firm's matching tenant scope step, which #2899 shows is routine) lets the model read its own approve link through a sanctioned tool, and boot smoke still passes whenever that approver is not first in the list. The probe's own failure text ("must scope it to the Operator's own mailbox") also no longer describes the policy the window authored.
   Why medium: this is the one lane that puts a named person's signature on outside mail, and the stated control is now one authoring step from vacuous with nothing that would turn red. Recommendation: `authorize()` refuses any address in `scope.staff_send_as` (read from the same file at call time); the probe iterates every approver; a customer.yaml validator (or `tests/`) asserts `staff_send_as ∩ staff_mailbox_reads = ∅` for every seat.

2. **[LOW, NEW] A local `npm run build` bakes `CLERK_SECRET_KEY` from the shell into the Worker bundle.** `gitleaks dir --redact` over the worktree: 7 hits, three in `dist/`; `dist/server/chunks/server_BCNuqVLB.mjs:9221,9321` read `CLERK_SECRET_KEY: "<32 chars>"` inside `getContextEnvVar`'s `Object.assign({...import.meta.env...}, {_: ".../node_modules/.bin/astro", CLERK_SECRET_KEY: ...})` (`:9199-9222`). Astro statically inlines private `import.meta.env.*` reads in server code at build time, and `@clerk/astro` reads the key that way. This review shell carries the variable (`env \| awk -F= '/^[A-Z_]+=/ {print $1}' \| grep -c '^CLERK_SECRET_KEY$'` → 1). Production is not affected: `.github/workflows/deploy.yml:36-37` runs `npm run build` with no Clerk env. The exposure is a laptop `npm run build && wrangler deploy` (script body visible in the Cloudflare dashboard and any artifact) or a `dist/` shared for debugging. **Orchestrator: this worktree's `dist/` contains that inlined value; do not publish or attach it.** Recommendation: a post-build assertion in `verify.yml` (and a local `postbuild` script) that fails when `dist/server` contains `CLERK_SECRET_KEY: "`; or build under `env -u CLERK_SECRET_KEY`.

3. **[LOW, CHANGED from 09-10 #6] The nosemgrep justification gate now scans `#`, but its regex passes any rule-id-only suppression.** `security.yml:91`: `nosemgrep(:[^[:space:]]+)?[[:space:]]*(?:[—:][[:space:]]*.{0,19})?[[:space:]]*$`. For `# nosemgrep: <rule-id>` the `:` branch can only match if the id is ≤19 characters; every real Semgrep id is longer, so no match, so the line passes. Replayed with `grep -P` (the workflow's engine) on a sample file: `# nosemgrep` and `// nosemgrep: short` are flagged; `# nosemgrep: python.lang.security.audit.dynamic-urllib-use-detected.dynamic-urllib-use-detected` is not. `sec-test-nosemgrep.py` over tracked `.ts/.js/.py`: 79 lines mention nosemgrep, the gate flags 0, and 22 are rule-id-only (19 Python, 3 in `tests/typography-tokens.test.ts`). Most carry their reason on the comment line above (read: `msgraph_ops.py:333-337`, `rehearse-card.py:196-199`, `typography-tokens.test.ts:44-46`), which is a fine style, but the gate cannot tell that from a bare suppression. Recommendation: require either ≥20 chars after the rule id on the same line or a non-empty comment line immediately above, and test the regex against fixtures in `tests/`.

4. **[LOW, NEW] `seat-probe.sh` strips root-only secrets by deny-list, and the list has grown twice in three days after live leaks.** `operator/bin/seat-probe.sh:99-100` excludes seven named variables from both the ENVV copy and (`env -u`) the inherited ssh environment. #2879 (`d0b4e4dd`) added the Graph send credential after boot smoke caught it on the law-firm seat 09-22; #2902 (`c54799d1`) added the R2 account keys after the same thing on 09-24. Both fixes are correct and carefully measured, and `tests/forbidden-strings.test.ts:1285-1296` pins the flag order. The class stays open: the next root-only secret staged on the Machine rides into an agent-uid process until boot smoke catches it live. Recommendation: build the agent environment from an allowlist (the gateway's agent-uid environ is already the authoritative set; start `env -i` and copy only its keys).

5. **[LOW, NEW] The firm-config currency guard (#2904) passes in three cannot-evaluate states.** `operator/bin/lib/firm-config-currency.sh:17-28`: not a git checkout → `WARN` and `return 0`; fetch fails → compares against stale `origin/main`; file absent from `origin/main` → uploads the local file. The repo's own doctrine is that "cannot evaluate" must not read as "permitted" (CLAUDE.md, Law 2 guard). This is an integrity control over a paying client's chronology model tiers, not a confidentiality control, hence low. Recommendation: die in all three branches unless `SS_ALLOW_DIVERGENT_SOURCE=1`; the escape hatch already exists.

6. **[POSITIVE, verified] Headers live on all three hosts, including the 302 paths.** `curl -sI` on `https://smd.services` (200), `https://admin.smd.services` (302), `https://portal.smd.services` (302): each returns `strict-transport-security: max-age=31536000; includeSubDomains`, `content-security-policy: frame-ancestors 'none'`, `x-content-type-options: nosniff`, `x-frame-options: DENY`, `referrer-policy: strict-origin-when-cross-origin`. `/api/mcp` GET → 410; POST with a JSON body → `{"error":"gone",...}`.

7. **[POSITIVE, verified] Injection surface clean in both languages.** `sec-test-inject.sh`: 419 `.prepare(` sites in `src`+`workers` (non-test); every `${...}` inside a template-literal prepare is a code constant: `placeholders` from `.length`, `COLUMNS`/`BILLING_COLUMNS`, `EMAIL_IDENTITY_PREDICATE`, `NOW_SQL` (`grant-store.ts:21`), `mutation` chosen from two literals (`subscriptions.ts:143-148`), `conditions` built from literals with bound values (`services.ts:193-205`). Zero `dangerouslySetInnerHTML`, `eval(`, `new Function(`; all 8 `set:html` go through `serializeJsonLd`. Python: `shell=True` 0, `os.system(` 0, `pickle` 0, unsafe `yaml.load(` 0, `yaml.safe_load` 98. New window surfaces: `staff_mailboxes.py:34` restricts an address interpolated into a Graph URL path to `^[a-z0-9._+'-]+@...$`; `letter_pages.py:277` sanitizes composed filenames; `send_as_links.render_html` escapes the body.

8. **[POSITIVE, verified] Every webhook verifies before effect.** `sec-test-webhooks.sh` over `src/pages/api/webhooks/*`: healthchecks (constant-time bearer, `:116-126`), resend, sentry (HMAC + finite timestamp), signwell, stripe. Public non-webhook POSTs (`assessment/*`, `intake/send`, `events`, `booking/*`) carry `rateLimitByIp`. Nit, not graded: `healthchecks.ts:116-126` hand-rolls a constant-time compare instead of importing `lib/auth/constant-time`.

9. **[POSITIVE, verified] The obligation register keeps client text out of the public repo.** Captured quotes go to D1 and a local journal under `~/.claude/ss-obligation-journal` (`.claude/hooks/lib/register.mjs:28-44`). The CI reconciler's findings, which become Actions log lines and a `gh issue create` body in this public repo, carry `obligation_id`, slug and kind, never `row.what` (`scripts/ci-reconcile-obligations.ts:678-697`, pinned by `tests/obligation-reconcile.test.ts` with synthetic fixture text). `git grep -l "ashtonandprice.com"` → 1 file, the seat's own `customer.yaml`; 0 in any test tree.

10. **[POSITIVE, verified] The MCP grant model holds.** `customer-resolution.ts:199-200` and `grant-store.ts:137,176` filter `revoked_at IS NULL AND expires_at > now` in SQL; `GRANT_TTL_MAX_DAYS = 90` with clamp (`grant-store.ts:25,42-45`).

**Not checked:** the overlay's `SESSION_INBOUND_ORIGIN` population (whether a spoofed external `From:` claiming the approver can ever reach it; ADR 0089 relies on the firm's EOP intra-tenant authentication, which I did not test); whether any link scanner submits the approve page's POST form; Clerk's cookie attributes; secret values; Cloudflare R2/D1 policy; the live ApplicationAccessPolicy scope on either tenant; live behaviour of any authenticated endpoint; a full non-baselined Semgrep run.

**Suggested grade: C (top of band, up in substance).** Rubric: "C any medium OR 3+ lows." One medium (finding 1) and four lows stand, so C is the literal reading. Either one alone would hold it at C. Not D: no high, no critical. Seventeen of eighteen carried findings closed at the mechanism with tests, history scanning is live and clean over 3,667 commits, and the window's riskiest feature (send-as) has every gate its ADR names. Not B: the one gap sits on the lane that signs a person's name to outside mail, and the lows are real, if small.

---

### 4. Testing

**Carried findings.**

| 09-10 finding | Verdict | Instrument |
|---|---|---|
| T1 `substrate` required, reports every PR | **CLOSED, holds** | `gh api .../rulesets/15555219` → active, contexts `Security Summary`, `substrate`; HEAD check runs (`gh api .../commits/ceaca4c0.../check-runs`): `substrate success`, `pip audit (seat image Python) success`, all 11 success. Residual: no `integration_id` pin (finding 5). |
| `tests/build-output.test.ts` fails on clean checkout / inert | **CLOSED** | `ls tests/build-output.test.ts` → no such file; `git log --diff-filter=D -- tests/build-output.test.ts` → `264b176b` (#2750). Orchestrator's full local run: 0 failed. |
| `src/lib/db/assessments.ts` no behavioural test | **CLOSED** | `tests/assessments-behaviour.test.ts` (17 `it(`) imports the real module (`:25-32`), runs it on a miniflare D1 with every numeric migration applied, and carries an org-isolation case per query (header `:1-13`). |
| `src/lib/stripe/checkout.ts` only mocked | **CLOSED** | `tests/stripe-checkout.test.ts` (16 `it(`) imports the real module (`:16-21`), stubs fetch, asserts Stripe wire shapes including the fixed coupon id and the full-price retry. |
| 17 source-text-only test files | **CLOSED (14 upgraded, the rest re-justified)** | `sec-test-sourcetext.py`: 14 of the 17 now import their subject (e.g. `git show 1da55373:tests/contacts.test.ts \| grep -c "from '../src"` → 0; HEAD → 1), landed in `31f2f984` (#2773). Remaining: `assessments.test.ts` (now has a behavioural sibling), `client-identity-gate.test.ts` (a repo-wide policy scanner, legitimately source-reading), `clients.test.ts` (6 `toContain` against `src/pages/admin/index.astro`). |
| `test_packet.py` reads signing key from ambient env | **CLOSED** | `operator/adapter/evidence/tests/test_packet.py:290,295` `monkeypatch.delenv("EVIDENCE_PACKET_SIGNING_KEY_B64", raising=False)`. |
| `tests/sow-render.test.ts` `describe.skip` | **OPEN** | `:31-36` `describe.skip` + two `it.skip`. The WASM renderer still has no executing test. |
| conformance test does not pin fixture paths | **CLOSED** | `.github/operator-substrate-paths.txt` lists `operator/fixtures/**`. `sec-test-paths.sh`: the unlisted `operator/` dirs (`observability`, `assessment-eval`, `bundles`, `grading`, `references`, `voice-gate`) hold 0 `.py` files and no Python test reads them (`git grep -e heartbeat-fields -e operator/references -- 'operator/*.py'` → 0). |
| Nothing measures coverage in either language | **OPEN, deliberate** | `git grep -E -- "--coverage\|pytest-cov\|--cov" .github operator/pyproject.toml operator/pytest.ini` → 0; `vitest.config.ts:57-92` records why. `package.json:13` still carries a `test:coverage` script that cannot run (`ls node_modules/@vitest/` → `mocker spy`, no coverage provider). |

**Skipped tests, both suites.**

| Suite | Local | CI on HEAD | CI-only |
|---|---|---|---|
| vitest | 5 skipped, 7 todo | 15 skipped, 7 todo (`gh run view 36154740893 --log`) | 10 |
| pytest (substrate) | 0 (2,873 passed) | 2 skipped (2,871 passed) | 2 |

CI-only vitest skips by file (from the CI log): `heartbeat-field-parity` 3 (overlay not checked out), `doctrine-integrity` 2 (engagements repo absent, skipped honestly via `ctx.skip` with the reason in the name), `provisioner-gates-parse-not-grep` 4 (`skipIf(!hasUv())`), plus `operator-fixture-contract` 3, `assessment-web-loop` 1 (LIVE model smoke), `sow-render` 2. Python: 23 skip markers (`git grep -E "pytest\.mark\.skip|pytest\.skip\(|unittest\.skip|importorskip" -- 'operator/*.py'`), mostly `importorskip` and corpus/overlay presence.

**New findings.**

1. **[MEDIUM, NEW] `runtime-ac-proof.yml` is not a required check, so the Law 9 gate cannot block a merge.** The ruleset requires `Security Summary` and `substrate`; legacy protection requires `Typecheck, Lint, Format, Test` (`gh api .../branches/main/protection --jq .required_status_checks.contexts`). The job name `Runtime ACs carry a verification ID` is in neither. CLAUDE.md states "Merge gate is `.github/workflows/runtime-ac-proof.yml`"; a red run shows on the PR and merges anyway. Four more ways it passes vacuously, from `scripts/runtime-ac-proof.mjs:31-51` and the workflow: a PR body with no `## Acceptance criteria status` section; an AC not tagged `(runtime)`; any string matching `\bvfy_[0-9A-Za-z]{20,}\b` (`:38`), since the ID is never looked up in the verify ledger; and a PR body edited after the check ran (the check re-runs on `edited`, but merge does not wait for it). `unmet-ac-on-close.yml` calls the crane-console reusable workflow pinned at `8dade7d5`; I read that file at that SHA: it returns without checking when the closer is a PR or a commit (`:92-105`, by design, deferring to tick-acs-on-merge), when the issue has no H2 starting "Acceptance" (`:126-133`), and when the `force-close` label is present (`:49-56`). So for a PR-closed issue the only AC control is runtime-ac-proof, and it is advisory. Why medium for Testing: the repo's definition of done for anything client-facing rests on this chain, and its weakest link is a missing ruleset entry. Recommendation: add `Runtime ACs carry a verification ID` to ruleset 15555219 (it runs on every PR, so it cannot strand one); resolve each `vfy_` ID against the ledger with `crane_verify` lookup or the ledger API before passing.

2. **[LOW, NEW] Ten vitest tests run on developer machines and never in CI, four of them behavioural.** Table above. `tests/provisioner-gates-parse-not-grep.test.ts:82-88,145` drives the extracted provisioner gate block through `uv`; `verify.yml` installs no `uv` (steps: checkout, setup-node, npm ci, typecheck, format, lint, deadcode, build, test), so the four behavioural cases of a provisioning gate that previously failed silently on the law-firm seat are skipped on every PR. `heartbeat-field-parity` checks emitter keys against a local overlay checkout (`:335`) that CI never has; the pinned-ref stamp test at `:319-325` does run. Recommendation: add `astral-sh/setup-uv` to `verify.yml`; fetch the overlay tarball at the pinned `OVERLAY_REF` for the parity block (the substrate job already fetches the overlay for its hash gate).

3. **[LOW, carried] The SOW PDF render path has no executing test.** Unchanged from 09-10 T13/finding 5. `tests/sow-template.test.ts` remains source-text over `src/lib/pdf/*`.

4. **[LOW, NEW] The 5a probe and the staff-mailbox allowlist have no test for the case that matters.** Security finding 1 is also a test gap: `test_msgraph_read_app_cannot_read_staff_probe.py` has seven verdict-arm tests, all single-approver; `test_staff_mailboxes.py` has nine refusal cases, none for an address that is also a send-as approver. The fix and its test belong together.

5. **[LOW, carried residual] `substrate` required context carries no `integration_id`.** `gh api .../rulesets/15555219` → `{"context":"substrate"}` only. Any app that posts a check run named `substrate` satisfies the rule.

6. **[POSITIVE, verified] Every large feature in the window ships a behavioural test against the real module.**

| Feature | Test file(s) importing the real module | What turns them red |
|---|---|---|
| Send-as ADR 0089 (#2858, #2880) | `operator/workspace_broker/tests/test_send_as.py` (33 tests; imports `workspace_broker.send_as_acts, send_as_links`, drives `Broker` with gateway/agent peers); overlay `tests/test_send_as.py`, `tests/test_approve_page.py` at the pin | admin sending, a forged Sent-Items answer (`:309-314`), a tampered or foreign token, an expired link, a caller Bcc reaching the draft, the digest not covering rendered HTML, a double send |
| 5a boot probe | `operator/bin/tests/test_msgraph_read_app_cannot_read_staff_probe.py` (7) | a 200 read passing, a missing credential or failed mint reading as pass, the secret reaching output |
| Staff mailbox reads (#2896) | `operator/connectors/msgraph-mail/tests/test_staff_mailboxes.py` | any unauthored or path-changing address making a Graph request (every refusal asserts zero requests); commit body records mutation probes (drop allowlist → 5 red) |
| Daily post filing (#2882) | `connectors/smokeball/tests/test_letter_pages.py` (28), `test_letter_tools.py` (18) | page-range overlap, filename composition, filing to the wrong matter |
| Medchron resume/probe/verdict | `runners/medchron/tests/` (305 passed in CI, own venv; `test_rehearse.py`, `test_runner_stdout_is_the_verdict.py`, `test_probes.py`, resume across 8 files) | a paid stage running before its free probe, a non-verdict stdout |
| Obligation register (#2830, #2845) | `tests/register-cli.test.ts` (31), `register-deliver.test.ts` (23), `obligation-reconcile.test.ts` (44), `register-libs.test.ts` (14) | an ungrounded quote accepted, `row.what` in a public finding, a cannot-evaluate reading as converged |
| Decommission (#2738) | `bin/tests/test_decommission.py` (32), `test_decommission_backends.py` (27) | `--allow-unwired` without a fixture root, `--live` without `--confirm-slug`, Fly armed by a logged-in CLI, a `_row_deleted` not observed, the archive bucket listed |
| Per-tenant machine credentials | `tests/machine-key-auth.test.ts`, `tests/machine-credentials.test.ts` (spawns the Python minter), `bin/tests/test_machine_credential.py` | either side changing its HMAC input shape (shared vector) |
| Firm-config currency (#2904) | `tests/provisioner-firm-config-currency.test.ts` (executes the bash guard against real git fixtures) | a divergent file uploading |

7. **[POSITIVE, verified] Both suites are green, large and nearly skip-free, with no hollow assertions.** Local vitest 5,385 passed / 0 failed; CI 5,375 passed. Python 2,873 local / 2,871 CI plus 305 medchron and per-connector conformance. `git grep -c "toMatchSnapshot|toMatchInlineSnapshot" -- tests` → 0; `git grep -n "expect(true).toBe(true)" -- tests` → 2, both inside the documented `sow-render` skip. `doctrine-integrity` reports un-runnable cross-repo checks as skips with the reason in the test name rather than as passes (`:150-158,315-326`), which is the right shape.

**Not checked:** whether any merged PR in the window had a red `Runtime ACs carry a verification ID` run (needs per-PR check-run history); the 2 CI-only Python skips by name; mutation testing beyond what commit bodies record; the overlay's own test suite at the pin beyond reading two file names; Python coverage of `send_as_links._link_key` concurrency (first-use mint race).

**Suggested grade: B (up from C+).** Rubric: "A meaningful coverage of critical paths; B minor gaps." Both named critical-path gaps from 09-10 (assessment data layer, Stripe checkout) now have behavioural tests against real D1 and real wire shapes, 14 source-text suites were upgraded, both environment-dependent tests were fixed, and every large feature in the window arrived with a test that exercises the real module. Not A: the SOW render still has no executing test, ten tests never run in CI (four behavioural), nothing measures coverage, and the AC gate the repo calls a merge gate is not required. Not C+: no critical path I probed is untested, and the remaining gaps are narrow and named.



<!-- ===== agent: deps-docs-gp ===== -->

# Agent report: deps-docs-gp (Dependencies, Documentation, Golden Path)

Worktree `/Users/scottdurgan/dev/ss-console/.claude/worktrees/code-review-2026-09-25` at ceaca4c0. Read-only except `dist/` (gitignored) produced by `npm run build`. Scripts used are under the scratchpad as `deps-docs-gp-*.sh`.

**Baseline correction for the orchestrator.** The brief says `.github/audit-allowlist.json` no longer exists. It does: `git ls-files .github/` lists it, and its content is `"expires": null, "tracking": null, "allow": {}`. Last touched by 4c328341 (#2731). There is also a sibling `.github/pip-audit-allowlist.json`, same shape, `allow: {}`. The gate question is still answered below, because `scripts/audit-allowlist.mjs` does handle absence.

---

### 5. Dependencies

**Carried findings.**
- Zero npm audit findings: **CHANGED.** Root now has 1 moderate (devalue); three workers 0/0/0/0 (`deps-docs-gp-outdated.sh`: `audit {"moderate":1,...}` root, `total:0` each worker). See finding 1.
- Seat-image Python installs unpinned (HIGH, three reviews): **CLOSED** for every third-party venv; residual on the overlay install, finding 3. `operator/templates/Dockerfile:1215-1216` (broker), `:1426-1428` (connectors), `:1448-1449` (medchron) all install `--no-config --require-hashes -r /app/requirements/*.txt`; `grep -c 'hash=sha256' operator/requirements/broker.txt` = 435; five lockfiles with 23/28/32/35/66 pins. `tests/operator-dockerfile.test.ts:1685` fails any `uv pip install` that is not a hashed file, a `--no-deps` local path, or the pinned overlay; `operator/bin/tests/test_requirements_pins.py` exists and `bin/tests` is in `operator/pytest.ini` testpaths.
- Nothing audits Python: **CLOSED.** `security.yml` job `pip-audit` runs `pip-audit==2.10.1` via `operator/bin/pip_audit_gate.py` over `operator/requirements/*.txt` (`pip_audit_gate.py:106` globs; `:109` returns 2 when there is nothing to scan; `--disable-pip --no-deps -r`), and `Security Summary` `needs: [audit, gitleaks, semgrep, pip-audit]` and fails on any non-success (`security.yml:365-372`). Fails closed.
- No dependabot `pip`: **CLOSED.** `.github/dependabot.yml` now has two `pip` entries: `/operator/requirements`, and `directories:` `/operator`, `/operator/connectors/{_sdk,_reference,msgraph-mail,smokeball}`, `/operator/runners/medchron`. See finding 4 for a duplication defect.
- `typescript` and `@cloudflare/workers-types` one major behind: **OPEN, deliberate, narrowed.** Root `typescript 6.0.3` vs `7.0.2` (npm outdated). The three workers are on TS 7 (`gh pr list --search 'typescript 7'`: #2707, #2412, #1968 MERGED; root #1972 CLOSED). workers-types is now current within its `5.x` line at the root (`5.20260907.1` wanted `5.20260918.1`, no MAJOR tag).
- No runtime-incompatible dep in the Worker bundle: **still true.** After `npm run build` (exit 0), `grep -rlE 'new Function\(|\beval\(' dist/server` hits only `chunks/schemas_T7AY4Sav.mjs:70`, zod's capability probe, which returns false at `:68` when `navigator.userAgent` includes `Cloudflare`. `npm ls ajv handlebars --omit=dev` is `(empty)`.
- Unused deps: **still zero real; the two false positives still print.** See finding 2.
- All actions SHA-pinned, no `npm install` in workflows: **still true.** `grep -h 'uses:' .github/workflows/*.yml | wc -l` = 55; the same list filtered for `@[0-9a-f]{40}` leaves nothing. `grep -n 'npm install\|npm i ' .github/workflows/*.yml` is empty. 21 workflow files.

1. **[MEDIUM, NEW] A moderate advisory sits on the root lockfile, and nothing in the repo's automation will fix it.** `npm audit --json`: `devalue moderate <5.9.1 fixAvailable:true GHSA-9rgm-9g3h-6x36`; `npm ls devalue` gives `astro@7.3.1 └── devalue@5.8.1` (also under `--omit=dev`). Three facts decide the fix path:
   - **The cooldown is not the blocker.** `npm view devalue time --json`: 5.9.1 published 2026-08-20, 5.9.2 on 08-27, 5.9.4 on 09-18. The advisory was published 2026-09-17 (`gh api /advisories/GHSA-9rgm-9g3h-6x36`, `first_patched_version 5.9.2`). Every patched 5.x release cleared a 7-day cooldown before the advisory existed. The `.npmrc` itself says `min-release-age` is inert on the CI Node 22 toolchain.
   - **Semver already admits the fix.** `node -p 'require("./node_modules/astro/package.json").dependencies.devalue'` = `^5.8.1`. A lockfile refresh of that one package resolves 5.9.4. The lockfile is simply stale.
   - **Dependabot will not do it.** `gh api repos/venturecrane/ss-console/automated-security-fixes` = `{"enabled":false}`, so Dependabot security updates are off, while alerts are on (`vulnerability-alerts` 204; Dependabot alert #49 open for this GHSA). Version updates do not bump transitive packages, and the open root group PR #2870 does not touch `node_modules/devalue` (`gh pr diff 2870 | grep 'node_modules/devalue"'` empty).
   
   Reachability: devalue is bundled (`dist/server/chunks/scope_DnJgd6_c.mjs` re-exported into `entrypoints_BRU3uRBT.mjs`). Its `parse`/`unflatten` callers are Astro actions (`deserializeActionResult`, `:8953`) and Astro sessions (`:10525`). The repo defines no actions (`ls src/actions` fails) and does not use Astro sessions (`astro.config.mjs:8-11` says so, pins an in-memory driver). I'm inferring from those two facts that no attacker-controlled input reaches `devalue.parse`. The gate stays green because `scripts/audit-allowlist.mjs` fails only on high/critical by design. Recommendation: `npm update devalue`, commit the lockfile, and turn on Dependabot security updates so a transitive advisory gets a PR without a human noticing it.

2. **[LOW, NEW] The knip fix for the two false "unused dependency" warnings is written down but does not work.** `knip.jsonc:46-53` says declaring `astro.config.mjs!` as a production entry "is the fix" (#2750). `npm run deadcode` at HEAD still prints `Unused dependencies (2) @astrojs/cloudflare package.json:29:6, @astrojs/sitemap package.json:30:6`; exit 0 because `rules.dependencies` is `warn` (`knip.jsonc:147`). The comment is now a false in-code claim, and the warning list is still all noise. Recommendation: add `astro.config.mjs!` to the workspace `project` array too (it is absent from `knip.jsonc:88-95`) or `ignoreDependencies` both with the reason, and add a vitest case that runs knip and asserts zero dependency findings.

3. **[LOW, NEW] The seat image still resolves some third-party code at build time, unpinned and unaudited.** Three install lines sit outside the lockfiles:
   - `Dockerfile:1181-1182` installs the overlay WITH dependency resolution. At the pinned ref, `gh api .../hermes-smd-overlay/contents/pyproject.toml?ref=a15f0971...` declares `pyyaml>=6.0`, `boto3>=1.34`, `PyJWT[crypto]>=2.12`, `sentry-sdk>=2.0,<3`. The test at `tests/operator-dockerfile.test.ts:1698` exempts the overlay line by design. Whatever the Hermes `uv sync --frozen` venv does not already satisfy resolves fresh on each build. The overlay ships a `uv.lock`, which `uv pip install git+...` ignores.
   - `:1233` `npm install -g @oktopeak/clio-mcp@2.0.0`: the top package is pinned, its transitives float, with no lock.
   - `:1126` `npm install --prefer-offline --no-audit` in the Hermes tree.
   
   `pip_audit_gate.py` scans only `operator/requirements/*.txt`, so none of these is audited. This is far smaller than the 09-10 HIGH. Recommendation: install the overlay `--no-deps` and add its four dependencies to a hashed `hermes-overlay.txt` compiled against the Hermes venv, or record the exemption's residual in the Dockerfile comment.

4. **[LOW, NEW] Two Dependabot pip entries cover the same files and open identical PRs.** #2897 (`dependabot/pip/operator/requirements/...`) and #2898 (`dependabot/pip/operator/...`) both touch the same four `operator/requirements/*.txt`. `cmp` of their `gh pr diff` output reports `IDENTICAL` at 1,428 lines each. Merging one leaves a conflicting twin. Recommendation: drop `/operator` from the second entry's `directories`, or scope the first entry with an `ignore`.

5. **[LOW, NEW] Six green Dependabot PRs have sat unmerged for up to three days.** #2870, #2898, #2897, #2868, #2867, #2866 are OPEN and `MERGEABLE`, and `gh pr checks` shows no non-pass row on any. Only #2866/#2867 have auto-merge enabled. #2870 (18 root updates, including `@sentry/cloudflare`, `astro 7.3.2`, `zod 4.6.5`) needs a human merge because the group carries runtime minors (`dependabot-auto-merge.yml:38-41`). Two further observations on that workflow:
   - Its `update-type == 'version-update:semver-security'` branch cannot match. fetch-metadata at the pinned SHA emits only `version-update:semver-{major,minor,patch}` (`src/dependabot/update_metadata.ts:168-175`); security PRs are identified through `ghsa-id`/`alert-state`.
   - Its header (`:23-25`) says the `pull_request_target` job "needs a hard review before this repo is ever made public again". The repo is public (orchestrator fact). The job runs no PR code, so the risk is small, but the comment is now stale.
   
   Recommendation: split runtime minors into their own group, merge the backlog, and replace the dead security clause with `steps.metadata.outputs.ghsa-id != ''`.

6. **[LOW, carried, deliberate] Outdated majors.** `npm outdated --json` at the root flags four packages as MAJOR:
   - `typescript` 6.0.3 vs 7.0.2 is real and deliberate.
   - `prettier-plugin-astro` 0.14.1 vs 1.0.1 is real and new. 1.0.0 was published 2026-09-08 and 1.0.1 on 09-17 (`npm view ... time`), and no Dependabot PR exists for it (`gh pr list --search 'prettier-plugin-astro in:title'` shows only #528 from long ago).
   - `eslint-plugin-astro` 3.1.0 vs 1.7.0 and `vitest` 5.0.0 vs 4.1.11 are registry `latest`-tag artifacts, since current is higher than latest.
   
   The three workers report nothing outdated. Also `@formepdf/{core,react}` sit at 0.18.0 against 0.23.0. For 0.x packages a minor is breaking, and caret `^0.18.0` holds them there.

7. **[POSITIVE, verified] The npm audit gate handles an absent allowlist by design and fails closed on highs.** `scripts/audit-allowlist.mjs:28-44`: on `ENOENT` it returns `{allow:{}, expired:true}`, and any other read or parse error rethrows, which fails the job. `highSeverityGhsas` throws when `npm audit` produces no stdout (`:52-54`). Every high or critical not allowlisted increments `blocking`. Moderates never fail, matching the former `--audit-level=high`, which is why finding 1 is green.

8. **[POSITIVE, verified] `OVERLAY_REF` pin discipline.** `git log 1da55373..ceaca4c0 -G'ARG OVERLAY_REF=' -- operator/templates/Dockerfile | wc -l` = 26 pin moves in the window. `operator/contracts/overlay-pairs.json` carries exactly one `overlayRef`, `a15f0971...`, equal to `Dockerfile:983`. `operator-substrate.yml:200` runs `verify-overlay-pairs.py`, which fetches the overlay at that ref and hash-checks every twin, failing closed (exit 1 mismatch, 2 fetch failure; docstring `:1-30`).

9. **[POSITIVE, verified]** Three workers still carry zero production dependencies (`prod deps: none` each). The pip-audit allowlist exists with the same time-box contract and is empty.

**Not checked:** the Hermes upstream `uv.lock` contents (so whether `boto3`/`sentry-sdk` are already satisfied in the Hermes venv); pip-audit output itself (orchestrator reports the job green on HEAD); whether the `ALERTER_HEALTHCHECKS_PING_URL` value is valid (name-only probe below).

**Suggested grade: C+** (was B). Rubric: "C mediums" applies literally, because one moderate advisory is live on the lockfile. Not B: the advisory is real, and the repo's own automation is configured so that it never gets fixed without a human (security updates off, version updates skip transitives). Not C: the finding is unreachable on inspection and one lockfile refresh from closed. The 09-10 independent gap, the unpinned and unaudited seat Python, closed properly with hashes, a test, a fail-closed audit job and Dependabot coverage. Only one real major is behind, and it is deliberate. If the orchestrator weighs reachability over the literal rubric, B- is defensible. B is not, while the fix path is switched off.

---

### 6. Documentation

**Carried findings.**
- Three numbers for one fact in CLAUDE.md, decision-stack and index; ADRs "through 0061"; README TS 5: **CLOSED.**
  - `CLAUDE.md:309`, `docs/adr/decision-stack.md:26` and `docs/adr/index.md:9` all say 38 active, through #56, 3 superseded.
  - `CLAUDE.md:310` no longer bounds the ADR range. It delegates to `docs/adr/index.md` and `tests/adr-integrity.test.ts`.
  - `README.md:18` says TypeScript 6.
  - `tests/docs-facts.test.ts` derives the numbers from source (979adc58, #2748). `npx vitest run tests/docs-facts.test.ts tests/adr-integrity.test.ts tests/handbook-integrity.test.ts`: 3 files, 16 tests passed.
- No API docs for 98 route files: **CLOSED as an inventory.** `docs/api/README.md` is generated by `scripts/api-inventory.ts` and lists 95 rows. `git ls-files src/pages/api | wc -l` = 95, and `grep -c '^| \`' docs/api/README.md` = 95. `tests/api-inventory.test.ts` passes 6 of 6 and fails on any tree/page disagreement. It is an inventory, not a contract: it gives path, methods, gate, purpose and file, with no request or response schemas. 4 rows read `(no header comment)`. At least one purpose is a stray comment rather than the route's purpose: `/api/admin/assessments/:id/complete` reads "Default hourly rate at launch (per Decision Stack #16, evolved)."
- False in-code claims naming `workers/booking-cleanup/`: **CLOSED.** `grep -rn 'booking-cleanup' src operator scripts` is empty.
- `data-model.md` behind migrations: **CLOSED.** `ls migrations | tail` ends at `0118_alert_source_obligation.sql`. `docs/handbook/data-model.md:83,86,88,89,99` cite migrations 0114, 0115, 0116, 0117 and 0118. Last page commit a6caf618, 2026-09-17, is the same commit that added 0117/0118.
- `tests/handbook-integrity.test.ts`, `adr-integrity`, `docs-facts` run in CI: **true.** `verify.yml:73` runs `npm test`. `vitest.config.ts:45` excludes only node_modules, dist and worktrees.
- ADR index completeness (`docs/adr/index.md`): **true.** 87 numbered ADR files, highest `0089-staff-send-as-on-approval.md`. `grep -c 0089 docs/adr/index.md` = 1, and adr-integrity passes.
- `docs/reviews/index.md` lists prior reviews: **true.** Every `code-review-*.md` in `ls docs/reviews` has an entry, 2026-05-06 through 2026-09-10 including both agent-report siblings.

1. **[MEDIUM, NEW] The handbook's ADR Index says it lists every ADR and stops at 0065, 24 records short.** `docs/handbook/adr-index.md` frontmatter summary: "Every Architecture Decision Record, numerically". The highest number it mentions is 0065 (`grep -oE '\b00[0-9]{2}\b' | sort -u | tail`). `ls docs/adr | grep -E '^00(6[6-9]|[78][0-9])-' | wc -l` = 24 missing, including 0086 identifier gate, 0088 obligation register and 0089 staff send-as. The page was last changed 2026-07-27 (86d5cf8d). `grep -n adr-index tests/*.test.ts` is empty: no test pins it, unlike its source `docs/adr/index.md`. This page renders in the admin console as the Reference map into the why-canon. Recommendation: generate it from `docs/adr/index.md` the way `docs/api/README.md` is generated, or extend `adr-integrity.test.ts` to require every ADR number on this page too.

2. **[MEDIUM, NEW] The handbook maintenance contract is honoured for schema and admin work, not for connectors or new ADRs.** I classified every `feat` commit in the window that touches `src/` or `operator/` against the "if you change X, update Y" table (`docs/handbook/README.md`), using `deps-docs-gp-contract.sh` and `-contract2.sh`. Ten sampled commits trigger a row:

   | Commit | Trigger | Named page updated? |
   |---|---|---|
   | a6caf618 (#2830) | new ADR 0088, migrations 0117/0118, admin surface | data-model, admin-console, new obligation-register: yes. `adr-index.md`: no |
   | b5c21a1d (#2858) | new ADR 0089 | topic page security-trust: yes. `adr-index.md`: no |
   | a1b10c20 (#2896) | msgraph-mail gains `staff_mailboxes.py` | `connectors-channels.md`: no |
   | bca32a45 (#2882) | smokeball gains `letter_tools`, `letter_pages`, `attachment_tools` | `connectors-channels.md`: no |
   | 61018cd8 (#2854) | smokeball `letterhead.py` | operator-platform updated; `connectors-channels.md`: no |
   | 94c19e02 (#2747) | admin `chronology.astro` + ADR 0087 amendment | `admin-console.md`: no |
   | 78d0bb3f, 86cfb693, ad5e2369, a141e869 | amendments inside an existing ADR (0087, 0089) | row arguably N/A (no new ADR) |

   `git log -1 -- docs/handbook/connectors-channels.md` = a53c1517, 2026-07-03. `grep -i 'msgraph\|staff mailbox\|letter' docs/handbook/connectors-channels.md` is empty, while the window added a staff-mailbox reader and a letter-splitting pipeline to live connectors. Of six applicable commits, 1 fully complied, 2 partly, 3 missed. `npm run handbook:drift` flags 27 of 35 handbook pages as having a cited source newer than the page. It is advisory, so nothing blocks. Recommendation: extend handbook-drift to fail CI when `operator/connectors/<name>/manifest.toml` changes without `connectors-channels.md`, mirroring the api-inventory control.

3. **[LOW, NEW] `operator/CLAUDE.md`'s "Load these ADRs before any Operator architectural work" list stops at ADR 0021** (plus 0037 and 0053). The 15 ADRs it names all exist, and none is superseded: the status grep shows only 0015 as `partially-superseded`. But the list omits the records that now govern Operator architecture: 0050 task execution, 0052 portal console, 0056 entitlements, 0057 connector access, 0086 identifier gate, 0089 send-as. File last changed 2026-09-01 (cc162818). An agent following it loads a mid-2026 architecture. Recommendation: add the current load-bearing ADRs, or point the list at a maintained section of `docs/adr/index.md`.

4. **[LOW, NEW] `README.md:18` says "Vitest 4"; `package.json` declares `"vitest": "^5.0.0"`** since a6ca13c7 (#2758). `tests/docs-facts.test.ts` pins the TypeScript major from package.json but not the Vitest major, so the test passes on a drifted README. Recommendation: pin every version the Stack line states.

5. **[LOW, NEW] Stale or false comments in CI config**, counted here once:
   - `knip.jsonc:46-53` claims a fix that does not hold (Dependencies finding 2).
   - `dependabot-auto-merge.yml:23-25` presumes the repo is private (Dependencies finding 5).

6. **[POSITIVE, verified] The window's largest new modules document themselves well.**
   - `.claude/hooks/lib/register.mjs` (892 raw lines) opens with a header naming ADR 0088 and migration 0117, what it is for, and why the grounding gate exists.
   - `operator/bin/lib/decommission.py` (1,063 raw) cites #820, #1355 and ADRs 0016/0043 and states the data-plane doctrine.
   - `operator/workspace_broker/send_as_acts.py` (709 raw) cites ADR 0089 and states the four impersonation rules the broker enforces.

7. **[POSITIVE, verified]** The API inventory and docs-facts are generated-and-tested controls rather than prose, the pattern the 09-10 report asked for. `docs/handbook/deployment-release.md:81-93` now records why zero required reviews is a decision (40639f1d).

**`crane_docs_drift_audit`:** ran it. It audited 199 files across `company`, `ventures`, `instructions` and similar directories, which is crane-console's docs tree, not this repo's. Its two errors (`docs/instructions/design-system.md:148`, `docs/instructions/session-reflexes.md:32`) are not ss-console paths. The tool does not reach ss-console, the same scope note as 09-10.

**Not checked:** content accuracy of the 95 inventory purposes beyond the one sampled misread; the 27 drift-flagged pages individually.

**Suggested grade: B-** (was C+). Rubric: "B missing 1-2 sections": CLAUDE.md, README, schema and API inventory are all present and test-pinned. Not B: the handbook's own ADR index is 24 records behind while claiming completeness, and the maintenance contract missed on three of six sampled commits. Not C+: the "no API documentation" clause that held C+ last time is no longer true.

---

### 7. Golden Path Compliance (Tier 2)

| Requirement | Status | Instrument |
|---|---|---|
| Source control, CLAUDE.md, TS + ESLint, no hardcoded secrets | Met | CLAUDE.md present; `verify.yml:49-63` typecheck/lint; gitleaks full history green on HEAD (orchestrator) |
| Error monitoring (Sentry) | Improved, partial | 13 files import `captureError`, 27 import `src/lib/api/failures.ts`; union 39 of 170 catch-bearing non-test `src/` files (`deps-docs-gp-sentry*.sh`); middleware wraps every request (`src/middleware.ts:261`); `tests/api-error-vocabulary.test.ts` + `api-failed-response.test.ts` 12/12 pass and refuse a 5xx not through the helpers |
| Full CI/CD | Met | 21 workflows; deploy on merge; required checks on ruleset + legacy (orchestrator) |
| Branch protection | Partial (documented decision) | ruleset 15555219 active, `bypass_actors: []`, required checks `Security Summary`, `substrate`; legacy `Typecheck, Lint, Format, Test`, strict true; required reviews 0, `enforce_admins` false: unchanged from 09-10; rationale now in `deployment-release.md:81-93` |
| Uptime monitoring | Met | `workers/fleet-alerts/src/edge-poll.ts:1-40` polls `https://smd.services/api/health` (real `SELECT 1` on D1) on `crons = ["*/2 * * * *"]` (`wrangler.toml:25-26`), down after N consecutive failures; the poller has its own dead-man switch, `ALERTER_HEALTHCHECKS_PING_URL` (`index.ts:147-153,594`), present as a secret name (`npx wrangler secret list --format json` in `workers/fleet-alerts`; value not verified) |
| API docs | Met (inventory) | `docs/api/README.md`, 95/95 routes, test-pinned |
| PWA | Missing (expected) | not applicable to this venture's surfaces |
| Secrets scanning | Met | `security.yml:200` runs `gitleaks git` over full history (`fetch-depth: 0`); `--no-git` gone |

**Carried findings.**
- Zero required reviews / admin bypass: **OPEN, now documented as a decision** (`docs/handbook/deployment-release.md:88-93`).
- Sentry adoption did not move: **CHANGED**, from 5 importers to 39 files reaching Sentry, with the 5xx path test-enforced (40639f1d, #2782).
- No uptime poller: **CLOSED** (50b878c8 #2775, 635166a7 #2778).
- API docs missing: **CLOSED** as an inventory.
- Gitleaks `--no-git` on a public repo: **CLOSED.**

1. **[LOW, NEW] The outside-in poller shares a failure domain with what it watches.** It runs as a Cloudflare Worker cron on the same account as `ss-web`. An account-level or platform-wide Cloudflare incident silences both the web Worker and its poller. The dead-man ping to healthchecks.io is what still pages then, provided the secret's value is live (not verified here). Recommendation: record that dependency in `edge-poll.ts`'s header, or add a healthchecks.io HTTP check or another external poller on `/api/health`.

2. **[LOW, NEW] The gitleaks binary is fetched as `releases/latest` with no checksum.** `security.yml:167-188` resolves the tag from the API and extracts the tarball without verifying `gitleaks_*_checksums.txt`. This is inconsistent with the SHA-pinning of all 55 `uses:` lines: an unverified binary executes inside a required gate. Recommendation: verify the release checksum, or pin a version and let Dependabot-style automation bump it.

3. **[POSITIVE, verified]** Error monitoring reaches every 5xx a route answers, through a test that refuses the bypass, not a sweep.

**Not checked:** Sentry project-side alert rules; whether healthchecks.io actually has a check wired to the alerter ping; `enforce_admins` and reviews beyond the orchestrator's read.

**Suggested grade: B+** (was B-). Rubric: "B 1-2 non-critical missing". Zero required reviews (a documented, reasoned decision) and PWA (not applicable) are the only gaps, and uptime, API docs and secrets scanning all moved to met. Not A: required reviews are still zero against the rubric's literal language, and Sentry reaches under a quarter of catch-bearing files. Not B: every item the 09-10 report scored partial or missing has moved except branch reviews.

