---
title: Code Review 2026-09-09
---

# Code Review: SMD Services

**Date:** 2026-09-09
**Reviewer:** Claude Code (automated)
**Scope:** Full codebase
**Mode:** Full (Phase 1: Claude-only, multi-agent fan-out)
**Models Used:** Fable orchestrator (independent re-probe of every claim graded below), Opus agents (`src/` + `workers/`; `operator/`; Security + Testing), Sonnet agent (Dependencies, Documentation, Golden Path)
**Golden Path Tier:** 2
**Commit reviewed:** 0a3722a3 (== origin/main at review time)
**Window since last full review:** cc97518d..0a3722a3, 124 commits, 476 files, +66,635 / -7,963 lines (operator/ +43,405, tests/ +8,390, src/ +4,727)
**Agent reports:** every claim below was either re-derived by the orchestrator or checked against the agent's stated instrument. The four raw agent reports are preserved verbatim in [code-review-2026-09-09-agent-reports.md](./code-review-2026-09-09-agent-reports.md). Last review, three agent reports never reached the orchestrator; this time each agent wrote its report to disk before returning, and all four arrived.

## Summary

**Overall: C-** (up from D+ on 2026-08-23).

The two findings that put the last review at D are both closed, and closed at the layer that matters. Security response headers are live on all three hosts, verified by `curl -sI` against smd.services, admin.smd.services and portal.smd.services, including on the 302 early-return paths that carry most of the auth surface. The trust-ceiling parity claim that was a documented-but-unenforced CRITICAL is now pinned by `tests/operator-taint-gate.test.ts`, which derives the taint-gated class set from the enum and fails on drift. The sticky-stop circuit breaker, reported last time as 890 unwired lines, turns out to be live on seats in the overlay; what remains unwired is the ss-console copy of it and one time-budget arm. The 09-04 claims probe, re-run at HEAD, reports 32 closed, 6 still open, 0 regressed.

Two dimensions moved the other way. Dependencies drops to D because `npm audit` at HEAD reports 1 critical and 7 high advisories. They are time-boxed in a Captain-authorized allowlist expiring 2026-09-23, tracked in #2711, and blocked from fixing by the repo's own 7-day supply-chain cooldown. The critical (Astro AVIF RCE) is not reachable in this app: the adapter runs the passthrough image service, the built Worker imports no sharp, and no page uses astro:assets. The grade follows the rubric, not the exposure; the exposure is the expiry date. Documentation drops to C because there is still no API documentation for 99 route files, and the issue that tracked it (#2385) was closed in the 08-31 backlog reset without the work being done, which removed the only reminder.

The pattern the last review named, an annotation standing in for a control, is still the dominant shape. Three of the fifteen `@public` waivers that exempt exports from the dead-code gate cite consumers that do not exist. `knip.jsonc` says ten categories are "reported but non-blocking" when `off` means not reported at all. The Python `noqa` pile grew from 261 to 392 annotations and the inert share got worse (260 name rules ruff does not enable). Five `# shellcheck disable=` directives sit in workflows for a linter that never runs. And the one gate over 2,486 Python tests, ruff, connector conformance and the overlay runtime-hash check, `operator-substrate`, is still not a required check, for the third review running. Where the venture fixed a carried finding at the level of the control (the coverage-threshold removal plus `tests/coverage-config-honesty.test.ts`, the VERTICAL_FLOORS synthetic-entry injection, the module-size ratchet), it fixed it well. The list of places that need the same treatment has not shrunk.

## Scorecard

| Dimension | Grade | Trend | Rubric line applied |
|-----------|-------|-------|---------------------|
| Architecture | C | up (was D) | 3+ files over 500 lines; mixed concerns in two route handlers; declared boundaries not kept |
| Security | C | up (was D) | Five mediums, no highs; both prior highs closed at runtime |
| Code Quality | C | stable | 3+ dead exports; notable DRY; error handling inconsistent in one class |
| Testing | C | stable | Critical paths untested (assessments data layer, stripe checkout); Python gate advisory |
| Dependencies | D | down (was C) | High-severity audit findings present at HEAD; seat-image Python installs unpinned |
| Documentation | C | down (was B) | No API documentation for a project with 99 API route files |
| Golden Path | C | stable | 3+ non-critical items partial: required checks, review floor, Sentry adoption, API docs |

**Overall: C-.** Mode of the seven grades is C; the rubric pulls the overall toward the worst grade when any dimension is D.

## Top action items

1. **Add `operator-substrate` to the required status checks on `main`.** `gh api repos/venturecrane/ss-console/branches/main/protection --jq '.required_status_checks.contexts'` returns exactly `["Typecheck, Lint, Format, Test"]`; the ruleset adds `Security Summary`. Nothing that runs ruff, the 2,486 Python tests, connector conformance, the medchron runner suite, or `verify-overlay-pairs.py` can block a merge. Two teams have already routed around this by moving individual Python gates into the vitest suite. This is one line in a ruleset and it is the highest-leverage change in this report.
2. **Watch the 2026-09-23 allowlist expiry.** `.github/audit-allowlist.json` tolerates six high/critical GHSAs until that date. The patched Astro (7.2.8) clears the 7-day `min-release-age` cooldown well before then. When it does, bump and remove the entries; if the date passes first, the Security Summary gate goes red on every PR, which is the mechanism working. Do not extend the expiry.
3. **Make the `@public` waiver falsifiable.** Fifteen tags exempt exports from the dead-code gate. Three cite consumers that do not exist (`workers/booking-cleanup/` was never created; the overlay contains zero `.ts` files so it cannot call `isCustomerSeat`; the named invoice page has no local variant of `formatRelativeDueCaption`). Six cite tests that assert the export's own source text. Require the tag to name a path and add a test that the path exists and references the symbol, the same shape as `tests/coverage-config-honesty.test.ts`. Then delete or wire the four dead ones.
4. **Turn the ten `off` knip categories to `warn` and enable `types`.** `npm run deadcode` prints nothing; with `files` enabled it reports 15 unused files including the whole promotion-cards cluster and three barrel `index.ts` files; with `types` enabled it reports nothing at all, so the config comment claiming ~450 exported types would need a mass un-export is false. Two lines.
5. **Pin the seat image's Python installs and stand up a Python dependency audit.** `operator/templates/Dockerfile:1017-1018` runs `uv pip install ... google-api-python-client google-auth pyyaml` with no version specifier. No `requirements.txt`, `uv.lock`, pip Dependabot ecosystem, or `pip-audit` exists anywhere. Carried from 08-23 unchanged.
6. **Clone the admin route enumerator for the portal.** `enforcePortalAuth` in `src/middleware.ts:144-162` admits any signed-in Clerk user; tenant binding is per-route. All 20 portal routes comply today (the 20th is a 410 tombstone), but nothing enumerates the set, so a new route that forgets its helper is readable by any authenticated user of the shared Clerk instance. `tests/admin-routes-require-session.test.ts` is the template.

## Detailed Findings

### 1. Architecture

**Carried findings.** Of the 2026-08-23 Architecture findings: the CRITICAL (trust-ceiling parity untracked) is **closed** by `tests/operator-taint-gate.test.ts`, with the source comment at `operator/adapter/trust_ceiling.py:186-201` rewritten to state exactly what is and is not proven; the cross-repo half remains a hand check (residual below). The sticky-stop HIGH is **reframed**, not closed (finding 3). Broker positional authorization is **changed, core still open** (finding 1). No Python packaging is **still open and larger** (finding 2). Tracker-skill duplication is **fixed for the measured pair and reappeared in another** (finding 4). `adapter/` off the request path is **still open with a correction** (finding 7). The hand-maintained sync tuple is **still open, now three tuples** (finding 5). On the `src/` side, all six carried items are **still open, unchanged**: the dead admin OAuth callback and its middleware exemption at `src/middleware.ts:120`; two helper modules registered as public endpoints (`src/pages/api/booking/reserve-helpers.ts`, `confirmation-emails.ts`); `src/lib/db/` reaching up into stripe, follow-ups, sow and booking (four edges, same four); raw SQL in 17 route files (was 18); the Operator domain split across `src/lib/operator` and `src/lib/portal/operator` (27 edges one way, 1 back); the promotion-cards feature with no importer; two incompatible API error-body shapes under one key.

1. **[HIGH] `workspace_broker/server.py:363` `handle()` is 313 lines at cyclomatic complexity 63, and authorization is still positional.** The structural gate `if peer_pid != self.gateway_pid: raise PermissionError` moved from line 545 to line 595. Everything above it is gateway-only by position, with five hand-rolled agent-uid checks inline (lines 381, 410, 460, 521, 572) and a `medchron_dispatch(...)` delegation at line 371 that gates internally through a proper dispatch table (`workspace_broker/medchron_verbs.py:76-80`, `_gate` as the first statement). The Security agent independently enumerated every pre-gate action and confirmed each carries an inline uid check today; the only ungated pre-gate action is `health`, read-only. `tests/broker-verb-registry.test.ts` was added since the last review and pins the 32-verb set, but asserts only that each verb is named in the broker test suite, not that it carries a gate. Instrument: read of `server.py:363-600`; read of the registry test; `npx vitest run tests/broker-verb-registry.test.ts` (3 passed). Falsifier: a fourth assertion in the registry test enumerating pre-gate verbs and requiring a uid check. Recommendation: replace the if-chain with a verb table carrying `{handler, auth: gateway | agent_uid | root}` and assert every entry declares one; the medchron group already has the shape.

2. **[HIGH] No Python packaging for the four core subsystems; 118 `sys.path` mutations (33 non-test) and 126 `# noqa: E402` are the cost.** `bin -> workspace_broker` is 22 edges, every one resolved by `sys.path.insert(...)` then a bare `from chain import ...` (`bin/verify-audit-chain.py:45-46`). The F9 probe list is unchanged at seven library modules (`safety-substrate/refusal.py:148`, `trust_ceiling_log.py:127`, `invariants/invariant_6.py:134`, `invariant_7.py:138`, `bin/lib/decommission_cli.py:43`, `bin/lib/evidence.py:47`, `bin/lib/sender_class.py:19`). The three packaged subtrees (`connectors/_sdk`, `connectors/*`, `runners/medchron`) prove the pattern works here. No open issue tracks it. Instrument: AST import walk over 448 files; `find . -maxdepth 3 -name pyproject.toml` (five connector/runner manifests, none at root); `gh issue list --state open --limit 200` filtered for packaging terms. Recommendation: one root `pyproject.toml` with the four subsystems as packages, on the medchron precedent.

3. **[HIGH, reframed] `safety-substrate/sticky_stop.py` (526 logical lines) is an unwired ss-console copy of a control that is live in the overlay.** In ss-console the only non-test reader of `sticky_stop_state` is `runners/medchron/medchron/daemon.py:126`; nothing calls `record_tool_failure` or `record_refusal`. The arming is entirely overlay-side at the pinned ref 5770b79f: `shared/cost_breaker.py:374-384` writes `HARD_STOP`, `plugins/hermes-smd-trust/__init__.py:261` reads it and blocks every tool, `shared/gate_inbound_cap.py:135` parks inbound. So the control the last review said had "no brake" is live on seats; the module in this repo has no runtime role, is not in `overlay-pairs.json`, and holds its own slot in the module-size baseline. The genuine inert arm is narrower: `sticky_stop_time_budget` (`runtime-controls.yaml`, `status: inert`, `wired_via: ''`), a single run with no wall-clock brake. Instrument: `grep -rn "sticky_stop_state" operator/ src/ migrations/`; `git grep -n "UPDATE sticky_stop_state\|HARD_STOP" 5770b79f -- '*.py'` in the overlay clone. Falsifier: one non-test `record_tool_failure(` call site in ss-console. Recommendation: the Captain's wire-or-delete call is now a delete-or-enrol call: retire the ss-console copy, or add it to `overlay-pairs.json` the way `escalation_ledger.py` is.

4. **[MEDIUM] The tracker-skill duplication moved.** The pair measured last time (client-verification-tracker and medical-records-chaser) is down from 71% to 16% shared function-lines. A new pair, medical-records-chaser and lien-ledger-tracker, shares 48% (~214 lines), including nine byte-identical infrastructure helpers (`_emit_suppress`, `_find_skill_settings`, `_handoff_values`, `_is_iso_day`, `_load_ledger_module`, `_plan_counts`, `_pos_int`, `_pos_int_or_none`, `_write_pre_run_handoff`). The "cannot split because the scheduler stages `pre_run.py` alone" rationale no longer holds for these: the two skills that did split use `_load_sibling_module` (`skills/client-verification-tracker/pre_run.py:301-320`), which falls back to `/app/skills/<skill>`, and `templates/Dockerfile:1115` copies `operator/skills/` there. Instrument: AST plus token-similarity over the four `pre_run.py` files. Falsifier: `/app/skills/` absent from the image. Recommendation: give the two remaining skills the sibling loader and extract the nine helpers.

5. **[MEDIUM] Three vendored-copy gates use hand-maintained membership tuples, and one twin pair has no gate.** Byte-identical non-test duplicates: `escalation_ledger.py` x6 (gated by `VENDORED_SKILLS` tuple), `routing.py` x2 (hardcoded skill list), `pre_run_gate.py` x9 (`GATED_SKILLS` tuple). None discovers by glob. The class has already produced a live instance: `skills/deadline-miss-escalator/broker_writer.py` and `skills/client-verification-tracker/broker_writer.py` are 133 lines each and differ by one docstring word (`diff` output is a single hunk, "escalator" vs "chase"), pinned by nothing (I5, still open). Instrument: md5 census of non-test `.py` under `operator/`; `diff` of the pair; `grep -n "glob\|rglob"` in each sync test (none). Recommendation: discover copies by hashing the canonical and globbing `skills/*/<name>.py`; closes all four cases.

6. **[MEDIUM] `bin/lib/decommission.py` (1,060 lines) has five of six destructive backends unimplemented, and the shell wrapper does not recognize its refusal code.** `unwired_destructive_backends()` (`:686-716`) checks six; the shipped CLI injects exactly one (`decommission_cli.py:243`, the seam audit preserver). `r2_deleter`, `vectorize_deleter`, `agentmail`, `fly`, `observability` are injected only in `bin/tests/test_decommission.py`. A `--live` run refuses at exit 5 unless `--allow-unwired`. That is fail-closed and correct, and it is why this has stayed invisible. But `bin/decommission-customer.sh:69-74` has `case` arms for 0, 2, 3, 4 and 130 only, so the documented entry point logs `unknown exit 5` for the one refusal the pipeline is designed to produce. The venture's "gone means gone" doctrine names this pipeline as the mechanism, and a seat retirement on 2026-08-25 was done by hand. Instrument: read of both files; `grep -rn "r2_deleter\|vectorize_deleter\|fly=\|agentmail=" --include='*.py' operator/` (hits under `bin/tests/` only). Recommendation: add the exit-5 arm now; confirm with the Captain whether the five backends are intended work or superseded by the manual runbook.

7. **[MEDIUM] `adapter/` (8,295 non-test lines) is reached at boot and by CLIs, never per request, and two modules have no importer at all.** `bootstrap.sh:807` runs `/app/safety-substrate/run_invariants.py` at every container start and those invariants import `adapter.audit_log`, so reachability is real and the last review understated it. Beyond that, importers are four Captain CLIs. Zero-importer modules: `adapter/inbound_envelope.py` (298 lines, the ss-console re-implementation of ADR 0027; the live one is the overlay's `hermes-smd-webhook-router`) and `adapter/resolve_skill_pins.py` (108 lines, whose lookup rules are re-implemented in `scripts/validate-customer-yaml.ts`, the position that got `adapter/validate_customer_yaml.py` retired). Instrument: repo-wide grep for both names excluding self and tests (orchestrator re-ran: only two comment mentions in `scripts/validate-customer-yaml.ts`). Recommendation: enrol or retire, on the `validate_customer_yaml.py` precedent.

8. **[MEDIUM] Two route files are the domain layer, and three library files sit at exactly 499 logical lines.** `src/pages/api/booking/reserve.ts` holds 11 functions including a multi-table D1 transaction and a Google Calendar sync; `src/pages/api/internal/heartbeat.ts` holds ~20 field parsers and two raw upserts to a `fleet_status` table with no `src/lib/db/` module. `src/pages/api/webhooks/stripe.ts` delegates to `src/lib/webhooks/*-handler.ts` and is the shape the others should follow. Separately, `src/lib/operator/customer-yaml/types.ts` (1,431 raw), `src/lib/sow/service-finalize.ts`, and `src/lib/webhooks/stripe-subscription-handler.ts` each measure 499 logical lines against a 500 ceiling, and `src/pages/portal/products/operator/[instance]/settings/index.astro` measures 498. Three files landing one under the ceiling is the ceiling being met by trimming rather than decomposition. Instrument: orchestrator's own blank-and-comment-stripped count, agreeing with the agent's ESLint count. Recommendation: extract the booking commit and the heartbeat parsers to `src/lib/`; either add a reasoned exemption for pure type modules or split `types.ts`.

9. **[MEDIUM] `workers/fleet-alerts` deploys with no CI build gate.** `.github/workflows/deploy.yml:105-112` deploys it; `.github/workflows/verify.yml:92-98` dry-run builds only cost-anomaly and cost-telemetry under a comment whose rationale ("own wrangler configs + live consumers") applies equally to fleet-alerts, the heartbeat-red / HARD_STOP pager. Its unit tests do run via `test:workers`; the gap is a wrangler config break surfacing at deploy time, on the path whose job is to page when the fleet is down. Instrument: `grep -ln fleet-alerts .github/workflows/*.yml` (deploy.yml only). Recommendation: add the third dry-run step.

10. **[MEDIUM] `adapter/evidence/packet.py` is a god object; `workspace_broker/establishment_store.py` is a four-lifecycle facade.** `packet.py` (1,652 raw / 1,128 logical) carries request validation, chain-pin verification, a 169-line audit-coverage class, a `ReadExecutor` abstraction, YAML redaction and secret scanning, six `_fetch_*` data-access functions, three serializers, a 225-line prose renderer, and a 206-line `build()`. Five unrelated responsibilities. `establishment_store.py` (2,107 / 1,415) is one class with 40 methods hosting four independent state machines, already split once on 2026-08-24; the tell is four near-identical private refusal-namers. The other large files read (smokeball `server.py`, `voice/transform.py`, the two tracker `pre_run.py`, `decommission.py`, broker `server.py` outside `handle()`) are large-but-cohesive leaves. The module-size ratchet (`operator/contracts/operator-module-size.json`) currently baselines 16 modules over 500 logical lines and only tightens; 45 exceed 500 raw.

11. **[LOW] Two shell scripts over 1,000 lines sit outside every size ceiling.** `bin/provision-customer.sh` (1,369) and `templates/entrypoint.sh` (1,309), plus `bootstrap.sh` (916). All structured (`set -euo pipefail`, functions), shellcheck-clean at warning severity. The ratchet walks `*.py` only.

12. **[LOW] Six of ten directories under `workers/` are untracked build residue from workers deleted in June and July.** `git ls-files` returns zero for `enrichment-workflow`, `job-monitor`, `new-business`, `review-mining`, `scan-workflow`, `social-listening`; each holds only `node_modules`/`dist`. Not a repo defect; the developer-machine layer of a removal. Recommendation: an `rm -rf` line in the retirement runbook.

13. **[POSITIVE, verified] No package cycles in `operator/`, no runtime cycles in `src/`, zero dead Python modules, every new `src/` module wired.** The operator agent withdrew a first result of nine cycles (all basename collisions, e.g. `adapter.evidence` matching `bin/lib/evidence.py`). `npx madge --circular` reports four cycles in `src/`, all `import type` back-edges, erased at compile time. Of 245 non-test Python modules, five had zero textual references; all five are hand-run `argparse` entry points with a docstring stating why. All 20 non-test modules added under `src/` in the window have a production referrer.

14. **[POSITIVE, verified] The medchron runner is the strongest architecture in the tree, and the registries are honest.** `runners/medchron/medchron/dag.py:82-185` is an explicit 40-stage tuple with declared `requires`, `paid`, `scope`, `invalidates`, and an `exit_map` binding each non-zero exit to a client-legible terminal state. `contracts/runtime-controls.yaml` carries 20 controls (`enforced` 12, `unprobed` 7, `inert` 1), every overlay-side `wired_via` resolves at the pinned ref, and `bin/tests/test_runtime_control_conformance.py` enforces the registry's own shape. `tests/shipped-customer-configs.test.ts` discovers live seats by `readdirSync` and validates each (I13 closed).

**Residual on the closed CRITICAL [MEDIUM].** `tests/operator-taint-gate.test.ts` pins the class tuple against the enum in this repo. The cross-repo half is a hand check: `operator/adapter/trust_ceiling.py:203-210` and the overlay's `plugins/hermes-smd-trust/enforce.py:87-97` at 5770b79f both list exactly the same seven classes today, and nothing in CI asserts they continue to. `verify-overlay-pairs.py` already fetches the overlay at the pinned ref in `operator-substrate.yml` and is the place to add the assertion.

**Grade: C (up from D).** Rubric: "3+ files over 500 lines OR unclear boundaries OR mixed concerns in route handlers." All three hold. Not D: the CRITICAL that anchored D is closed with a real test, the sticky-stop exposure is live in the overlay rather than absent, boundaries are discernible and mostly followed, there are no cycles, and the ratchet only tightens.

### 2. Security

**Carried findings.** Both HIGHs are **closed**: the booking manage-token leak (fixed 08-24, still closed; `src/layouts/Base.astro:57-64` now holds the tracker inside the `disableAnalytics` guard) and missing security headers (closed at the runtime layer, finding 1). **Still open, unchanged:** `/api/events` rate-limit bypass via client `session_id` (C3); gitleaks `--no-git` working-tree-only scan despite `fetch-depth: 0` (C4); fresh `createRemoteJWKSet` per request at `src/lib/operator/mcp/token-validation.ts:62-71` (C5); JIT MCP identity returning the Clerk subject as `localUserId` at `mcp-route.ts:137-146` (C6); the six LOWs (audience enforced only when present, documented; booking limiter allowing when KV undefined at `rate-limit.ts:48-51`; raw upstream body logged at `integrations.ts:255-256`; client email logged on Clerk invitation failure at `invitations.ts:150`; `ss_sid` analytics cookie without HttpOnly, by design; unescaped filename in `Content-Disposition` at `transcript.ts:35,40`); no Python security lint in Semgrep (C9, packs are `p/typescript`, `p/javascript`, `p/security-audit`, `p/owasp-top-ten`); the two unintended public helper modules; the dead OAuth callback. I12b: `FLY_API_TOKEN` is present on the deployed `ss-web` Worker per `wrangler secret list` (name only), so the token store's fall-open path is not the live path.

1. **[POSITIVE, verified] Security headers are live on all three hosts.** Orchestrator's own `curl -sI` on smd.services, admin.smd.services, portal.smd.services: each returns `strict-transport-security: max-age=31536000; includeSubDomains`, `content-security-policy: frame-ancestors 'none'`, `x-frame-options: DENY`, `x-content-type-options: nosniff`, `referrer-policy: strict-origin-when-cross-origin`. The admin and portal probes returned 302s and carried the full set, so the early-return path is covered, which is the correct wiring point (`src/middleware.ts:247-250` wraps `handleRequest`). No resource CSP, no `Permissions-Policy`, no HSTS `preload`; each absence carries its reason at `src/lib/security/response-headers.ts:12-24,54-55` (SignWell iframe and Clerk's bundle break under a blind `script-src`; preload is irreversible and the Captain's call). Pinned by `tests/security-headers.test.ts`.

2. **[MEDIUM] A portal route that forgets its tenant helper is a cross-tenant read, and nothing enumerates the set.** `enforcePortalAuth` (`src/middleware.ts:144-162`, orchestrator read) returns allow for any request carrying a Clerk `userId`, with no role or tenant check; tenant binding happens per route in `getPortalClient`, `resolveOperatorAccess`, `resolveHostedAgentAccess`, or `authorizeAdvancedSettings`. Today 19 of 20 files under `src/pages/api/portal/**` call one of those and the 20th, `settings/trust-ceiling.ts`, is an 11-line 410 tombstone. Both routes added in the window are scoped. Admin has a mechanical enumerator (`tests/admin-routes-require-session.test.ts`); portal has none. Falsifier: a portal route with no helper after reading it (two greps produced 30 and then 25 false candidates because the helper vocabulary is not a fixed list; reading each resolved all of them). Recommendation: clone the admin enumerator with an `EXEMPT` entry for the tombstone.

3. **[MEDIUM] `/api/events` is an unauthenticated, effectively unbounded D1 write.** `resolveSessionId` (`src/pages/api/events.ts:65-73`) prefers the client-supplied `body.session_id` whenever it matches a UUID pattern, `checkRateLimit` (`:286`) counts rows for that id and fails open on D1 error, and unlike `/api/contact`, `/api/intake/send`, `/api/booking/reserve`, `/api/auth/magic-link` and the `/api/assessment/*` routes, this one calls no `rateLimitByIp` (orchestrator grep: zero hits in the file). Carried C3, unchanged. Recommendation: `rateLimitByIp` ahead of the session-keyed bucket.

4. **[MEDIUM] Gitleaks discards history on a public repository.** `.github/workflows/security.yml` checks out with `fetch-depth: 0` then runs `gitleaks detect --source . --no-git`. A secret committed and later removed is invisible to it. Carried C4, unchanged. The repo is public by decision (2026-08 visibility decision), which raises the cost of this gap.

5. **[LOW] The Sentry webhook's replay window is bypassable with a non-numeric timestamp.** `src/pages/api/webhooks/sentry.ts:64-66` guards the staleness check with `if (Number.isFinite(timestampSec))`; a header of `abc` skips it. The HMAC (`:135-152`) is over `rawBody` only, so the timestamp is not bound into the signature. Blast radius is bounded: the handler upserts one `cost_anomaly_alerts` row keyed `(entity_id, alert_date, driver)`. Stripe does this correctly (`stripe.ts:400` signs `${timestamp}.${body}`). Recommendation: reject non-finite timestamps.

6. **[LOW] A `# noqa` justification that covers the safe half of the same f-string.** `operator/bin/lib/seam_pull.py:218` justifies S608 with "table from MEMORY_EXPORT_TABLES", which is true; the same statement interpolates `col_defs`, built at `:216` from the keys of a JSON body served by the seat, into a quoted-identifier list, and line 220's `# noqa: S608` carries no justification. Reaching it means already controlling the Machine. Recorded as the review theme in miniature. The nosemgrep-justification gate in `security.yml` covers `*.ts`/`*.js` only, so Python suppressions have no equivalent. Recommendation: reject keys failing `str.isidentifier()`; add a Python `noqa` justification gate.

7. **[LOW] CSRF rests on `SameSite=Lax` alone.** Both SS-owned cookies are correctly attributed (`src/lib/auth/session.ts:251` `HttpOnly; Secure; SameSite=Lax`; the change-request flash cookie matches). `SameSite=Lax` blocks cross-site form POSTs in current browsers, and there is no write-on-GET anywhere (every admin and portal `GET` export scanned for `INSERT`/`UPDATE`/`DELETE`: zero). But there is no `Origin`/`Sec-Fetch-Site` check on any state-changing route and no test pins the cookie attributes. Recommendation: defence-in-depth origin check on state-changing POSTs; a cookie-attribute test.

8. **[POSITIVE, verified] Every API route's gate is accounted for.** All 48 files under `src/pages/api/admin/**` carry `requireAdminSession`. All 20 portal routes per finding 2. The three `src/pages/api/internal/*` routes gate on `verifyMachineRequest`. The public set: `/api/health`; `/api/contact`, `/api/intake`, `/api/intake/send`, `/api/booking/{slots,reserve}` (all `rateLimitByIp`); `/api/booking/manage/[token]*` (the token is the auth, hashed before lookup, expiry-checked); `/api/assessment/*` (all `rateLimitByIp`); `/api/auth/*` and `/api/oauth/callback`; `/api/webhooks/*` (signature-verified); `/api/mcp` (410 tombstone); `/api/operator/[customer]/mcp` (gated inside `handleMcpPost`); `/api/checkout/hosted-agent` (303s to sign-up when no client). Instrument: `git ls-files src/pages/api` (99 files) and reading every file a helper scan could not classify.

9. **[POSITIVE, verified] Webhook verification is uniformly correct.** Stripe: parsed signature, 300s tolerance, `${timestamp}.${body}`, constant-time compare, verified before parse. Resend: full Svix scheme with `svix-id` as the idempotency key. SignWell: HMAC over event fields with an explicit note that parse must precede verify because the hash lives in the body, plus staleness. Healthchecks.io: shared bearer, constant-time. Idempotency on the money paths is real (`stripe_checkout_orders` with `INSERT OR IGNORE` and a `processed` short-circuit in both checkout handlers).

10. **[POSITIVE, verified] Injection surface clean on both languages.** Nine `.prepare(` template-literal sites, all interpolating code-controlled constants (placeholder lists, column-name constants, condition arrays assembled from literals with every value bound). Zero `dangerouslySetInnerHTML`, `eval(`, `new Function(`; all 8 `set:html` sites pass through `serializeJsonLd`, which escapes `<`. Python: 0 `shell=True`, 0 `os.system(`, 0 `pickle`, 0 unsafe `yaml.load` against 63 `safe_load` sites; the one `exec(` (`medchron/seat.py:250`) runs a constant base64 script through list-form `subprocess.run`. `operator/bin/lib/console_d1.py:49-67` is the model of the class: because `wrangler d1 execute` has no parameter binding, it emits `CAST(x'<hex>' AS TEXT)` rather than doubling quotes, and its docstring names the threat.

11. **[POSITIVE, verified] Secrets clean; the mirrored overlay contract is byte-identical at the pinned ref.** The shape-based sweep returns only documentation, scrubber regex constants, and the deliberate planted fixture at `tests/provisioning.test.ts:177`. The one tracked `.pem` is a public key. `.dev.vars` is untracked. `operator/contracts/gate-coverage-snapshot.overlay.json` is byte-identical to the overlay's `tests/contract/seat_gate_binding_snapshot.json` at 5770b79f, which is the tip of the overlay's `origin/main`. Method note: the agent's first diff reported a divergence because the local overlay checkout sat on a feature branch; re-probing against the pinned blob resolved it. A stale working copy nearly produced a confident, wrong cross-repo finding, the same lesson as the 08-23 handoff incident.

12. **[POSITIVE, verified] Operator container privilege model.** The Dockerfile has no `USER` directive; PID 1 is root deliberately, and `entrypoint.sh:10` asserts it, then builds the uid/gid separation (`hermes`, `workspace-broker`, `medchron`; `audit-readers`, `smokeball-token`; `2750` audit dir, `0640` ledger) and verifies it with `setpriv` at `:242`. No `curl | sh`, no `set -x`, no secret echoed. The residual is that "root by default" is a property of the image rather than a stated contract.

13. **[NOTE] Semgrep is diff-aware on PRs**, baselining to the merge-base, so pre-existing findings never block; the daily cron and manual dispatch run full audits and neither is a gate. Deliberate and documented; recorded so the coverage claim is read at its real strength.

**Not checked:** Clerk's own `__session` cookie attributes (vendor bundle); the `FLY_API_TOKEN` value; R2/D1 access policy in the Cloudflare dashboard; the overlay's `enforce.py` source beyond the class-set comparison; live behaviour of any authenticated endpoint.

**Grade: C (up from D).** Rubric: "any medium OR 3+ low." Five mediums stand (events write, gitleaks history, JWKS per request, JIT attribution, no Python security lint), none escalated. Not B: the mediums have now survived two reviews.

### 3. Code Quality

**Carried findings.** Coverage thresholds declared but never run: **closed, exemplary** (`vitest.config.ts:57-84` removes them with the reason, and `tests/coverage-config-honesty.test.ts` fails if they return without a runner). The 261 `noqa` for an unconfigured linter: **partly closed, ratio worse** (finding 3). No Python size ceiling: **half closed** (file ceiling exists and gates; function and complexity ceilings do not). **Still open:** knip counting `tests/**` as project (now sized, finding 2); the `@public` on `cleanupExpiredHolds` naming `workers/booking-cleanup` (finding 1); `yaml` undeclared (finding 7); the ten knip categories `off` (finding 4); `captureError` adoption at 3 files (denominator grew from 94 to 156); `middleware.ts:90` `renewSession` without `waitUntil`; `conformance.ts:17` naming `runConformanceSuite`; ESLint at exactly 13 of 13 warnings (orchestrator re-ran: 13 warnings, 0 errors, rules `no-unsafe-assignment` 9 and `no-unsafe-member-access` 4).

1. **[HIGH] The `@public` waiver is the only dead-export escape hatch, and its reasons are unverified prose; three of fifteen are false at HEAD and six rest on source-text greps.** `knip.jsonc` states the rule: an export that must survive without an importer carries `@public` with the reason beside it. Orchestrator count: 15 tags in non-test `src/`. `src/lib/booking/holds.ts:106` says "Called by the daily cleanup cron at `workers/booking-cleanup/`"; `workers/` holds cost-anomaly, cost-telemetry, fleet-alerts and never held booking-cleanup. `src/lib/operator/customer-yaml/sections-seat.ts:104` says callers of `isCustomerSeat` "live in the overlay and in provisioning tooling"; the overlay contains zero `.ts` files (orchestrator: `find ~/dev/hermes-smd-overlay -name '*.ts'` outside node_modules returns 0) and no provisioning script references it. `src/lib/portal/formatters.ts:32` says the invoice page wraps a local variant of `formatRelativeDueCaption`; that page has none and composes its caption inline. Six tags (`getDocument`, `getTranscriptUrl`, `ASSESSMENT_STATUSES`, `TIME_ENTRY_CATEGORIES`, `MILESTONE_STATUSES`, `bulkCreateMilestones`) cite tests that read the defining file as a string and assert the export line exists; the assertion is the text of the line the tag sits above. `LEGACY_PROBLEM_LABELS` is kept "so historical rows stay renderable" and nothing renders them. `INVOICE_TYPES` is the counter-example, genuinely imported by `IssueInvoiceForm.astro`, so the check distinguishes. Recommendation: require the tag to name a path; add a test that the path exists and contains the symbol; delete or wire the four dead ones.

2. **[HIGH] 39 exports have no referrer outside test files, confirmed by two independent methods, and the gate reports zero.** `npx knip` (what `npm run deadcode` runs) prints nothing. With `tests/**` removed from `project` and from the vitest plugin's entry set, knip reports 58 unused exports; a token index over 1,653 files reports 198 test-only exports; the two agree on 39. Ten were read (`createMeeting`, `listTimeEntries`, `updateServiceStatus`, `getServicesForEntity`, `listOAuthProviderSlugs`, `jaroWinklerSimilarity`, `FAST_MODEL`, `templateHygieneViolations`, `buildManualExtractionPrompt`, `loadHomeFeeds`) and every one has only its definition as a repo hit outside tests; none is re-exported via a barrel, used in `.astro` frontmatter, or referenced by string. `updateServiceStatus` and `getServicesForEntity` mean the services state machine's write path has no caller. One caveat: a live peer worktree on the billing surface holds copies of `services.ts`, `time-entries.ts` and `meetings.ts`, so some of the 39 may be about to acquire callers. Recommendation: move `tests/**` to a test-only workspace; triage the 39.

3. **[HIGH] `noqa` annotations grew 50% and the inert share got worse; no type checker runs over 448 Python files; no function or complexity ceiling covers Python.** Orchestrator census: 392 `noqa` across 166 files (was 261 across 121). `operator/ruff.toml` selects `E4`, `E7`, `E9`, `S102`; 132 annotations are activated, 260 are inert, led by 209 `BLE001` for a rule the config itself calls "the cheapest next step" (uncovered BLE001 sites went 5 to 11 since 08-24). `uvx ruff@0.15.17 check .` passes clean, so the gate that exists is real. `grep -rn "mypy\|pyright\|pytype" .github/ operator/ruff.toml package.json` returns only the sentence in `ruff.toml` saying no mypy config exists; 2,955 of 4,732 defs are annotated and nothing verifies any of it. AST census over 2,572 non-test functions: 99 exceed 75 lines, 91 exceed complexity 15, versus TS-side error-level rules at 75 and 15 with zero violations. Worst: `workspace_broker/server.py:363 handle` (313 lines, cx 63), `medchron/stages/billing_chart.py:126 run` (cx 52), `adapter/evidence/packet.py:994 _readme_text` (225 lines). Recommendation: enable `BLE001` with a baseline; add `max-complexity`; run pyright in `operator-substrate`.

4. **[MEDIUM] `knip.jsonc` claims ten categories are "reported but non-blocking"; `off` means excluded from the report, and the stated reason for disabling `types` is false.** Orchestrator: `npm run deadcode` prints nothing; `npx knip --include files --no-exit-code` reports 15 unused files (`PromotionCard.astro`, `PromotionCardList.astro`, `AlivenessHeader.astro`, `ConnectorStatusSection.astro`, `SkillTogglesSection.astro`, `PackCardGrid.astro`, `StatusDot.astro`, `OperatorScope.astro`, `facet-doors.ts`, `facets/contract.ts`, three barrel `index.ts` files, two hook lib files); `npx knip --include types --no-exit-code` prints nothing, so the comment that enabling `types` "would force either a mass un-export or a mass ignore" costs zero today. Recommendation: `off` to `warn` for the ten; enable `types`.

5. **[MEDIUM] Byte-identical helper duplication in `src/`, including five copies of the function that addresses the live Operator Machine.** Orchestrator confirmed `function machineBaseUrl` in five files (`src/lib/admin/sticky-stop-clear.ts`, `src/lib/operator/runtime-read-transport.ts`, `src/lib/operator/mcp/webhook-transport.ts`, `src/lib/portal/operator/pause-control.ts`, `src/lib/portal/operator/entitlement-change.ts`) and `function escapeHtml` in seven, one of them the canonical `src/lib/api/helpers.ts:24` whose header says it is the dedup outcome of the 2026-06-12 review. `jsonError` 8, `isRecord` 8, `redirectWithStatus` 11, `back` 8, `trimString` 3, `stripeHeaders` 3; a `base64UrlEncode`/`base64UrlDecode`/`importSigningKey` HMAC toolkit in four modules (`assessment/session.ts`, `booking/signed-link.ts`, `booking/tokens.ts`, `oauth/state.ts`) whose signing parameters were not compared. The `no-restricted-syntax` rule that blocks re-declaring `jsonResponse` covers only that name. Recommendation: `machineBaseUrl` to `src/lib/operator/machine-url.ts`; extend the guard to `escapeHtml`, `trimString`, `jsonError`, `isRecord`; the HMAC trio to `src/lib/crypto/`.

6. **[MEDIUM] Error handling: `captureError` is imported by 3 of 156 files with a catch, and the one middleware write path both swallows and may never run.** `src/middleware.ts:90` is `renewSession(env.DB, env.SESSIONS, token, sessionData).catch(() => {})` (orchestrator read): no `waitUntil`, so Workers may cancel it when the response returns, and the error is discarded. The repo already has the mechanism: `src/lib/portal/session.ts:53` passes `locals.cfContext.waitUntil` into `stampLoginIfNewSession`, and `reserve.ts:538` and `intake/send.ts:159` do the same. Middleware is the one place that does not. Python side: of 535 non-test `except` handlers, 184 log or re-raise, 303 degrade while doing real work, 48 are silent, and only 21 are broad-and-silent (4%); the inconsistency is one class, the wake-observability write swallowed with no log at four skills' `pre_run.py` sites with the identical comment. Recommendation: `waitUntil` plus `captureError` on the middleware line; one `log.warning` in the shared helper once it is extracted.

7. **[MEDIUM] `yaml` is a phantom dependency of a merge-gate script.** Orchestrator: absent from `dependencies`, `devDependencies`, `peerDependencies`; present only in `overrides` (2.8.3), which installs nothing; 28 importers. It resolves only as a transitive of `@astrojs/check`, `@astrojs/cloudflare` via vite, and `knip`. Blast radius verified as CI, not runtime: `send-render.ts` is reached only by `scripts/validate-customer-yaml.ts` and tests, never from `src/pages`, so nothing bundles it into the Worker. A hoisting change breaks the customer.yaml merge gate. Downgraded from the prior review's HIGH on that basis. Recommendation: `devDependencies`.

8. **[MEDIUM] `shellcheck` never runs in CI, and five workflows carry `# shellcheck disable=SC2086` directives that suppress nothing.** `unaudited-send-reconcile.yml:171`, `terminal-state-reconcile.yml:100`, `cron-slot-watchdog.yml:101`, `control-probes.yml:93`, `audit-chain-verify.yml:133`. `shellcheck -f gcc` over all 16 `operator/**/*.sh` yields 7 findings (1 error on the generated `_env-arrays.generated.sh`, 2 warnings, 4 notes). The tree is clean and nothing keeps it so. Recommendation: one shellcheck step at `--severity=warning`, generated file excluded.

9. **[LOW] The 13-warning lint ceiling is one fixable pattern and has not ratcheted since 2026-08-09.** All 13 are `no-unsafe-*` from `const parsed = JSON.parse(raw)` yielding `any`, eight of them in `src/lib/db/quotes.ts:201-245`; the surrounding code already validates with type predicates. Annotating `const parsed: unknown` closes the class and the ceiling drops to 0.

10. **[LOW] Three skills' `SKILL.md` frontmatter lacks the `metadata.smd` block the other 55 carry** (`email-reply`, `inbox-triage`, `workspace`), and `operator/bin/tests/test_skill_frontmatter.py:41-64` cannot see it. The retired-seat references those three carried (I1b) are gone (orchestrator grep: zero).

11. **[LOW] `bin/decommission-customer.sh` and its CLI disagree about the exit-code contract** (Architecture finding 6).

12. **[LOW] `src/lib/operator/capabilities/conformance.ts:17` still names `runConformanceSuite`, which does not exist.** Carried, unchanged.

13. **[POSITIVE, verified] Type discipline in `src/` is strong.** Strict on; zero `any` in non-test `src/` (every grep hit read: all English prose); zero `@ts-ignore`, zero `@ts-expect-error`; nine `as unknown as` sites, all narrowing after validation or WebCrypto `ArrayBuffer` workarounds; `npm run typecheck` 0 errors over 986 files; zero `locals.runtime`; 172 files import `env` from `cloudflare:workers`; the only module-level mutable binding in Worker code is the memoized WASM init at `src/lib/pdf/render.ts:36`; zero empty catch blocks; file naming consistent. Note: TypeScript is on Astro's `strict`, not `strictest` (`noUncheckedIndexedAccess`, `exactOptionalPropertyTypes` off); the code partly writes to the stricter standard by hand.

14. **[POSITIVE, verified] The module-size ratchet is a real merge gate.** `tests/operator-module-size.test.ts` counts logical lines with a tracker that survives triple-quote state, regenerates and enforces from the same counter, ratchets in both directions, and self-tests. Baseline 16 modules, ceiling 500, top entry `establishment_store.py` at 1,415. It runs inside the one required context deliberately. A correction to its own comment: line 18 says PR #2526 "merged with the substrate suite red"; `gh run list` shows the branch red at 3fad83b8 (18:05), green at ed4c9183 (18:18), merged at 18:24 against the green head. The structural exposure is real; that instance is not.

**Grade: C (stable).** Rubric: "3+ dead exports OR inconsistent error handling OR notable DRY." All three hold on the TS side; DRY and one error-handling class hold on the Python side. Not D: no `any`, swallowed errors are one line in `src/` and 4% of handlers in `operator/`, and the dead modules are one feature cluster plus two adapter files rather than a pervasive condition. Not B: finding 1 is the escape hatch of the only dead-code gate and its reasons are false.

### 4. Testing

**Carried findings.** `VERTICAL_FLOORS` empty-table tests: **closed correctly** (`tests/operator-configure.test.ts:30-42` and `tests/config-governance.test.ts:125-132,177-182` inject a synthetic `floored-test-vertical`, assert the machinery resolves it, and delete it in a `finally`). Coverage thresholds never evaluated: **closed honestly** (Code Quality carried). `operator-substrate` not required: **still open** (finding 2). `src/lib/db/assessments.ts` zero coverage: **still open** (finding 3). The 09-04 wave fixes A1/A2/A3/B3: **confirmed behaviourally pinned** (finding 1).

1. **[POSITIVE, verified] The tests pinning the 09-04 money-path fixes are genuinely behavioural and would fail on revert.** A1 (`invoice.paid` before `checkout.session.completed`): `tests/webhooks/stripe-subscription-linkage.test.ts:343-420` creates a miniflare D1, runs the real numeric migrations, seeds rows, POSTs a signed event through the actual webhook route, and asserts the resulting D1 row is `{ status: 'active', stripe_subscription_id }` for both the dahlia metadata shape and the pre-basil shape. A2 (`payment_status` gating go-live): `tests/operator-go-live-billing.test.ts:428-490` drives the handler with `unpaid`, `no_payment_required`, `paid` and asserts promotion only on the third. A3: `tests/r2-key-collision.test.ts` has nine cases including two keys whose filenames sanitize identically. B3: `operator/bin/tests/test_send_verify.py:350` asserts the unlabelled case (`skill_name == ""`) with a comment recording that the broker never wrote the column, alongside `:364` for the labelled case, so the prior concern (fixtures hand-authoring a field production never writes) is addressed by testing the pair.

2. **[MEDIUM] The gate over the Python half is advisory.** Orchestrator: `gh api repos/venturecrane/ss-console/branches/main/protection` returns contexts `["Typecheck, Lint, Format, Test"]`, `required_approving_review_count: 0`, `enforce_admins: false`; the ruleset adds `Security Summary` and a pull-request rule with zero required approvals. `operator-substrate` (ruff, 2,486 pytest tests, the SEC-32 overlay runtime-hash drift gate, connector conformance, medchron runner suite) appears in neither. Every Python-side guard credited in two reviews is enforced by a workflow that cannot block a merge.

3. **[MEDIUM] A 216-line test file stands in for coverage of the assessment data layer and measures nothing about behaviour.** Orchestrator: `src/lib/db/assessments.ts` has eight production importers under `src/pages/api` and zero test importers. `tests/assessments.test.ts` has 50 `toContain` assertions of the form `expect(source()).toContain('export async function listAssessments')` against `readFileSync`. It cannot detect a dropped `org_id` predicate, an inverted `WHERE`, or an SQL typo. `tests/clients.test.ts:1-24` has the same shape. Recommendation: port the miniflare-D1 pattern from `tests/portal/tenant-scoping.cross-org.test.ts`.

4. **[MEDIUM] 124 of 324 test files never import their subject, and 95 of those assert on source text.** Many are legitimate policy or drift guards (`forbidden-strings`, `doctrine-integrity`, `handbook-integrity`, `adr-integrity`, `migration-numbering`, `workflow-permissions`, `gate-coverage-snapshot`, `operator-overlay-pairs`, `operator-module-size`, `typography-tokens`, `landing-page`), where source text is the subject. The set whose subject is runtime behaviour: `assessments`, `clients`, `contacts`, `engagements`, `invoices`, `milestones`, `quotes`, `follow-ups`, `time-entries`, `parking-lot`, `reply-log`, `signwell`, `contact-endpoint`, `portal-quotes`, `portal-docs`, `client-identity-gate`, `middleware` (mitigated by `middleware-behavior.test.ts`). Twenty of these files carry 127 `expect(source()).toContain('export ...')` assertions, and six `@public` waivers cite them as their contract, so they are load-bearing for the dead-export gate as well as weak as tests. Recommendation: a backlog, not a sweep; convert the ones whose module carries a tenant predicate first.

5. **[LOW] Nothing measures coverage in either language.** No workflow runs `vitest --coverage`; no `pytest-cov` or coverage config under `operator/`. Not a regression: the thresholds were removed deliberately with a guard against their silent return. Recorded so the absence is a known state.

6. **[LOW] Two Python tests are environment-dependent.** Running the suite as `operator-substrate.yml` does, in a clean `uv` venv: 2 failed, 2,486 passed, 105s. `adapter/evidence/tests/test_packet.py::test_build_emits_targz_with_every_expected_file` reads `EVIDENCE_PACKET_SIGNING_KEY_B64` from ambient `os.environ` without isolating it; with the variable unset, 33 pass. `bin/tests/test_decommission.py::test_cli_live_allow_unwired_runs_and_tombstones` makes a real outbound request in a unit suite and fails without network. Both pass in CI, where neither condition holds. Recommendation: `monkeypatch.delenv` in the packet test; stub the network step.

7. **[POSITIVE, verified] Both suites are large, fast, and green, and almost nothing is skipped.** `npx vitest run`: 321 files passed, 3 skipped; 5,236 tests passed, 5 skipped, 7 todo; 97s. Python: 2,486 passed in the CI-equivalent venv. Across 324 `.test.ts` files exactly two have zero `expect(` and both are pure `it.todo` placeholders; the single `expect(true).toBe(true)` sits inside a `describe.skip` with the reason stated. Python: 176 test files, 14 `skipif` markers all conditional on a real precondition, no `xfail`. Flakiness is handled structurally (no `retry` in vitest config; the one retry in CI wraps a transient GitHub call with the incident named).

8. **[NOTE] No browser or end-to-end suite exists.** `.playwright-mcp/` is an interactive session log directory, not a suite. Given the venture's "done means the client can do it" rule this is a real gap, but a build-something recommendation rather than a defect.

**Grade: C (stable).** Rubric: "significant gaps OR critical paths untested." `assessments.ts` and `src/lib/stripe/checkout.ts` have zero importing tests, and the Python gate is advisory. The direction is clearly improving: the newest work is the best-tested in the tree.

### 5. Dependencies

**Carried findings.** `npm audit` clean: **changed, regressed but time-boxed** (finding 1). Zero unused dependencies: **still true** (`npx knip --dependencies` clean). No dynamic-codegen dependency reachable from the Worker bundle: **still true**. `yaml` undeclared: **still open** (Code Quality 7). Seat-image Python installs unpinned: **still open, unchanged** (finding 2). `typescript` and `@cloudflare/workers-types` one major behind by caret: **still open, deliberate** (the dependabot config says so).

1. **[HIGH] `npm audit` at HEAD reports 12 vulnerabilities: 1 critical, 7 high, 3 moderate, 1 low, six of them allowlisted until 2026-09-23.** Orchestrator: `npm audit --json` metadata `{critical: 1, high: 7, moderate: 3, low: 1}`; packages `astro` (critical), `@astrojs/cloudflare`, `@cloudflare/vite-plugin`, `js-yaml`, `miniflare`, `sharp`, `svgo`, `wrangler` (high). `.github/audit-allowlist.json`, created 2026-09-09 and Captain-authorized per its own comment, tolerates six GHSAs (Astro AVIF RCE GHSA-26w7-cxv4-gfx2; Astro base-path authorization bypass GHSA-376h-93r7-7g6f; js-yaml; sharp/libheif; two SVGO) with `expires: 2026-09-23`, tracked in #2711 (open). The fix is blocked by `.npmrc` `min-release-age=7`, the repo's deliberate supply-chain cooldown; Astro 7.2.8 clears it before the expiry. `gh run list --workflow=security.yml` shows the last five runs green, so the mechanism is working as designed. **Reachability of the critical, verified by the orchestrator:** `astro.config.mjs:29` sets `imageService: 'passthrough'`; the built `dist/server` contains `image-passthrough-endpoint` and no `sharp` import (the three "sharp" string hits are English prose in content chunks); zero files under `src/` import `astro:assets`; zero tracked `.avif`/`.heic`/`.heif` files. The advisory's precondition, "an attacker can cause Astro to process an untrusted AVIF image", is not met at runtime or at build. The base-path bypass is rated moderate by npm and `base` is unset in `astro.config.mjs`. The grade follows the rubric ("high-severity audit findings"); the exposure is the expiry date and the cooldown, not the app. Recommendation: bump when 7.2.8 clears the cooldown; never extend the expiry.

2. **[HIGH] The seat image installs Python dependencies unpinned, and nothing audits Python dependencies anywhere.** Orchestrator: `operator/templates/Dockerfile:1017-1018` is `uv pip install --python /opt/workspace-broker/.venv/bin/python google-api-python-client google-auth pyyaml`, no version specifier. No `requirements*.txt`, `uv.lock` or root `pyproject.toml` under `operator/`; no pip or docker Dependabot ecosystem; no `pip-audit`, `uv audit`, `safety` or `bandit` in any workflow. Carried unchanged. Recommendation: pin, lock, and add a pip ecosystem to `.github/dependabot.yml`.

3. **[MEDIUM] `typescript` 6.0.3 vs 7.x and `@cloudflare/workers-types` one major behind**, both capped by caret and documented as deliberate.

4. **[POSITIVE, verified]** All roughly 40 unique GitHub Actions `uses:` lines are SHA-pinned with version comments and Dependabot maintains them. All workflows use `npm ci`, none `npm install`. No redundant dependency pairs (`date-fns` is a required peer of `date-fns-tz`; one validator, `zod`; the three workers carry zero production deps). The three workers with their own `package.json` trace their audit findings to the same allowlisted sharp advisory.

**Grade: D (down from C).** Rubric: "high-severity audit findings OR runtime-incompatible dependency." High-severity findings are present at HEAD, and the unpinned Python install is an independent supply-chain gap. Not F: the critical is verified unreachable and is time-boxed with a working gate. The drop is driven by advisories published this week against a fix the repo's own cooldown deliberately delays; the trend line to watch is 2026-09-23.

### 6. Documentation

**Carried findings.** CLAUDE.md accuracy: **changed, partially stale** (finding 1). No API docs: **still open, grown to 99 routes, tracking issue closed without delivery** (finding 2). Three false in-code claims: **still open, all three, plus a fourth** (finding 3). ADR index vs decision-stack mismatch: **still open, widened**. Handbook drift: **still open, with a concrete instance** (finding 4). 86 ADRs: now 87, indexed.

1. **[MEDIUM] CLAUDE.md's ADR range and decision count have drifted.** Orchestrator: `CLAUDE.md:305` says "Operator ADRs `0004-*.md` through `0061-*.md`"; the highest is `docs/adr/0087-chronology-package-content-control.md`. `CLAUDE.md:304` says "37 active decisions ... numbered through #55"; `docs/adr/decision-stack.md:26` says 38 through #56; `docs/adr/index.md:9` says 34 through #51. Three numbers for one fact. Everything else spot-checked in CLAUDE.md held: the three hostnames, `ADMIN_BASE_URL` strict with no fallback, `env` from `cloudflare:workers`, all seven named tests exist, all named hooks exist. `README.md:18` says "TypeScript 5" against `^6.0.3`.

2. **[MEDIUM] No API documentation for 99 route files, and the issue that tracked it was closed by policy, not delivery.** Orchestrator: `ls docs/api` returns "No such file or directory"; #2385 ("Stand up docs/api/README.md") shows CLOSED with the closing comment "Closed in the 2026-08-31 backlog reset ... closing is not a judgment the underlying condition is fake." The same reset closed #2384 (uptime poller); see Golden Path. The condition stands with no reminder. Recommendation: a route inventory generated from `src/pages/api` by a test that fails when a route lacks an entry, so the doc cannot drift.

3. **[MEDIUM] Four in-code documentation claims are false, and the family has a name.** `src/lib/booking/holds.ts:102-106` names `workers/booking-cleanup/`; `src/lib/db/oauth-states.ts:84` is a dangling section comment "Cleanup (optional, called by booking-cleanup cron)" with no function beneath it (orchestrator read); the worker never existed. `knip.jsonc:105-107` says ten categories are "reported"; they are not. `src/lib/operator/capabilities/conformance.ts:17` names `runConformanceSuite`, undefined. Add the three false `@public` justifications from Code Quality 1 and the module-size test's #2526 citation. One repo-wide grep for `booking-cleanup` plus one decision (build the cron or delete the comments) resolves the phantom-worker family.

4. **[MEDIUM] `docs/handbook/data-model.md` has not changed since 2026-07-03; eight migrations since (0105 to 0112) add `invoices.implementation_type`, `operator_agreement_documents`, `sticky_stop_reason`/`sticky_stop_condition`, and `send_refusals`, none mentioned.** A dated instance of what `npm run handbook:drift` reports on nearly every page.

5. **[LOW] `mcp__crane__crane_docs_drift_audit` audits the enterprise crane-console docs tree, not this repo.** Recorded so the next reviewer does not expect ss-console-scoped output.

6. **[POSITIVE]** 87 ADRs, numbered and indexed, with `tests/adr-integrity.test.ts` guarding numbering. `tests/handbook-integrity.test.ts` runs in `npm run verify`. Comment density on four sampled complex files ranged 7 to 32 percent of lines. The handbook's prose is accurate where spot-checked; the drift is in the reference pages, not the narrative ones.

**Grade: C (down from B).** Rubric: "no API documentation for a project with API endpoints." The rubric line is explicit and the prior review held B on the strength of the prose. What changed is that the tracking issue is gone and the condition is not, and the schema page is now demonstrably behind eight migrations.

### 7. Golden Path Compliance (Tier 2)

| Requirement | Status | Instrument |
|---|---|---|
| Source control, CLAUDE.md, TypeScript + ESLint, no hardcoded secrets (Tier 1) | Met | `git grep` shape sweep; `tsconfig` strict; `eslint.config.js`; CLAUDE.md present and mostly accurate |
| Error monitoring (Sentry) | Partial | Worker wrapper live; `captureError` in 3 of 156 catch-bearing files; Machine-side init is overlay-scoped per ADR 0023 |
| Full CI/CD | Partial | 20 workflows; deploy on merge; only `Typecheck, Lint, Format, Test` and `Security Summary` required |
| Branch protection | Partial | Status checks enforced; `required_approving_review_count: 0`; `enforce_admins: false`; force-push blocked; deletion blocked |
| Uptime monitoring | Partial | Push-based via healthchecks.io per ADR 0023 (`src/pages/api/webhooks/healthchecks.ts`, bearer-verified); no poller; #2384 closed by backlog reset |
| API docs | Missing | `docs/api/` absent; 99 route files |
| PWA | Missing (expected) | The compliance dashboard's own stated next action for SS |
| Secrets scanning | Partial | `.gitleaks.toml` present; scanner runs `--no-git` |

1. **[HIGH] Required checks cover the TypeScript half only.** Third review running. `operator-substrate`, `runtime-ac-proof`, and every other workflow are absent from both branch protection and the ruleset. Instrument: orchestrator's `gh api` calls, verbatim in Testing 2.
2. **[MEDIUM] Zero required reviews and admin bypass permitted.** Both the legacy protection and the ruleset carry `required_approving_review_count: 0`; `enforce_admins` is `false`. CLAUDE.md's own rule is "all changes through PRs."
3. **[MEDIUM] Sentry moves from Met to Partial on measurement**, unchanged from last review: the numerator is 3 and the denominator grew.
4. **[LOW]** The API-docs gap, counted once, in Documentation.

**Grade: C (stable).** Rubric: "3+ non-critical items missing." Four are Partial and one Missing at Tier 2. Not D: no critical Tier 2 requirement is absent outright; CI, Sentry, branch protection and monitoring all exist and function at reduced coverage.

## Model Convergence

Single model family (Claude), four agents with disjoint dimensions. Where scopes overlapped, findings converged independently: the required-checks set was pulled verbatim by three agents and the orchestrator and agreed to the character; the `booking-cleanup` phantom was found separately by the src agent (via `@public`) and the docs agent (via `oauth-states.ts`); the broker positional-authorization finding was raised by the operator agent from the architecture side and refined by the security agent from the enumeration side; the source-text-assertion tests were sized by the testing agent (95 files) and named as load-bearing for the dead-export gate by the src agent (127 assertions in 20 files, cited by six `@public` tags). The orchestrator re-derived every count that feeds a grade.

## Trend Analysis

Versus 2026-08-23 (Overall D+), across 124 commits and +66,635 / -7,963 lines in 476 files, 65% of the additions under `operator/`:

- **Architecture D to C.** The CRITICAL is closed by a test that derives its expectation from the enum. The sticky-stop HIGH was a mis-scoped finding: the control is live in the overlay; the ss-console copy is what is unwired. Broker authorization gained a declarative dispatch table in its newest verb group and a verb-registry test, and the older groups are still positional. Everything on the `src/` side carried unchanged.
- **Security D to C.** Both HIGHs closed, one of them verified at the runtime layer by the orchestrator. Five mediums carried unchanged. New findings are all LOW (a webhook timestamp guard, a half-justified `noqa`, CSRF single-layer).
- **Code Quality C, stable, with the instrument corrected.** The `@public` waiver is now sized (15 tags, 3 false, 6 on source-text tests); the test-only export count is agreed by two methods at 39; the knip `types` claim is falsified; the `noqa` pile grew from 261 to 392 with the inert share rising from 59% to 66%. Against that, the coverage-threshold finding was closed at the level of the control, ruff is a real gate, and the module-size ratchet is exemplary.
- **Testing C, stable, direction good.** The 09-04 money-path fixes are pinned by tests that run real migrations against miniflare D1 and assert on row state. The empty-table instrument was fixed by injection rather than deletion. The assessment data layer is still covered by a file that reads it as a string.
- **Dependencies C to D.** Driven by six advisories published this week against a fix the repo's own 7-day cooldown delays; the allowlist, the expiry, and the tracking issue are the mechanism working. The unpinned Python install is unchanged from two reviews ago.
- **Documentation B to C.** No new prose defect; the tracking issue for the one missing section was closed by policy and the schema page is measurably behind.
- **Golden Path C, stable.** Same four partials. The required-checks list has not moved in three reviews.

**Previous issue resolution.** Of the `source:code-review` issues open at the last review: #1623 (Astro 7 tail) delivered and closed; #2385 (API docs index) and #2384 (uptime poller) closed in the 2026-08-31 backlog reset without delivery. The 09-04 claims probe (`docs/reviews/claims-2026-09-04.probe.sh`), re-run at HEAD: 32 closed, 6 still open (F1, F9, I3, I5, I12, I12b), 0 regressed. Of the last review's five top action items: 1 closed (historical events rows probed clean), 2 closed (headers, live on three hosts), 3 closed (taint-gate test; cross-repo half still manual), 4 open (`operator-substrate` still not required), 5 partial (ruff shipped; knip categories still `off`).

**Method notes carried forward.** Four instruments produced confident wrong answers before a file was read, and each was caught: a helper-name grep called 30 gated portal routes ungated (the helper vocabulary is five names, not a fixed pattern); a cross-repo `diff` reported the overlay contract out of sync because the local overlay checkout sat on a feature branch (resolve cross-repo refs to the pinned SHA, never a working copy); a basename-matching import walk reported nine Python cycles that were all name collisions; and the module-size test's own comment about PR #2526 does not match the run history. The 08-23 review made the same class of correction twice. The next preamble should say: enumerate helpers before scanning; resolve cross-repo refs to a pinned SHA; read the hit before counting it.

## File Manifest

2,800+ tracked files; 510,090 tracked text lines; 337,640 source lines across `src/`, `operator/`, `scripts/`, `tests/` in TS, TSX, Astro, Python, shell and MJS. By area: `operator/` 218,887 lines, `src/` 113,850, `tests/` 73,408, `docs/` 56,902, `workers/` 14,929, `migrations/` 8,761, `scripts/` 4,944, `.github/` 3,363, `.claude/` 2,544. By extension: 884 `.md`, 776 `.ts`, 448 `.py`, 228 `.astro`, 163 `.sql`, 71 `.json`, 50 `.yaml`, 33 `.sh`, 21 `.yml`, 16 `.mjs`, 1 `.tsx`. 99 API route files. 324 TypeScript test files and 176 Python test files. 20 CI workflows, 2 of them required. Three deployed workers with tracked source (cost-anomaly, cost-telemetry, fleet-alerts); six untracked residue directories.

## Raw Model Outputs

The four agent reports are preserved verbatim in [code-review-2026-09-09-agent-reports.md](./code-review-2026-09-09-agent-reports.md): `src-arch-quality` (Opus), `operator-arch-quality` (Opus), `security-testing` (Opus), `deps-docs-goldenpath` (Sonnet). Codex review: skipped (Phase 1). Gemini review: skipped (Phase 1).
