---
title: Code Review 2026-09-10
---

# Code Review: SMD Services

**Date:** 2026-09-10
**Reviewer:** Claude Code (automated)
**Scope:** Full codebase
**Mode:** Full (Phase 1: Claude-only, multi-agent fan-out)
**Models Used:** Fable orchestrator (independent re-probe of every claim graded below), Opus agents (`src/` + `workers/`; `operator/`; Security + Testing), Sonnet agent (Dependencies, Documentation, Golden Path)
**Golden Path Tier:** 2
**Commit reviewed:** 1da55373 (== origin/main at review time)
**Window since last full review:** 0a3722a3..1da55373, 11 commits, 195 files, +5,950 / -10,074 lines. Nine of the eleven are the follow-through the Captain approved from the 2026-09-09 review; the other two are the memory-system fixes (#2724, #2727) and the retainer rail (#2730).
**Agent reports:** the four raw agent reports are preserved verbatim in [code-review-2026-09-10-agent-reports.md](./code-review-2026-09-10-agent-reports.md). Every claim graded below was re-derived by the orchestrator or checked against the agent's stated instrument; where the orchestrator's number differs from an agent's, the orchestrator's is used and the difference is noted.

## Summary

**Overall: C+** (up from C- on 2026-09-09).

This is a one-day re-grade after the remediation waves, and the waves worked at the layer that matters. Every one of the six top action items from yesterday is closed or moved. `substrate` is a required status check on `main` via ruleset 15555219, confirmed by the ruleset read, by `gh pr checks 2735`, and by the check run on 1da55373; 2,510 Python tests, ruff, connector conformance and the overlay hash gate can block a merge for the first time. `npm audit` reports zero vulnerabilities at every severity at the root and in all three tracked workers, on a real version bump (astro 7.2.10, sharp, svgo, js-yaml) rather than an allowlist, and `.github/audit-allowlist.json` is `allow: {}`. The `@public` waiver is falsifiable: `tests/public-waivers.test.ts` requires each tag to name a path that exists and imports the symbol, and the three false consumers named yesterday would each fail it today. The dead-export gate runs `knip --production` inside the required check, and an independent scanner found zero unwaived, unexplained dead exports; 30 dead files are gone. The decommission pipeline has five real backends with 20 tests, and the shell wrapper recognises exit 5. sticky_stop is enrolled as the tenth overlay pair with both hashes verifying against the manifest.

Two things keep this from B. First, the structural findings that have survived three reviews did not move: the 313-line, complexity-51 broker `handle()` with positional authorization; no root Python packaging under 451 files; 262 of 398 `noqa` annotations naming rules ruff does not enable; no type checker, formatter, function-length or complexity ceiling on the Python side; byte-identical helper duplication on both sides of the tree; four upward edges from `src/lib/db/`; two route files that are the domain layer. Second, the window's own new code carries four new mediums that the review reached first: a non-atomic write of the retainer price and payment rail on the money path (#2730); a `--allow-unwired --live` flag on the new destructive pipeline that nothing restricts to a fixture root, alongside a Fly backend that arms itself from an ambient CLI login and an observability backend that reports a D1 row deletion it never observed (#2735); and `operator/fixtures/**` sitting outside the new required check's own trigger list while a substrate test executes Python from there (#2726). None of these is severe. All four are the same shape: the mechanism landed, and the report the mechanism produces is not yet a probe of what it did.

## Scorecard

| Dimension     | Grade | Trend            | Rubric line applied                                                                                      |
| ------------- | ----- | ---------------- | -------------------------------------------------------------------------------------------------------- |
| Architecture  | C+    | up (was C)       | 16 operator modules over 500 logical lines in the ratchet baseline; mixed concerns in two route handlers  |
| Security      | C     | stable           | Seven mediums, no highs; five carried three reviews, two new in the window                               |
| Code Quality  | C+    | up (was C)       | Dead-export clause now false by two methods; DRY and inert Python suppressions hold                      |
| Testing       | C+    | up (was C)       | Critical paths untested (assessments data layer, stripe checkout); Python gate now blocks                 |
| Dependencies  | B     | up (was D)       | Zero audit findings; seat-image Python installs still unpinned and unaudited                             |
| Documentation | C+    | up (was C)       | No API documentation for 98 route files; two of four false in-code claims closed                          |
| Golden Path   | B-    | up (was C)       | Required checks now cover both halves; zero required reviews and no uptime poller remain                  |

**Overall: C+.** Mode of the seven grades is C+; no dimension is D or F, so nothing pulls the overall down.

## Top action items

1. **Make the retainer price and payment rail one write.** `src/pages/api/admin/clients/[id]/operator-price.ts:50-51` awaits `setOperatorPrice` then `setOperatorPaymentMethod`; each is a read-modify-write in `src/lib/db/services.ts:243,279` and there is no `db.batch()` in the path (`grep -rn 'batch(' src/lib/db/services.ts` hits only a comment). If the second write fails the row carries the new price and the old rail, and the rail decides whether the 3% card fee line is added to checkout and every monthly invoice. New code from #2730, on the money path, one `db.batch()` from correct.
2. **Execute the machine-key upgrade its own header scheduled.** `src/lib/auth/machine-key.ts:5-6,17` documents one shared `MACHINE_HEARTBEAT_KEY` for every seat with `X-Tenant-Slug` as an unbound header, and names "customer #2" as the trigger for per-tenant credentials. `ls -d operator/customers/*/` shows four real seats including a paying production client. Any seat holding the key can write another tenant's `fleet_status` and `operator_runtime_summary` rows and drive that tenant's audit-write-failure alert by changing one header.
3. **Make the decommission pipeline's report a probe.** Three findings in #2735, one fix: (a) `operator/bin/lib/decommission_cli.py:305-312` lets `--allow-unwired --live` proceed with every wired backend after printing a warning, and nothing ties the flag to a non-default `--customers-root`; (b) `_fly_authenticated` at `decommission_backends.py:446-452` treats a logged-in `fly` CLI as wired with no staged token, so the refusal that protected the pipeline silently stops covering the Fly layer on a Captain shell; (c) `HealthchecksAndFleetStatusCleanup.cleanup()` at `:416-428` returns `fleet_status_row_deleted: True` unconditionally after a `DELETE` whose result set is empty by construction. The venture's own removal doctrine requires a negative probe per runtime layer; the manifest this pipeline emits is the artifact a completion report will cite. Refuse `--allow-unwired` outside an explicit non-default root, key Fly on `FLY_API_TOKEN` only, and re-`SELECT` after the deletes.
4. **Add `operator/fixtures/**` to the substrate path list and make the conformance test walk executed paths.** `operator/tests/test_closeout_seed.py:181` runs `operator/fixtures/law-firm/pi/lien-ledger-tracker/seed/render_seed.py` by subprocess; loading `operator/bin/substrate-paths-changed.py` and calling `matches()` on that path against every pattern in `.github/operator-substrate-paths.txt` returns `False` (orchestrator re-ran it; the control path `operator/bin/x.py` returns `True`). `bin/tests/test_ci_coverage_conformance.py:149` pins test files, not the modules those tests execute. A fixture-only PR merges reporting "No substrate paths changed".
5. **Clone the admin route enumerator for the portal, twice.** `enforcePortalAuth` (`src/middleware.ts:153-171`) admits any signed-in Clerk user; tenant binding is per route. All 19 API routes and all 31 page routes under the portal comply today (18 helpered plus one 410 tombstone; 25 helpered plus five redirect tombstones plus one OAuth callback bound by signed state). Nothing keeps it so. `tests/admin-routes-require-session.test.ts` is the template; assert on a call site rather than a substring, since the admin version at `:43` matches a mention.
6. **A hygiene batch, each one line or one file.** Delete `tests/build-output.test.ts` (fails on any clean checkout because `dist/` is gitignored; orchestrator reproduced it in the fresh worktree; its second test passes trivially by its own comment while `tests/pricing-figures.test.ts` is the real control). Set `--max-warnings` to 12 (actual is 12, the config comment says the number only ratchets down). Add `fleet-alerts` to the dry-run build step in `verify.yml` (its dependencies were bumped this window). Add the root config files to knip's `project` set so `npm run deadcode` stops printing two false "unused dependency" warnings for packages `astro.config.mjs` imports. Extend the nosemgrep-justification regex in `security.yml:88` from `//` to `(//|#)` so the 52 Python suppressions are held to the same rule. Drop `--no-git` from the gitleaks step on a repository that `gh api` confirms is public.
7. **Enable `BLE001` and `F401` in `operator/ruff.toml`.** `ruff check --isolated --select BLE001 --statistics operator/` reports 11 uncovered sites; 210 suppressions already exist for it. The two rules retire 238 of the 262 inert annotations, and the ruff gate is now required.

## Detailed Findings

### 1. Architecture

**Carried findings.** Of the 2026-09-09 Architecture findings: sticky_stop **closed** (enrolled as the tenth overlay pair; `operator/contracts/overlay-pairs.json` names it and the operator agent recomputed both sha256 values against the manifest). Decommission stubs and the exit-5 arm **closed** (finding 8 below, and `operator/bin/decommission-customer.sh:73`). promotion-cards cluster **closed** (deleted by #2732). Broker positional authorization **open, unchanged** (finding 1). No Python packaging **open, unchanged** with a corrected edge count (finding 2). Tracker-skill duplication **open, wider than reported** (finding 4). Hand-tuple sync gates and the ungated `broker_writer` twins **open, unchanged** (finding 5). `adapter/` zero-importer modules **open, unchanged** (finding 9). `packet.py` and `establishment_store.py` **open, unchanged** (finding 10). Shell scripts outside the ratchet **open** (finding 11). On the `src/` side: the dead admin OAuth callback **open, scope corrected** (finding 6); two helper modules registered as public endpoints **open, unchanged**; `src/lib/db/` upward edges **open, plus one inversion** (finding 7); raw SQL in route files 17 to 16; Operator split 27 edges to 24, still one back-edge; two error-body vocabularies **open, now measured** (Code Quality 5); route files as domain layer **open** (finding 3); the near-ceiling cluster **worse** (finding 3); fleet-alerts build gate **open** (finding 12).

1. **[HIGH] `operator/workspace_broker/server.py:363` `handle()` is unchanged: 313 lines, ruff McCabe 51, authorization positional.** The gateway-PID gate is at `:595`; five hand-rolled agent-uid checks sit above it (`:381`, `:410`, `:460`, `:521`, `:572`) and `medchron_dispatch(...)` at `:371` gates through a proper table. `ruff check --isolated --select C901 --config 'lint.mccabe.max-complexity=15'` puts `handle` at 51 and the next-worst function at 31. Carried from 08-23. Recommendation: a verb table carrying `{handler, auth: gateway | agent_uid | root}` with `tests/broker-verb-registry.test.ts` asserting every entry declares one; the medchron group already has the shape.

2. **[HIGH] No Python packaging for the four core subsystems.** No `pyproject.toml` at the root or at `operator/`; the five that exist are leaves (`runners/medchron`, `connectors/_sdk`, `connectors/smokeball`, `connectors/msgraph-mail`, `connectors/_reference`). Orchestrator census: 33 non-test `sys.path.insert`/`append` mutations and 130 `# noqa: E402`, a third of every suppression in the tree. Correction to the 09-09 report: a name-resolved import walk measures 3 direct `bin -> workspace_broker` edges, not 22; the packaging conclusion is unaffected. Recommendation: one root `pyproject.toml` on the medchron precedent.

3. **[MEDIUM] Zero `src/` files exceed the 500-line ceiling, and six sit between 494 and 499; two route files are still the domain layer.** Orchestrator's own blank-and-comment-stripped count: `src/lib/sow/service-finalize.ts` 499, `src/pages/portal/products/operator/[instance]/settings/index.astro` 499 (was 498), `src/lib/operator/customer-yaml/types.ts` 497, `src/lib/webhooks/stripe-subscription-handler.ts` 494, `src/lib/db/quotes.ts` 494, plus `src/lib/pdf/sow-template.tsx` 494 per the agent. Yesterday three files sat at exactly 499; the cluster within six lines of the ceiling is now six. `src/pages/api/internal/heartbeat.ts` (16 functions, 12 field parsers, two raw D1 calls, no `src/lib/db/fleet-status.ts`) and `src/pages/api/booking/reserve.ts` (12 functions including the multi-table commit and the Calendar sync) are unchanged. Recommendation: extract the booking commit and the heartbeat parsers to `src/lib/`; decide `types.ts` by exemption or split rather than by trimming.

4. **[MEDIUM] Skill helper duplication is wider than measured yesterday.** AST census over the six substantive `skills/*/pre_run.py` modules: 15 helper bodies duplicated byte-identically, 176 redundant lines, with `paid-media-anomaly-watcher` and `retainer-hours-reconciler` newly in the family. The heaviest pair remains medical-records-chaser and lien-ledger-tracker at 8 identical helpers and 86 lines per side (the 09-09 "48%" figure is not reproducible by this method; the family is larger, not smaller). The stated reason for not splitting stays false: `_load_sibling_module` (`skills/client-verification-tracker/pre_run.py:301-320`) falls back to `/app/skills/<skill>` and `templates/Dockerfile:1115` copies `operator/skills/` there; adoption is 2 of 6. Recommendation: sibling loader for the two remaining skills, one vendored `skill_helpers.py`, gated by glob.

5. **[MEDIUM] Three vendored-copy gates discover by hand tuple; a fourth pair has no gate and no test names it.** md5 over non-test `.py`: `pre_run_gate.py` x9, `escalation_ledger.py` x6, `routing.py` x2, each gated by a named tuple; `grep -n 'glob\|rglob\|iterdir\|listdir'` across the three sync tests returns nothing. `skills/deadline-miss-escalator/broker_writer.py` and `skills/client-verification-tracker/broker_writer.py` still differ by one docstring word (09-04 probe I5, still open) and are loaded by runtime string, so no static scan sees them either. Recommendation: hash the canonical and glob `skills/*/<name>.py`.

6. **[MEDIUM] `src/pages/api/oauth/callback.ts` is a 231-line route with no initiator and no middleware auth; the supporting library is live.** The sole production caller of `issueOAuthState` is `src/pages/portal/products/operator/oauth/[connector]/index.ts:90`, whose flow returns to its own portal callback. `/api/oauth` matches neither `isAdminRoute` (`src/middleware.ts:54`) nor the admin API prefix, and `src/middleware.ts:129` carries a rewrite exemption for it; the route's only gate is its own `reviewerMatches` at `:190`. Correction to the carried finding: `src/lib/oauth/` is not dead, each helper has a live portal caller. Recommendation: delete the route and the middleware clause together, or open the initiator.

7. **[MEDIUM] `src/lib/db/` still calls upward on four edges, and now owns a pricing rule the Stripe layer imports back down.** `db/integrations.ts -> booking/encryption`, `db/quotes.ts -> sow/store`, `db/engagements.ts -> follow-ups/scheduler`, `db/milestones.ts -> stripe/client`, all value imports. New this window: `src/lib/stripe/subscriptions.ts:42` imports `CARD_FEE_LINE_DESCRIPTION` and `cardProcessingFeeCents` from `../db/invoices`. The Operator split is 24 edges from `src/lib/portal/operator` into `src/lib/operator` and one value back-edge at `src/lib/operator/entitlement-compiler.ts:56` importing `config-governance` from the portal half (orchestrator read). Recommendation: the fee rule to `src/lib/stripe/`; `VERTICAL_FLOORS` and its accessors down into `src/lib/operator/`.

8. **[MEDIUM, NEW] `src/pages/api/admin/clients/[id]/operator-price.ts:50-51` writes price and rail non-atomically.** Top action item 1; orchestrator read both files.

9. **[MEDIUM] `adapter/` is reached at boot and by CLIs, never per request, and its two zero-importer modules are untouched.** `templates/bootstrap.sh:807` runs the invariants at every container start, which import `adapter.audit_log`. `adapter/inbound_envelope.py` (298 lines) has one reference, its own test; `adapter/resolve_skill_pins.py` (108 lines) has two comment mentions in `scripts/validate-customer-yaml.ts`. Carried unchanged. Recommendation: enrol or retire, on the `validate_customer_yaml.py` precedent.

10. **[MEDIUM] `adapter/evidence/packet.py` (1,652 raw / 1,128 logical) and `workspace_broker/establishment_store.py` (2,107 / 1,415) are unchanged.** Five unrelated responsibilities in the first, including a 225-line `_readme_text`; one 40-method class with four private refusal-namers in the second. The module-size ratchet baselines 16 modules over 500 logical lines and only tightens.

11. **[LOW] Shell scripts outside every ceiling.** `bin/provision-customer.sh` 1,369, `templates/entrypoint.sh` 1,309, `bootstrap.sh` 916, `bin/boot-smoke-test.sh` 577. `tests/operator-module-size.test.ts:89` walks `.py` only. Clean under `shellcheck -S warning` except the generated `_env-arrays.generated.sh`.

12. **[LOW] `workers/fleet-alerts` has no dry-run build in CI, and its dependencies were bumped this window.** `grep -ln fleet-alerts .github/workflows/*.yml` returns `deploy.yml` only; `verify.yml` dry-run builds the two cost workers under a rationale that applies identically to the pager. One line.

13. **[LOW, NEW] `tests/operator-module-size.test.ts:254-261` gives stale advice.** The NOTE it appends for any `skills/*/pre_run.py` says the scheduler stages the file alone, which is true only for skills still on the shared stamp; the two that adopted `_load_sibling_module` can split. Three of the four modules the NOTE fires on are the ones that should.

14. **[LOW, NEW] `operator/fixtures/validator-regression/` is dead data.** Two YAML files with zero references anywhere in the repo. Wire them into `tests/customer-yaml-validator.test.ts` as the negative cases their names promise, or delete them.

15. **[LOW, NEW] `.claude/hooks/trap-recurrence.mjs` is a Captain CLI no hook registers.** `.claude/settings.json` registers ten hook scripts; the directory holds eleven. This one is reached through `.claude/bin/trap-recurrence` and is legitimate, but `knip.jsonc` declares `.claude/hooks/*.mjs!` as entries, so a hook that later stops being registered (a guard silently ceasing to enforce) is invisible to every gate. Recommendation: a test asserting every `.claude/hooks/*.{mjs,sh}` appears in `settings.json`.

16. **[POSITIVE, verified] The decommission pipeline's five real backends are the strongest new code in the window.** `bin/lib/decommission_backends.py` (516 raw lines) implements each Protocol as the inverse of provisioning, and the operator agent checked each inverse against the provisioning side: the healthchecks deleter queries the same tag and name the provisioner creates; the AgentMail deleter matches inbox ids the way `agentmail_ops.inbox_id()` does; R2 walks `vaults/<slug>/`, `customers/<slug>/` and the skills bucket and deliberately omits `smd-audit-archive` with the reason stated; `backends_from_env` only adds a backend when its credential is staged, so an absent credential leaves the stub and the exit-5 refusal in place, and `BACKEND_REQUIREMENTS` names the exact credential each missing backend needs. `python3 -m pytest bin/tests/test_decommission_backends.py -q` gives 20 passed. Not yet run live; the next real seat retirement is the runtime proof. See Security 3 for the three edges the same module leaves open.

17. **[POSITIVE, verified] No runtime import cycles in either tree.** The operator agent's AST graph (172 modules, 246 edges, local-directory-first resolution so basename collisions cannot masquerade as cycles) finds two mutual pairs, both broken by deferred imports: `send_verify.py` and `send_attribution.py` via `TYPE_CHECKING`, and the smokeball connector's `extract.py` and `vision.py` via a function-local import at `extract.py:203`. The second is a genuine mutual dependency papered over by placement.

18. **[POSITIVE, verified] The new admin pricing route is the layering the venture asks for.** `operator-price.ts` is 61 lines: gate, parse, delegate, redirect; no SQL, no business rule, org-scoped on every call. Finding 8 is about the data layer it delegates to, not the route.

**Grade: C+ (up from C).** Rubric: "3+ files over 500 lines OR unclear boundaries OR mixed concerns in route handlers." The first clause is now false in `src/` and still true in `operator/` (16 baselined modules); the other two hold on both sides. Above a flat C because both Captain-owned carried items closed at the layer that matters and the newest module in the tree is the best-argued code in it. Not B: the same boundary findings have survived three reviews, and the one new architectural defect is a non-atomic write on the money path.

### 2. Security

**Carried findings.** All five mediums **open, unchanged** and re-probed by the orchestrator: `/api/events` has no `rateLimitByIp` (grep: zero hits; `checkRateLimit` keyed on a client-supplied session id at `:128,:286`); gitleaks runs `--no-git` at `security.yml:191` under `fetch-depth: 0`; `createRemoteJWKSet` is called per request at `token-validation.ts:69`; JIT MCP identity returns the Clerk subject as `localUserId`; the portal enumerator gap (finding 2, now larger). The Sentry non-finite timestamp (`sentry.ts:65`), the `seam_pull.py` half-justified `noqa`, CSRF on `SameSite=Lax` alone, and the six LOWs all **open, unchanged**. One carried finding was **wrong and is corrected** (finding 6). The three positives (live headers on all three hosts, injection surface, secrets and the public `.pem`) were **re-derived** by the security agent with its own `curl`, a read of all 24 template-literal `.prepare(` sites, and the shape sweep.

1. **[MEDIUM, NEW] One shared Machine bearer for every seat; the tenant is an unbound header; the documented upgrade trigger has passed.** Top action item 2. `src/lib/auth/machine-key.ts:48` reads `X-Tenant-Slug` after a constant-time compare of the single key; `operator/bin/provision-customer.sh` stages the same value onto every seat. Consumers: `runtime-summary.ts` upserts on the `entity_id` resolved from the slug; `heartbeat.ts:564-583` drives `reportAuditWriteFailureDelta` for that tenant. Blast radius requires a seat that already holds the key, which is why this is medium and not high. Recommendation: the per-tenant `key_hash` with dual-key rotation the header describes.

2. **[MEDIUM] The portal enumerator gap covers 50 routes, not 20, and the invariant holds on all of them today.** Top action item 5. The security agent enumerated and read every file a helper scan could not classify: 19 API routes (18 helpered plus the 410 tombstone), 31 page routes (25 helpered, five redirect tombstones echoing the caller's own instance param, one OAuth callback bound by HMAC-signed state with a 10-minute TTL plus a Clerk-subject match at `:190-195`). Admin has `tests/admin-routes-require-session.test.ts`; portal has nothing for either set. Residual on the admin template: `:43` is a substring match, so a mention satisfies it.

3. **[MEDIUM, NEW] The decommission pipeline's three open edges.** Top action item 3. Orchestrator read `decommission_cli.py:286-312` and `decommission_backends.py:416-428,446-452,489-497`: the `--allow-unwired` branch prints a warning and falls through to `pipeline.run()`; `_fly_authenticated` returns true on `FLY_API_TOKEN` or on `fly auth whoami` exiting 0; `cleanup()` returns both `_row_deleted` booleans as literal `True`. The #2735 commit body itself records that the CLI tests had to be neutralised against ambient fly credentials, which is the same hazard one layer up. The archive-bucket exclusion is pinned by `test_decommission_backends.py:102-122`, which asserts no listed URL contained `smd-audit-archive`, so that half is genuinely protected.

4. **[MEDIUM] gitleaks scans the working tree only, and the repository is public.** Mechanically unchanged from three reviews. The premise changed: `gh api repos/venturecrane/ss-console --jq .private` returns `false`. A secret committed and later removed is invisible to `--no-git` and permanently readable in public history. `tests/pricing-figures.test.ts:6` still reasons from "This repo is private today"; that control is now load-bearing rather than defence in depth, and its header misinforms the next reader. The checkout already pays for `fetch-depth: 0`; the fix is deleting one flag and triaging the first historical run against `.gitleaks.toml`.

5. **[LOW, NEW] `src/pages/api/assessment/llm.ts:89` compares the bearer with `!==`.** Orchestrator read: `if (auth !== \`Bearer ${expected}\`)`. Every sibling shared-secret check uses the constant-time helper (`machine-key.ts:64`, the Sentry, Resend and healthchecks handlers). The route is otherwise the best-designed public endpoint in the tree (fail-closed 503 when unset, reasoned per-IP exemption, size caps, metadata-only logging). Timing extraction against a Worker behind Cloudflare is not practical; the finding is consistency on the one endpoint whose compromise spends the Anthropic budget.

6. **[LOW, CORRECTION] Semgrep does scan Python; the gap is that the justification gate matches `//` only.** The 09-09 report said no Python security lint runs. `p/security-audit` and `p/owasp-top-ten` carry Python rules, and the proof is 52 `# nosemgrep: python.lang.security.audit.*` suppressions in `operator/`, which exist only because those rules fire. The justification gate at `security.yml:88` is `'//[[:space:]]*nosemgrep...'`; Python comments start with `#`, so all 52 are exempt from the 20-character requirement. Today 4 of 52 lack a reason. One-character fix.

7. **[LOW, NEW] `operator/workspace_broker/server.py:986-991` returns `str(exc)` for any unhandled exception to the socket peer.** The authorization around it is sound (SO_PEERCRED uid, per-verb `PermissionError`, gateway-PID gate, 0o660 socket, request-size cap). An exception message is not a bounded vocabulary; an OS error carrying a path or a vendor exception carrying a URL becomes a reply to whatever holds the socket group. Recommendation: generic `internal_error` outward, detail logged broker-side.

8. **[LOW, NEW] `src/pages/portal/products/operator/oauth/[connector]/callback.ts:230-244` checks the reviewer's access as it was at authorize time.** The Clerk-subject match against signed state is the right binding; the reviewer's current entitlement on `customer_id` is not re-resolved before `store()`. Narrow window (revocation inside the 10-minute TTL). Recommendation: `resolveOperatorAccess(customer_id)` after the match.

9. **[POSITIVE, verified] Headers, injection, secrets, webhook verification, the MCP grant model.** Re-derived at HEAD by the security agent: all three hosts return HSTS, `frame-ancestors 'none'`, nosniff, DENY and strict-origin-when-cross-origin including on the 302 paths; 468 `.prepare(` sites with 24 template literals, every interpolation a code-controlled constant; zero `dangerouslySetInnerHTML`, `eval(`, `new Function(`; all 8 `set:html` sites through `serializeJsonLd`; Python 0 `shell=True`, 0 `os.system`, 0 `pickle`, 0 unsafe `yaml.load` against 87 `safe_load`; the tracked `.pem` is an Ed25519 public key served on purpose; grants read with `revoked_at IS NULL AND expires_at > now`, TTL never infinite, JIT capped at 7 days and 50 per customer, sticky revoke, append-only grant audit; the retired `/api/mcp` is a genuine 410 on all three verbs.

**Not checked:** Clerk's own cookie attributes; any secret value; R2/D1 access policy in the Cloudflare dashboard; live behaviour of any authenticated endpoint; the overlay's runtime source beyond what `verify-overlay-pairs.py` hashes; whether the diff-aware Semgrep baseline is hiding pre-existing Python findings (settling that needs the daily cron's full run).

**Grade: C (stable).** Rubric: "any medium OR 3+ low." Seven mediums stand: five have survived three reviews and two joined them from the window's own code. Not D: no high, no critical, injection and secrets clean on both languages, headers live, webhook verification uniform, and the destructive code written this window lists before it deletes and pins the archive exclusion in a test.

### 3. Code Quality

**Carried findings.** Both HIGHs on the `src/` side are **closed at the gate layer**: the `@public` waiver (finding 1) and the 39 test-only exports (finding 2). The ten `off` knip categories **closed** (all `warn`; `types` now `error`). `yaml` phantom dependency **closed** (in `devDependencies`). The middleware `renewSession` swallow **closed, exemplary** (`src/middleware.ts:96-99`: `.catch` into `captureError`, handed to `waitUntil`, with a comment naming the review). `conformance.ts` naming a nonexistent function **closed** (file deleted by #2732). Byte-identical duplication in `src/` **open, unchanged, sharpened** (finding 3). `captureError` reach **open** (finding 4). Lint ceiling **changed, slack opened** (finding 5). The inert `noqa` pile **open, ratio slightly worse** (finding 6). No type checker or Python ceilings **open** (finding 7). shellcheck **open** (finding 9). Three skills lacking the `metadata.smd` block: not re-measured this run.

1. **[POSITIVE, verified] The `@public` waiver is falsifiable.** `tests/public-waivers.test.ts` walks `src`, `workers`, `scripts`, and for every `@public` block asserts: at least one repo-relative path is named; every named path exists; at least one named path consumes the symbol, and when that path is a test, consumption means a named-brace `import`, not a source-text match. A fourth test requires at least ten waivers found, so an empty walk cannot pass vacuously. 13 waivers at HEAD, every one citing a test that imports the symbol; 41 tests green. What turns it red: removing the path from a waiver, a named path that does not exist, or converting a citing test from `import { X }` to `toContain('export ... X')`. The three false consumers named on 09-09 would each fail today. Residuals (LOW): the production-path branch is a bare word regex over the file body; the walk covers three roots while knip honours `@public` in `operator/*.ts` and `.claude/hooks` too (no such tag exists there today); the walk skips `.well-known/`.

2. **[POSITIVE, verified] The dead-export gate runs in production mode, blocks, and an independent method finds nothing it misses.** `npm run deadcode` is `knip --production`, run at `verify.yml:67` inside the required `Typecheck, Lint, Format, Test` job; orchestrator confirmed exit 0 at HEAD and a green run on 1da55373. The src agent's own scanner over `src/`, `scripts/`, three `workers/*/src`, `operator/assessment-eval` and `operator/voice-gate` reports 22 exports with no production consumer: 11 carry `@public`, 11 sit in files `knip.jsonc` declares as entries, zero unexplained (two apparent residues were the scanner skipping `.well-known/`, where both are imported). Seven of the ten exports named on 09-09 are deleted; two survive only as words in comments; one carries a checked waiver. Five exports knip does not flag were read by hand and are all used inside their own file, the documented `ignoreExportsUsedInFile` class. Two things to record so the gate's silence is read at its real strength: `operator/*/index.ts!` and `operator/*/cli.ts!` are entries, so every export in the voice-gate and assessment-eval subsystems reads as used by construction (the entry designation is correct, both CLIs are real doors); and the gate prints two false "unused dependency" warnings (finding 8).

3. **[MEDIUM] Byte-identical duplication is unchanged, and the sharpest case is three copies in one directory on the money path.** Orchestrator counts: `function stripeHeaders` plus `STRIPE_API_BASE` in `src/lib/stripe/checkout.ts`, `subscriptions.ts` and `client.ts`; `function machineBaseUrl` in five files, the function that addresses the live Operator Machine; `function escapeHtml` in seven, one of them the canonical `src/lib/api/helpers.ts:24` whose header says it is the 2026-06-12 dedup outcome; `function jsonError` in seven; `function redirectWithStatus` in eleven. Two corrections from reading the copies: the seven `jsonError` are now one-line pass-throughs to `errorResponse`, so they are aliases to delete rather than drift; the eleven `redirectWithStatus` are same-name, different-behaviour (each builds a different destination), which is the genuinely hazardous class. The `no-restricted-syntax` guard at `eslint.config.js:180-188,198-210` still covers only `jsonResponse`. Recommendation: `machineBaseUrl` to `src/lib/operator/machine-url.ts`; `stripeHeaders` to `stripe/client.ts`; delete the `jsonError` aliases; rename each `redirectWithStatus` to its destination; extend the guard to `escapeHtml`, `trimString`, `isRecord`, `machineBaseUrl`.

4. **[MEDIUM] Error handling is consistent; error observability is five files of 174.** Non-test `src/` has zero empty catch blocks and zero `.catch(() => {})`. `captureError` is imported by 5 files against 174 with a `catch` (orchestrator counts). The window's own money route is the pattern: `operator-price.ts:58` catches into `console.error`, which reaches a Worker log nobody pages on. Recommendation: adopt on the webhook and money paths first, not by sweep.

5. **[MEDIUM] One API error key carries two vocabularies.** Under `error`, `src/pages/api` returns 36 distinct snake_case codes and 33 distinct human sentences, including `'Invalid JSON.'` and `'Invalid JSON'` and three phrasings of rate-limiting. `errorResponse` is imported by 29 of 98 route files. No `error: {` object form exists to reconcile them. Recommendation: machine code in `error`, prose in `message`, and the existing ESLint guard extended to require `errorResponse`.

6. **[HIGH] Two thirds of the Python `noqa` annotations are inert, and the ratio got slightly worse.** Orchestrator census: 398 annotations (392 on 09-09, 261 on 08-24); `operator/ruff.toml` selects `E4`, `E7`, `E9`, `S102`; activated 136 (E402 130, E731 4, S102 2), inert 262 led by BLE001 210 and F401 28, naming ten rule codes ruff does not enable. Zero bare `# noqa` without a code. `ruff check .` passes clean and now runs inside a required check, so the gate that exists is real. `ruff check --isolated --select BLE001 --statistics operator/` reports 11 uncovered sites, and 61 of the 210 BLE001 suppressions carry no reason after the code. Recommendation: top action item 7, plus a reason-required gate in the shape `security.yml` already applies to `nosemgrep`.

7. **[HIGH] No type checker, formatter, function-length or complexity ceiling covers 451 Python files.** `grep -rn 'mypy\|pyright\|pytype' .github/ operator/ruff.toml` finds only the sentence in `ruff.toml` saying no mypy config exists. 4,435 of 6,574 defs carry a return annotation that nothing verifies. `ruff --select C901` at 15: 32 non-test functions over, worst `handle` at 51; six of the 32 are the same `validate_append` (27) in the six vendored `escalation_ledger.py` copies. 99 non-test functions exceed 75 lines, worst `seed_data.py:241 build_documents` at 346. No `line-length`, no `[format]` block; `ruff check --select E501` at 120 reports 380 lines. The TS side enforces 75 and 15 at error level with zero violations. Recommendation: `lint.mccabe.max-complexity` with a per-file baseline on the module-size ratchet's model; `ruff format --check` at 120; pyright in the `substrate` job.

8. **[LOW, NEW] `npm run deadcode` prints two false "unused dependency" warnings.** `@astrojs/cloudflare` and `@astrojs/sitemap` are imported at `astro.config.mjs:2,4`, which matches none of knip's `project` patterns. Plain `npx knip --dependencies` reports zero. The category is `warn`, so nothing blocks; but the config's own instruction is "read the warnings", and a list whose only two entries are known-false trains the reader to skip it. Recommendation: add the root config files to the `project` array.

9. **[MEDIUM] `shellcheck` never runs anywhere, and five directives suppress nothing.** `grep -rn shellcheck .github/ package.json operator/bin/` finds only the directives and two `source=` hints. The five disables sit inside GitHub Actions `run:` blocks in `unaudited-send-reconcile.yml:171`, `control-probes.yml:93`, `cron-slot-watchdog.yml:101`, `terminal-state-reconcile.yml:100`, `audit-chain-verify.yml:133`. The tree is clean at warning severity except the generated file. One step in `operator-substrate`.

10. **[MEDIUM] One copy-pasted swallow class in six skills.** Of 542 non-test `except` handlers, 24 are `try/except/pass` or `continue` (`ruff --select S110,S112`). The block `except Exception: # noqa: BLE001 - observability never gates the wake` / `pass` appears with an identical comment in five `pre_run.py` files and both `blind_wake.py` copies. The premise is right; there is no `log.warning`, so a wake-row write that starts failing on a live seat is indistinguishable from one that never ran, on the rows built to make wake behaviour auditable. Recommendation: extract with the shared helper (Architecture 4) and add the one warning.

11. **[LOW] The lint ceiling did not ratchet, and a free slot opened.** `npm run lint` is `eslint . --max-warnings 13`; actual at HEAD is 12 warnings, 0 errors (orchestrator ran it after `astro sync`; without the sync the count is an environment artifact of 58). All twelve are the `JSON.parse` returning `any` pattern, seven in `src/lib/db/quotes.ts:201-245`. The count fell because a file was deleted; `eslint.config.js:53-58` says the number "only ever ratchets DOWN", which is now unenforced. Recommendation: cap at 12 now; `const parsed: unknown` at the twelve sites to reach 0.

12. **[LOW, NEW] Two `noqa` and naming defects in the new destructive module.** `decommission_backends.py:94` justifies `S310` with "fixed https hosts, method-locked" on an `http_request` that accepts any URL and calls `urlopen`, which honours `file://` and `ftp://`; every caller passes an `https://` constant today, so the claim is about callers, written at the callee, in a module whose purpose is destructive. Two lines rejecting non-`https://` turn the comment into the truth. Separately, `_plan_counts` is two different functions with one name (`lien-ledger-tracker/pre_run.py:909`, 4 lines; `retainer-hours-reconciler/pre_run.py:333`, 15 lines); rename before any extraction de-duplicates by name.

13. **[POSITIVE, verified] Type discipline in `src/` is intact.** Zero real `any` in non-test `src/`, `scripts/`, `workers/*/src` (eight grep hits, all English prose, each read); zero `@ts-ignore`, zero `@ts-expect-error`; 0 lint errors, so the error-tier rules hold (75-line functions, complexity 15, depth 4, params 5, no floating promises). `tsconfig.json` extends `astro/tsconfigs/strict`, not `strictest`.

14. **[POSITIVE, verified] Effectively zero dead Python modules; the duplicate census is small and enumerated.** Of 247 non-test modules, two have no reference outside themselves and tests (`bin/killtest-msgraph-send.py`, `templates/drafting/corpus_measure.py`), both hand-run entry points whose docstrings say why. md5 finds three duplicate groups and 14 copies, every one a deliberate vendored copy with a stated reason; the defect is discovery (Architecture 5), not existence.

**Grade: C+ (up from C).** Rubric: "3+ dead exports OR inconsistent error handling OR notable DRY." The first clause is false by two independent methods, and false because the gate was fixed rather than the symptom swept. Error handling is consistent on the TS side and disciplined in the aggregate on the Python side. What holds the grade: DRY did not move on either side, 262 Python suppressions still mean nothing, and nothing type-checks, formats or bounds the size of the Python half. Not B: the duplication class has survived three reviews and the guard that exists covers one symbol.

### 4. Testing

**Carried findings.** `operator-substrate` advisory: **closed, verified at three layers** (finding 1). Coverage thresholds never evaluated: **closed, exemplary** (unchanged). `src/lib/db/assessments.ts` zero test importers: **open** (orchestrator: the one test hit is a `readFileSync` path at `tests/assessments.test.ts:6`, not an import). `src/lib/stripe/checkout.ts` never exercised: **open, sharpened** (`tests/portal-billing-manage.test.ts:28` mocks it; no test imports the real module). The 17 source-text test files: **open, all 17 still have zero relative imports of a subject**; nine were touched by #2732's export deletions, touched not upgraded. Nothing measures coverage in either language: **open, deliberate**. Two environment-dependent Python tests: **half closed** (finding 4).

1. **[CLOSED, verified] `substrate` is a required check that reports on every PR and decides inside the job.** `gh api repos/venturecrane/ss-console/branches/main/protection` still returns only `["Typecheck, Lint, Format, Test"]`, which is where a shallow probe stops and concludes the finding is open. The ruleset is the live mechanism: `gh api repos/venturecrane/ss-console/rulesets/15555219` returns `Security Summary` and `substrate` as required contexts, enforcement active, `bypass_actors` empty; the check run on 1da55373 shows `substrate success`; `gh pr checks 2735` shows it gating a real merge. The mechanism is right, not merely present: #2726 removed the `paths:` filter (a path-filtered workflow reports nothing on the PRs it skips, and a required check that never reports strands the PR at "Expected"), moved the decision into `operator/bin/substrate-paths-changed.py`, which answers `true` on an uncomputable diff or an absent base sha, and reports success on the no-change path. The path list covers all ten directories holding Python tests (`find operator -name 'test_*.py'` grouped by top-level dir), and `bin/tests/test_ci_coverage_conformance.py` pins the two consumers to each other. Residuals: the required context is the bare string `substrate` with no `integration_id` pin; and the conformance test pins test files, not the fixture and helper modules those tests execute (Architecture top action item 4).

2. **[MEDIUM, NEW] `tests/build-output.test.ts:26` fails on a clean checkout, and its second test is inert.** Orchestrator reproduced in the fresh worktree: `npx vitest run tests/build-output.test.ts` gives 1 failed, 2 passed, the failure being `expect(existsSync(resolve('dist'))).toBe(true)`. `dist/` is gitignored; `npm run verify` runs `build` before `test`, so CI is green, but `npm test` alone is red on any fresh clone or worktree. The second test walks `dist/` for `.html` files and asserts no `$2,500`; `astro.config.mjs` is `output: 'server'` and only `404.astro` prerenders, and the test's own comment says the assertion "passes trivially when nothing is prerendered". The real control is `tests/pricing-figures.test.ts` at source level. Recommendation: delete the file.

3. **[MEDIUM] The assessment data layer and the Stripe checkout module have no behavioural test.** `src/lib/db/assessments.ts` has nine production importers and a 208-line test with 48 `toContain` assertions against `readFileSync`; it detects a renamed export and not a dropped `org_id` predicate. `src/lib/stripe/checkout.ts` has three production importers and is mocked wherever it appears. Carried unchanged. The miniflare-D1 pattern in `tests/webhooks/stripe-subscription-linkage.test.ts` is the template.

4. **[LOW] One environment-dependent Python test remains; the other was fixed for the right reason.** `bin/tests/test_decommission.py` **closed** by #2735's `_no_ambient_backends` fixture (`:466-481`), which monkeypatches `backends_from_env` to report everything unwired after a logged-in `fly` CLI in a developer shell wired the real Fly destroyer inside a unit test; a security fix wearing a test fixture's clothes. `adapter/evidence/tests/test_packet.py::test_build_emits_targz_with_every_expected_file` **open, reproduced**: the security agent's CI-equivalent run in a clean `uv` venv gives 1 failed, 2,510 passed in 94.7s, the failure being this test reading `EVIDENCE_PACKET_SIGNING_KEY_B64` from ambient `os.environ` (`signing.py:72`; no `conftest.py` under `adapter/evidence/tests`, orchestrator confirmed). It passes in CI only because CI has no key staged.

5. **[LOW] The SOW PDF render path has no executing test.** `tests/sow-render.test.ts` is a `describe.skip` with two `it.skip` placeholders, deliberate and documented (the WASM renderer does not load under Node). The one artifact a client signs is validated by `npm run build` compiling the template and by hand. Recommendation: a Workers-pool vitest project, or render one fixture SOW after build in CI and assert on extracted text.

6. **[POSITIVE, verified] The window's money path and destructive path are the best-tested code in the tree.** `tests/stripe-subscriptions.test.ts` (247 lines) imports the real `src/lib/stripe/subscriptions`, stubs fetch per URL, records every call, and asserts wire-level Stripe form params: the fee product line item, the metadata marker used to find-or-create it, `pause_collection[behavior]=void`, `DELETE` for cancel. A regression in the ACH-versus-card rail or the 3% line turns it red. `tests/operator-go-live-billing.test.ts:44,183-185` exercises `cardProcessingFeeCents` with three cases including the rounding boundary. `test_decommission_backends.py` is 20 tests over five backends: delete path, absent-artifact skip, transport failure, paginated R2 listing, five credential-wiring permutations, and the archive-bucket exclusion.

7. **[POSITIVE, verified] Both suites are large, fast and nearly skip-free, and the shape is honest.** vitest at HEAD: 320 files, 5,112 tests (down 124 from #2732 deleting dead files and the tests only they consumed), 121s. Python CI-equivalent: 2,510 passed. Zero snapshot tests, zero `assert True` in 177 Python test files, the only two `expect(true).toBe(true)` inside the documented `describe.skip`. `tests/public-waivers.test.ts` (new) converts a prose escape hatch into a merge gate.

**Grade: C+ (up from C).** Rubric: "significant gaps OR critical paths untested" still applies to `assessments.ts` and `stripe/checkout.ts`. The plus is the Python gate becoming real and the window's own tests being behavioural over a money path and a destructive path. Not B: the gap set did not shrink; it was held while the gate around it improved, and one test in each language fails outside CI for reasons unrelated to the code.

### 5. Dependencies

**Carried findings.** `npm audit` high and critical: **closed**. Zero unused dependencies: **true** (the two knip warnings are false positives, Code Quality 8). No dynamic-codegen dependency reachable from the Worker bundle: **true, re-verified beyond a grep** (the one `new Function(` in `dist/server/chunks` is zod's own capability probe, which short-circuits to false on the Cloudflare runtime; `npm ls ajv --omit=dev` is empty; no `handlebars`). `yaml` undeclared: **closed**. Seat-image Python installs unpinned: **open, unchanged**. `typescript` and `@cloudflare/workers-types` one major behind: **open, deliberate**.

1. **[CLOSED, verified] Zero vulnerabilities at every severity, on a real bump.** Orchestrator: `npm audit --json` at the root and in `workers/cost-anomaly`, `cost-telemetry`, `fleet-alerts` each return `{critical:0, high:0, moderate:0, low:0}`; `.github/audit-allowlist.json` is `allow: {}` with `expires: null`. #2731 bumped astro 7.1.3 to 7.2.10, sharp, svgo, js-yaml and others, each chosen to clear the `.npmrc` `min-release-age=7` cooldown by publish date; #2711 closed. The 2026-09-23 expiry that yesterday's report told the next review to watch no longer exists. Caveat for readers of the primary checkout: its `node_modules/astro` still reads 7.1.3 (the stale-`node_modules` trap); `npm audit` reads the lockfile, so the result stands.

2. **[HIGH] The seat image installs Python dependencies unpinned, and nothing audits Python dependencies anywhere.** `operator/templates/Dockerfile:1017-1018`: `uv pip install --python /opt/workspace-broker/.venv/bin/python google-api-python-client google-auth pyyaml`, no version specifier, unchanged since 08-23. No root `pyproject.toml`, `requirements*.txt` or `uv.lock`; `.github/dependabot.yml` carries `github-actions` and four `npm` ecosystems, no `pip` or `docker`; no `pip-audit`, `uv audit`, `safety` or `bandit` in any workflow (a grep for "safety" hits only the `safety-substrate` path string, which the deps agent correctly excluded). Third review running. Recommendation: pin, lock, add the `pip` ecosystem.

3. **[MEDIUM] `typescript` 6.0.3 vs 7.0.2 and `@cloudflare/workers-types` one major behind**, both caret-capped and documented as deliberate in `dependabot.yml`. `eslint-plugin-astro` reports 2.1.1 against a "latest" of 1.7.0, which is a registry tag artifact, not a lag.

4. **[POSITIVE, verified]** All 51 GitHub Actions `uses:` lines are SHA-pinned; zero `npm install` in workflows; three workers with zero production dependencies.

**Grade: B (up from D).** Rubric: no audit findings, no runtime-incompatible dependency. Docked from A by the unpinned and unaudited Python supply chain, an independent gap now three reviews old.

### 6. Documentation

**Carried findings.** CLAUDE.md drift: **open, unchanged** (finding 1). No API docs: **open, unchanged, no tracking issue** (finding 2). Four false in-code claims: **two closed, two open** (finding 3). `data-model.md` behind migrations: **open, gap widened** (finding 4). `crane_docs_drift_audit` scope note: still applies.

1. **[MEDIUM] Three numbers for one fact, unchanged.** `CLAUDE.md:304` says 37 active decisions through #55; `docs/adr/decision-stack.md:26` says 38 through #56; `docs/adr/index.md:9` says 34 through #51. `CLAUDE.md:305` still says Operator ADRs run through `0061-*.md`; the highest is `0087-chronology-package-content-control.md`. `README.md:18` says TypeScript 5 against a declared `^6.0.3`. None of the four files was touched in the window.

2. **[MEDIUM] No API documentation for 98 route files.** `ls docs/api` returns no such directory; `git ls-files src/pages/api | wc -l` is 98 (was 99). #2385 remains closed by the 2026-08-31 backlog reset; `gh issue list --search "API documentation" --state open` is empty. Recommendation: a route inventory generated from `src/pages/api` by a test that fails when a route lacks an entry.

3. **[LOW] Two of four false in-code claims closed as a side effect of unrelated work.** `conformance.ts` and its phantom `runConformanceSuite` deleted outright by #2732; `knip.jsonc`'s "reported but non-blocking" claim made true by #2728 flipping ten categories from `off` to `warn`. Still open: `src/lib/booking/holds.ts:18` and `src/lib/db/oauth-states.ts:85` both name `workers/booking-cleanup/`, which never existed (`git ls-files workers` shows three workers).

4. **[MEDIUM] `docs/handbook/data-model.md` is nine migrations behind.** Last changed 2026-07-03; migrations now run through `0113_services_payment_method.sql` (added by #2730). `npm run handbook:drift` flags it. In the plus column, both in-window commits whose maintenance contract names a page did update it: #2730 touched `docs/handbook/client-portal.md`, #2735 touched `docs/specs/operator/decommission-customer.md`. The gap is the cross-cutting schema page, not the contract.

5. **[POSITIVE]** 87 ADRs numbered and indexed with `tests/adr-integrity.test.ts` guarding numbering; `tests/handbook-integrity.test.ts` runs in `npm run verify`.

**Grade: C+ (up from C).** Rubric: "no API documentation for a project with API endpoints" holds verbatim. Nudged up because two false claims are genuinely closed and nothing regressed.

### 7. Golden Path Compliance (Tier 2)

| Requirement                                                              | Status                          | Instrument                                                                                                                                            |
| ------------------------------------------------------------------------ | ------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| Source control, CLAUDE.md, TypeScript + ESLint, no hardcoded secrets     | Met                             | shape sweep; `tsconfig` strict; `eslint.config.js`; CLAUDE.md present                                                                                 |
| Error monitoring (Sentry)                                                | Partial                         | Worker wrapper live; `captureError` in 5 of 174 catch-bearing files; Machine-side init is overlay-scoped per ADR 0023                                 |
| Full CI/CD                                                               | Improved                        | 20 workflows; deploy on merge; required checks now `Typecheck, Lint, Format, Test` (legacy) plus `Security Summary` and `substrate` (ruleset)          |
| Branch protection                                                        | Partial                         | `required_approving_review_count: 0` on both; `enforce_admins: false`; `strict: true`; ruleset `bypass_actors: []`; force-push and deletion blocked   |
| Uptime monitoring                                                        | Partial                         | Push-based via healthchecks.io (`src/pages/api/webhooks/healthchecks.ts`, bearer-verified); no poller; #2384 closed by backlog reset                   |
| API docs                                                                 | Missing                         | `docs/api/` absent; 98 route files                                                                                                                    |
| PWA                                                                      | Missing (expected)              | the compliance dashboard's own stated next action for SS                                                                                              |
| Secrets scanning                                                         | Partial                         | `.gitleaks.toml` present; scanner runs `--no-git`; repository public                                                                                  |

1. **[CLOSED] Required checks cover both halves of the pipeline.** Testing 1; verified against the ruleset, a check run on HEAD, and a merged PR.
2. **[MEDIUM] Zero required reviews and admin bypass permitted.** Unchanged. CLAUDE.md's own rule "all changes through PRs" is met mechanically and not backstopped by a human-review requirement; `enforce_admins` was left false deliberately on 09-09 because strict up-to-date checks would force every concurrent session to rebase before merge.
3. **[LOW] Sentry adoption did not move.** Numerator 5 (was reported as 3; the difference is a prior undercount, not new adoption, since none of the call sites changed in the window); denominator fell from 156 to 174-in-a-different-count because the dead-file deletion shrank the tree.
4. **[LOW]** API docs, counted once in Documentation.

**Grade: B- (up from C).** Rubric: "1-2 non-critical items missing" now fits better than "3+"; the multi-review HIGH is closed. Not B: zero required reviews on a solo-operator repository is a deliberate but real gap against the rubric's own language, and API docs remain absent.

## Model Convergence

Four agents, disjoint dimensions, so convergence is between agents and the orchestrator rather than between agents. Every graded claim was re-probed by the orchestrator. Three agent numbers were replaced by orchestrator numbers and the difference recorded: `sys.path` mutations 33 (agent 32); `captureError` reach 5 of 174 (the deps agent's 4 of 104 used a different catch-file count); and the `bin -> workspace_broker` edge count of 3 replaces yesterday's 22. Two agent claims were corrected before grading: the security agent found yesterday's "no Python Semgrep" premise false, and the src agent found yesterday's "dead `src/lib/oauth/`" scope too wide. One orchestrator measurement was itself an environment artifact and is recorded so it is not repeated: `npm run lint` in a fresh worktree reports 58 warnings until `astro sync` has generated `.astro/types.d.ts`; after it, 12.

## Trend Analysis

| Dimension     | 08-23 | 09-09 | 09-10 |
| ------------- | ----- | ----- | ----- |
| Architecture  | D     | C     | C+    |
| Security      | D     | C     | C     |
| Code Quality  | C     | C     | C+    |
| Testing       | C     | C     | C+    |
| Dependencies  | C     | D     | B     |
| Documentation | B     | C     | C+    |
| Golden Path   | C     | C     | B-    |
| **Overall**   | D+    | C-    | C+    |

Six of seven dimensions moved up in one day and none moved down. The 09-04 claims probe re-run at HEAD reports 32 closed, 5 still open (F1 sticky_stop arms with no ss-console callers, which is now by design under the vendoring direction; F9 sys.path; I5 broker_writer twins; I12 `TODO(#891)`; I12b the OAuth token store's no-op `ok: true`), 0 regressed, 1 changed (I3 portal route count, re-measured above at 19). Of the 51 `source:code-review` issues ever filed, all 51 are closed; no issue from the 09-09 review was filed because the Captain chose follow-through PRs instead, and those PRs are the window this review graded.

What did not move is the list the last two reviews also named: the broker `handle()` if-chain, Python packaging, the inert `noqa` pile, Python type-checking and ceilings, DRY on both sides, the `db/` boundary, the two route-files-as-domain-layer, the assessments and checkout test gaps, the shared machine key, the events limiter, gitleaks history, the API docs. Each of these is the kind of finding a follow-through wave can close in a day, as this window demonstrated for six others.

## File Manifest

Tracked at 1da55373: 750 `.ts`, 451 `.py`, 221 `.astro`, 165 `.sql`, 71 `.json`, 50 `.yaml` + 21 `.yml`, 33 `.sh`, 17 `.mjs`, 1 `.tsx`, 886 `.md`. Code lines (ts, tsx, astro, py, sh, mjs, js): 342,223 total; `operator/` 159,100, `src/` 107,210, `tests/` 71,991, `scripts/` 4,898, `.github/` 3,164, `.claude/` 2,553. Three tracked workers (`cost-anomaly`, `cost-telemetry`, `fleet-alerts`). 20 CI workflows. Full manifest used by the agents: `/tmp/crane-file-manifest-ss.md` (not committed).

## Raw Model Outputs

The four agent reports are preserved verbatim in [code-review-2026-09-10-agent-reports.md](./code-review-2026-09-10-agent-reports.md): Architecture + Code Quality for `src/` and `workers/` (Opus), Architecture + Code Quality for `operator/` (Opus), Security + Testing (Opus), Dependencies + Documentation + Golden Path (Sonnet). Codex review: skipped (Phase 1). Gemini review: skipped (Phase 1).
