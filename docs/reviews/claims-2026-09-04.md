---
title: Claims 2026-09-04
---

# ss-console review — 2026-09-04

Run `bash docs/reviews/claims-2026-09-04.probe.sh`. Window: `bb84b7f5..1a4fb338` (89 commits, 389 files).

**16 closed · 21 still open · 0 regressed** — 2 carried, 19 new. Wave 1 (same day, #2695-#2700): 15 of the 19 flipped to FIXED in the probe; 6 remain (F1, F9, I3, I5, I12, I12b).

## Still open (carried from 2026-08-25)

| # | Claim | Now |
|---|---|---|
| F1 | `sticky_stop` arms have no dispatch caller. Restated: the 08-25 grep matched the table name; the medchron daemon now READS the state, it does not arm it. Probed daily by `control-probes.yml`; the open item is the Captain's wire-or-delete call. | 1 (a docstring) |
| F9 | Library modules mutate `sys.path` at import. 6 → 7: `operator/bin/lib/sender_class.py:19` (#2691) is new. | 7 |

## New findings (all ≥80; the command is in the probe)

| # | Claim | Where |
|---|---|---|
| B3 | Every templated send holds: `send_verify` joins dispatch↔wake on `skill_name`, and the broker's `CONFIRM_SEND_DISPATCHED` row never writes it (the ledger has the column). Test fixtures hand-author the field, so the suite cannot see it. | `operator/bin/lib/send_verify.py:412`, `operator/workspace_broker/server.py:763` |
| B1 | Any matter with a billing-named document fails at `billing_extract` after the paid stages: `billing_docs.json` rows carry `pages` (never written) and no `path` (read). | `medchron/decisions.py:150`, `stages/billing.py:229` |
| B2 | Every on-seat medchron job refuses at `classify_scanned`: nothing seeds `controls/controls.json` into the daemon's per-job `data_root`. | `stages/classify.py:173`, `daemon.py:255` |
| A1 | First retainer `invoice.paid` is dropped with a 200 when it beats `checkout.session.completed` (no row has the Stripe id yet); no fallback by the `smd_subscription_id` the checkout stamps, no reconciler. | `stripe-subscription-handler.ts:299` |
| A2 | Go-live ignores checkout `payment_status`: an ACH micro-deposit checkout goes `active` unpaid, then lands `cancelled` with no way to re-start. | `operator-checkout-handler.ts:116` |
| A3 | An amendment with the same filename overwrites the earlier executed document in R2 and D1 (key hashes the name only; upsert is DO UPDATE). | `r2.ts:45`, `db/operator-agreements.ts:111` |
| A4, A5, B6 | Client-facing sentences not authored per engagement: "all of them are in force together" (compliance), "Your subscription is active" from `?start=done` alone, "we will extend the chronology" in a delivered document. Pattern A/B. | see probe |
| B4 | A due client chase with `return_link` unauthored renders "matter number unavailable", a resolution failure that did not happen, in the firm's alert. | `client-verification-tracker/render.py:119` |
| B5 | A held medchron job's reason (file names, stage-log tail) reaches the wake prompt, the audit chain and D1 with only a 500-char cap. | `medchron/daemon.py:366` |
| I1a, I1b | The retired smd seat: `voice-ingest-corpus.py --slug` still defaults to it (would recreate `vaults/smd/` in R2), and three shipped skills' frontmatter still name it, baked into every image. The directory/registry gate cannot see either. | `operator/bin/voice-ingest-corpus.py:76`, `operator/skills/{email-reply,inbox-triage,workspace}/SKILL.md` |
| I12 | OAuth consent/revocation audit is `console.log` only; `TODO(#891)` is pinned by a test though #891 closed 2026-05-22. The token store falls open to a no-op returning `ok:true` when `FLY_API_TOKEN` is unset (prod value not probed). | `src/lib/oauth/audit.ts:52`, `store.ts:224` |
| I14 | `mission-smoke-managed-gmail.sh:49` runs `hermes -z` on a seat, the one-shot pattern operator/CLAUDE.md forbade on 09-01. Unreferenced. | `operator/bin/mission-smoke-managed-gmail.sh` |
| I15 | 17 `@venturecrane.com` hits in SMD code; 15 are in `inbox-triage/references/`, COPYed into every image. `client-identity-gate` allowlists the domain. | probe |
| I3, I5 | Not gated: portal routes' tenant scoping (19/20 call a helper; admin has an enumerating test, portal has none); `broker_writer.py` twins differ only by docstring and nothing pins them. | probe |

## Closed this run

I4: the escalation ledger has exactly one writer and it is the witnessed one (`send_witness.py:151`; `validate_append`'s witness is keyword-only). #2603 stays open as a runtime-proof gap, not a repo one. I13: all four live `customer.yaml` validate. Clean with a live control: `locals.runtime` 0; secrets 0 (gitleaks gated); forbidden-strings 323 assertions live; 61/61 workflow path references exist; migration rollbacks are forward-only by handbook design.

## Judgment

B3 is the one to fix first; it is why "every templated send HELD" after overlay#338. B7 (any inbox message in a wake's window is attributed to it) is masked by B3 today and surfaces the day B3 is fixed; fix both together, using the phase-1 message_id join that already exists in `reconcile-sends.py`. B1 and B2 mean routine 11 has never completed on a seat; the daemon tests use a fake runner, so daemon→driver is unexercised. A1–A3 are the first-client billing path and all three fail silent (200 to Stripe, no alert). Below the floor but worth knowing: `token-expiry.ts` pages once and stays silent through the actual expiry (author-acknowledged); CVT's `ledger_unavailable_fail_open` blind wake calls `asyncio.run` inside the loop and writes nothing (60). `docs/handbook/repository-map.md:24` says 68 migrations; there are 107.

## Coverage

Fan-out stated as three (diff-A `src/ workers/ migrations/ scripts/`, diff-B `operator/`, invariant). All three returned via SendMessage. diff-B disclosed it forked two sub-passes (medchron+broker; bin+templates+adapter) and re-verified their top findings itself; B1, B2, B3 were then re-derived here from the probe commands before inclusion. diff-A read every changed hunk in scope (71 files); diff-B read all non-test source in scope and tests by fixture shape, not line-by-line. Invariant pass: whole-tree commands only, no sampling, 0 `crane_verify` rows (repo layer; I12's `FLY_API_TOKEN` and I1a's R2 state are runtime facts left unprobed).
