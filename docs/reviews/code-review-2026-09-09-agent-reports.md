---
title: Code Review 2026-09-09 Agent Reports
---

# Code Review 2026-09-09: Agent Reports

The four agent reports behind [code-review-2026-09-09.md](./code-review-2026-09-09.md), verbatim. Each agent wrote its report to disk before returning, so all four reached the orchestrator; the orchestrator re-derived every count that feeds a grade and recorded corrections in the main report. Instruments and falsifiers are as the agents stated them. Where an agent corrected itself mid-review, the correction is left in place.


---

# Agent: src-arch-quality

## Review report — `src-arch-quality` (Architecture + Code Quality, TypeScript/Astro half)

Repo: venturecrane/ss-console @ `0a3722a3` (verified `git rev-parse HEAD`, tree clean).
Scope: `src/`, `workers/`, `scripts/`, `bin/`, `.claude/hooks/`, `migrations/`, `astro.config.*`, `wrangler.toml`, `eslint.config.js`, `knip.jsonc`, `tsconfig*.json`, `package.json`. `operator/` excluded (sibling agent).

---

### 1. Architecture

**Carried findings**

| Carried item | Status | Instrument |
|---|---|---|
| A1 (prior #8) Dead admin OAuth callback `src/pages/api/oauth/callback.ts` | **STILL OPEN, unchanged** | File present (7493 bytes). `src/middleware.ts:54` still scopes admin session to `/admin`\|`/api/admin`; `:120` still carries the `/api/oauth` rewrite exemption; `grep -rn issueOAuthState src --include='*.ts'` still returns exactly one caller, `src/pages/portal/products/operator/oauth/[connector]/index.ts:90`, pointing at its own portal callback. |
| A2 (prior #2) Two helper modules registered as public endpoints | **STILL OPEN, unchanged** | Scanned every `src/pages/api/**/*.ts` for an HTTP-method export; exactly two have none: `src/pages/api/booking/reserve-helpers.ts`, `src/pages/api/booking/confirmation-emails.ts`. |
| A3 (prior #3) `src/lib/db/` is not a leaf | **STILL OPEN, unchanged** | `grep -n "from '../stripe\|../follow-ups\|../sow\|../booking" src/lib/db/*.ts` → `milestones.ts:11`→stripe/client, `engagements.ts:8`→follow-ups/scheduler, `quotes.ts:14`→sow/store, `integrations.ts:12`→booking/encryption. Four upward edges, same four. |
| A4 (prior #4) Raw SQL in route files | **STILL OPEN, marginally改** | `.prepare(` in `src/pages/`: 30 occurrences across **17** files (prior review: 18). `src/lib/db/`: 160. |
| A5 (prior #5) Operator domain split across two trees | **STILL OPEN, slightly improved** | `src/lib/operator` 15,002 + `src/lib/portal/operator` 9,214 = 24,216 non-test lines = **22.3%** of `src` (108,670). Cross edges now **27 up / 1 back** (prior 33/2). The single back edge is still `src/lib/operator/entitlement-compiler.ts:56` → `../portal/operator/config-governance`. |
| A6 (prior #6) Promotion-cards feature built and unreachable | **STILL OPEN, unchanged** | `grep -rn PromotionCardList src/` returns no importer outside the file itself. `PromotionCard.astro` is imported only by `PromotionCardList.astro:19`. `src/pages/api/portal/operator/promotion-cards/[skill]/dismiss.ts:12` still names `PromotionCard.astro` as its only client. |
| A8 (prior #8) Two incompatible API error-body shapes under one key | **STILL OPEN, unchanged** | 28 sites in `src/pages/api` return `{ error: '<snake_code>', … }`; 64 call `errorResponse(status, sentence)`. `src/pages/api/booking/reserve.ts` still does both: `:430` `{ error: 'Invalid JSON' }` vs `:438` `error: 'rate_limited'` and `:462` `error: 'slot_taken'`. |
| Prior #11 (positive) Zero ceiling violations in `src/` | **CLOSED / holds** | `npx eslint . --max-warnings 0` → 0 errors. |

**Findings**

1. **[MEDIUM] `workers/fleet-alerts` deploys to production with no CI build gate, and the workflow comment states a rationale that covers only the other two workers.** `.github/workflows/deploy.yml:105-112` installs and `wrangler deploy`s fleet-alerts. `.github/workflows/verify.yml:92-98` dry-run builds only cost-anomaly and cost-telemetry, under a comment reading "The two cost workers have their own wrangler configs + live consumers … dry-run build them so a config break surfaces in CI rather than at deploy." fleet-alerts also has its own `wrangler.toml` and a live consumer (the heartbeat-red / HARD_STOP pager, #1709). `grep -n fleet-alerts .github/workflows/*.yml` returns hits in `deploy.yml` only. So a `wrangler.toml` break in the pager surfaces at deploy time, on the path whose job is to page when the fleet is down.
   *Instrument:* `grep -rn "workers/" .github/workflows/*.yml`; read of `verify.yml:85-98` and `deploy.yml:70-113`.
   *Falsifier:* a `fleet-alerts` dry-run step anywhere in `verify.yml` would have shown in the grep; there is none. Its unit tests **do** run (`test:workers` iterates `workers/*/` with a `package.json` guard), so this is a config-break gap, not a test gap.
   *Recommendation:* add the third dry-run step, or state in the comment why the pager is exempt.

2. **[MEDIUM] Two route files are the domain layer.** `src/pages/api/booking/reserve.ts` is 456 logical lines holding 11 functions including `commitBookingToDb` (a multi-table D1 transaction), `syncGoogleCalendarAndPromote`, `mintManageToken`, and `seedScheduleSidecars`. `src/pages/api/internal/heartbeat.ts` is 419 logical lines with 35 top-level declarations, ~20 of them field parsers, plus two raw `.prepare()` calls (`:568`, `:680` `FLEET_STATUS_UPSERT_SQL`) writing two tables — and no `fleet_status` module exists under `src/lib/db/`. By contrast `src/pages/api/webhooks/stripe.ts` (5 functions) delegates cleanly to `src/lib/webhooks/*-handler.ts`, which is the shape the rest should follow.
   *Instrument:* `npx eslint --rule '{"max-lines":["error",{"max":1,…}]}'` on each file for the logical count; `grep -n '^\(export \)\?\(async \)\?function ' <file>`; `grep -n '\.prepare(' src/pages/api/internal/heartbeat.ts`.
   *Falsifier:* if these handlers had delegated, the function inventory would have been a thin `handlePost` plus imports, as `webhooks/stripe.ts` is. It is not.
   *Recommendation:* extract `commitBookingToDb`/`syncGoogleCalendarAndPromote` to `src/lib/booking/`, and the heartbeat parsers + upsert to `src/lib/db/fleet-status.ts`.

3. **[MEDIUM] Three files sit at exactly 499 logical lines — one under the ceiling.** ESLint's own counter reports `src/lib/operator/customer-yaml/types.ts` **499**, `src/lib/sow/service-finalize.ts` **499**, `src/lib/webhooks/stripe-subscription-handler.ts` **499**, and `src/pages/portal/products/operator/[instance]/settings/index.astro` **499**. `types.ts` is 1,431 raw lines. Three independent files landing on 499 is not chance; the ceiling is being met by trimming rather than by decomposition, and a reader still opens 1,431 lines. This is the same mechanism the prior review named in Architecture #2: the ceiling pushed two helper modules into the routing tree.
   *Instrument:* `npx eslint --rule '{"max-lines":["error",{"max":1,"skipBlankLines":true,"skipComments":true}]}' <files>` — the message prints the real count. A regex approximation agreed to the line.
   *Falsifier:* a spread of counts (e.g. 380, 440, 470) would have shown the ceiling was non-binding. Four files at 499 shows it is binding and being worked around.
   *Recommendation:* `types.ts` is a type-only file, so it is not exempt today — the `max-lines` rule counts declarations regardless of kind, and there is no override for it in `eslint.config.js`. Either add a scoped, reasoned exemption for pure type modules, or split it; either is better than 499.

4. **[LOW] Six of ten directories under `workers/` are untracked build detritus from workers deleted in June–July 2026.** `enrichment-workflow`, `job-monitor`, `new-business`, `review-mining`, `scan-workflow`, `social-listening` each contain only `node_modules`/`dist` and **zero** tracked files. They were removed in `caaf012f` (2026-06-30, new-business + social-listening) and `2ae1627d` (2026-07-01, the scrape-score-enrich machine). Git is clean; the local checkout is not. `npm run typecheck:workers`/`test:workers` skip them correctly via a `package.json` guard, so nothing breaks — but "gone means gone" enumerates the layers a removal must clear, and this is the developer-machine layer.
   *Instrument:* `for d in …; do git ls-files workers/$d | wc -l; done` → 0 for all six; `git log --diff-filter=D -1 -- workers/<d>`.
   *Falsifier:* a non-zero tracked-file count for any of the six would have made it a real repo defect rather than local residue.
   *Recommendation:* none in-repo; note it in the removal runbook so the next retirement includes an `rm -rf` step.

5. **[POSITIVE-VERIFIED] There are no runtime circular dependencies in `src/`.** `npx madge --circular --extensions ts,tsx src` reports 4 cycles. Reading all four shows every back edge is `import type` — `login-events.ts:23` (`PortalUserRow`), `providers/google-workspace.ts:28` and `providers/ms-graph.ts` (`OAuthProvider`), `format-assertions.ts:42` (`SpecProperty`). Type imports are erased at compile time, so none of the four exists at runtime.
   *Falsifier:* a value import on the back edge would have made it real; I read the import line of each of the five files rather than trusting madge's verdict.

6. **[POSITIVE-VERIFIED] JSON response construction is uniform, and the guard that makes it so is real.** Zero occurrences of `new Response(JSON.stringify(` and zero of `Response.json(` anywhere under `src/pages/api`. The 39 route files that do not import `src/lib/api/helpers` are form-post/redirect routes returning `redirect()` or a 303 `Location` (sampled `admin/clients/index.ts`, `admin/entities/[id]/stage.ts`, `portal/billing/manage.ts`). The `no-restricted-syntax` rule at `eslint.config.js` blocks only the `new Response(JSON.stringify())` form, so `Response.json()` would have slipped past it — nothing uses it.
   *Falsifier:* one `Response.json(` hit would have shown the guard was narrower than the practice. There are none.

7. **[POSITIVE-VERIFIED] Adapter v13 env access is clean, and Worker module state is disciplined.** Zero `locals.runtime` references in `src/` or `workers/`; 172 files import `env` from `cloudflare:workers`. The only module-level mutable binding in Worker-reachable code is `src/lib/pdf/render.ts:36` (`let wasmReady`), which memoizes an idempotent WASM init carrying no per-request data and is annotated with the coding-standards exception; the two other `let` sites are `src/scripts/assessment*.ts`, which are browser bundles loaded by `<script>` from `.astro` pages, not Worker code. Every other hit is a frozen `new Set([...])` lookup table.
   *Falsifier:* a module-level `let` or mutable `Map` holding request-derived data would have been the defect. I read each of the four `let` sites rather than counting grep lines.

8. **[POSITIVE-VERIFIED] Every module added in the review window is wired.** All 20 non-test modules added in `cc97518d..HEAD` under `src/` have at least one production referrer outside tests. Zero dead, zero test-only.
   *Instrument:* `git log --diff-filter=A --name-only --format= cc97518d..HEAD -- src`, then a referrer scan per module excluding the file itself.
   *Falsifier:* any new module whose only referrers were tests would have printed. None did.

**Not checked (Architecture):** `migrations/` content and ordering; `bin/`; `astro.config.mjs` internals; whether the 17 raw-SQL route files' queries are correct (only their location); Cloudflare-side reality of the three worker deploys (read-only, did not call the CF API); the `.claude/worktrees/billing-detail-cohesion/` peer worktree (present on disk — another session may be mid-change).

**Summary.** `src/` has no god objects, no runtime cycles, and no unwired new work, but the boundaries it declares are not the boundaries it keeps: the data layer reaches up into Stripe and SOW, two route files hold the domain layer, 17 hold raw SQL, the Operator domain runs across two trees with 27 edges one way, and two helper modules plus one incapable OAuth callback are live public routes. Every one of those was named in the 2026-08-23 review and none has moved.

**Proposed grade: C.** Rubric line applied: *"C: 3+ files >500 lines OR unclear boundaries OR mixed concerns in handlers."* Two of the three clauses are independently met — mixed concerns in handlers (finding 2) and unclear boundaries (carried A2/A3/A5 plus the dead route). Not D: *"monolithic structure with significant coupling OR god objects OR no discernible architecture"* does not hold — the architecture is discernible and mostly followed, the import graph is acyclic at runtime, and new work is wired.

---

### 2. Code Quality

**Carried findings**

| Carried item | Status | Instrument |
|---|---|---|
| CQ knip counts `tests/**` as project files | **STILL OPEN, and now sized** | `knip.jsonc` `project` still lists `"tests/**/*.ts"`. `npx knip` reports **nothing**. With tests out of scope, knip reports **58 unused exports**; an independent token index over 1,653 files reports 198 test-only exports; the two methods agree on **39**. See finding 2. |
| `@public` on `cleanupExpiredHolds` names `workers/booking-cleanup` | **STILL OPEN, unchanged** | `src/lib/booking/holds.ts:106` still reads "Called by the daily cleanup cron at `workers/booking-cleanup/`". `ls workers/booking-cleanup` → No such file. Zero callers repo-wide. |
| `yaml` declared in package.json? | **STILL OPEN** | `p.dependencies.yaml` and `p.devDependencies.yaml` both `undefined`. `npm ls yaml` shows it only as a transitive of `@astrojs/check`, `@astrojs/cloudflare`→vite, and `knip`. See finding 5. |
| The ten knip categories set to `off` | **STILL OPEN, and the comment is false** | See finding 3. |
| `captureError` adoption | **STILL OPEN, worse** | 3 files import it (prior: 3); files with real `catch` syntax grew 94 → **156**. See finding 4. |
| `middleware.ts` `renewSession` without `waitUntil` | **STILL OPEN, and now demonstrably fixable in-repo** | `src/middleware.ts:90` `renewSession(...).catch(() => {})`. See finding 4. |
| `conformance.ts` names `runConformanceSuite` | **STILL OPEN, unchanged** | `src/lib/operator/capabilities/conformance.ts:17` still names it; repo-wide grep returns only that comment and the prior review's own text. |
| eslint `--max-warnings` sitting at its ceiling | **STILL OPEN** | `npx eslint . --max-warnings 0` → exactly **13 warnings, 0 errors**; `package.json` lint script is `--max-warnings 13`. Ratchet has not moved since the 2026-08-09 baseline. See finding 6. |
| vitest coverage thresholds declared but never run | **CLOSED — exemplary** | Thresholds removed; `vitest.config.ts:57-84` documents that `@vitest/coverage-v8` had never been installed and that v8 collects `0/0` under `getViteConfig`; `tests/coverage-config-honesty.test.ts` (5,565 bytes, present) fails if thresholds return without a workflow that runs them. This is the one carried item fixed at the level of the control rather than the symptom. |

**Findings**

1. **[HIGH] The `@public` waiver is the repo's only dead-export escape hatch, and its stated reasons are unverified prose — three of the fifteen are demonstrably false at HEAD, and six more cite "contracts" that are source-text greps.** `knip.jsonc` states the rule outright: "An export that must survive without an importer carries an explicit `@public` JSDoc tag at its definition, with the reason next to it." There are 15 such tags in `src/`. Checked each:
   - `src/lib/booking/holds.ts:106` — "Called by the daily cleanup cron at `workers/booking-cleanup/`." **That directory does not exist** and never did (`workers/` holds only cost-anomaly, cost-telemetry, fleet-alerts). Carried from 08-23, untouched.
   - `src/lib/operator/customer-yaml/sections-seat.ts:104` — "callers live in the overlay and in provisioning tooling, not in this repo." `~/dev/hermes-smd-overlay` (HEAD `606fade`) contains **zero `.ts` files**, so a TypeScript predicate cannot be called from it; `grep -rn isCustomerSeat` across this repo returns only the definition and the prior review's prose.
   - `src/lib/portal/formatters.ts:32` — "`src/pages/portal/billing/invoices/[id].astro` wraps its own local variant; that comment names this function as the alternative." That file contains no `formatRelativeDueCaption`, no local variant, and no "Overdue" branch; at `:119` it composes `` `Due ${formatShortDate(invoice.due_date)}` `` inline.
   - Six tags justify themselves as "pinned as a contract by `tests/X`" (`getDocument`, `getTranscriptUrl`, `ASSESSMENT_STATUSES`, `TIME_ENTRY_CATEGORIES`, `MILESTONE_STATUSES`, `bulkCreateMilestones`). Those tests read the defining file as a **string** and assert e.g. `expect(source()).toContain('export async function getDocument')` (`tests/assessments.test.ts:12-30`, `source = () => readFileSync(resolve('src/lib/db/assessments.ts'), 'utf-8')`). The assertion is the text of the line the tag sits above. It exercises no behavior and can fail only if the export is deleted — which is the thing the tag exists to permit.
   - `LEGACY_PROBLEM_LABELS` (`src/portal/assessments/extraction-schema.ts:58`) — "so historical extraction rows stay renderable." Nothing renders them; `grep -rn LEGACY_PROBLEM src tests scripts` outside the defining file returns nothing.
   - `listPromotedCorrections` / `listCorrectionsForEntity` (`voice-corrections.ts:237,345`) — "the portal view that consumes it has not shipped yet." Honest, and still true two and a half weeks after 08-23. Built, not wired.
   *Instrument:* `grep -rn "@public" src/ --include='*.ts'` then reading each site and probing its named justification (`ls workers/booking-cleanup`; `find ~/dev/hermes-smd-overlay -name '*.ts' | wc -l` → 0; grep of the named `.astro` file; reading each cited test).
   *Falsifier:* for each tag, finding the named consumer would have closed it. `INVOICE_TYPES` is exactly that case — `src/components/admin/IssueInvoiceForm.astro:7,38` really does import and render it, so its tag is earned. The check distinguishes.
   *Recommendation:* make the waiver falsifiable. Require `@public` to name a path, and add a test that the path exists and contains the symbol — the same shape as `tests/coverage-config-honesty.test.ts`. Then delete `cleanupExpiredHolds`, `isCustomerSeat`, `formatRelativeDueCaption`, and `LEGACY_PROBLEM_LABELS`, or build their consumers.

2. **[HIGH] 39 exports have no referrer outside test files, confirmed by two independent methods, and the gate reports zero.** `npx knip` (what `npm run deadcode` runs in `verify.yml`) prints nothing. Re-running knip with `tests/**` out of `project` and out of the vitest plugin's entry set yields **58 unused exports**. A separate token index over 1,653 files (src, workers, scripts, tests, operator, .claude/hooks, including `.astro`, `.json`, `.yaml`, `.md`) yields 198 exports whose only referrers are tests. The two agree on **39**. I read ten of the 39 — `createMeeting`, `listTimeEntries`, `updateServiceStatus`, `getServicesForEntity`, `listOAuthProviderSlugs`, `jaroWinklerSimilarity`, `FAST_MODEL`, `templateHygieneViolations`, `buildManualExtractionPrompt`, `loadHomeFeeds` — and for every one the only repo hits are the definition line and a copy inside the stale `.claude/worktrees/billing-detail-cohesion/` checkout. None is re-exported through a barrel (the three barrels, `src/lib/auth|config|email/index.ts`, are themselves unused files), none appears in `.astro` frontmatter, none is referenced by string in a registry. `updateServiceStatus` and `getServicesForEntity` mean the services state machine's write path has no caller.
   *Instrument:* `npx knip` (empty); knip re-run against a scratch config with tests removed from scope; a Node token index; then per-symbol `grep -rn "\b<sym>\b" src scripts workers operator .claude --include='*.ts' --include='*.tsx' --include='*.astro' --include='*.mjs' --include='*.json' --include='*.yaml' | grep -v '\.test\.'`.
   *Falsifier:* a barrel re-export or an `.astro` frontmatter use would have shown in the per-symbol grep, which covered `.astro` and `.json`/`.yaml` registries; and the intersection method discards the 159 symbols only one method flagged.
   *Recommendation:* move `tests/**` from `project` to a test-only workspace so a test-only export is reported, then triage the 39 — each is either a missing caller (wire it) or dead (delete it).

3. **[MEDIUM] `knip.jsonc` claims ten issue categories are "reported but non-blocking"; they are not reported at all, and the reason given for disabling `types` is false.** The config comment reads: "Reported by `npm run deadcode`, but non-blocking: .astro islands, wrangler config and the Python operator tree all create edges knip cannot follow." The categories are set to `"off"`. In knip, `"warn"` means reported-and-non-blocking; `"off"` means excluded from the report. Verified: `npm run deadcode` produces **zero output**, while the same knip with those categories enabled reports **15 unused files** — including `PromotionCard.astro`, `PromotionCardList.astro`, `AlivenessHeader.astro`, `ConnectorStatusSection.astro`, `SkillTogglesSection.astro`, `PackCardGrid.astro`, `StatusDot.astro`, `facet-doors.ts`, `facets/contract.ts`, and the three barrel `index.ts` files. Separately, the comment on `"types": "off"` says "Exported *types* number ~450 on the current tree … Enabling this today would force either a mass un-export or a mass ignore." `npx knip --include types --no-exit-code` returns **nothing** — enabling it today costs zero.
   *Instrument:* `npm run deadcode` (empty); `npx knip --include files --no-exit-code` → "Unused files (15)"; `npx knip --include types --no-exit-code` → empty; `npx knip --config /dev/null` for the full unfiltered list.
   *Falsifier:* if `off` had meant printed-but-non-blocking, `npm run deadcode` would have printed the 15 files. It printed nothing.
   *Recommendation:* change the ten from `off` to `warn` so the comment becomes true, and turn `types` on — it is already clean.

4. **[MEDIUM] Error handling is inconsistent, and the one write path in middleware both swallows its failure and may never run.** `captureError` (`src/lib/observability/sentry.ts:60`) is imported by **3** files — `src/pages/api/portal/operator/settings/customer-yaml-update.ts`, `src/pages/api/internal/heartbeat.ts`, `src/pages/api/webhooks/resend.ts` — against **156** non-test files under `src/` containing real `catch` syntax. Prior review: 3 of 94; the numerator did not move while the denominator grew 66%. The sharpest instance is `src/middleware.ts:90`: `renewSession(env.DB, env.SESSIONS, token, sessionData).catch(() => {})`. Two problems in one line. It has no `waitUntil`, so on Workers the promise can be cancelled when the response returns and the sliding-window renewal silently may not happen; and the `.catch(() => {})` discards the error, so a systematically failing renewal is invisible. The repo already has the mechanism: `locals.cfContext` is typed at `src/env.d.ts:291`, and `src/lib/portal/session.ts:53` passes `(p) => locals.cfContext!.waitUntil(p)` into `stampLoginIfNewSession`, which uses it at `src/lib/auth/login-events.ts:93-94`. Middleware is the one place that does not.
   *Instrument:* `grep -rl captureError src/ --include='*.ts' --include='*.astro' | grep -v '\.test\.'`; `grep -rlE "\}\s*catch\s*(\(|\{)" src/ … | wc -l`; `grep -rn waitUntil src/ workers/`; read of `src/middleware.ts:78-93`.
   *Falsifier:* a `waitUntil` on that line, or a `captureError` inside the catch, would have shown in either grep. Neither is there. (Zero empty `catch {}` blocks exist — `preserve-caught-error` is on at error level and holds.)
   *Recommendation:* pass `context.locals.cfContext?.waitUntil` into the renewal the way `portal/session.ts` already does, and replace the bare catch with `captureError(err, 'middleware.renew-session')`.

5. **[MEDIUM] `yaml` is a phantom dependency of a merge-gate script.** It appears in neither `dependencies` nor `devDependencies`, yet is imported by `src/lib/operator/send-render.ts:30`, `scripts/project-customer-config.ts:31`, and `scripts/validate-customer-yaml.ts:25`. It resolves only as a transitive of `@astrojs/check`, `@astrojs/cloudflare`→`vite`, and `knip` (`npm ls yaml`) — all dev-time. A hoisting change or a minor bump in any of the three removes it and breaks the customer.yaml merge gate, whose own header (`scripts/validate-customer-yaml.ts:128-131`) describes it as the gate. Knip's `unlisted: "off"` is the category that would have caught it, and even with `--include unlisted` knip stays silent because the package is physically present in `node_modules`.
   *Instrument:* `node -e "const p=require('./package.json');console.log(p.dependencies?.yaml, p.devDependencies?.yaml)"` → `undefined undefined`; `npm ls yaml`; `npx knip --include duplicates,unlisted --no-exit-code`.
   *Falsifier:* I first read this as a production-Worker exposure and it is **not** — `send-render.ts` is reached only by `scripts/validate-customer-yaml.ts` and tests, never from `src/pages`, so nothing bundles it into the Worker. The blast radius is CI, not runtime. Verified by `grep -rn send-render` across all `.ts`/`.astro`.
   *Recommendation:* add `yaml` to `devDependencies` with the version currently deduped (2.8.3).

6. **[MEDIUM] Byte-identical helper duplication, including five copies of the function that addresses the live Operator Machine.** All copies verified identical by printing each body, not by matching names:
   - `machineBaseUrl` — **5 identical copies**: `src/lib/admin/sticky-stop-clear.ts`, `src/lib/operator/runtime-read-transport.ts`, `src/lib/operator/mcp/webhook-transport.ts`, `src/lib/portal/operator/pause-control.ts`, `src/lib/portal/operator/entitlement-change.ts`. Each is `template.includes('{app}') ? template.replace('{app}', app) : \`https://${app}.fly.dev\``. Changing how a seat is addressed takes five edits with nothing failing if one is missed.
   - `escapeHtml` — **7 copies**, one of them the canonical `src/lib/api/helpers.ts:24`, whose own header states it is the dedup outcome of the 2026-06-12 review ("previously each carried as byte-identical private copies"). The copies came back.
   - `trimString` — 3 copies including the canonical one. `jsonError` — 8 copies, alongside the canonical `errorResponse`. `isRecord` — 8. `redirectWithStatus` — 11. `back` — 8. `stripeHeaders` — 3, all inside `src/lib/stripe/`.
   - `base64UrlEncode` / `base64UrlDecode` / `importSigningKey` — an HMAC-signing toolkit duplicated across `src/lib/assessment/session.ts`, `src/lib/booking/signed-link.ts`, `src/lib/booking/tokens.ts`, `src/lib/oauth/state.ts`. Handing to the Security agent.
   The relevant asymmetry: `eslint.config.js` carries a `no-restricted-syntax` rule that blocks re-declaring `jsonResponse`, added after the same class of drift. `escapeHtml` and `trimString` live in the same file and got no rule, and both re-duplicated.
   *Instrument:* a Node scan of `^(export )?(async )?function NAME` across all non-test `src/**/*.ts(x)`, then `awk '/function <name>/,/^}/'` on each copy to compare bodies.
   *Falsifier:* same-name-different-body would have made these false positives — which is why I printed the bodies. `handlePost` (37) and `handleDelete` (4) **are** that case and are excluded: they are a per-route naming convention, not duplication.
   *Recommendation:* `machineBaseUrl` → `src/lib/operator/machine-url.ts`; extend the `no-restricted-syntax` guard to `escapeHtml`, `trimString`, `jsonError`, `isRecord`; the base64/HMAC trio → `src/lib/crypto/`.

7. **[LOW] The 13-warning lint ceiling is one fixable pattern, and it has not ratcheted since 2026-08-09.** `npx eslint . --max-warnings 0` → exactly 13 warnings, 0 errors; the lint script pins `--max-warnings 13`, and its comment says the number "only ever ratchets DOWN". Eight of the 13 are in `src/lib/db/quotes.ts:201-245`, and all 13 are the same shape: `const parsed = JSON.parse(raw)` yields `any`, which then trips `no-unsafe-assignment`/`no-unsafe-member-access`. The code around them is already correct — `parseLineItems`, `parseSchedule`, `parseDeliverables` all validate with type predicates and never cast. Annotating `const parsed: unknown = JSON.parse(raw)` at each site closes the class.
   *Instrument:* full `npx eslint . --max-warnings 0` output; read of `src/lib/db/quotes.ts:190-250`.
   *Falsifier:* if the warnings were spread across unrelated rules or genuinely unsafe code, "one fixable pattern" would be wrong. All 13 are the five `no-unsafe-*` rules plus none other, in eight files, from `JSON.parse`.
   *Recommendation:* annotate the parse sites and drop the ceiling to 0.

8. **[LOW] `src/lib/operator/capabilities/conformance.ts:17` still instructs adapter authors to import `runConformanceSuite`, which does not exist.** Repo-wide grep returns the comment and nothing else; zero `*.conformance.test.ts` files. Carried from 08-23, unchanged.
   *Instrument:* `grep -rn runConformanceSuite . --include='*.ts' --include='*.astro' --include='*.md'`.
   *Falsifier:* a definition anywhere would have printed.

9. **[POSITIVE-VERIFIED] There is no `any` in non-test `src/`.** Zero `@ts-ignore`, zero `@ts-expect-error`. Every grep hit for `as any` and `: any` is English prose in a comment (e.g. `src/lib/operator/capabilities/internal-comms.ts:27` "Whether the channel has any external members"; `src/lib/operator/customer-yaml/types.ts:870` "as untrusted as any other") — I read all seven. The nine `as unknown as` sites are all narrowing after validation or a lib.dom workaround: `src/lib/auth/session.ts:144` and `src/lib/auth/admin-session-shim.ts:63` both cast to `SessionData` only after checking every field's type; four are `sigBytes as unknown as ArrayBuffer` for WebCrypto. Four `eslint-disable` comments exist in non-test `src/`, each with a stated reason. `npm run typecheck` → 0 errors, 0 warnings, 17 hints across 986 files.
   *Falsifier:* this is the exact grep the last review got wrong, so every hit was opened. A real `any` would have appeared as a type position rather than a sentence.

10. **[POSITIVE-VERIFIED] File naming is consistent.** Zero files under `src/lib/**` have an uppercase character in the basename; every file under `src/components/**` is PascalCase except one, `control-classes.ts`, which is a helper rather than a component. No mixed kebab/camel.
    *Instrument:* `git ls-files 'src/lib/**/*.ts' | xargs -n1 basename | grep -E '[A-Z]'` → empty.

11. **[NOTE, not a defect in my dimensions] TypeScript is on Astro's `strict`, not `strictest`.** `npx tsc --showConfig` resolves to `{ strict: true }` only; `noUncheckedIndexedAccess` and `exactOptionalPropertyTypes` are off (they live in `astro/tsconfigs/strictest.json`). The codebase is partly writing to the stricter standard by hand anyway — `src/lib/api/helpers.ts:19-20` destructures `const [local, domain] = parts` and then guards `if (!local || !domain)`, which is what `noUncheckedIndexedAccess` would have typed for it. Worth a costed trial, not a finding.

**Not checked (Code Quality):** whether the 39 test-only exports are individually worth keeping (I read 10 of 39); `workers/*/src/**` internals beyond line counts and CI wiring; `.claude/hooks/*.mjs` logic (eslint ignores `.claude/**` entirely, and `scripts/**` too — both are unlinted, which I note but did not audit); Prettier/format drift; the operator Python tree.

**Summary.** The type discipline is genuinely strong — strict on, zero `any`, zero `@ts-ignore`, typecheck clean over 986 files, zero ceiling violations, naming consistent. What is weak is everything that depends on a claim being true rather than a check running: three `@public` waivers name consumers that do not exist, six more cite tests that grep their own source text, the dead-export gate reports zero while 39 exports have no non-test referrer, and `knip.jsonc` asserts two things about its own behavior that a single command falsifies. The one carried item that was fixed — the coverage thresholds — was fixed by deleting the false claim and adding a test that keeps it deleted, which is the pattern the rest of these need.

**Proposed grade: C.** Rubric line applied: *"C: strict:false OR 3+ `any` OR inconsistent error handling OR notable DRY OR 3+ dead exports."* Three of the five clauses hold independently: 3+ dead exports (5 zero-reference value exports plus 15 unused files plus 39 test-only, finding 2), notable DRY (finding 6), inconsistent error handling (finding 4). Not D — *"pervasive `any` OR swallowed errors throughout OR entire dead modules"* — because there is no `any` at all, only one swallowed-error site (in middleware, and it is a single line), and the dead modules are 15 files in one identified feature cluster rather than a pervasive condition. Not B, because B is "1-2 minor issues" and finding 1 is not minor: it is the escape hatch for the repo's only dead-code gate, carrying reasons that are false.

---

### Overall notes for the orchestrator — three things worth a second pair of eyes

1. **The `@public` waiver and the 127 source-text assertions are one problem, and it spans my dimension and Testing's.** 20 test files contain 127 `expect(source()).toContain('export …')` assertions that read a module as a string. Six `@public` tags cite exactly those tests as the "contract" justifying an export with no importer. Whoever owns Testing should be told that these assertions are load-bearing for the dead-export gate, not just weak tests — removing them without replacing the waivers would flip six exports to reported-dead, which is arguably the correct outcome.

2. **Hand to Security: the HMAC/base64 toolkit is triplicated.** `base64UrlEncode`, `base64UrlDecode`, and `importSigningKey` each exist in three to four independent copies across `src/lib/assessment/session.ts`, `src/lib/booking/signed-link.ts`, `src/lib/booking/tokens.ts`, and `src/lib/oauth/state.ts`. I verified the duplication but did not compare the signing parameters, constant-time comparison, or TTL handling between copies — that comparison is the security question and I did not make it.

3. **Two things I could not settle and would want re-probed.** First, `workers/fleet-alerts` deploys with no CI config gate (Architecture finding 1) — I verified the workflow files but did not call the Cloudflare API, so I cannot say whether the deployed version matches `main`; whoever holds read access should confirm. Second, a live peer worktree exists at `.claude/worktrees/billing-detail-cohesion/` containing copies of files I flagged (`src/lib/db/services.ts`, `time-entries.ts`, `meetings.ts`, `src/lib/portal/operator/home.ts`). If another session is mid-change on the billing surface, several of my 39 test-only exports may be about to acquire callers, and the orchestrator should reconcile before filing.


---

# Agent: operator-arch-quality

## operator-arch-quality — Architecture + Code Quality for `operator/`

HEAD `0a3722a3`. Scope: `operator/` (218,887 lines, 448 `.py`, 16 `.sh`) plus the console-side twins under `src/lib/operator/customer-yaml/`. Overlay read at the pinned ref `5770b79f` from `~/dev/hermes-smd-overlay` (clone present; `git cat-file -e 5770b79f` succeeds).

---

## 1. Architecture

### Subsystem map

| Subsystem | Purpose (from its own docstring/README) | Runtime entry point | Tests |
|---|---|---|---|
| `adapter/` (8,295 non-test lines) | Trust-ceiling / audit / evidence / voice policy cores | `COPY operator/adapter/ /app/adapter/` (Dockerfile:1112); reached at boot by `safety-substrate/run_invariants.py`, and by 4 Captain CLIs | `adapter/tests`, `adapter/evidence/tests`, `adapter/voice/tests` |
| `safety-substrate/` | Phase A.5 boot invariants + sticky-stop + identifier/citation filters | `bootstrap.sh:807` runs `/app/safety-substrate/run_invariants.py` before the gateway | `safety-substrate/tests` |
| `workspace_broker/` | Unix-socket verb surface; the uid-gated door to the append-only audit ledger | `COPY … /opt/workspace-broker/`; `PYTHONPATH=/opt/workspace-broker` in `entrypoint.sh:384` | `workspace_broker/tests` |
| `connectors/` | Author-built MCP servers (`smokeball`, `msgraph-mail`) over a shared `_sdk` | per-connector venv + `<name>-mcp` console script | own venv conformance step in CI |
| `skills/` (58) | Skill bodies: `SKILL.md` + optional `pre_run.py` | `COPY operator/skills/ /app/skills/`; overlay `translate.stage()` copies `pre_run.py` into `<profile>/scripts/<skill>/` | per-skill `test_*.py` |
| `runners/medchron/` | The chronology pipeline, an explicit 40-stage DAG | `/opt/medchron/.venv`, `medchron.daemon` | own venv step in CI |
| `bin/` + `bin/lib/` | Captain-side CLIs (provision, decommission, reconcile, probes) | hand-run, plus 5 scheduled workflows | `bin/tests` |
| `templates/` | Dockerfile, `entrypoint.sh`, `bootstrap.sh`, `pre_run_gate.py`, drafting gate | the seat image itself | `templates/tests`, `templates/drafting/tests` |
| `customers/` | 4 live seats + 2 templates: `customer.yaml`, `routine-grid.yaml`, seed scripts | consumed by `provision-customer.sh` and the D1 projection | `tests/shipped-customer-configs.test.ts` |
| `contracts/` | The registries: `runtime-controls.yaml`, `overlay-pairs.json`, `operator-module-size.json`, `customer-yaml-blocks.yaml` | read by gates | `bin/tests/test_runtime_control_conformance.py` and siblings |

**Import graph.** AST walk over all 448 files, resolving only basenames unique to one package (`impgraph2.py`). Cross-package edges: `bin -> workspace_broker` (22), `safety-substrate -> adapter` (11), `bin -> adapter` (6), `skills -> adapter` (4), `bin/runners/templates/customers -> connectors` (7). **No package imported by nearly everything; no cycles.**

I withdrew a first result. A naive leaf-name match reported nine cycles; every one was a basename collision (`adapter.evidence` matching `bin/lib/evidence.py`, `server.py` existing in both `connectors` and `workspace_broker`, and ten more listed by the script). Confirmed by reading `adapter/evidence/tests/test_signing.py:21` and `bin/verify-audit-chain.py:45`.

### Carried findings

| # | Headline | Verdict |
|---|---|---|
| A1 [CRITICAL] | `trust_ceiling.py` claims parity, absent from `overlay-pairs.json` | **CLOSED** — `tests/operator-taint-gate.test.ts` (147 lines) derives the gate's expected class set from the `ActionClass` enum and pins the tuple; the source comment at `operator/adapter/trust_ceiling.py:186-201` was rewritten to state exactly what is and is not proven. Instrument: read both, plus `npx vitest run` (3 passed). Residual medium below. |
| A2 [HIGH] | `sticky_stop.py` has zero dispatch callers | **STILL OPEN**, restated — see finding 3 |
| A3 [HIGH] | Broker authorization enforced by source order | **CHANGED, core still open** — see finding 1 |
| A4 [HIGH] | No Python packaging; `sys.path` + `noqa: E402` | **STILL OPEN and larger** — see finding 2 (F9 also open) |
| A5 [HIGH] | Tracker skills are serial forks (71% of function-lines shared) | **CHANGED — the pair named is fixed, the class moved** — see finding 4 |
| A6 [MEDIUM] | `adapter/` not on any live request path | **STILL OPEN with a correction** — see finding 7 |
| A7 [MEDIUM] | `escalation_ledger` sync gate is a hand-maintained tuple | **STILL OPEN, now three such tuples** — see finding 5 |
| I13 (09-04) | Do all four live `customer.yaml` validate, and is there a gate? | **CLOSED** — `tests/shipped-customer-configs.test.ts:41-48` discovers seats by `readdirSync` (glob, not a list), runs the same `validate()` + `validateRoutineGrid()` the D1 projection runs, and self-tests with "discovers at least one shipped seat". 12 tests pass. |
| I1b (09-04) | Three skills' frontmatter names the retired `smd` seat | **CLOSED** — `grep -rn "slug: *smd\b\|smd-seat\|vaults/smd" operator/skills/*/SKILL.md` returns zero. |
| I15 (09-04) | 17 `@venturecrane.com` hits under `operator/` | **CLOSED** — `grep -rn "@venturecrane.com" --include='*.py' --include='*.md' --include='*.yaml' operator/ \| wc -l` returns 0. |
| F1, F9, I5 | (orchestrator: still open) | Confirmed and characterised precisely in findings 2, 3, 6 |

### Findings

**1. [HIGH] `workspace_broker/server.py:363` — `handle()` is 313 lines at cyclomatic complexity 63, and authorization is still positional.** The structural gate `if peer_pid != self.gateway_pid: raise PermissionError` moved from line 545 to **line 595**; everything above it is gateway-only *by position*, with five hand-rolled agent-uid checks inline at lines 381, 410, 460, 521, 572, plus a new `medchron_dispatch(...)` delegation at line 371 that runs before any gate in this function (it gates internally, via a proper dispatch table at `workspace_broker/medchron_verbs.py:76-80`). `tests/broker-verb-registry.test.ts` was added since the last review and is a real gate, but it asserts three things only: the extractor works, the verb set equals a 32-entry literal, and every verb is *named* somewhere in the broker test suite (five exempted). None of the three checks that a verb positioned above line 595 carries a uid gate. *Instrument:* read `workspace_broker/server.py:363-600`; read all of `tests/broker-verb-registry.test.ts`; `npx vitest run tests/broker-verb-registry.test.ts` (3 passed); AST census `fnlen.py`. *Falsifier:* had a fourth assertion enumerated pre-gate verbs and required a uid check, the registry test would have shown it, and my read of its three `it(` blocks would have found it. *Recommendation:* replace the if-chain with a verb table carrying `{handler, auth: gateway|agent_uid|root}`, and assert in the registry test that every entry declares one.

**2. [HIGH] There is still no Python packaging for the four core subsystems, and the cost is now measurable in three places.** `bin -> workspace_broker` is 22 edges, all resolved by `sys.path.insert(...)/"workspace_broker"` then a bare `from chain import ...` (`bin/verify-audit-chain.py:45-46`). Tree-wide: **118 `sys.path` mutations** (33 non-test), and **126 `# noqa: E402`**. F9's own probe list, verbatim from `grep -rn 'sys.path.insert' operator/safety-substrate/ operator/bin/lib/ --include='*.py' | grep -v test`:

```
operator/safety-substrate/refusal.py:148
operator/safety-substrate/trust_ceiling_log.py:127
operator/safety-substrate/invariants/invariant_6.py:134
operator/safety-substrate/invariants/invariant_7.py:138
operator/bin/lib/decommission_cli.py:43
operator/bin/lib/evidence.py:47
operator/bin/lib/sender_class.py:19
```

No fix is scheduled: `gh issue list --state open --limit 200` returns 52 issues, none matching `packag|sys.path|pyproject|monolith|module size` except #2613, which is medchron-specific and already shipped as a package. The three packaged subtrees (`connectors/_sdk`, `connectors/*`, `runners/medchron`) prove the pattern works here. *Falsifier:* an open issue or a root `pyproject.toml` would have shown in either probe; `find . -maxdepth 3 -name pyproject.toml` returns only the five connector/runner manifests.

**3. [HIGH → reframed] `safety-substrate/sticky_stop.py` (526 logical lines) is an unwired ss-console copy of a control that is live in the overlay.** F1 is open but its shape was not previously stated. In ss-console, the only non-test reader of `sticky_stop_state` is `runners/medchron/medchron/daemon.py:126`; nothing calls `record_tool_failure` or `record_refusal`. The arming is entirely overlay-side: `shared/cost_breaker.py:374-384` writes `HARD_STOP`, `plugins/hermes-smd-trust/__init__.py:261` reads it and blocks every tool, `shared/gate_inbound_cap.py:135` parks inbound. So the *control* is live on seats; the *module in this repo* has no runtime role, is not in `overlay-pairs.json`, and is 526 logical lines carrying its own baseline slot. The genuine inert arm is narrower: `sticky_stop_time_budget` (`runtime-controls.yaml`, `status: inert`, `wired_via: ''`) — a single run has no wall-clock brake. *Instrument:* `grep -rn "sticky_stop_state" operator/ src/ migrations/`; `git grep -n "UPDATE sticky_stop_state\|HARD_STOP" 5770b79f -- '*.py'` in the overlay; `python3 -c` dump of `runtime-controls.yaml`. *Falsifier:* one non-test `record_tool_failure(` call site in ss-console would have flipped this; the grep covered `.py`, `.sh`, `.yaml`, `.ts` and found one docstring. *Recommendation:* the Captain's wire-or-delete call is now a delete-or-enrol call — either retire the ss-console copy or add it to `overlay-pairs.json` the way `escalation_ledger.py` is.

**4. [MEDIUM] The tracker-skill duplication was fixed for the pair the last review measured and reappeared in a different pair.** AST + token-similarity over the four `pre_run.py` files (`dupfn.py`):

| pair | common fns | byte-identical | ≥0.8 similar | shared lines |
|---|---|---|---|---|
| client-verification-tracker ↔ medical-records-chaser | 20 | 7 | 3 | 16% |
| **medical-records-chaser ↔ lien-ledger-tracker** | **20** | **8** | **7** | **48% (~214 lines)** |
| deadline-miss-escalator ↔ lien-ledger-tracker | 12 | 3 | 2 | 11% |

The 71% pair is down to 16%. The new 48% pair shares nine byte-identical infrastructure helpers: `_emit_suppress`, `_find_skill_settings`, `_handoff_values`, `_is_iso_day`, `_load_ledger_module`, `_plan_counts`, `_pos_int`, `_pos_int_or_none`, `_write_pre_run_handoff`. **The "cannot split" rationale does not apply to them.** `tests/operator-module-size.test.ts` warns that the scheduler stages `pre_run.py` alone, and the overlay confirms it — `bootstrap/translate.py:1880` is a single `shutil.copy2`. But the two skills that *did* split solved this: `_load_sibling_module` (`skills/client-verification-tracker/pre_run.py:301-320`) falls back to `/opt/data/skills/<skill>` and `/app/skills/<skill>`, and `templates/Dockerfile:1115` is `COPY operator/skills/ /app/skills/`, so the fallback resolves. `client-verification-tracker` carries 9 sibling modules, `deadline-miss-escalator` 6; `medical-records-chaser` and `lien-ledger-tracker` carry one each and have no `_load_sibling_module` at all. *Falsifier:* if `/app/skills/` were absent from the image the fallback would be fiction and the duplication forced; the Dockerfile line is the check. *Recommendation:* give the two remaining skills the sibling loader and extract the nine helpers.

**5. [MEDIUM] Three vendored-copy gates, three hand-maintained membership tuples, and one duplicate pair with no gate at all.** Byte-identical non-test duplicates under `operator/` (md5 census): `escalation_ledger.py` ×6 (gated by `operator/tests/test_escalation_ledger_sync.py`, tuple `VENDORED_SKILLS` at line 29), `routing.py` ×2 (gated by `test_case_alert_routing_sync.py`, hardcoded skill list), `pre_run_gate.py` ×9 (gated by `test_pre_run_gate.py`, tuple `GATED_SKILLS` at line 28). None discovers by glob. The class has already produced a live instance: **`skills/deadline-miss-escalator/broker_writer.py` and `skills/client-verification-tracker/broker_writer.py` are 133 lines each and differ by one docstring word** (`diff` output is a single hunk: "escalator" vs "chase"), pinned by nothing — I5, still open. *Falsifier:* a `glob`/`rglob` in any of the three sync tests would have shown in `grep -n "def test_\|glob\|rglob"`; none has one. *Recommendation:* discover copies by hashing the canonical and globbing `skills/*/<name>.py`, which closes all four cases at once.

**6. [MEDIUM] `bin/lib/decommission.py` is 1,060 lines of off-boarding machinery whose destructive half has no real implementation in this repo.** `unwired_destructive_backends()` (`decommission.py:686-716`) checks six backends; the shipped CLI injects exactly one (`decommission_cli.py:243` wires `SeamAuditLogPreserver` when the runtime-read env is staged). `r2_deleter`, `vectorize_deleter`, `agentmail`, `fly`, `observability` are never injected outside `bin/tests/test_decommission.py`. A `--live` run therefore refuses at exit 5 unless `--allow-unwired`, which by the code's own words "tolerates skipped deletions." This is **fail-closed and correct** — it is the single best built-not-wired detector I found in the tree — but the capability is absent, and this is the venture's own "gone means gone" mechanism. Compounding it: **`bin/decommission-customer.sh:68-75` has no `case` arm for exit 5**, so the documented entry point logs `unknown exit 5` for the one refusal the pipeline is designed to produce. *Instrument:* read both files; `grep -rn "r2_deleter\|vectorize_deleter\|fly=\|agentmail=" --include='*.py' operator/` returns hits only under `bin/tests/`. *Falsifier:* one non-test injection site would have shown in that grep. *Recommendation:* add the exit-5 arm now; treat the five backends as the real work.

**7. [MEDIUM] `adapter/` (8,295 non-test lines) is reached at boot and by CLIs, never on a per-request path — and two of its modules have no Python importer at all.** `templates/Dockerfile:1112` and `:1255` place `adapter/` and `safety-substrate/` under `/app`, but only `/opt/workspace-broker` gets a `PYTHONPATH` (`entrypoint.sh:384`). The reachability that does exist is real and I under-stated it in a first pass: `bootstrap.sh:807` runs `/app/safety-substrate/run_invariants.py` at every container start, and those invariants import `adapter.audit_log` (`safety-substrate/refusal.py:150`). Beyond that, importers are four Captain CLIs. Zero-importer modules: `adapter/inbound_envelope.py` (298 lines — the ss-console re-implementation of ADR 0027, with the overlay's `hermes-smd-webhook-router` as the live one per `runtime-controls.yaml`) and `adapter/resolve_skill_pins.py` (108 lines, a hand-run CLI whose lookup rules are re-implemented in `scripts/validate-customer-yaml.ts`, the same position that got `adapter/validate_customer_yaml.py` retired for going stale). *Recommendation:* enrol or retire, on the `validate_customer_yaml.py` precedent.

**8. [MEDIUM] `adapter/evidence/packet.py` is a god object; `workspace_broker/establishment_store.py` is a four-lifecycle facade; the other three named files are large-but-cohesive leaves.** Read via AST outline:

- `packet.py` (1,652 raw / 1,128 logical) — request validation, chain-pin verification, audit-coverage tallying with its own 169-line class, a `ReadExecutor` abstraction, YAML redaction and secret-leak scanning, six `_fetch_*` data-access functions, CSV/JSON/YAML serializers, a **225-line prose renderer** (`_readme_text`), and a builder whose `build()` is 206 lines. Five unrelated responsibilities. **God object.**
- `establishment_store.py` (2,107 / 1,415) — one class, 40 methods, already split once from `establishment.py` on 2026-08-24. Four independent state machines share it: rule propose/decline/lapse, operations request propose/resolve, act propose/commit, submission/materialize. The tell is four near-identical private refusal-namers (`_refuse_undeclinable`, `_refuse_unresolvable`, `_claim_refusal`, `_refuse_restated`). **Multi-lifecycle facade, not a leaf.**
- `connectors/smokeball/.../server.py` (2,132 / 1,099) — ~50 MCP tool wrappers over the REST surface plus ~20 enrichment helpers. One responsibility. **Cohesive leaf**; its own comment at line 623 records the ratchet catching it and the helpers already being aliased out.
- `adapter/voice/transform.py` (1,379 / 670) — one domain. **Cohesive leaf**, with a 143-line `transform()`.
- `skills/lien-ledger-tracker/pre_run.py` (1,474 / 969) and `skills/deadline-miss-escalator/pre_run.py` (1,470 / 832) — **cohesive by force**: the single-file staging contract is why they are this size (finding 4).
- `bin/lib/decommission.py` (1,060 / 591) — 6 Protocols, 6 stubs, the pipeline. Ports-and-adapters, **cohesive leaf**.
- `workspace_broker/server.py` (1,011 / 580) — cohesive except `handle()` (finding 1).

**9. [LOW] Two shell scripts over 1,000 lines sit outside every size ceiling.** `bin/provision-customer.sh` (1,369) and `templates/entrypoint.sh` (1,309), plus `bootstrap.sh` (916) and `boot-smoke-test.sh` (577). All four are structured — `set -euo pipefail` present (lines 69, 4, 41, 25 respectively, after their header comments) and 3-11 functions each — and `shellcheck` 0.11.0 is essentially clean over them (details in Code Quality finding 6). The module-size ratchet covers `*.py` only (`tests/operator-module-size.test.ts:88` walks `entry.endsWith('.py')`).

**10. [POSITIVE, verified] Zero dead modules under `operator/`.** Scripted: 245 non-test, non-`__init__` modules; textual-reference scan across every `.py/.sh/.ts/.json/.yaml/.md/.toml/Dockerfile` in the repo found 5 with zero references. I read all five — `bin/retire-output-class-spec.py`, `bin/gmail-sa-smoke.py`, `bin/voice-corpus-extract.py`, `customers/pilot-smokeball/seed/set_client_contact_email.py`, `customers/pilot-smokeball/seed/delete_memo.py`. Every one is `#!/usr/bin/env python3` + `argparse` + `if __name__ == "__main__"` with a docstring stating why it exists and when. Hand-run entry points, not dead code.

**11. [POSITIVE, verified] The medchron runner is the strongest architecture in the tree.** `runners/medchron/medchron/dag.py:82-185` is an explicit 40-stage tuple with declared `requires`, `paid`, `scope`, `invalidates`, and an `exit_map` binding each non-zero exit to a terminal state (`FAILED`/`HELD`/`REFUSED`) with a client-legible reason. The four controls `runtime-controls.yaml` marks `enforced` with `wired_via: runner / …/gates/<name>.py` all resolve, and I traced past the shim: `gates/*.py` are 13-21 line re-exports; the enforcement is `stages/strip.py`, `stages/coverage.py`, `audit/coverage.py`, `gates/cross_client.flags`, and all are named in the DAG (`strip_falsify`, `strip_dry`, `strip_apply`, `coverage_gate`, `audit`, `identity`). Worth a note: `wired_via` points at the shim, so a reviewer verifying it lands on a 15-line file one hop from the answer.

**12. [POSITIVE, verified] The registries are honest and gated.** `contracts/runtime-controls.yaml` carries 20 controls with `status ∈ {enforced 12, unprobed 7, inert 1}`, each `unprobed`/`inert` row carrying a dated `risk_review` that names the exposure in plain terms. I verified every overlay-side `wired_via` against the pinned ref: `pre_tool_call / hermes-smd-trust` → `plugins/hermes-smd-trust/__init__.py:1367`; `post_tool_call` and `post_llm_call / hermes-smd-audit` → `:617` and `:618`; `pre_gateway_dispatch / hermes-smd-webhook-router` → `:833`; `tool / smd_deliver_draft` → present in `plugins/hermes-smd-drafting/__init__.py`. The two empty `wired_via` values are the two rows whose status already says they are not wired. `operator/bin/tests/test_runtime_control_conformance.py` enforces the registry's own shape (enforced rows must name an existing probe; unprobed/inert rows must carry a dated risk review; every probe must name a real control). This is the opposite of the review's standing theme: an annotation that correctly declares itself not to be a control.

### Residual on the closed CRITICAL

**[MEDIUM] The cross-repo half of the taint-gate parity claim is still a human reconciliation, and the two cores do agree today.** `tests/operator-taint-gate.test.ts` pins the tuple against the enum *in this repo* and its docstring says so. I checked the other half by hand: `operator/adapter/trust_ceiling.py:203-210` and `plugins/hermes-smd-trust/enforce.py:87-97` at `5770b79f` both list exactly `EXTERNAL_SEND, EXTERNAL_SEND_INTERNAL, EXTERNAL_SEND_CLIENT, EXTERNAL_SEND_VENDOR, DESTRUCTIVE, COMMITMENT, CODE_EXECUTION`. Nothing in CI checks that. `verify-overlay-pairs.py` already fetches the overlay at the pinned ref in `operator-substrate.yml` and could assert the two sets match.

### Not checked (Architecture)

- Whether the `unprobed` controls fire on a live seat. Requires a running Machine; the venture's own rule forbids a second runtime beside a live gateway.
- D1 / R2 / Fly-volume layers of any removal claim (no read-only credentials exercised).
- `assessment-eval/`, `grading/`, `verticals/`, `voice-gate/`, `fixtures/` (no `.py`; content, not architecture).
- Whether the 11 unenabled skills are a deliberate catalog. ADR 0037 tenet 5 says packs are templates, so I did not treat `SKILL.md`-only entries as dead; I report only the two carrying executable code.

### Summary

The operator tree has no package cycles, no dead modules, clear subsystem boundaries, and a set of registries and gates that are unusually honest about their own limits. What holds it back is size and one authorization shape: 16 modules over the logical 500 ceiling (45 over raw), one genuine god object in `adapter/evidence/packet.py`, a 313-line broker dispatcher at complexity 63 where authorization is positional, and no Python packaging, so byte-copy vendoring remains the only sharing mechanism and three hand-maintained tuples guard it.

### Proposed grade: **C**

Rubric line: "C — 3+ files >500 lines OR unclear boundaries." Sixteen non-test modules exceed 500 **logical** lines (the ratchet's own measure, matching `eslint.config.js:20`'s `skipBlankLines`/`skipComments`); 45 exceed 500 raw. I graded on the logical measure because it is the one the repo enforces and the one that makes the two halves of the codebase comparable. It is not D despite one god object because the boundaries are clear, the ratchet only tightens, and there are no cycles.

---

## 2. Code Quality

### Carried findings

| # | Headline | Verdict |
|---|---|---|
| CQ7 [HIGH] | 261 `noqa` across 121 files naming rules nothing evaluates; no linter for `operator/` | **PARTLY CLOSED, ratio worse** — see finding 1 |
| CQ (implicit) | No size or complexity ceiling for Python | **HALF CLOSED** — file ceiling exists and gates; function/complexity ceiling does not — finding 3 |
| Sec-6 [POSITIVE] | 0 `shell=True`, 0 `os.system` | **CONFIRMED** — `grep -rn "shell=True\|os.system(" --include='*.py' operator/ \| grep -v /tests/ \| wc -l` = 0 |

### Findings

**1. [HIGH] The ruff gate is real, and it runs in a workflow that cannot block a merge.** `ruff 0.15.17 check .` from `operator/` exits 0 with "All checks passed!" — the selection in `operator/ruff.toml` (`E4`, `E7`, `E9`, `S102`) genuinely reports zero, exactly as its header claims. But branch protection tells a different story:

```
$ gh api repos/venturecrane/ss-console/branches/main/protection --jq '.required_status_checks.contexts'
["Typecheck, Lint, Format, Test"]
```

That one context is `verify.yml:24`. `operator-substrate.yml` — which runs ruff, every Python suite, the connector conformance venvs, the medchron runner tests, and the SEC-32 overlay-runtime hash gate `verify-overlay-pairs.py` — is not required. `ruff.toml`'s header calls itself "A GATE, NOT A BASELINE"; the CI wiring does not deliver that. `tests/operator-module-size.test.ts` already knows this and cites it as its reason for living in the vitest suite instead.

A correction to the repo's own citation, since this review's theme is checking claims: that comment says "On 2026-08-21 PR #2526 merged with the substrate suite red." The run history does not support it as written. PR #2526's branch failed at 3fad83b8 (18:05), the author pushed ed4c9183 which passed at 18:18, and the merge was at 18:24 against the green head. The structural exposure is real; that particular instance is not.

*Falsifier:* if `operator-substrate` were a required context it would appear in the API response above; if ruff were failing, `ruff check .` would have printed violations. *Recommendation:* add `operator-substrate` to `required_status_checks.contexts`. Everything else in this section is downstream of this one line.

**2. [HIGH] `noqa` annotations grew 50% and the inert share got worse.** Counted with `grep -rhoE 'noqa: *[A-Z]+[0-9]*' --include='*.py' operator/ | sort | uniq -c`:

| rule | count | activated by `ruff.toml`? |
|---|---|---|
| BLE001 | 209 | no |
| E402 | 126 | yes (`E4`) |
| F401 | 28 | no |
| ANN001 | 6 | no |
| S310 | 5 | no |
| F403 | 5 | no |
| E731 | 4 | yes (`E7`) |
| SLF001, S608, S102, S603, F841, ARG002 | 9 | only S102 (2) |

**392 annotations across 166 files; 132 activated, 260 inert.** Last review: 261 across 121, 106 activated (41%). Now 34%. Zero bare `noqa` with no code, which is good hygiene on the annotations themselves. The unselected debt has also grown against `ruff.toml`'s own 2026-08-24 measurements: F401 34→55, BLE001 5→11, S110 14→21, S603 29→38, E501 2,119→4,285. The file names BLE001 (then 5 uncovered) as "the cheapest next step"; it has doubled. *Instrument:* the grep above, plus `ruff check . --select <code> --no-cache` per code. *Falsifier:* if the selection covered BLE001, `ruff check .` would fail on the 11 uncovered sites; it exits 0.

**3. [HIGH] No type checker runs over 448 Python files, and no function-length or complexity ceiling exists for Python.** `grep -rn "mypy\|pyright\|pytype" .github/ operator/ruff.toml operator/pytest.ini package.json` returns exactly one hit, the sentence in `ruff.toml`'s header saying no mypy config exists. No `mypy.ini`, no `pyrightconfig.json`, no `[tool.mypy]` in any of the five `pyproject.toml` files. The tree annotates heavily (`ruff.toml` records 2,955 of 4,732 defs annotated) — nothing verifies any of it. On ceilings, AST census over 2,572 non-test functions:

| measure | TS side (`eslint.config.js:20-27`, error-level, 0 violations) | Python side |
|---|---|---|
| file lines | `max-lines` 500 | ratchet exists, 16 baselined |
| function lines | `max-lines-per-function` 75 | none — **99 functions exceed 75** |
| complexity | `complexity` 15 | none — **91 exceed 15** |

Worst: `workspace_broker/server.py:363 handle` (313 lines, cx 63), `medchron/stages/billing_chart.py:126 run` (cx 52), `medchron/stages/strip.py:110 _run` (cx 47), `adapter/evidence/packet.py:994 _readme_text` (225 lines), `skills/client-verification-tracker/dispatch_envelope.py:204 build_and_write` (251 lines, cx 35). My complexity counter is approximate — it adds 1 per `If/For/While/ExceptHandler/BoolOp/IfExp/Assert` node, so a multi-operand `BoolOp` is undercounted and `Assert` inflates test-adjacent code; the ranking is what matters, and 63 survives any reasonable discount. *Falsifier:* a `[tool.mypy]` section or a `max-complexity` setting would have shown in the config sweep.

**4. [MEDIUM] Error handling is disciplined but inconsistent in exactly one place.** AST classification of every non-test `except` handler under `operator/`: **535 total — 184 log or re-raise, 303 degrade while doing real work, 48 are a bare `pass`.** Of the 48 silent ones, 27 are narrow-typed (`except ValueError` in a date-parse ladder at `workspace_broker/escalation_ledger.py:189`, `except OSError` on a best-effort chmod at `:775`) and only **21 are broad-and-silent** (`except Exception: pass`, zero bare `except:`). That is 4% — not pervasive. I read six and all six carry an inline rationale: closing a connection in `finally` (`bin/lib/evidence.py:309`), a receipt write that must not kill a paid call (`medchron/ledger.py:65`), logging that must not break auth (`smokeball_connector/client.py:292`), a best-effort widen where the flag stands (`medchron/audit/run.py:108`).

The inconsistency is narrower and worth fixing: the *same* failure — a wake-observability write — is swallowed with no log at `skills/lien-ledger-tracker/pre_run.py:935`, `skills/paid-media-anomaly-watcher/pre_run.py:396`, `skills/medical-records-chaser/pre_run.py:633`, `skills/retainer-hours-reconciler/pre_run.py:393`, all with the identical comment "observability never gates the wake". Twelve of the 48 are the same two sites amplified by the 6× `escalation_ledger.py` vendoring. *Recommendation:* one `log.warning` in the shared helper covers all four at once, once finding 4 of Architecture extracts it.

**5. [MEDIUM] `shellcheck` never runs in CI, and five workflows carry `# shellcheck disable=` directives that suppress nothing.** `grep -rn "run:.*shellcheck\|actionlint\|uses:.*shellcheck" .github/workflows/` returns nothing. Meanwhile `unaudited-send-reconcile.yml:171`, `terminal-state-reconcile.yml:100`, `cron-slot-watchdog.yml:101`, `control-probes.yml:93` and `audit-chain-verify.yml:133` each carry a `# shellcheck disable=SC2086` with a written justification. That is five more inert annotations, the same shape as the pre-ruff `noqa` pile. The tree happens to be clean — `shellcheck -f gcc` over all 16 `operator/**/*.sh` produces **7 findings total: 1 error, 2 warnings, 4 notes**, and the error is `SC2148` (no shebang) on the generated `templates/_env-arrays.generated.sh`. Nothing keeps it clean. *Recommendation:* one `shellcheck operator/**/*.sh .github/**/*.sh` step; it passes today at `--severity=warning` with the generated file excluded.

**6. [MEDIUM] DRY: ~214 duplicated lines between two skills, an unpinned 133-line twin pair, and three hand-maintained sync tuples.** Full detail in Architecture findings 4 and 5; recorded here because the DRY rubric line is a Code Quality line. The measured pieces: 9 byte-identical + 6 near-identical functions across `medical-records-chaser/pre_run.py` and `lien-ledger-tracker/pre_run.py`; `broker_writer.py` differing by one docstring word with no gate; `VENDORED_SKILLS`, `GATED_SKILLS` and the routing skill list all hardcoded where a glob would do.

**7. [LOW] Three skills' frontmatter diverges from the other 55 and the gate cannot see it.** All 58 `SKILL.md` files parse and carry the identical eight top-level keys (`name`, `description`, `version`, `author`, `license`, `platforms`, `prerequisites`, `metadata`) — a clean conformance result. Under `metadata`, 58 carry `hermes` and 55 carry `smd`; the three without are `email-reply`, `inbox-triage`, `workspace`, the same three the 09-04 I1b claim named. `operator/bin/tests/test_skill_frontmatter.py:41-64` asserts only that the block parses, is a mapping, that `name` matches the directory, and that `description`/`version` are non-empty strings. A schema divergence like this is invisible to it. *Recommendation:* assert the `metadata.smd` block's presence and shape, or record the three as a shrinking exemption the way `broker-verb-registry.test.ts` does.

**8. [LOW] `bin/decommission-customer.sh` and its CLI disagree about the exit-code contract.** Detailed in Architecture finding 6: the CLI added exit 5 for its fail-closed refusal; the wrapper's `case` covers 0/2/3/4/130 and reports "unknown exit 5" for the one code that matters most.

**9. [POSITIVE, verified] The module-size ratchet is a real merge gate and it is green.** `tests/operator-module-size.test.ts` walks `operator/**/*.py`, counts logical lines with a character-walk that tracks triple-quote state across the line, and both enforces and regenerates `operator/contracts/operator-module-size.json` from the *same* counter, so the two cannot disagree. It ratchets in both directions — a file that shrinks also fails, with a message telling you to regenerate. It carries a self-test proving the counter distinguishes code from comments and docstrings, including a regression case for a bug its first draft actually had. Current baseline: **16 modules, ceiling 500**, top entry `establishment_store.py` at 1,415. `npx vitest run tests/operator-module-size.test.ts` → 3 passed. It sits in `npm test`, inside the one required context, deliberately.

**10. [POSITIVE, verified] `subprocess` hygiene holds.** Zero `shell=True`, zero `os.system(` in non-test `operator/` code. Re-confirmed at HEAD.

**11. [POSITIVE, verified] The shell scripts are structured, not sprawling.** All four large ones open with `set -euo pipefail`, use functions, and pass shellcheck at warning severity apart from the generated file. `bin/provision-customer.sh` hard-validates the customer.yaml through the canonical TS validator before any Fly action (`:255`).

### Not checked (Code Quality)

- Runtime behaviour of any Python module (no seat exercised).
- `black`/formatting consistency — no formatter is configured, so there is no standard to measure against.
- Test quality inside `operator/**/tests/` — another agent owns Testing.
- Whether the 4,285 `E501` and 1,843 `ANN001` hits represent real defects; they are style debt outside the enabled selection, counted only to show the trend since 2026-08-24.

### Summary

The Python half has gone from "no linter at all" to a genuinely clean, honestly-scoped ruff gate plus a well-built file-size ratchet, and error handling is far better than the raw `except Exception` count suggests (184 handlers log or re-raise; 21 of 535 are broad-and-silent). Two things hold the grade down. The gate that matters most runs in a workflow that branch protection does not require, so ruff, the Python suites, the connector conformance and the overlay runtime-hash gate can all be red at merge. And the annotation pile grew rather than shrank — 260 of 392 `noqa` still name rules nothing evaluates, 209 of them for a single rule the config itself calls the cheapest next step, joined now by five `# shellcheck disable` directives for a linter that never runs.

### Proposed grade: **C**

Rubric line: "C — inconsistent error handling OR notable DRY OR 3+ dead modules." The DRY is notable and measured (~214 duplicated lines in one pair, an ungated 133-line twin, three hardcoded sync tuples), and error handling is inconsistent in one identified class. It is not D: there are zero dead modules, swallowed errors are 4% of handlers rather than pervasive, and every silent handler I read carried a rationale. It is not B because no type checker covers 448 files, no function or complexity ceiling covers Python at all, and the lint gate is not a required check.

---

## Overall notes for the orchestrator

Three things I would want a second pair of eyes on.

1. **The required-check list is one line long, and I think it is the highest-leverage finding in my scope.** `gh api repos/venturecrane/ss-console/branches/main/protection --jq '.required_status_checks.contexts'` returns `["Typecheck, Lint, Format, Test"]`. Everything in `operator-substrate.yml` — ruff, ~640 Python tests, connector conformance, the medchron runner suite, and `verify-overlay-pairs.py`, which is the *only* check that can see a neutered overlay runtime file — is advisory. Two teams have already routed around it by moving individual gates into the vitest suite. Worth cross-checking with the Testing and Golden-Path agents, since it changes what "CI is green" means for both.

2. **`bin/lib/decommission.py`: five of six destructive backends have no implementation.** It fails closed and refuses `--live`, which is why it has stayed invisible, but the venture's own "gone means gone" doctrine names this pipeline as the mechanism, and a seat retirement on 2026-08-25 was done by hand across git, D1, R2 and Fly. Someone should confirm with the Captain whether the pipeline is intended to work or is superseded by the manual runbook, because the tests, the archive machinery and the 1,060 lines all read as though it is intended to work.

3. **Two claims I corrected mid-review — check my corrections, not just my findings.** I reported nine package cycles and then withdrew all nine as basename collisions (`adapter.evidence` matching `bin/lib/evidence.py`). And `tests/operator-module-size.test.ts` cites PR #2526 as having "merged with the substrate suite red"; the run history shows it went red at 3fad83b8, green at ed4c9183, and merged against the green head. If either correction is wrong, one finding gets stronger and one gets weaker.

**Handed to other dimensions:**
- *Security:* `workspace_broker/server.py:363` — authorization by source position inside a 313-line, complexity-63 dispatcher, with a new `medchron_dispatch` delegation ahead of every gate in that function.
- *Security:* `operator/adapter/trust_ceiling.py` and `plugins/hermes-smd-trust/enforce.py` agree on the taint-gated class set today (I diffed them by hand at the pinned ref); nothing in CI checks that they continue to.
- *Testing:* `tests/broker-verb-registry.test.ts` exempts five verbs (`health`, `job_cancel`, `job_heartbeat`, `job_list`, `job_list_claimable`) from its "named in a test" assertion, and the assertion is "named", not "tested".
- *Dependencies/Docs:* no root Python packaging; 118 `sys.path` mutations and 126 `noqa: E402` are the cost, and no open issue tracks it.


---

# Agent: security-testing

## Review report — SECURITY + TESTING
Repo: venturecrane/ss-console @ 0a3722a3 (== origin/main). Agent: security-testing. Date: 2026-09-09.

---

### 2. Security

**Carried findings**

| # | Headline (2026-08-23 §2) | Status | Instrument |
|---|---|---|---|
| C1 | HIGH booking manage token written to `events` (FIXED 08-24) | **CLOSED, still closed** | `src/pages/api/events.ts:179,238` + `:215 redactOpaqueSegments`; `src/components/EventsTracker.astro:111,171`; `src/layouts/Base.astro:57-64` shows the tracker now inside the `disableAnalytics` guard |
| C2 | HIGH no security response headers anywhere | **CLOSED, live-verified** | `src/lib/security/response-headers.ts:39-58`, wired at `src/middleware.ts:247-250`; `curl -sSI` on all three hosts returns HSTS + CSP frame-ancestors + Referrer-Policy + nosniff + XFO (see finding 1 below) |
| C3 | MEDIUM `/api/events` rate limit bypassable via client `session_id` | **STILL OPEN** | `src/pages/api/events.ts:96-100` `resolveSessionId` still prefers `body.session_id`; `:286-300` counts rows for that id and fails open on error |
| C4 | MEDIUM gitleaks scans working tree only | **STILL OPEN** | `.github/workflows/security.yml` Run Gitleaks step: `./gitleaks detect --source . --verbose --no-git --config .gitleaks.toml`, despite `fetch-depth: 0` on the checkout |
| C5 | MEDIUM fresh `createRemoteJWKSet` per request | **STILL OPEN** | `src/lib/operator/mcp/token-validation.ts:62-71` constructs it inside `verifyPinnedClerkToken` |
| C6 | MEDIUM JIT MCP identity corrupts audit attribution | **STILL OPEN** | `src/lib/operator/mcp/mcp-route.ts:137-146` returns `localUserId: subject` (Clerk id); authored path returns the local id at `token-validation.ts:167` |
| C7a | LOW MCP audience enforced only when present | **STILL OPEN, documented** | `token-validation.ts:99-107` `if (claims.aud && !audIncludes(...))`, with an inline rationale for Clerk DCR tokens that omit `aud` |
| C7b | LOW booking limiter allows when KV binding undefined | **STILL OPEN** | `src/lib/booking/rate-limit.ts:48-51` `if (!kv) return { allowed: true, ... }` |
| C7c | LOW raw upstream body logged on Google token refresh | **STILL OPEN** | `src/lib/db/integrations.ts:255-256` logs `${response.status} ${body}` |
| C7d | LOW client email in Worker logs on Clerk invitation failure | **STILL OPEN** | `src/pages/api/portal/products/operator/invitations.ts:150` `console.error('Clerk invitation failed', { email, message })` |
| C7e | LOW `ss_sid` cookie missing `HttpOnly` | **STILL OPEN, by design** | `src/pages/api/events.ts:328-334` builds `Path=/; Max-Age=…; SameSite=Lax` (+`Secure`) with no `HttpOnly`; the cookie is read by client JS in `EventsTracker.astro`, so this is the analytics cookie, not a session cookie |
| C7f | LOW unescaped `filename` in `Content-Disposition` | **STILL OPEN** | `src/pages/api/admin/assessments/[id]/transcript.ts:35,40` interpolates R2 `customMetadata.originalName` unescaped; same shape at `.../deliverables/[...key].ts:27,33` |
| C9 | MEDIUM no Python security lint; `# noqa: S603` names a tool that never runs | **STILL OPEN** | `.github/workflows/security.yml:268-271` — packs are `p/typescript`, `p/javascript`, `p/security-audit`, `p/owasp-top-ten`. No `p/python`. The nosemgrep-justification gate greps `--include='*.ts' --include='*.js'` only |
| C9a | LOW seam-pull record keys become SQL identifiers | **STILL OPEN** | `operator/bin/lib/seam_pull.py:216-221`; see finding 5 |
| C10 | LOW two unintended public endpoints | **STILL OPEN** | Both files still under `src/pages/api/booking/`; `grep -E '^export (const\|async function\|function) (GET\|POST\|...)'` returns zero in each |
| C11 | LOW latent auth bug in dead `oauth/callback` | **STILL OPEN** | `src/pages/api/oauth/callback.ts` present; the dedicated middleware exemption survives at `src/middleware.ts:120` |
| I3 | Portal has no route-enumerating scoping test | **STILL OPEN, 20th named** | See finding 2 |
| I12b | OAuth token store returns `ok:true` when `FLY_API_TOKEN` unset | **code-level, not live** | `npx wrangler secret list --name ss-web` lists `FLY_API_TOKEN` as `secret_text` on the deployed worker. Presence only; no value read |

**Findings**

1. **[POSITIVE, verified] Security headers are not merely merged, they are live on all three hosts.** `curl -sSI https://smd.services https://admin.smd.services https://portal.smd.services` each returned `strict-transport-security: max-age=31536000; includeSubDomains`, `content-security-policy: frame-ancestors 'none'`, `x-frame-options: DENY`, `x-content-type-options: nosniff`, `referrer-policy: strict-origin-when-cross-origin`. The wiring point is deliberate and correct: `src/middleware.ts:247-250` wraps `handleRequest` so the headers land on the early-return 401/403/redirect paths too, which is most of the auth surface. No resource CSP, no `Permissions-Policy`, no HSTS `preload` — each absence is documented with its reason at `src/lib/security/response-headers.ts:12-24,54-55` (the SignWell signing iframe and Clerk's bundle would break under a blind `script-src`; preload is irreversible and the Captain's call). Pinned by `tests/security-headers.test.ts`. **Instrument:** the three curls above plus reading the module. **Falsifier:** a host returning none, or returning them only on 200s — the admin and portal probes both returned 302s and carried the full set, so the early-return path was exercised, not assumed. **Recommendation:** none; the remaining CSP work is correctly scoped as separate.

2. **[MEDIUM] A portal route that forgets its tenant helper is a cross-tenant read, and nothing enumerates the set.** Admin has a mechanical guard: `tests/admin-routes-require-session.test.ts:37-48` walks every file under `src/pages/api/admin`, requires `requireAdminSession`, and keeps an `EXEMPT` set (currently empty) with a second test pruning stale entries. Portal has no counterpart — searching every test that mentions `api/portal` for a directory walk (`readdirSync`/`collectRouteFiles`) returns only `tests/advanced-settings-surface.test.ts`, which is not an enumerator. This matters more than "an untested convention" because of what the middleware does and does not do: `enforcePortalAuth` (`src/middleware.ts:144-162`) admits **any** signed-in Clerk user and returns; tenant binding happens per-route in `getPortalClient` / `resolveOperatorAccess` / `resolveHostedAgentAccess` / `authorizeAdvancedSettings`. So a new portal route missing its helper is not merely unscoped — it is readable by any authenticated user of the shared Clerk instance. Today the invariant holds: 19 of the 20 files under `src/pages/api/portal/**` call one of those four helpers, and the 20th is **`src/pages/api/portal/operator/settings/trust-ceiling.ts`**, an 11-line `errorResponse(410, 'Scalar trust ceiling is retired')` tombstone that needs none. Both routes added since `cc97518d` are scoped (`admin/clients/[id]/operator-agreements.ts` on `requireAdminSession`, `portal/products/operator/[instance]/start-subscription.ts` on `resolveOperatorAccess`). **Instrument:** `git ls-files 'src/pages/api/portal/**/*.ts'` (20 files) with a per-file helper scan, every "NONE" opened and read; `git log --diff-filter=A --name-only --format= cc97518d..HEAD -- src/pages/api`. **Falsifier:** a portal route with no helper after reading it. My first two greps produced 30 and then 25 false candidates because they used a fixed helper vocabulary; reading each resolved all of them, which is the same correction the last review had to make. **Recommendation:** clone the admin enumerator for `src/pages/api/portal/**` with an explicit `EXEMPT` entry for the 410 stub.

3. **[MEDIUM] `/api/events` is an unauthenticated, effectively unbounded D1 write.** Three properties compose. `resolveSessionId` (`src/pages/api/events.ts:96-100`) prefers the client-supplied `body.session_id` over the cookie whenever it matches `UUID_RE`, so a caller minting a fresh UUID per request always counts zero. `checkRateLimit` (`:286-300`) counts rows for that id and, on a D1 error, returns allow. And unlike `/api/contact`, `/api/intake/send`, `/api/booking/reserve`, `/api/auth/magic-link` and all three `/api/assessment/*` routes, this one calls no `rateLimitByIp`. Carried C3, unchanged at HEAD. **Instrument:** read of the three code regions; `git grep -nE 'checkRateLimit|rateLimitByIp'` for the comparison set. **Falsifier:** an IP limiter or a server-authoritative session id on this path — neither exists. **Recommendation:** add `rateLimitByIp(env.BOOKING_CACHE, 'events', clientIp)` ahead of the session-keyed bucket; keep the session bucket for per-visitor fairness.

4. **[LOW] The Sentry webhook's replay window is bypassable with a non-numeric timestamp.** `src/pages/api/webhooks/sentry.ts:64-70` guards the staleness check with `if (Number.isFinite(timestampSec))`. A header of `abc` yields `NaN`, the check is skipped, and the request proceeds. The HMAC at `:135-152` is computed over `rawBody` **only**, so the timestamp is not bound into the signature and a captured valid request replays indefinitely. Contrast Stripe, which does this correctly: `src/pages/api/webhooks/stripe.ts:400` signs `${timestamp}.${body}`, so a forged timestamp invalidates the MAC. Blast radius is bounded — the handler upserts one row in `cost_anomaly_alerts` keyed `(entity_id, alert_date, driver)` (`sentry.ts:99-111`), so a replay overwrites an alert rather than creating unbounded rows or granting anything. No test covers the timestamp path (`git grep -n 'sentry-hook-timestamp' -- tests` returns nothing). **Instrument:** read of `:44-70` and `:135-152`, compared against `stripe.ts:386-427`. **Falsifier:** the timestamp appearing in the signed payload, or the guard being an `else return 401` — neither. **Recommendation:** treat a non-finite timestamp as a rejection, not a skip.

5. **[LOW] A `# noqa` justification that covers the safe half of the same f-string.** `operator/bin/lib/seam_pull.py:218` carries `# noqa: S608 — table from MEMORY_EXPORT_TABLES`. That claim is true — `table` reaches the function from the literal tuple at `:74` via `:272`. But the same statement interpolates `col_defs`, built at `:216` from `rows[0].keys()` — the keys of a JSON body served by the seat, validated nowhere — into a quoted-identifier list. Line 220's `# noqa: S608` carries no justification at all. `sqlite3.Connection.execute` runs one statement, so this is statement-mangling rather than multi-statement injection, and reaching it means already controlling the Machine. Recorded because it is the review's standing theme in miniature: the annotation names the input that is safe and is silent about the one beside it. Note the structural reason the bare `# noqa` on line 220 survives: the justification gate in `security.yml` covers `nosemgrep` in `*.ts`/`*.js` only, so Python suppressions have no equivalent. **Instrument:** read `:198-226`, `:74`, `:272`. **Falsifier:** `col_defs` drawn from an allowlist, or a key validation before the f-string — neither present. **Recommendation:** reject keys failing `str.isidentifier()`; add a Python `# noqa` justification gate mirroring the nosemgrep one.

6. **[LOW] CSRF rests on `SameSite=Lax` alone, with no origin check and no test.** The portal ships 0KB JS and drives state changes through plain `<form method="POST">` (e.g. `src/pages/api/portal/operator/change-request.ts`). The two SS-owned cookies are correctly attributed — `src/lib/auth/session.ts:251` is `HttpOnly; Secure; SameSite=Lax; Path=/; Max-Age=<role-derived>`, and `src/lib/portal/operator/change-request-flash.ts:18` matches with `Max-Age=60`. `SameSite=Lax` does block cross-site form POSTs in current browsers, so the posture is defensible, but it is a single layer: `git grep -niE "csrf|verifyOrigin|sec-fetch-site" -- src` finds only prose and the OAuth `state` HMAC (`src/lib/oauth/state.ts:7`), no request-time origin check on any state-changing route. **Positive within this:** there is no write-on-GET anywhere — scanning every admin and portal API file that exports `GET` for `INSERT`/`UPDATE`/`DELETE FROM` returns zero, so the Lax carve-out for top-level GET navigation is not reachable. **Instrument:** the two greps above plus reading both cookie builders. **Falsifier:** a state-changing GET, or a session cookie without `SameSite` — the scan could have returned either and returned neither. **Recommendation:** add a `Sec-Fetch-Site`/`Origin` check on state-changing POSTs as defence in depth, and a test pinning the cookie attributes.

7. **[POSITIVE, verified] Every API route's gate is accounted for; the public set is small and each member is justified.** All 48 files under `src/pages/api/admin/**` carry `requireAdminSession`. All 20 under `src/pages/api/portal/**` are covered per finding 2. The three `src/pages/api/internal/*` routes gate on `verifyMachineRequest(request, env.MACHINE_HEARTBEAT_KEY, env.DB)` (`heartbeat.ts`, `runtime-summary.ts:51`, `sentry-probe.ts:26`) — my first grep called all three ungated, which is why each was opened. The genuinely public routes and their justification: `/api/health` (liveness); `/api/contact`, `/api/intake`, `/api/intake/send`, `/api/booking/{slots,reserve}` (public forms, all `rateLimitByIp`); `/api/booking/manage/[token]*` (the token **is** the auth, hashed before lookup and expiry-checked — `manage/[token].ts:9-16`); `/api/assessment/*` (all four `rateLimitByIp`, 40/200/60 per hour); `/api/auth/*` and `/api/oauth/callback` (auth handshakes); `/api/webhooks/*` (signature-verified, item 8); `/api/mcp` (9-line 410 tombstone); `/api/operator/[customer]/mcp` (gated by `validateMcpToken` inside `handleMcpPost`); `/api/checkout/hosted-agent` (calls `getPortalClient`, 303s to sign-up when absent). The two exceptions are C10's helper modules. **Instrument:** `git ls-files src/pages/api` (99 files), a helper scan, then reading every file the scan could not classify. **Falsifier:** a route with a handler export and no gate after reading it.

8. **[POSITIVE, verified] Webhook verification is uniformly correct, with one asymmetry noted above.** Stripe: signature parsed from the header, ±300s tolerance, `${timestamp}.${body}` signed payload, constant-time compare, verified before parse (`stripe.ts:316-322,386-427`). Resend: full Svix scheme over `${svix-id}.${svix-timestamp}.${rawBody}`, staleness check, and `svix-id` reused as the idempotency key (`resend.ts:78-125`, `resend-handler.ts:147`). SignWell: HMAC-SHA256 over the event fields with an explicit note that the hash lives in the body so parse must precede verify, plus a staleness check (`signwell.ts:114-125`). Healthchecks.io: shared bearer, constant-time compared, with a header comment saying plainly that the vendor does not sign (`healthchecks.ts:9-14`). Idempotency is real on the money paths — `stripe_checkout_orders` with `INSERT OR IGNORE` and a `processed` short-circuit in both `operator-checkout-handler.ts:107-132,170` and `hosted-agent-checkout-handler.ts:107-122`. **Falsifier:** a handler parsing before verifying, or a missing tolerance — the Sentry timestamp gap (finding 4) is what the check did surface.

9. **[POSITIVE, verified] Injection surface is clean on both languages, and the Python side has a control worth naming.** TypeScript: 9 `.prepare(\`` sites interpolate a template literal; all 9 carry code-controlled constants — generated `?,?,?` placeholder lists (`admin/cost-query.ts:109`, `admin/fleet-roster.ts:105`), the `EMAIL_IDENTITY_PREDICATE` constant `'lower(email) = ?'` (`identity/email.ts:56`), `BILLING_COLUMNS`/`COLUMNS` name lists, and in `db/services.ts:182,229` `conditions`/`updates` arrays assembled from string literals with every value bound. Zero `dangerouslySetInnerHTML`, `eval(`, `new Function(`; all 8 `set:html` sites pass through `serializeJsonLd` (`src/lib/seo/json-ld.ts:15-17`), which escapes `<` to `<` and so closes the `</script>` breakout. Python: 0 `shell=True`, 0 `os.system(`, 0 `pickle`, 0 unsafe `yaml.load(` against 63 files using `safe_load`. The one `exec(` (`operator/runners/medchron/medchron/seat.py:250`) builds `sys.argv=[...]` from `json.dumps` per element with `ensure_ascii` on, executes a constant base64 script, and passes the whole thing as one element of a list-form `subprocess.run` — no shell, no breakout. And `operator/bin/lib/console_d1.py:49-67` is the model of the class: because `wrangler d1 execute` has no parameter binding, `sql_text` emits `CAST(x'<hex>' AS TEXT)` rather than doubling quotes, and its docstring names the exact threat (break text lifted from a seat's own export). **Falsifier:** zero `subprocess` sites would make the shell sweep vacuous; there are 36 list-form ones. Any of the 9 `.prepare` sites carrying request data would have falsified the SQL claim; all 9 were opened.

10. **[POSITIVE, verified] Secrets clean, and the mirrored overlay contract is genuinely in sync.** `git grep -nE "sk-ant-|sk_live_|sk_test_|ghp_|AKIA|-----BEGIN (RSA |EC |OPENSSH )?PRIVATE KEY"` over tracked files returns only documentation, regex constants inside the scrubbers themselves, and test fixtures — including the deliberate planted falsifier at `tests/provisioning.test.ts:177`. The one tracked `.pem` is `public/keys/evidence-packet-signing-key.pem`, first line `-----BEGIN PUBLIC KEY-----`. `.dev.vars` is not tracked (only `.dev.vars.example`). `wrangler.toml [vars]` holds four base URLs and a feature flag, nothing secret-shaped. Separately: `operator/contracts/gate-coverage-snapshot.overlay.json` is **byte-identical** to `hermes-smd-overlay:tests/contract/seat_gate_binding_snapshot.json` at the pinned `OVERLAY_REF` (`5770b79f`), and that ref is the tip of the overlay's `origin/main`. **Method note worth recording:** my first diff reported a divergence, because my local overlay checkout sat on `feat/ss2616-handoff-door`. Re-probing against the pinned blob (`git cat-file -p 5770b79f:tests/contract/...`) and `origin/main` both returned rc=0. A stale working copy nearly produced a confident, wrong cross-repo finding.

11. **[POSITIVE, verified, refined] Broker authorization is not currently ungated, though it is still enforced by position.** Carried Architecture finding 3 said the structural gate sits at `server.py:545` with everything above it gateway-only by position. At HEAD the handler opens at `:363` and the gate is at `:595` — 232 lines above it. I enumerated every action handled in that region and checked each for an inline check: `suppressed_wake_append`, `emitted_wake_append`, `correction_propose`, the 13-verb `establish_*`/`act_*`/`ops_*` group, and `webhook_suppressed_append` each open with `agent_uid = self._resolve_agent_uid(); if agent_uid is None or peer_uid != agent_uid: raise PermissionError`. `escalation_event_append` delegates to `send_witness.py:119`, which performs the same check at `:136-138`. The medchron group delegates to `medchron_verbs.MedchronVerbs.handle`, which calls `self._gate(action, peer_pid, peer_uid)` as its **first** statement — the declarative shape the last review asked for, applied in the newest verb group. The only ungated pre-gate action is `health` (`:588-594`), read-only, disclosing readiness booleans and `supported_ops` to a peer already on the Unix socket. Transport is `socketserver.ThreadingUnixStreamServer` (`:995`), not HTTP, so the checklist's "HTTP auth on every route" does not apply. The exposure remains structural rather than live: `grep -rn '_VERBS|VERB_REGISTRY|verb_table'` returns nothing, so a verb added above `:595` without the four-line idiom is silently ungated and no test enumerates the set. **Recommendation:** extend the `_gate`-first pattern from `MedchronVerbs` to the rest, plus a test asserting every dispatchable action has a non-null gate.

12. **[LOW] Operator container privilege model, stated for the record.** The Dockerfile has **no `USER` directive**, so PID 1 is root — deliberately: `entrypoint.sh:10` asserts `id -u == 0` and then builds the separation itself (uids `hermes`, `workspace-broker`, `medchron`; groups `audit-readers`, `smokeball-token`; `chmod 2750` on the audit dir, `0640` on the ledger, `2770` on the token dir), verifying the result with `setpriv --reuid=hermes --regid=hermes --init-groups test -r "${AUDIT_DB}"` at `:242`. No `curl | sh` in the Dockerfile, no `set -x` in the entrypoint, no secret echoed. The residual is that "root by default" is a property of the image rather than a stated contract, so a future consumer that runs it without this entrypoint gets root. **Recommendation:** none blocking; consider a comment at the Dockerfile's tail naming the entrypoint as the privilege-drop mechanism.

13. **[NOTE] Semgrep is diff-aware on PRs.** `.github/workflows/security.yml` baselines to the merge-base on `pull_request` and to the pre-push commit on `push`, so pre-existing findings never block a merge; only the daily 13:00 UTC cron and manual dispatch run a full audit, and neither is a gate. Deliberate and documented (a registry pack update would otherwise freeze all merges), recorded so the coverage claim is not read as stronger than it is.

**Not checked**
- Clerk's own session cookie attributes (`__session`) — set inside `@clerk/astro`, not in tracked source; I did not read the vendor bundle.
- Whether the `ss-web` `FLY_API_TOKEN` **value** is valid. Presence only, from `wrangler secret list`.
- R2 bucket-level and D1 database-level access policy in the Cloudflare dashboard.
- The overlay's own trust-plugin source (`enforce.py`) — only the mirrored contract fixture was compared.
- Live behaviour of any authenticated endpoint. Every auth conclusion is from reading code plus the three unauthenticated header probes.
- npm dependency advisories (the deps agent owns these, per the brief).

**Summary.** The two HIGHs from the last review are both genuinely closed, and the headers one is closed at the runtime layer on all three hosts rather than in source only — the strongest single improvement in the window. Authentication, tenant isolation, injection resistance and webhook verification remain layered and fail-closed, and the newest broker verb group adopted the declarative gate the last review asked for. What holds the grade down is a set of unglamorous mediums that have now survived two reviews: an unauthenticated unbounded D1 write on `/api/events`, a secret scanner that discards history on a public repo, a JWKS fetch per unauthenticated request, two id namespaces in one audit column, and 155 non-test Python modules handling client legal documents with no language-specific security lint.

**Proposed grade: C.** Rubric: "C — any medium OR 3+ low." Five mediums stand (C3, C4, C5, C6, C9), none escalated to high.

---

### 3. Testing

**Carried findings**

| # | Headline | Status | Instrument |
|---|---|---|---|
| T6 (08-23 §Testing) | Four test files iterate `VERTICAL_FLOORS`, empty by design, so cannot fail | **CLOSED, correctly** | `tests/operator-configure.test.ts:30-42` and `tests/config-governance.test.ts:125-132,177-182` now **inject** a synthetic `floored-test-vertical` entry, assert the machinery resolves it, and `delete` it in a `finally`. The table is still `{}` (`src/lib/portal/operator/config-governance.ts:85`); the tests no longer depend on that |
| T7 | Coverage thresholds that had never been evaluated | **CLOSED, honestly** | `vitest.config.ts:57-84` removes all four with a comment stating `@vitest/coverage-v8` was never a dependency and the provider reports `0/0` under Astro's `getViteConfig`; `tests/coverage-config-honesty.test.ts` fails if thresholds return without a workflow that runs them |
| GP1 | `operator-substrate` is not a required check | **STILL OPEN** | See finding 2 |
| — | `src/lib/db/assessments.ts` has zero coverage | **STILL OPEN, and worse than "zero"** | See finding 3 |
| A1/A2/A3/B3 | 09-04 wave fixes claimed FIXED | **CONFIRMED, behaviourally pinned** | See finding 1 |

**Findings**

1. **[POSITIVE, verified] The tests pinning the 09-04 money-path fixes are genuinely behavioural and would fail on revert.** **A1** (`invoice.paid` arriving before `checkout.session.completed`): `tests/webhooks/stripe-subscription-linkage.test.ts:343-420` creates a miniflare D1, runs the **real numeric migrations** via `discoverNumericMigrations(resolve(process.cwd(), 'migrations'))`, seeds org/entity/subscription rows, POSTs a **signed** event through the actual webhook route, and asserts the resulting D1 row is `{ status: 'active', stripe_subscription_id: 'sub_stripe_1' }` — for both the 2026-03-25.dahlia `parent.subscription_details.metadata` shape and the pre-basil top-level shape. Revert the metadata fallback and `rowState()` returns `provisioning`/`null`. **A2** (`payment_status` gating go-live): `tests/operator-go-live-billing.test.ts:428-490` drives `handleOperatorCheckoutCompleted` with `unpaid`, `no_payment_required` and `paid`, asserting attach-without-promote for the first and promotion only on the third. **A3**: `tests/r2-key-collision.test.ts` has nine cases including two distinct keys whose filenames sanitize identically, re-upload idempotence, and a guard that the agreement path builds its key through the shared helper rather than its own sanitizer (`:163`). **B3**: `operator/bin/tests/test_send_verify.py` now covers **both** halves — `:350` asserts `unlabelled[0].skill_name == ""` with a comment at `:327` recording that the broker never wrote the column, alongside `:364` for the labelled case. The prior concern (fixtures hand-authoring a field production never writes) is addressed by testing the pair rather than only the happy shape. **Instrument:** read of each named region. **Falsifier:** a mocked handler or an assertion on source text instead of D1 state — the A1 test's `rowState()` query is what rules that out.

2. **[MEDIUM] The gate over the Python half of the tree is still advisory.** `gh api repos/venturecrane/ss-console/branches/main/protection --jq '.required_status_checks.contexts, .required_pull_request_reviews, .enforce_admins'` returns, verbatim:
   ```
   ["Typecheck, Lint, Format, Test"]
   {"dismiss_stale_reviews":true,"require_code_owner_reviews":false,"require_last_push_approval":false,"required_approving_review_count":0,"url":"…"}
   {"enabled":false,"url":"…"}
   ```
   and `gh api repos/venturecrane/ss-console/rules/branches/main` adds one ruleset requiring `[{"context":"Security Summary"}]`, plus `deletion`, `non_fast_forward`, and a `pull_request` rule with `required_approving_review_count: 0` and `require_extra_approval_for_unattributed_changes: true`. So the required set is exactly **`Typecheck, Lint, Format, Test`** and **`Security Summary`**. `operator-substrate` — which runs ruff, the run()-style safety invariants, 2,488 pytest tests, the SEC-32 overlay runtime-hash drift gate, per-connector venv conformance and the medchron runner suite — is **not required**, and `enforce_admins` is `false`. Every Python-side guard the last two reviews credited (the byte-identity `pre_run.py` check, the CI-coverage conformance test, the block-conformance gate) is enforced by a workflow that cannot block a merge. **Falsifier:** `operator-substrate` appearing in either list; it appears in neither. **Recommendation:** add `operator-substrate` to the ruleset's required checks.

3. **[MEDIUM] A 216-line test file stands in for coverage of the assessment data layer and measures nothing about behaviour.** `src/lib/db/assessments.ts` has **zero** test files importing it (mechanical importer scan over all 324 `.test.ts` files). `tests/assessments.test.ts` is 216 lines with 56 `expect(` calls, and every one is of the form `expect(source()).toContain('export async function listAssessments')` against `readFileSync(resolve('src/lib/db/assessments.ts'))` (`:6-26`). It cannot detect a dropped `org_id` predicate, an inverted `WHERE`, or an SQL typo — only a renamed export. This is the failure mode the review theme names, in test form: the file's name and size read as coverage of a critical path. The same shape appears at `tests/clients.test.ts:1-24`, which asserts `src/pages/admin/index.astro` contains the strings `'/admin/services'` and `'buildActionQueue'`. **Instrument:** the importer scan plus reading both files. **Falsifier:** any `import … from '../src/lib/db/assessments'` in the suite. **Recommendation:** port `tests/portal/tenant-scoping.cross-org.test.ts`'s miniflare-D1 pattern onto `assessments.ts`; it is the same shape of module and the harness already exists.

4. **[MEDIUM] 124 of 324 test files never import their subject, and 95 of those assert on source text.** Measured by scanning each file for `from '…src/…'` or `…operator/…`. Many are legitimate — `forbidden-strings`, `doctrine-integrity`, `handbook-integrity`, `adr-integrity`, `migration-numbering`, `workflow-permissions`, `gate-coverage-snapshot`, `operator-overlay-pairs`, `operator-module-size`, `typography-tokens`, `landing-page`: for a policy or drift guard, the source text **is** the subject. The set whose subject is runtime behaviour and that therefore should be behavioural includes `assessments`, `clients`, `contacts`, `engagements`, `invoices`, `milestones`, `quotes`, `follow-ups`, `time-entries`, `parking-lot`, `reply-log`, `signwell`, `contact-endpoint`, `portal-quotes`, `portal-docs`, `client-identity-gate`, and `middleware` (this last is mitigated — `tests/middleware-behavior.test.ts` imports and exercises the real module alongside it). **Falsifier:** a much smaller no-import count, or those files turning out to import via a helper — the scan pattern matched `from '../src/…'` correctly, verified against a known-importing file. **Recommendation:** treat this list as a backlog, not a sweep; convert the ones whose module carries a tenant predicate first.

5. **[LOW] Nothing measures coverage in either language.** `grep -rn coverage .github/workflows/` returns only comment prose in `operator-substrate.yml`; no workflow runs `vitest --coverage`, and there is no `pytest-cov`, `--cov`, or coverage config anywhere under `operator/`. This is not a regression — `vitest.config.ts:57-84` removed the four thresholds deliberately and honestly, and `tests/coverage-config-honesty.test.ts` prevents them returning without a runner. Recorded so the absence is a known state rather than an assumed floor.

6. **[LOW] Two Python tests are environment-dependent and fail outside CI's exact conditions.** Running the suite exactly as `operator-substrate.yml` does, in a clean `uv` venv with `pytest pyyaml cryptography ruff`, gives **2 failed, 2486 passed, 5 subtests passed in 105.38s**. Both failures are environmental, and both are worth naming. `adapter/evidence/tests/test_packet.py::test_build_emits_targz_with_every_expected_file` fails with `Left contains one more item: 'manifest.sig'` because `load_signer` (`operator/adapter/evidence/signing.py`) reads `EVIDENCE_PACKET_SIGNING_KEY_B64` from ambient `os.environ` and the test does not isolate it — my shell has the production secret injected, so the packet was signed and the expected-file list did not include the signature. Re-running as `env -u EVIDENCE_PACKET_SIGNING_KEY_B64 … pytest adapter/evidence/tests/test_packet.py -q` gives **33 passed in 0.25s**, confirming the cause. `bin/tests/test_decommission.py::test_cli_live_allow_unwired_runs_and_tombstones` fails with `[live] HALTED at step 02_preserve_machine_data: <urlopen error [Errno 8] nodename nor servname provided, or not known>` — a `--live` path making a real outbound request inside a unit suite. **Recommendation:** `monkeypatch.delenv(SIGNING_KEY_ENV, raising=False)` in the packet test; stub the network step in the decommission test.

7. **[POSITIVE, verified] Both suites are large, fast, and green; almost nothing is skipped.** `npx vitest run`: **321 passed | 3 skipped (324 files)**, **5236 passed | 5 skipped | 7 todo (5248 tests)**, 97.37s wall. Python, in the CI-equivalent venv: **2486 passed, 5 subtests passed**, 105.38s, plus the two environmental failures above. Across all 324 `.test.ts` files there are exactly **two** with zero `expect(` (`tests/operator-config-writeback.test.ts`, `tests/operator-legibility-boundary.test.ts`) and both are pure `it.todo` files — honest placeholders, not fake passes. The single `expect(true).toBe(true)` sits inside `describe.skip` in `tests/sow-render.test.ts:24-31`, with the reason stated at `:22` ("WASM not available in vitest/Node.js environment. Validated via Astro build and live deployment"). Totals: 7 `it.todo`, 3 `.skip`. Python: 176 test files, 14 `skipif` markers, all conditional on a real precondition (console-script on PATH, rehearsal corpus present) and none `xfail`. **Falsifier:** a mass of zero-assertion or `expect(true)` tests; the scan could have found many and found three.

8. **[NOTE] No browser/e2e suite exists.** No Playwright, Cypress or Puppeteer config is tracked. `.playwright-mcp/` in the manifest is a log directory from an interactive MCP session (`console-2026-04-23T23-40-53-989Z.log`), not a suite. `scripts/e2e-extraction-test.ts` is a manually-run script requiring `ANTHROPIC_API_KEY`; `operator/adapter/evidence/tests/test_packet_signature_e2e.py` is a pytest file, not a browser test. Given the venture's "done means the client can do it" rule this is a real gap, but it is a build-something recommendation rather than a defect.

9. **[POSITIVE] Flakiness is managed structurally, not by retries.** `vitest.config.ts` sets no `retry`. `git log --oneline -40 --grep="flak\|retry\|rerun" -- tests .github` returns 15 commits, and the ones that are actually about flakiness fix the cause rather than mask it: `fb038442` "give the subprocess suite a timeout proportional to its work", `e54af0d1` "unfreeze + harden the Security gate (gitleaks retry + time-boxed npm-audit allowlist)" — the retry there wraps a transient GitHub infrastructure call, with the 2026-07-22/23 incident named in the comment, not a flaky assertion.

**Not checked**
- Mutation testing or any measure of whether the passing assertions would catch a real regression, beyond the four fixes read in finding 1.
- Whether `Typecheck, Lint, Format, Test` actually runs the whole vitest suite in CI (I ran it locally; I did not read the `verify` workflow's invocation).
- Over-mocking as a systematic count. Spot checks on the stripe/webhook and portal suites found real modules under test with only external boundaries stubbed; I did not scan all 324 files for a module mocking itself.
- Connector conformance and medchron-runner suites — they build their own venvs in CI and I ran only the main pytest invocation.
- Test coverage of `workers/*` beyond noting the six test files there.

**Summary.** The suites are large, fast, near-fully green, and the newest work is the best in the tree — the 09-04 money-path fixes are pinned by tests that run real migrations against miniflare D1 and assert on row state, and the "empty table" instrument the last review flagged was fixed by injecting a synthetic entry rather than deleted. Against that: the assessment data layer still has no behavioural coverage while a 216-line text-matching file occupies the slot, 95 files assert on source text, nothing measures coverage in either language, and the workflow that runs 2,488 Python tests over the client-facing runtime still cannot block a merge.

**Proposed grade: C.** Rubric: "C — significant gaps OR critical paths untested." `src/lib/db/assessments.ts` and `src/lib/stripe/checkout.ts` both have zero importing tests, and the Python gate is advisory. Same letter as the last review, with the direction clearly improving.

---

### Overall notes for the orchestrator

1. **The middleware's portal predicate is the finding I would most want a second reader on.** `src/middleware.ts:144-162` returns `null` (allow) for any request carrying a Clerk `userId`, with no role or tenant check; everything downstream depends on each route calling its own helper. I verified all 20 current routes comply, but I want someone to confirm my reading that a Clerk user of *any* tenant clears the middleware, because that is what turns finding 2 from a hygiene gap into a cross-tenant one.

2. **Two items belong to other dimensions and I am handing them over.** For **Architecture/Code Quality**: `src/pages/api/booking/reserve-helpers.ts` and `confirmation-emails.ts` still export no HTTP method while living in the routing tree (carried C10/Arch-2, still open), and `src/pages/api/oauth/callback.ts` plus its dedicated middleware exemption at `src/middleware.ts:120` are both still present (carried Arch-8/C11, still open). For **Deps/Docs/Golden Path**: the required-checks output in Testing finding 2 is verbatim and feeds the Golden Path directly — `operator-substrate` is not required and `enforce_admins` is `false`. Also for that agent: the operator module-size ratchet baseline (`operator/contracts/operator-module-size.json`) currently holds **16 modules** above the 500 ceiling, largest `workspace_broker/establishment_store.py` at 1,415, then `templates/drafting/drafting_gate_check.py` 1,191 and `adapter/evidence/packet.py` 1,128; the ratchet only tightens, so this is a shrinking allowlist, not a growing one.

3. **A method warning worth carrying into the next review.** Two of my instruments produced confident wrong answers before I read the underlying files: a helper-name grep called 30 gated routes ungated (the vocabulary is `getPortalClient` / `resolveOperatorAccess` / `resolveHostedAgentAccess` / `authorizeAdvancedSettings` / `verifyMachineRequest`, not a fixed list), and a cross-repo `diff` reported the overlay contract fixture out of sync when the real cause was my local overlay checkout sitting on a feature branch. Both are the third and fourth instances of this exact correction across three reviews. A note in the next preamble — "enumerate helpers before scanning; resolve cross-repo refs to a pinned SHA, never a working copy" — would pay for itself.


---

# Agent: deps-docs-goldenpath

## SMD Services (ss-console) Code Review — 2026-09-09
## Agent: deps-docs-goldenpath — Dependencies, Documentation, Golden Path Tier 2

HEAD 0a3722a3. Repo read-only throughout; all commands were read-only probes (npm audit/outdated/knip, vitest run, astro build, gh api GET, git log).

---

### 1. Dependencies

**Carried findings:**
1. `npm audit` clean (0 vulns) -> **CHANGED (regressed but covered).** Instrument: `npm audit --json` at root today reports 12 vulnerabilities (1 critical, 7 high, 3 moderate, 1 low), not 0. Falsifier: a clean `npm audit --json` metadata block would have shown 0 across the board — it didn't. However this is not an unmanaged regression: `.github/audit-allowlist.json` (new today, `"2026-09-09 (Captain-authorized)"`) time-boxes exactly the 6 actionable high/critical GHSAs to 2026-09-23, tracked in issue #2711 (open, filed 2026-09-09T14:07:57Z, body matches the allowlist verbatim). `gh run list --workflow=security.yml --limit 5` shows 5/5 recent runs `conclusion: success`, confirming the gate is currently green by design, not by accident.
2. Zero unused dependencies (positive) -> **CLOSED, still true.** Instrument: `npx knip --dependencies` produces zero unused/unlisted findings, only one configuration hint (`cloudflare` in `knip.jsonc` `ignoreDependencies` — a Workers-runtime builtin knip misreads as a package, per the file's own comment at line 68-69). Falsifier: any listed unused dependency line would have shown otherwise.
3. No dynamic-codegen dependency reachable from the Worker bundle (positive) -> **CLOSED, still true.** Instrument: `npm run build` completed clean (astro build, server output for `@astrojs/cloudflare` adapter); `ajv` (which uses `new Function()`) resolves in `package-lock.json` only under `eslint`/`yaml-language-server` dev-tool edges, confirmed structurally unchanged from last review. Falsifier: a grep hit for `eval(`/`new Function(` in `src/` or `workers/`, or an `ajv` edge from a prod dependency, would have shown otherwise; neither occurred.
4. `yaml` undeclared, only in `overrides` (HIGH) -> **STILL OPEN.** Instrument: `python3 -c "json.load(open('package.json'))"` shows `dependencies`, `devDependencies`, `peerDependencies` all `None` for `yaml`; only `overrides: "2.8.3"`. `git grep -l "from 'yaml'"` across `.ts/.mjs/.js/.astro` finds 28 importers (3 `scripts/`, 1 `src/lib/`, 24 `tests/`) — a slightly different count than the carried "26" (likely a file-glob difference in the prior instrument), but the defect is identical: it resolves as someone else's transitive dependency, not a declared one. Falsifier: a `"yaml"` key under `dependencies` or `devDependencies` would have closed this.
5. Seat image installs Python deps unpinned (HIGH) -> **STILL OPEN, unchanged.** Instrument: `operator/templates/Dockerfile:1017-1018` — `uv pip install --python /opt/workspace-broker/.venv/bin/python google-api-python-client google-auth pyyaml` carries no version specifier of any kind. The nearby comment at line 1049 ("The pip install above is also pinned") refers to the *overlay* install two blocks earlier (`git+${OVERLAY_REPO}@${OVERLAY_REF}`, itself SHA-pinned), not to this line — read in context, the comment does not contradict the finding. `git log -S` for the original install string still resolves only to the introducing commit (`3a498b1b`, #1297); no pinning commit followed. Falsifier: a version specifier (`==`, `>=`, or a `uv.lock`/`requirements.txt` covering these three packages) would have closed this.
6. `typescript` / `@cloudflare/workers-types` one major behind, each capped by caret (MEDIUM) -> **STILL OPEN, unchanged, deliberate.** Instrument: `npm outdated --json` — `typescript` current 6.0.3, wanted 6.0.3, latest 7.0.2; `@cloudflare/workers-types` current 4.20260702.1, wanted 4.20260702.1, latest 5.20260902.1. `.github/dependabot.yml`'s comment explicitly defers the workers-types major bump until wrangler's major moves (peer-dep constraint), confirming intent rather than drift.

**New findings:**
1. [MEDIUM] **A fresh critical/high npm-audit exposure landed today, allowlisted rather than fixed, with a hard expiry in 14 days.** `.github/audit-allowlist.json` (created 2026-09-09) covers GHSA-26w7-cxv4-gfx2 (Astro RCE via AVIF, critical), GHSA-376h-93r7-7g6f (Astro auth-bypass), GHSA-2883-xcg3-v3hh (js-yaml CPU DoS), GHSA-rgj7-g3m4-5g8c (sharp/libheif), GHSA-w27v-7q3p-w38r + GHSA-4vpr-x523-8j87 (SVGO removeScripts x2). Instrument: `npm audit --json` cross-referenced against `.github/audit-allowlist.json` and `scripts/audit-allowlist.mjs`'s GHSA-extraction logic (`node_modules`-independent read of the script) confirms all 6 found high/critical GHSAs are exactly the 6 allowlisted ones — no gap, no unlisted advisory slipping through. `@astrojs/cloudflare`, `@cloudflare/vite-plugin`, `miniflare`, `wrangler` also show "high" in the audit summary but their `via` arrays are package-name chains (no direct GHSA), all resolving to the already-allowlisted `sharp` advisory — confirmed by reading `scripts/audit-allowlist.mjs`'s `highSeverityGhsas()`, which only extracts GHSAs from `via` entries that are objects with a `.url`, so these chain-only entries correctly contribute nothing new to enforce. Issue #2711 is open, unlabeled, with a clear fix plan (bump Astro/js-yaml/sharp/svgo once past the `.npmrc` `min-release-age=7` cooldown, verify the three subdomains, remove the allowlist entries). Falsifier: an un-allowlisted GHSA in the `found` map, or a CI run with `conclusion: failure` on `main`, would have shown the gate was not actually covering this; neither appeared. Recommendation: nothing to do differently — the mechanism is working exactly as designed (same shape as the closed nanoid/#2196 precedent) — but flag the 2026-09-23 expiry so it doesn't lapse unnoticed; a critical Astro RCE sitting allowlisted for two weeks is real exposure even though it's tracked.
2. [LOW] **Same allowlist pattern reproduces cleanly across all 3 workers with their own `package.json`.** Instrument: `npm audit --json --prefix workers/{cost-anomaly,cost-telemetry,fleet-alerts}` — cost-anomaly: 3 high + 2 moderate; cost-telemetry: 3 high; fleet-alerts: 3 high; all traced (via the same `via`-chain read) to `sharp`'s already-allowlisted GHSA-rgj7 plus non-blocking moderates (`@vitest/mocker`/`vitest`). `security.yml`'s audit job iterates `workers/*/` dirs with a `package.json`, so these are in scope and covered by the same allowlist call. Falsifier: a distinct GHSA in a worker not present in the root's `.allow` map would have shown a gap; none found. The other 6 `workers/*/` directories (`enrichment-workflow`, `job-monitor`, `new-business`, `review-mining`, `scan-workflow`, `social-listening`) have no `package.json` and are correctly out of npm-audit scope (confirmed by `[ -f "${dir}package.json" ]` in `security.yml` and by direct `ls`).
3. [MEDIUM] **No dependency-freshness or vulnerability tracking exists for the Python side at all**, and it cannot, structurally, until pinning exists. Instrument: `find operator -iname "requirements*.txt" -o -iname "pyproject.toml" -o -iname "uv.lock"` at the operator root returns nothing (only per-connector `pyproject.toml` under `operator/connectors/*/`, confirmed by the carried finding, re-verified present and unchanged); `.github/dependabot.yml` has no `pip`/`docker` ecosystem entry (grep for `docker`/`pip` in the file returns nothing); no `pip-audit`, `uv audit`, `safety`, or `bandit` invocation anywhere in `.github/workflows/*.yml` or `operator/` (only a `ruff.toml` comment noting the historical absence of any Python static-analysis config). Falsifier: a Dependabot `package-ecosystem: pip` or `docker` block, or a CI step running `pip-audit`/`uv audit`, would have closed this. This is the same root cause as Carried Finding 5 — unpinned installs cannot be meaningfully audited even if a scanner were added, since there is no lockfile to diff.
4. [POSITIVE, verified] **GitHub Actions are fully SHA-pinned with version comments, and Dependabot maintains the pins.** Instrument: `grep -rhn "uses:" .github/workflows | sort | uniq -c` — every one of the ~40 unique `uses:` lines is a 40-character commit SHA followed by a `# vX` comment; zero floating tags. `.github/dependabot.yml`'s `github-actions` ecosystem entry (weekly, grouped) is the stated maintenance mechanism, and its own header comment explains the SHA-pin rationale (Semgrep's `github-actions-mutable-action-tag` rule enforces it). Falsifier: any bare `uses: owner/action@v4`-style floating ref would have failed the grep-uniqueness check; none found.
5. [POSITIVE, verified] **Lockfile hygiene is clean.** Instrument: `git log --oneline -15 -- package-lock.json` shows exclusively `chore(deps)`/`fix(security)`/`feat` commits with clear intent, no manual-conflict artifacts; `grep -rn "npm install" .github/workflows` (excluding `npm ci`) returns nothing — every workflow installs via `npm ci`. Falsifier: an `npm install` (not `ci`) in a workflow, or an unexplained lockfile diff, would have shown drift; neither appeared.
6. [POSITIVE, verified] **No obviously redundant dependency pairs.** Instrument: full listing of root `dependencies`+`devDependencies` (16 + 19 = 35) shows one date library pair (`date-fns` + `date-fns-tz`, a documented peer relationship per the prior review, not a duplicate), one validation library (`zod`), no second HTTP client, no second PDF/date/schema library. Workers carry zero prod deps each, only 4 shared devDeps (`@cloudflare/workers-types`, `typescript`, `vitest`, `wrangler`). Falsifier: two libraries serving the same purpose (e.g., a second date or validation library) would have shown otherwise; none found.

**Not checked:**
- Whether the CI runner's actual Node 22 bundled npm version is >= 11.10.0 (required for `.npmrc`'s `min-release-age=7` to take effect, per that file's own comment). Verified locally (npm 11.12.1, active), but did not inspect the GitHub-hosted `ubuntu-latest` runner's npm version directly — if it's older, the allowlist's stated justification ("cooldown-blocked fix") would be inert on the very runner it's protecting, though the allowlist and its expiry would still function correctly either way.
- Whether `pip install`/`uv pip install` version resolution for the three unpinned packages (`google-api-python-client`, `google-auth`, `pyyaml`) has actually drifted to a broken combination on any real seat — no live Machine was probed (out of scope for a read-only repo review).

**Summary:** The dependency posture regressed from the last review's clean audit to 12 vulnerabilities, but the regression is fully accounted for by a fresh, well-documented, Captain-authorized, self-expiring allowlist (issue #2711, expires 2026-09-23) that is the exact mechanism this venture used successfully before (nanoid/#2196). The two carried structural gaps — undeclared `yaml` and unpinned Python installs in the seat Dockerfile — are both still open and unchanged. Python has no dependency-audit surface at all, which is a consequence of having no lockfile to audit.

**Proposed grade: C** — medium: 2+ majors-behind-adjacent items are actually deliberate/capped (not counted), but a critical severity finding present (even allowlisted) plus the pre-existing unpinned-Python-install defect together put this above a clean B; not D because the audit gate is genuinely enforced and green by a legitimate, time-boxed, tracked exception rather than by silence.

---

### 2. Documentation

**Carried findings:**
1. CLAUDE.md accurate (positive, 6/6 spot-checks) -> **CHANGED — partially stale.** Re-checked the same 6 facts plus 2 new ones. Middleware hostname behavior (`src/middleware.ts:27-30`), `ADMIN_BASE_URL` strict-no-fallback (`src/lib/config/app-url.ts:109-131`, explicit "Unlike the portal helper, does NOT fall back to APP_BASE_URL"), `env` from `cloudflare:workers` (5 importers including `src/middleware.ts:21`), and all 7 named tests + all 7 named hooks still exist exactly as claimed — all CONFIRMED. But CLAUDE.md's **"Operator ADRs: 0004-*.md through 0061-*.md"** is now badly stale: `ls docs/adr/ | sort -n | tail -5` shows the highest ADR is **0087**, 26 numbers past what CLAUDE.md claims. And CLAUDE.md's **"37 active decisions ... numbered through #55"** no longer matches `docs/adr/decision-stack.md:26`, which now reads **"38 active decisions ... numbered through #56."** Falsifier: an ADR range or decision count matching CLAUDE.md's stated numbers would have confirmed it; neither did.
2. No systematic API documentation for 97 route files (MEDIUM) -> **STILL OPEN, count grown to 99.** Instrument: `git ls-files src/pages/api | wc -l` = 99. `find docs -iname "*api*"` finds only the unrelated `docs/spikes/d1-batch-api.md`; `docs/api/` does not exist at all. Falsifier: a `docs/api/README.md` file would have closed this. Important correction to the preamble's premise: issue #2385 ("Stand up docs/api/README.md") shows `state: CLOSED, closedAt: 2026-08-31T17:25:06Z`, but `gh issue view 2385 --json comments` shows the closing comment is the **2026-08-31 backlog reset** — Captain's own words: "Closing is not a judgment that the underlying condition is fake... If the condition is real, it comes back as work in the session that hits it." The issue was closed as backlog hygiene, not because the work landed. No merged commit touches `docs/api/`. The gap is real and unticketed, not resolved.
3. Three false in-code documentation claims (HIGH) -> **STILL OPEN, all three, plus one new sibling found.** `src/lib/booking/holds.ts:18,102-103` still names `workers/booking-cleanup/` as the caller of `cleanupExpiredHolds`; `ls workers/` shows 9 directories (`cost-anomaly`, `cost-telemetry`, `enrichment-workflow`, `fleet-alerts`, `job-monitor`, `new-business`, `review-mining`, `scan-workflow`, `social-listening`) and no `booking-cleanup`. `knip.jsonc:105-107` still says ten rule categories are "Reported by `npm run deadcode`, but non-blocking" — re-verified against `node_modules/knip/dist/IssueCollector.js:139` (`if (this.rules[issue.type] === 'off') return`) on the currently-installed knip 6.31.0: an `"off"` rule is dropped before collection, so nothing is reported, contradicting the comment's own claim. `src/lib/operator/capabilities/conformance.ts:17` still instructs adapter authors to import `runConformanceSuite`; `git grep -rn "runConformanceSuite"` finds only this one comment, no definition anywhere in the repo. Falsifier for each: the named worker/behavior/symbol actually existing would have closed the finding; none do.
4. ADR index vs decision-stack count mismatch (LOW) -> **STILL OPEN, gap widened.** `docs/adr/index.md:9` still says "34 active ... numbered through #51; 3 superseded" while `docs/adr/decision-stack.md:26` now says "38 active decisions ... numbered through #56; 3 superseded." At the last review this was a 3-decision/4-number gap (34/#51 vs 37/#55); it is now a 4-decision/5-number gap. Falsifier: matching counts would have closed it; the delta grew instead.
5. Handbook drift near-universal (LOW, advisory) -> **STILL OPEN, same shape, with a concrete new instance.** `npm run handbook:drift` runs clean (exit 0, advisory) but reports drift against essentially every page it touches (CLAUDE.md, ADRs, `src/middleware.ts`, etc. — same universality as last review). `npx vitest run tests/handbook-integrity.test.ts` passes (6/6 tests). New concrete instance found under Documentation task 8 below (schema doc staleness).
6. 86 ADRs, current and indexed (positive) -> **CHANGED (grown, still current).** Now 87 ADRs, highest `0087-chronology-package-content-control.md`. `tests/adr-integrity.test.ts` (not independently re-run here, but its guard class is unchanged) still blocks duplicate prefixes per the file naming scheme observed.

**Findings:**
1. [MEDIUM] `CLAUDE.md`'s two ADR/decision-count references are stale by a widening margin. Instrument: see Carried Finding 1 above (`docs/adr/decision-stack.md:26` vs CLAUDE.md's stated "37...#55"; highest ADR file `0087` vs CLAUDE.md's stated range ending at `0061`). Falsifier: `crane_doc`-style automated regeneration or a same-PR update discipline enforcing the numbers would have kept them in sync; no test or CI gate checks CLAUDE.md's numeric claims against `docs/adr/`. Recommendation: either drop the specific numbers from CLAUDE.md in favor of "see decision-stack.md for the current count" (self-healing), or add a lightweight test analogous to `tests/adr-integrity.test.ts` that fails when CLAUDE.md's stated range falls more than N ADRs behind the actual highest file.
2. [LOW] `README.md:18` states **"TypeScript 5, Vitest 4, ESLint 10"** as the stack. Instrument: `package.json` devDependencies show `typescript: "^6.0.3"` (current 6.0.3, confirmed via `npm outdated`). Vitest (4.1.10) and ESLint (10.6.0) are accurately described; TypeScript is not — it's one full major ahead of what the README states. Falsifier: `typescript: "^5.x"` in `package.json` would have confirmed the README; it shows `^6.0.3` instead. Recommendation: bump the README line to "TypeScript 6" in the same commit that next touches the Stack section — this is exactly the kind of stale numeric claim CLAUDE.md's own ADR-count problem (Finding 1) also exhibits, suggesting version numbers in prose docs are a recurring category worth a lint rule rather than three independent one-off fixes.
3. [MEDIUM] **A fourth false in-code documentation claim, same family as the three carried ones, naming the same nonexistent worker.** `src/lib/db/oauth-states.ts:84-86` — the file's last three lines are a dangling section header: `// Cleanup (optional — called by booking-cleanup cron) // ---...` with **no function beneath it** (confirmed: `wc -l` = 86, and the header is lines 84-86, the literal end of the file). It names the same `workers/booking-cleanup/` that does not exist (see Carried Finding 3). Unlike `holds.ts`, this isn't even a false claim about a real function's caller — it's a stub comment for cleanup logic that was never written, left pointing at infrastructure that was never built. Instrument: `sed -n '80,86p' src/lib/db/oauth-states.ts`; `ls workers/` (9 real directories, no `booking-cleanup`); `grep -n "^export"` on the file shows only `createOAuthState` and `consumeOAuthState`, no cleanup function at all. Falsifier: an actual exported cleanup function beneath the header, or the worker existing, would have closed this. Recommendation: since this pattern has now recurred twice (holds.ts, oauth-states.ts) pointing at the same fictional worker, whoever resolves Carried Finding 3 should grep the whole tree for `booking-cleanup` in one pass rather than fixing file-by-file.
4. [MEDIUM] **The per-console-D1 schema doc (`docs/handbook/data-model.md`) has not been touched since 2026-07-03, and 8 migrations landed since then that it does not mention.** Instrument: `git log -1 --format=%ai -- docs/handbook/data-model.md` = 2026-07-03; `ls migrations/ | grep -E "^01(0[5-9]|1[0-2])"` lists `0105_cron_containment_observability.sql` through `0112_sticky_stop_cause.sql` (dated 2026-08-11 through 2026-09-01, per per-file `git log`); `grep -n "implementation_type\|operator_agreement_documents\|sticky_stop_cause\|sticky_stop_reason\|sticky_stop_condition\|send_refusals" docs/handbook/data-model.md` returns **zero matches** — none of the tables or columns these 8 migrations add (`invoices.implementation_type`, the new `operator_agreement_documents` table, `fleet_status.sticky_stop_reason`/`sticky_stop_condition`, `send_refusals`) appear in the schema doc at all. This is a concrete, dated instance of the near-universal handbook drift the advisory tool already flags (Carried Finding 5), specific enough to act on. Falsifier: any of those 6 identifiers appearing in `data-model.md` would have shown the doc was current; none do. Recommendation: same-PR maintenance contract already exists in CLAUDE.md ("when a change to the venture changes what a handbook page says, update that page in the same PR") — this is 8 consecutive migrations where that contract wasn't honored for this specific page. `docs/specs/operator/d1-schema.md` (the per-customer D1 schema, a different database) is separately last-touched 2026-08-11 and is not implicated here.
5. [LOW] **`mcp__crane__crane_docs_drift_audit` audits the enterprise `crane-console` docs tree, not this repo.** Instrument: running the tool returned "199 markdown file(s) audited across 10 site-published dir(s)" referencing paths like `/Users/scottdurgan/dev/crane-console/site/astro.config.mjs` and `docs/ventures/smd`/`config/ventures.json` — none of which exist in `ss-console`. This is not an ss-console defect; it means the preamble's suggested instrument doesn't apply here and the manual fallback (file counts, dead-link check) is the correct method for this repo, which is what the other findings in this section used. Falsifier: an ss-console-scoped output (referencing this repo's own `docs/` tree) would have meant the tool was usable directly; it wasn't. Not counted against the grade — noting it so the next reviewer doesn't waste a call re-trying it expecting ss-console-scoped output.
6. [LOW] **`#2384` (uptime monitoring, "external poller for /api/health") was also closed via the 2026-08-31 backlog reset, not delivery** — same mechanism, same caveat as Finding for #2385 above. Instrument: `gh issue view 2384 --json state,closedAt,comments` shows the identical backlog-reset boilerplate closing comment. However, unlike #2385, the underlying capability does appear to exist in a different shape than the issue's title assumed: `docs/adr/0023-operator-per-customer-observability.md` documents a **push-based** healthchecks.io mechanism (Machine POSTs every 60s; grace-expiration triggers a webhook), not a poller, and `src/pages/api/webhooks/healthchecks.ts` (a real, documented receiver with bearer-token auth) plus `src/pages/api/health.ts` both exist in this repo. See Golden Path Finding 3 below for the full assessment — flagged here because the doc/issue mismatch (issue title implies one architecture, ADR + code implement a different one) is itself a documentation-accuracy point: nothing in `docs/adr/index.md` or CLAUDE.md cross-references #2384 to ADR 0023 to make the substitution legible to a future reader.
7. [POSITIVE, verified] **Inline comment density on sampled large/complex files is substantial in both languages**, a light-touch check rather than a rigorous per-function review. Instrument: comment-line counts via `grep -c "^\s*//\|^\s*\*"` / `grep -c "^\s*#"` against total lines — `src/lib/webhooks/stripe-subscription-handler.ts` (733 lines): 158 comment lines (~22%); `src/lib/db/quotes.ts` (774 lines): 183 (~24%); `operator/workspace_broker/server.py` (1,011 lines): 328 (~32%); `operator/adapter/evidence/packet.py` (1,652 lines): 119 (~7%, the lowest of the four — worth a closer look by whoever owns that file, but not examined function-by-function here). Falsifier: a complex file with near-zero comments would have shown a documentation gap; the worst sampled case (`packet.py` at 7%) is still non-trivial. This is file-level density, not a claim that every non-obvious branch is explained — see Not Checked.

**Not checked:**
- Full per-function review of "are the non-obvious parts commented" for 5 functions in `src/lib` and 5 in `operator/` as literally specified — did a file-level density proxy instead (Finding 7) due to time budget; a line-by-line function audit would need to be done by whoever picks up Architecture/Code-Quality's deeper dive into `operator/adapter/evidence/packet.py` specifically, which came out lowest on the density proxy.
- Whether Dependabot's `pip`/`docker` gap (Dependencies Finding 3) has a corresponding documentation gap (e.g., a runbook describing how Python deps are supposed to be updated) — not searched.
- Full one-by-one verification of all ~26 handbook pages' drift entries against their cited sources — spot-checked one (data-model.md) in depth per Finding 4; trusted the tool's advisory output for the rest.

**Summary:** CLAUDE.md and the handbook remain broadly trustworthy on architecture and process claims, but the specific numeric claims about ADR range and decision count have drifted further out of sync with reality since the last review, and a fourth instance of the "worker that doesn't exist" false-claim family was found. The API-docs gap (99 routes, zero docs/api/) and the schema-doc staleness (8 undocumented migrations) are the two most concrete, actionable Documentation gaps.

**Proposed grade: C** — rubric: "CLAUDE.md incomplete OR ... no API docs with endpoints" — CLAUDE.md is not incomplete but has drifted numeric claims, and API docs remain completely absent for 99 routes with the tracking issue closed as backlog hygiene rather than resolved; schema docs exist but are demonstrably stale against 8 recent migrations.

---

### 3. Golden Path Compliance (Tier 2)

**Carried findings:**
1. Required checks cover TypeScript half only, `operator-substrate` advisory (HIGH) -> **STILL OPEN, unchanged.** Instrument: `gh api repos/venturecrane/ss-console/branches/main/protection --jq '.required_status_checks.contexts'` = `["Typecheck, Lint, Format, Test"]`; `gh api repos/venturecrane/ss-console/rules/branches/main` (ruleset 15555219) adds exactly one more required context, `"Security Summary"`. `operator-substrate`, `runtime-ac-proof`, `adr-0022-pi-prefix-gate`, `ui-drift-audit`, etc. are all absent from both lists. Falsifier: `operator-substrate` (or any of the others) appearing in either required-checks list would have closed this; none do.
2. Branch protection: 0 required approving reviews, admin bypass permitted (MEDIUM) -> **STILL OPEN, unchanged.** Instrument: same `branches/main/protection` call — `required_pull_request_reviews.required_approving_review_count: 0`, `enforce_admins.enabled: false`. The newer ruleset (15555219) independently sets `required_approving_review_count: 0` too, and `allow_force_pushes.enabled: false` / `allow_deletions.enabled: false` are correctly on. Falsifier: a nonzero `required_approving_review_count` in either the legacy protection or the ruleset would have closed this; both show 0.
3. Sentry adoption partial — `captureError` at 3 of 94 files (MEDIUM) -> **STILL OPEN, unchanged.** Instrument: `grep -rl "captureError" src/` returns 4 files, one of which (`src/lib/observability/sentry.ts`) is the definition itself, leaving 3 consumer call sites (`customer-yaml-update.ts`, `heartbeat.ts`, `resend.ts` webhook handler) — matches the carried "3 of 94" count exactly, no regression, no improvement. `grep -rl "} catch" src/ | wc -l` = 156 files carrying at least one catch block, the denominator this is measured against. Falsifier: a materially higher `captureError` file count would have shown improvement; none occurred.
4. API-docs gap, counted once against Documentation (LOW) -> unchanged, see Documentation Finding above (route count now 99, still zero `docs/api/`).

**Findings:**
1. [MET] **Source control, CLAUDE.md, TypeScript+ESLint, no-hardcoded-secrets (Tier 1) all remain satisfied.** Instrument: repo is on GitHub with the enterprise ruleset above; CLAUDE.md exists and is substantially accurate (Documentation section); `tsconfig.json`/`eslint.config.js` both present and enforced via the required "Typecheck, Lint, Format, Test" context; gitleaks (see Finding 6 below) plus the `.npmrc` cooldown plus Infisical-based secret management cover the no-hardcoded-secrets bar. Falsifier: a missing config file or an unenforced check would have shown a Tier 1 gap; none found.
2. [PARTIAL] **Error monitoring (Sentry) — Worker-side wrapper real and correct; adoption thin; Python side out of scope for this repo.** Instrument: `wrangler secret list --name ss-web` lists `ANTHROPIC_API_KEY`, `CLERK_SECRET_KEY`, etc. (name-only, no `SENTRY_DSN` visible in the first ~15 entries returned before truncation — see Not Checked) and `src/lib/observability/sentry.ts` (86 lines) implements a correct `wrapRequestHandler`-based no-op-when-unset pattern, invoked from middleware. Adoption is Carried Finding 3 (3 consumer call sites). For the Operator Machines: `docs/adr/0023-operator-per-customer-observability.md` explicitly states "The Python init lives in `venturecrane/hermes-smd-overlay`... that repo is not in the `ss-console` workspace; implementation lands in a separate PR there," and `operator/runners/medchron/medchron/daemon.py:21` carries a comment confirming the negative: "No Sentry here or in [boot smoke]." `grep -rl -i sentry operator/ --include="*.py"` finds only 3 files, none an SDK init — one references the `SENTRY_DSN` secret name as a string, one is its test, one is the daemon's explicit non-init comment. Falsifier: a `sentry_sdk.init(` call in this repo's `operator/` tree would have contradicted ADR 0023's own architecture statement; none found, consistent with the ADR. This is why it's listed Not Checked (repo boundary) rather than Missing.
3. [PARTIAL] **Uptime monitoring is push-based (healthchecks.io) rather than a poller, appears built in this repo, but end-to-end wiring on a live Machine is unverifiable from a read-only ss-console review.** Instrument: `src/pages/api/health.ts` exists (internal health surface); `src/pages/api/webhooks/healthchecks.ts` exists with a real, documented bearer-token-authenticated receiver matching ADR 0023's design exactly ("Healthchecks.io does not sign outbound webhooks... configure each check with an `Authorization: Bearer <HEALTHCHECKS_WEBHOOK_SECRET>` header"); `tests/fleet-alerts.test.ts:62`, `tests/customer-slug-pattern.test.ts:279`, and `tests/operator-dockerfile.test.ts:207` all reference the healthchecks mechanism, though no dedicated route-level test exists for `webhooks/healthchecks.ts` itself. #2384 (the issue literally titled around an external poller) was closed 2026-08-31 via the same backlog-reset boilerplate as #2385 — not because a poller was built, but because the venture's actual chosen architecture (push, per ADR 0023) makes the issue's original framing moot. Falsifier: the receiver file, the health endpoint, or the ADR not existing would have made this a clean Missing; a passing dedicated integration test hitting the live healthchecks.io service would have made it a clean Met. It's genuinely in between: real code exists, matches a real design decision, but the Machine-side 60-second push (Python, per-customer Fly Machine) is outside this repo and wasn't probed live.
4. [MISSING, as expected] **PWA is absent**, matching the dashboard's own stated next-action for this venture — not a new finding. Instrument: `ls public/` shows only `favicon.svg`; no `manifest.json`/`.webmanifest`, no `icon-192.png`/`icon-512.png`/`apple-touch-icon.png`, no service worker; `astro.config.mjs` has no `@vite-pwa/astro` or equivalent plugin. Falsifier: any of the above artifacts existing would have shown partial progress; none do.
5. [PARTIAL] **`.gitleaks.toml` present; secret-detection workflow runs but working-tree-only (`--no-git`), a known carried Security-dimension gap that also affects Golden Path's Tier 1 "no hardcoded secrets" bar.** Instrument: `.gitleaks.toml` exists at repo root with the default ruleset extended plus project allowlists; `.github/workflows/security.yml`'s `gitleaks` job runs `./gitleaks detect --source . --verbose --no-git --config .gitleaks.toml`, confirming the carried Security finding is unchanged (history scanning is disabled by the `--no-git` flag despite `fetch-depth: 0`). Flagging here for the orchestrator to avoid double-counting against Security's dimension while still surfacing it against the Tier 1 "no hardcoded secrets" line.
6. [POSITIVE, verified] **A real, mechanically-enforced Python module-size ratchet now exists**, closing part of a carried Code-Quality gap ("No size or complexity ceiling applies to Python at all") that isn't formally mine to grade but bears directly on Golden Path's "Code Quality" Tier-1/3 lines. Instrument: `tests/operator-module-size.test.ts` — a Vitest suite that counts *logical* lines (blanks/comments/docstrings excluded, matching `eslint.config.js`'s `max-lines` methodology) for every Python file under `operator/`, maintains a checked-in baseline, and fails both on growth past the ceiling and on unexplained shrinkage (forcing baseline regeneration rather than silent drift). Its own header cites the exact carried-finding trigger: `establishment.py` going 692 -> 2,752 logical lines in nine days with nothing catching it. Because this test lives in the TypeScript suite (`npm run test`, part of `npm run verify`), it runs under the **required** "Typecheck, Lint, Format, Test" context — unlike `operator-substrate.yml`, this control actually gates merges. Falsifier: the file not existing, or existing but not wired into a required check, would have kept the carried gap fully open; neither is the case. **Flagging to Architecture/Code-Quality agents under Overall Notes** since this may close or downgrade their carried Code-Quality Finding 10 from the 08-23 review.
7. [MET] **Full CI/CD pipeline exists and is broad** (20 workflows: `verify.yml`/`security.yml` on push+PR to main; `deploy.yml` gated on `vars.DEPLOY_ENABLED=='true'`, push-to-main only; plus 15 narrower policy/reconciliation workflows on schedules or issue/PR-lifecycle triggers — `control-probes`, `audit-chain-verify`, `customer-config-reconcile`, `terminal-state-reconcile`, `unaudited-send-reconcile`, `tick-acs-on-merge`, `unmet-ac-on-close`, `scope-deferred-todo`, `runtime-ac-proof`, `escape-analysis-on-close`, `regression-claim-origin`, `adr-0022-pi-prefix-gate`, `ui-drift-audit`, `hermes-release-watch`, `cron-slot-watchdog`, `dependabot-auto-merge`). Instrument: `grep -m1 -A3 "^on:"` across all 20 files in `.github/workflows/`. Only `verify.yml`+`security.yml` are in the required-check set (Finding 1's carried gap), but the breadth of automation for an internal-tools venture is real and substantial.

**Not checked:**
- Full `wrangler secret list --name ss-web` output beyond the first ~15 entries — did not confirm or deny `SENTRY_DSN`'s presence by name; the tool call returned a partial list before this session moved on, and re-running to completion wasn't prioritized given Sentry's Worker-side code path already no-ops cleanly either way.
- Whether the `Security Summary` required ruleset check actually re-derives from the `Security` workflow's job outcomes 1:1, or is a separate summary job with its own pass/fail logic that could diverge from the underlying `audit`/`gitleaks`/semgrep jobs — read the workflow's structure but did not trace the "Security Summary" context's exact composition.
- Live verification that the healthchecks.io push actually fires from a real customer Machine (Python/overlay repo, out of scope for a read-only ss-console review).
- Tier 3 items (security audit, performance baseline, compliance review) beyond confirming, per the instructions, that nothing beyond what's already documented exists — not required at Tier 2 and not deeply probed.

**Summary:** The two carried HIGH/MEDIUM gaps (Python CI not required; zero-review branch protection) are unchanged and remain the dimension's biggest structural weaknesses. A genuinely new positive — the Python module-size ratchet running inside the required TypeScript check — is worth surfacing to whoever owns Code Quality. Uptime monitoring is more built than the closed-issue trail suggests, just architected differently (push, not poll) than #2384 assumed.

**Proposed grade: C** — rubric: "1 critical missing OR 3+ non-critical missing." The Python-substrate-not-required gate is arguably the one critical miss (it's the sole gate over safety invariants, the overlay drift check, and 145 test files); layered with the zero-review branch protection and thin Sentry adoption, that's a critical plus two non-criticals, matching the C line rather than B.

---

## Overall notes for the orchestrator

1. **Hand to Architecture/Code-Quality:** `tests/operator-module-size.test.ts` appears to be a real, required-check-enforced Python logical-line ceiling that didn't exist (or wasn't found) at the 08-23 review. If it postdates that review, Code-Quality's carried Finding 10 ("No size or complexity ceiling applies to Python at all") should probably be marked CLOSED or CHANGED rather than re-asserted verbatim — worth a second pair of eyes to confirm it actually runs cleanly against the current `operator/` tree (I confirmed the file and its required-check wiring, but did not execute `npx vitest run tests/operator-module-size.test.ts` to completion myself).
2. **Hand to Security:** the `gitleaks detect --source . --no-git` working-tree-only scanning gap (carried Security Finding 4) is still present and unchanged — I re-confirmed it as part of the Golden Path "no hardcoded secrets" line, but it's scored against Security's dimension, not mine. Also worth Security's attention: the new `.github/audit-allowlist.json` (2026-09-23 expiry) allowlists a **critical** Astro RCE — legitimate and tracked, but Security should independently judge exploitability/reachability of the AVIF-optimization path in this app's actual image-handling code, which I did not assess (my check was limited to confirming the allowlist mechanism itself works as documented, not the underlying vulnerability's blast radius in this codebase).
3. **Worth a second look generally:** I found a fourth instance (`src/lib/db/oauth-states.ts:84-86`) of the "references a worker that doesn't exist" false-documentation-claim family, same `booking-cleanup` phantom worker as the carried `holds.ts` finding. A single repo-wide grep for `booking-cleanup` (2 hits now, both false) plus a decision on whether to build the cron or delete the dead comments would resolve the whole family at once rather than piecemeal.
