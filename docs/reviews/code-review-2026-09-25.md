---
title: Code Review 2026-09-25
---

# Code Review: SMD Services

**Date:** 2026-09-25
**Reviewer:** Claude Code (automated)
**Scope:** Full codebase
**Mode:** Full (Phase 1: Claude-only, multi-agent fan-out)
**Models Used:** Fable orchestrator (independent re-probe of every claim graded below), four Opus agents (`src/` + `workers/` + `scripts/` + `.claude/`; `operator/`; Security + Testing; Dependencies + Documentation + Golden Path)
**Golden Path Tier:** 2
**Commit reviewed:** ceaca4c0 (== origin/main at review time)
**Window since last full review:** 1da55373..ceaca4c0, 170 commits, 1,186 files, +79,372 / -24,781 lines. By tree: `operator/` 702 files (+49,034 / -14,815), `src/` 207 (+6,231 / -3,666), `tests/` 133 (+13,553 / -3,767), `workers/` 16, `scripts/` 9, `.claude/` 5 (+1,387), `docs/` 69, `migrations/` 10 (0109 to 0118). Twenty-six of the commits are the follow-through waves the Captain approved from the 2026-09-10 review (PRs #2738 through #2784); the rest are A&P client work: ADR 0089 staff send-as, the medchron runner, staff mailbox reads, the scanned daily post, the obligation register (ADR 0088), and 26 `OVERLAY_REF` pin moves.
**Agent reports:** the four raw agent reports are preserved verbatim in [code-review-2026-09-25-agent-reports.md](./code-review-2026-09-25-agent-reports.md). Every claim graded below was re-derived by the orchestrator with its own command or read, or checked against the agent's stated instrument; where the orchestrator's number differs from an agent's, the orchestrator's is used and the difference is noted. Orchestrator verify rows: vfy_01M3CMM922MMFCBCMMABXXHW7H (vitest, tsc, lint), vfy_01M3CMMNGE68X7K9C0P4KB7YZ9 (Python substrate, pyright ratchet), vfy_01M3CMNCYWCN58G1QGVADMEF5Q (ruleset, protection, check runs, issues), vfy_01M3CMM9E45R6HF493B39C58C0 and its correction vfy_01M3CN7ZAST0T8G93WWBAPSWB1 (npm audit, allowlist).

## Summary

**Overall: B-** (up from C+ on 2026-09-10).

This is the first review in which the carried list did not survive. Of the 09-10 report's seven top action items and roughly forty carried findings, every one is closed or moved, and closed at the layer that matters rather than by trimming: the broker dispatcher is a verb table where each verb declares who may call it and the dispatcher checks that first (`operator/workspace_broker/verbs.py`; `handle()` at `server.py:278-282` is one line); the Python half has root packaging, a pyright ratchet holding at 104 errors across 44 files, `ruff format --check` over 528 files, per-function length and complexity ratchets, shellcheck, and a suppression pile in which every one of 461 `noqa` annotations names an enabled rule and carries a reason (orchestrator ran each gate in a clean venv, vfy_01M3CMMNGE68X7K9C0P4KB7YZ9); every seat authenticates with its own credential and a slug with no row fails closed (`src/lib/auth/machine-key.ts`); the four upward edges out of `src/lib/db/` are gone and the Operator split has zero back-edges; the two routes that were the domain layer are 0-function and 8-function files; the assessment data layer and the Stripe checkout module have behavioural tests against real D1 and real wire shapes; gitleaks scans full history on a public repository and finds nothing across 3,667 commits; the seat image installs every third-party Python package from hash-locked files with a fail-closed pip audit inside the required Security Summary; an API inventory covers all 95 routes and a test fails on drift; an outside-in poller checks the site every two minutes. The lint cap is zero. Both suites are green in a fresh worktree: 5,385 vitest tests and 2,873 Python tests (vfy_01M3CMM922MMFCBCMMABXXHW7H).

What keeps this from B is the shape the last review predicted: the window's own new code carries the next set of mediums, and this time most of them are places the gates do not look. ESLint ignores `scripts/**` and `.claude/**` wholesale, and the window put its two largest new TypeScript modules there, one with a 249-line `main` at complexity 50. The verb-table refactor turned 35 type-checked `self.` accesses into 53 `broker: Any` parameters the pyright ratchet cannot see. A live decommission's step 7 always runs a stub archiver, reports the step as executed, and the live-run refusal does not list it. The control that makes ADR 0089 approve buttons safe is probed for the first approver only, and the new staff-mailbox read set has no rule keeping approvers out of it. The AC-proof workflow CLAUDE.md calls a merge gate is not a required check. A moderate npm advisory sits on the lockfile, unreachable on inspection and one `npm update` from closed, with Dependabot security updates switched off. None of these is severe. All are the same class as the 09-10 mediums: the mechanism landed, and the report the mechanism produces is not yet a probe of what it did.

## Scorecard

| Dimension     | Grade | Trend           | Rubric line applied                                                                                                 |
| ------------- | ----- | --------------- | ------------------------------------------------------------------------------------------------------------------- |
| Architecture  | B-    | up (was C+)     | 18 modules over 500 logical lines, 16 baselined and 2 outside any gate; boundaries and route concerns now clean       |
| Security      | C+    | up (was C)      | One medium (send-as approver read-set) and four lows; seventeen of eighteen carried findings closed with tests        |
| Code Quality  | B-    | up (was C+)     | 53 `broker: Any` parameters, money-path casts, three-copy codec; both Python HIGHs closed at the gate layer          |
| Testing       | B     | up (was C+)     | Both critical-path gaps closed behaviourally; AC-proof gate advisory, ten tests CI-only, SOW render still skipped     |
| Dependencies  | C+    | down (was B)    | One moderate advisory on the lockfile, fixable today, with security updates off; seat Python HIGH closed with hashes  |
| Documentation | B-    | up (was C+)     | API inventory and cross-file numbers now test-pinned; handbook ADR index 24 records behind while claiming completeness |
| Golden Path   | B     | up (was B-)     | Uptime, secrets scanning and API docs met; zero required reviews (documented), one declared merge gate not required   |

**Overall: B-.** Mode of the seven grades is B- (three of seven); no dimension is D or F.

## Top action items

1. **Make the send-as safety control cover every approver and every read set.** ADR 0089 amendment 5a says the approve link is safe because the only mailbox holding it is the approver's and a boot probe fails a seat whose read app can open that mailbox. `operator/templates/msgraph-read-app-cannot-read-staff-probe.py:162` probes `staff[0]` only; `operator/connectors/msgraph-mail/msgraph_mail_connector/staff_mailboxes.py:92-113` `authorize()` refuses the Operator's own mailbox and any unauthored address, but not an address in `scope.staff_send_as`. The sets are disjoint on every seat today (orchestrator ran the overlap census: ashton-price send-as 1, reads 3, overlap 0; smd-staging 1, 0, 0). One authored line adding the approver to the read list, plus the routine tenant-scope step #2899 shows, would let the model read its own approve link through a sanctioned tool while boot smoke stays green whenever that approver is not first in the list. Fix in three places: `authorize()` refuses any send-as approver; the probe iterates every approver; the customer.yaml validator asserts the two sets are disjoint.
2. **Make the AC-proof gate a required check and make it look the ID up.** `gh api repos/venturecrane/ss-console/rulesets/15555219` requires `Security Summary` and `substrate`; legacy protection requires `Typecheck, Lint, Format, Test`. The job `Runtime ACs carry a verification ID` (`.github/workflows/runtime-ac-proof.yml:35`) is in neither list, so the workflow CLAUDE.md names as the Law 9 merge gate cannot block a merge. `scripts/runtime-ac-proof.mjs:38` accepts any string matching `\bvfy_[0-9A-Za-z]{20,}\b` without resolving it against the ledger. Since `unmet-ac-on-close` defers PR-closed issues to this check by design, it is the only AC control on that path. Add the context to the ruleset (it reports on every PR, so it cannot strand one) and resolve each `vfy_` ID before passing.
3. **Make decommission step 7 report what it did.** `operator/bin/lib/decommission.py:665` defaults `archiver` to `InMemoryComplianceArchiver`, whose result carries `"stub": True` (`:459`); `unwired_destructive_backends()` at `:682-712` lists the audit preserver, R2, Vectorize, AgentMail, Fly and observability, not the archiver; the step runner at `:911-913` marks a step SKIPPED only on a `skipped` key, so the stub is recorded EXECUTED; `decommission_cli.py` never substitutes an archiver (zero `archiver` hits). The module docstring at `:79-80` still calls AgentMail and Fly "(stubbed)". This is the #1123 class the refusal was built to end, on the offboarding evidence rather than a deletion. Wire step 7 to the evidence builder the docstring at `:73-75` already names, or list the archiver as unwired and return `skipped: True` until then.
4. **Give the broker handlers back their types.** `operator/workspace_broker/verbs.py:53` types the handler's first parameter as `Any`, and `grep -rn "broker: Any" workspace_broker --include='*.py' | grep -v /tests/ | wc -l` is 53 across ten modules, 16 of them in `send_as_acts.py`. At 1da55373 the same bodies were `Broker` methods with 35 `self.` attribute reads pyright checked. A `BrokerContext` Protocol naming the attributes handlers read (`ledger`, `job_ledger`, `medchron`, `operations`, `credential_path`, `customer_path`, the transports) closes it; the pyright ratchet then covers the send-as lane.
5. **Lint the two trees the gate skips, and give the register CLI the real upsert.** `eslint.config.js:315` ignores `**/.claude/**` (added for worktrees) and `:323` ignores `scripts/**`. Running the repo's own error-tier ceilings over those trees with `--no-ignore` reports 20 violations, including `scripts/ci-reconcile-obligations.ts:533` `main` at 249 lines and complexity 50 and `.claude/hooks/lib/register.mjs` at 627 logical lines; both are live (`obligation-reconcile.yml` runs the first on a schedule, `.claude/bin/register` execs the second). `register.mjs:381-413` also string-builds the obligation upsert with a hand escaper `sqlLiteral` and ships it to `wrangler d1 execute --command`, a second write implementation of the table `src/lib/db/obligations.ts:238-275` binds with 22 placeholders, and the column lists already differ. Narrow the ignore to `**/.claude/worktrees/**`, replace the `scripts/**` ignore with a block that relaxes only the type-aware rules, split `main` along its four documented phases, and route the CLI through `scripts/lib/wrangler-d1.ts` and `upsertObligation`.
6. **Close the money-path residue in one wave.** `src/lib/stripe/milestone-invoicing.ts:84` and `src/lib/sow/service-finalize.ts:278` cast `JSON.parse(quote.line_items)` instead of calling `parseLineItems` (`src/lib/db/quote-content.ts:40-50`); at `:88` a missing `estimated_hours` puts `NaN` into an invoice amount. `src/lib/webhooks/stripe-handler.ts:158,205` and `src/pages/api/webhooks/stripe.ts:302,321` catch into `console.error` only; the `failedResponse` helper reaches Sentry, but the webhook deliberately never 500s, so its caught failures are invisible to the middleware and the helper alike. `stripeHeaders` is byte-identical in `src/lib/stripe/client.ts:16`, `checkout.ts:50`, `subscriptions.ts:62` (the 09-10 recommendation, not applied by wave 5a). The signed-token codec (`base64UrlEncode`, `base64UrlDecode`, `importSigningKey`) is hand-rolled in `oauth/state.ts`, `booking/signed-link.ts`, `assessment/session.ts`. Four edits, one PR.
7. **Refresh the lockfile and turn security updates on.** `npm audit` at the root reports one moderate: `devalue <5.9.1` (GHSA-9rgm-9g3h-6x36, DoS via malformed input), transitive under `astro@7.3.1`, whose range is `^5.8.1`; `npm view devalue time` shows 5.9.2 published 2026-08-27 and 5.9.4 on 09-18, so the `.npmrc` cooldown is not the blocker. `gh api repos/venturecrane/ss-console/automated-security-fixes` returns `enabled: false`, and the open root Dependabot group #2870 does not touch it. Reachability: the only `devalue.parse` callers in the bundle are Astro actions and sessions, and this app defines neither. `npm update devalue`, commit, enable security updates, drop `/operator` from the duplicate pip entry in `dependabot.yml` (#2897 and #2898 are byte-identical PRs), and merge the six green Dependabot PRs.
8. **Docs that claim completeness must be pinned.** `docs/handbook/adr-index.md` says "Every Architecture Decision Record" and stops at 0065; `ls docs/adr | grep -cE '^00(6[6-9]|[78][0-9])-'` is 24 missing, last page change 2026-07-27, no test names it. `docs/handbook/connectors-channels.md` was last changed 2026-07-03 and mentions neither the staff-mailbox reader nor the letter pipeline the window added to live connectors. `operator/CLAUDE.md`'s ADR load list stops at 0021 (plus 0037, 0053). `README.md:18` says Vitest 4 against `^5.0.0`. Generate the ADR index the way `docs/api/README.md` is generated; extend `handbook:drift` to fail when a connector manifest changes without its page.
9. **A hygiene batch, each one line or one file.** A post-build assertion that `dist/server` contains no `CLERK_SECRET_KEY: "` (a local `npm run build` inlines the shell's key; production builds carry no Clerk env, but a laptop `wrangler deploy` would ship it). The nosemgrep regex at `security.yml:91` passes any rule-id-only suppression whose id exceeds 19 characters, which is every real Semgrep id (22 such lines, 0 flagged). `seat-probe.sh:99-100` strips root secrets by a deny-list that grew twice in three days after live leaks; build the agent environment from the gateway's allowlist instead. `firm-config-currency.sh:17-28` returns 0 in three cannot-evaluate states. Add `astro.config.mjs` to knip's `project` array so the two false dependency warnings stop printing (the comment at `knip.jsonc:46-53` claims a fix that does not hold). Refresh the stale comments: `eslint.config.js:56-64` (cap is 0, not 13), `tests/operator-module-size.test.ts:16` (substrate is required), `ruff.toml` S110 table, `decommission.py:79-80`.

## Detailed Findings

### 1. Architecture

**Carried findings.** Every 09-10 Architecture item was re-probed. **Closed:** broker `handle()` if-chain (A1; `verbs.py` table, `test_verb_table.py`, `tests/broker-verb-registry.test.ts`); `adapter/inbound_envelope.py` and `resolve_skill_pins.py` (A9; deleted by #2772); the near-500 cluster in `src/` (A3; the six files decomposed by #2771, one non-exempt file at 498, `types.ts` at 504 under a reasoned exemption at `eslint.config.js:297-308`); `heartbeat.ts` and `booking/reserve.ts` as domain layer (A3; 0 and 8 functions, zero `.prepare(` in either); the dead OAuth callback and its middleware clause (A6; `src/pages/api/oauth` absent, zero `oauth` hits in `middleware.ts`); the four `db/` upward edges and the fee back-edge (A7; the src agent's import walk over 403 files and 1,083 edges lists six `db/` outbound value edges, all to leaves; `src/lib/pricing/card-fee.ts` has zero imports); the Operator back-edge (A7; 31 edges portal to operator, 0 back); non-atomic price and rail (A8; `setOperatorPriceAndPaymentMethod` at `services.ts:256-287`, one UPDATE; orchestrator read the header at `:14`); `fleet-alerts` dry-run (A12; `verify.yml:104`); the stale skill NOTE (A13). **Changed:** Python packaging (A2; root `pyproject.toml`, `pip install -e ./operator` in substrate, `safety_substrate/` renamed; non-test `sys.path` mutations 10 from 33, non-test E402 4 from 130, orchestrator recount 7 files / 10 sites); skill helper duplication (A4; bodies moved to `templates/skill_helpers.py`, vendored into six skills and glob-gated with a floor of 6); hand-tuple sync gates and the `broker_writer` twins (A5; `operator/tests/vendored_sync.py` discovers by glob and the twins are md5-identical); `packet.py` and `establishment_store.py` (A10; 456 and 598 logical lines from 1,128 and 1,415, both split at their seams by #2769 and c02f91fc); shell scripts (A11; now under `contracts/operator-shell-size.json`, two baselined). **Wrong:** the 09-04 probe's F7 threshold (16 rows against an expected 20) reads a smaller contract as lost gating; the bodies moved into `skill_helpers.py` and are gated there. Route SQL: 16 files to 15, none added in the window.

1. **[MEDIUM, carried, reshaped] 18 non-test modules sit over the 500 logical-line ceiling: 16 Python modules baselined by the ratchet and 2 TypeScript modules outside any gate.** `contracts/operator-module-size.json` holds 16 entries totalling 10,751 lines (12,761 at 1da55373), largest `templates/drafting/drafting_gate_check.py` 1,135 (stdlib-only by design, loaded by path under `execute_code`), `connectors/smokeball/smokeball_connector/server.py` 1,001 (64 defs, a 168-line `render_docx_template`), `skills/lien-ledger-tracker/pre_run.py` 880. The operator agent's ratchet history shows no baselined value grew between 2026-09-14 and HEAD; the entries that joined (`daemon.py`, `llm.py`, `seed_data.py`) came through the formatter commit #2744, proven AST-identical before and after. Six more Python modules sit within 44 lines of the ceiling, three new this window (`driver.py` 493, `send_as_acts.py` 489, `medchron_ledger.py` 486). The two TypeScript modules are finding 2. Recommendation: split the Smokeball connector server along its tool families and the two tracker `pre_run.py` files along `decide`/`run_once`, now that the sibling loader makes that legal.

2. **[MEDIUM, NEW] ESLint ignores `scripts/**` and `.claude/**`, and the window's two largest new TypeScript modules live there, over the ceiling.** Top action item 5. Orchestrator re-ran the agent's instrument: 20 error-tier violations under `--no-ignore` (the agent's 23 includes `max-depth` and `max-params`); `scripts/ci-reconcile-obligations.ts:533` `main` 249 lines / complexity 50, `:360` `probeEvidence` complexity 18; `.claude/hooks/lib/register.mjs:739` file 627 lines, `:481` `cmdAdd` 82 lines / complexity 16; `.claude/hooks/lib/board.mjs:83` complexity 35; `.claude/hooks/trap-recurrence.mjs:168` complexity 26. The `.claude/**` glob was added 2026-05-06 for worktrees and also exempts every enforcement hook (`worktree-guard.mjs`, `engagement-guard.mjs`) from lint; `.mjs` under a dot-directory is outside `tsc` too.

3. **[MEDIUM, NEW] A live decommission's step 7 always runs the stub archiver and reports EXECUTED.** Top action item 3; orchestrator read `decommission.py:665,682-712,905-920` and grepped the CLI.

4. **[MEDIUM, NEW] `src/lib/db/` is the data layer for one of eleven places SQL lives.** Orchestrator count: 61 non-test `src/lib` files outside `db/` call `.prepare(` against 25 inside (agent: 58 and 22 at 1da55373, 208 call sites outside now). By directory: `admin` 12, `portal/operator` 10, `portal` 8, `booking` 7, `auth` 7, `webhooks` 5, `sow` 4, `operator/mcp` 3, `operator` 3, `stripe` 1, `entities` 1. The 09-10 fix for the `db -> stripe` edge moved milestone logic into `src/lib/stripe/milestone-invoicing.ts` carrying four `.prepare(` calls with it. The "db calls nothing above it" invariant now holds and protects a small boundary. Recommendation: decide the rule in writing (`db/` owns all SQL, or each domain owns its tables) and pin it with an import restriction or a `.prepare(` allowlist test; the decision matters more than the direction.

5. **[LOW, NEW] One file-level import cycle, introduced by #2858, and no rule against the next.** `src/lib/operator/customer-yaml/sections-scope.ts:9` imports `checkStaffSendAs` from `sections-staff-send-as.ts`, whose `:3` imports `canonRosterAddress` back (orchestrator read both lines); `grep -c no-cycle eslint.config.js` is 0 although `eslint-plugin-import-x` is loaded. Both uses are inside functions, so it runs. Move `canonRosterAddress` to `customer-yaml/helpers.ts`; enable `import-x/no-cycle`.

6. **[LOW, NEW] `src/lib/admin` and `src/lib/portal` import each other by value.** `admin/governance.ts:23` and `admin/people-view.ts:17` import from `portal/operator/`; `portal/operator/pause-control.ts:30` imports `clearStopOnMachine` from `admin/sticky-stop-clear` (orchestrator grep). The governance accessors already have a neutral home in `src/lib/operator/vertical-floors.ts`.

7. **[LOW, NEW] `workers/fleet-alerts/src/index.ts` is 485 logical lines holding six concerns** (threshold readers, connector parsing, condition evaluation, D1 alert state, transition email, healthcheck ping, `runOnce`) while a sibling `conditions.ts` exists. This is the pager; the next feature lands it over the ceiling.

8. **[LOW, NEW] The Python near-twin modules in the two tracker skills carry four ungated identical helpers.** `blind_wake.py`, `dispatch_envelope.py`, `render.py` differ by hundreds of lines between `client-verification-tracker/` and `deadline-miss-escalator/`, yet `_load_sibling`, `_load_yaml`, `_write_envelope`, `canonical_body_sha256` are AST-identical inside them and no contract names them; the two `blind_wake.py` copies call the same concept `_plan_counts` and `plan_counts`. Move the four into `skill_helpers.py`, which both skills already vendor.

9. **[LOW, NEW] The pyproject "still shimmed" list undercounts by four, and one bare-name import survives.** `bin/medchron-backfill-covered.py:90`, `bin/seed-staging-matter.py:55`, and the two voice corpus CLIs (`:87`, `:106`) also insert the operator root that Stage 2 says was removed; `bin/lib/send_attribution.py:56` does `from send_verify import ...` under `TYPE_CHECKING`.

10. **[POSITIVE, verified] No import cycles in either tree, and the layers point one way.** Python: the operator agent's graph over 324 non-test modules and 404 edges finds 0 strongly connected components; cross-root edges are `bin -> adapter/connectors/workspace_broker/runners`, `safety_substrate -> adapter`, `templates -> connectors`, and zero `adapter -> workspace_broker`, `workspace_broker -> bin`, `skills -> bin`. Four mutual pairs are broken by deferred imports, two new (`driver.py`/`rehearsal.py`, `establishment_lifecycle.py`/`establishment_store.py`). TypeScript: one cycle (finding 5). What would show this false: any SCC larger than one, or a value import in the pairs.

11. **[POSITIVE, verified] Every large new module is reached from a real path and states its invariants.** `send_as_acts.py` via `verbs.py:48`; `medchron_ledger.py` via `medchron_verbs.py:41`; the medchron daemon via `entrypoint.sh:684-697` under a respawn loop with `boot-smoke-test.sh:636-637` asserting a fresh tick; decommission via `bin/decommission-customer.sh:91`; the drafting gate by path from the Smokeball connector's `record_check.py:63`; `transform.py` as an enrolled overlay twin (`verify-overlay-pairs.py` PASS on all 10). The send-as header (orchestrator read `:1-30`) states the four impersonation rules the broker enforces and why the broker owns them.

**Grade: B- (up from C+).** Rubric: "C: 3+ files over 500 OR unclear domain boundaries OR mixed concerns in route handlers." The first clause is true on both halves (18 files); the second and third are false by measurement for the first time in five reviews. Not B: eighteen is not "1-2 slightly large files", two of them are invisible to the gate, SQL ownership is undecided, and a live destructive pipeline reports a stub as executed. Not C+: every boundary and route-concern finding carried since 08-23 closed at the structure, both trees are cycle-free, and the debt that remains is held by ratchets that only tighten and have held for eleven days.

### 2. Security

**Carried findings.** Seventeen of eighteen 09-10 items **closed** at the mechanism, each with a test, re-probed by the orchestrator where the grade depends on it: per-tenant machine credentials (`machine-key.ts:1-28`, `migrations/0114_machine_credentials.sql`, sentinel HMAC on unknown slug, fleet fallback removed 2026-09-14); the portal enumerator (`tests/portal-routes-bind-tenant.test.ts` walks 19 API and 31 page routes on call sites, with a self-test that a comment mention does not match; the admin template at `:26-27,48-50` now matches `requireAdminSession\s*\(`); the three decommission edges (`_allow_unwired_refusal` at `decommission_cli.py:218-237` requires an explicit root outside `operator/customers`, `--live` requires `--confirm-slug`; `_fly_authenticated` at `decommission_backends.py:507-517` is `bool(env.get("FLY_API_TOKEN"))`; `_row_deleted` at `:487` is an observed post-delete count); gitleaks over full history (`security.yml:200` `gitleaks git` under `fetch-depth: 0`, the agent's own run over 3,667 commits clean, eight commit-pinned `.gitleaksignore` fingerprints all placeholders); `/api/events` per-IP limiter (`events.ts:5,157`); JWKS cached per issuer (`token-validation.ts:77-85`); JIT identity mapped to local user id (`customer-resolution.ts:127,137-151`); constant-time bearer on `assessment/llm.ts:34,95`; broker replies `internal_error` for anything but `PermissionError`/`ValueError` (`request_errors.py:29-34`); OAuth callback re-resolves access before `store()` (`callback.ts:234`); cross-site layer at `middleware.ts:239-242` with `tests/middleware-cross-site.test.ts` on both hosts; Sentry non-finite timestamp; four of five lows. **Changed:** the nosemgrep gate (finding 3). **Open by design:** `ss_sid` without HttpOnly (the client script reads it; it carries no authority).

1. **[MEDIUM, NEW] The ADR 0089 approve-button control is probed for the first approver only, and the staff-mailbox read set has no rule excluding approvers.** Top action item 1. The security agent read the chain end to end, including the overlay at the pinned `OVERLAY_REF` a15f0971 fetched as a tarball (never the working copy): proposal (gateway-only verb, gates on the proposing session), approval email (signed link naming row, verb and expiry; HMAC key 0600; `no_sent_copy`), emailed decision (`decided_by` is the verified inbound origin's sender, never a model argument; approver-only send, admin cancel, Sent Items forgery probe that refuses on failure) or button (`decide_link` at `send_as_acts.py:627-662`), send (`_apply_send` `:565-604`: digest, `from == approver`, approver still on roster, atomic consume, no retry). Every gate the ADR names is present and `workspace_broker/tests/test_send_as.py` holds 33 tests including a tampered token, an expired link, an admin trying to send, and a forged origin. The gap is the stated reason the button is safe. Orchestrator read `msgraph-read-app-cannot-read-staff-probe.py:162` (`mailbox = staff[0]`) and `staff_mailboxes.py:92-113` (refuses own mailbox and unauthored addresses; no send-as check), and re-ran the overlap census (0 on every seat). Nothing tests the case that matters (Testing 4).

2. **[LOW, NEW] A local `npm run build` bakes `CLERK_SECRET_KEY` from the shell into the Worker bundle.** Orchestrator confirmed by count only: `grep -c 'CLERK_SECRET_KEY: "' dist/server/chunks/server_*.mjs` returned 2 in this review worktree after the deps agent's build, and the worktree's `dist/` was deleted before this report was written. Astro inlines private `import.meta.env.*` reads in server code at build time and `@clerk/astro` reads the key that way. Production is unaffected: `deploy.yml:37` runs `npm run build` with no Clerk variable in the job. The exposure is a laptop `npm run build && wrangler deploy` or a shared `dist/`. The primary checkout's own `dist/` was not inspected (read-only tree). Recommendation: a post-build assertion in `verify.yml` and a local `postbuild` script that fail on the pattern, or build under `env -u CLERK_SECRET_KEY`.

3. **[LOW, CHANGED] The nosemgrep justification gate now scans `#`, and passes every rule-id-only suppression.** `security.yml:91` (orchestrator read): the `:` branch matches only when the rule id is 19 characters or shorter; every real Semgrep id is longer, so `# nosemgrep: python.lang.security.audit.dynamic-urllib-use-detected...` never matches and passes. The agent replayed the regex with `grep -P`: 79 lines mention nosemgrep, 0 flagged, 22 rule-id-only (19 Python, 3 in `tests/typography-tokens.test.ts`). Most carry a reason on the line above, which the gate cannot see. Require 20 characters after the id on the same line or a non-empty comment line immediately above, and test the regex against fixtures.

4. **[LOW, NEW] `seat-probe.sh` strips root-only secrets by deny-list, and the list grew twice in three days after live leaks.** `operator/bin/seat-probe.sh:99-100` (orchestrator read) excludes seven named variables; #2879 added the Graph send credential after boot smoke caught it on the law-firm seat on 09-22, #2902 added the R2 account keys after the same on 09-24. Both fixes are right; the class is open. Build the agent environment from the gateway's agent-uid environ as an allowlist.

5. **[LOW, NEW] The firm-config currency guard (#2904) returns 0 in three cannot-evaluate states.** `operator/bin/lib/firm-config-currency.sh:17-28` (orchestrator read): not a git checkout, fetch failed, file absent from `origin/main`. The repo's own Law 2 doctrine is that "cannot evaluate" must not read as "permitted". This is an integrity control over a paying client's chronology tiers, hence low. Die in all three unless `SS_ALLOW_DIVERGENT_SOURCE=1`, which already exists.

6. **[POSITIVE, verified] Headers, injection, webhooks, the grant model, client text in a public repo.** The security agent's own `curl -sI` on all three hosts: HSTS with includeSubDomains, `frame-ancestors 'none'`, nosniff, DENY, strict-origin-when-cross-origin, on the 302 paths too; `/api/mcp` a genuine 410. 419 `.prepare(` sites in `src` and `workers`, every template interpolation a code constant; zero `dangerouslySetInnerHTML`, `eval(`, `new Function(`; all 8 `set:html` through `serializeJsonLd`; Python 0 `shell=True`, 0 `os.system`, 0 `pickle`, 0 unsafe `yaml.load` against 98 `safe_load`; the window's new Graph URL path interpolation restricted by regex at `staff_mailboxes.py:34`. Every webhook verifies before effect; every public non-webhook POST carries `rateLimitByIp`. Grants filtered `revoked_at IS NULL AND expires_at > now` in SQL with a 90-day clamp. The obligation register keeps captured quotes in D1 and a local journal; the CI reconciler's findings carry id, slug and kind and never `row.what`, pinned by `tests/obligation-reconcile.test.ts`; a `git grep -l` of the client's own mail domain hits one file, the seat's own `customer.yaml`.

**Not checked:** whether a spoofed external `From:` claiming the approver can reach the overlay's verified inbound origin (ADR 0089 relies on the firm's intra-tenant authentication); whether any link scanner submits the approve page's POST; Clerk cookie attributes; any secret value; Cloudflare R2/D1 policy; the live ApplicationAccessPolicy on either tenant; live behaviour of any authenticated endpoint; a full non-baselined Semgrep run.

**Grade: C+ (up from C).** Rubric: "C: any medium OR 3+ low." One medium and four lows stand, so C is the literal band. The plus is the count adjustment the rubric allows: seven mediums became one, seventeen carried findings closed with tests, history scanning is live and clean on a public repository, and the window's riskiest feature has every gate its ADR names. Not B: the one gap sits on the lane that puts a named person's signature on outside mail, and the stated control is one authoring step from vacuous with nothing that would turn red.

### 3. Code Quality

**Carried findings.** **Closed:** the inert `noqa` pile (CQ6; `ruff.toml` selects `E4, E7, E9, S102, BLE001, F401, RUF100, S310, S603, S608`; `ruff check .` and `ruff check --extend-select RUF100 .` both clean in the orchestrator's venv, so zero of 461 annotations are inert; `bin/noqa-reasons.py` passes in substrate); no type checker, formatter or ceilings (CQ7; pyright basic mode ratcheted at `contracts/operator-pyright-baseline.json`, 104 errors / 44 files holding, fails on growth and on shrink-without-regenerate, stdlib diagnostics dropped by design so the two 3.14 `templatelib.py` errors on the orchestrator's laptop do not count; `ruff format --check` 528 files; `bin/tests/test_function_size_ratchet.py` with 52 length and 27 complexity entries and a test that plants an 87-line function to prove the counter can fail); shellcheck (CQ9; `--severity=warning` over 21 files inside substrate); the six-skill `except: pass` swallow (CQ10; `skill_helpers.py:321-322` and its six copies call `warn_observability_failure`); the `S310` callee justification and the `_plan_counts` collision (CQ12; `decommission_backends.py:95-96` refuses non-https before `urlopen`); the lint cap and the twelve `JSON.parse` sites (CQ11; `--max-warnings 0`, zero warnings, `quotes.ts` parses through `parseLineItems`); knip false positives (CQ8; **open**, see Dependencies 2); the `no-restricted-syntax` guard (widened to seven helper names plus hand-built error bodies). **Changed:** byte-identical duplication (CQ3; four of five named helpers have one home; `stripeHeaders` still x3, orchestrator diffed the three bodies); `captureError` reach (CQ4; orchestrator count: 181 non-test `src` files with `catch`, 39 files import `captureError` or `api/failures`, union 37, from 5 of 174); the error vocabulary (CQ5; closed in routes, 23 snake_case codes and `tests/api-error-vocabulary.test.ts`; open in `src/lib`, where `admin-session.ts:66` returns `'Unauthorized'` as the 401 body for 99 routes and six webhook/SOW sites return `'INTERNAL_ERROR'`); the dead-module census (CQ14; six hand-run CLIs, none library code). Type discipline in `src/` holds: zero real `any`, zero `@ts-*` suppressions, zero empty catch blocks.

1. **[MEDIUM, NEW] The verb-table refactor removed type checking from every broker handler.** Top action item 4; orchestrator counted 53 and read `verbs.py:53`. The authorization structure improved and the type coverage of the same code regressed, and no gate registers the second change because `Any` produces no pyright errors.

2. **[MEDIUM, NEW] Two money-path modules cast parsed line items instead of validating them, and 19 sites cast parsed JSON to a type.** Top action item 6; orchestrator read `milestone-invoicing.ts:84,88` and `service-finalize.ts:278`. Both carry the pattern from `db/milestones.ts:301` at 1da55373, so the 09-10 move preserved it. The src agent counted 19 `JSON.parse(...) as T` / `(await x.json()) as T` sites in non-test `src` and `scripts`; three were read, and the post-HMAC ones with field checks are defensible. Recommendation: `parseLineItems` in both, and `@typescript-eslint/no-unsafe-type-assertion` at warn with a ratchet.

3. **[MEDIUM, NEW] The signed-token codec is hand-rolled three times, and the `src/` duplicate census is 28 identical copies.** Orchestrator grep: `importSigningKey`, `base64UrlEncode`, `base64UrlDecode` in `oauth/state.ts:149-181`, `booking/signed-link.ts:150-176`, `assessment/session.ts:220-252`, plus `booking/tokens.ts:58`. All three verify with `crypto.subtle.verify`, so there is no drift; the hazard is the next fix landing in one of three. The src agent's AST census over `src/`: 48 names defined twice or more, 21 with identical bodies, 28 redundant copies, including `ok`/`serverError` in three webhook files (one of which imports them and redefines them) and `stripeHeaders` x3. Recommendation: `src/lib/security/signed-payload.ts`; `stripeHeaders` into `stripe/client.ts`; add the codec names to the guard.

4. **[MEDIUM, carried, sharpened] The Stripe webhook path reports caught failures only to `console.error`.** Orchestrator read `stripe-handler.ts:158,205` and `webhooks/stripe.ts:302,321`; `stripe.ts` imports only `misconfiguredResponse` from the failures helper and by its own comment at `:261` never 500s, so neither the middleware wrapper nor the `failedResponse` helper sees a caught failure there. The src agent counts 22 webhook and money files with a `catch` and no capture. The one route fixed in the window (`operator-price.ts:57`) is the model.

5. **[LOW, NEW] Every ratchet baseline regenerates from one environment variable with no merge-base comparison.** `UPDATE_OPERATOR_MODULE_SIZE_BASELINE=1`, `UPDATE_OPERATOR_FUNCTION_SIZE_BASELINE=1`, and `pyright-ratchet.py --update` each rewrite their contract unconditionally. The operator agent walked every commit touching the four contracts since 1da55373: every entry that joined or grew traces to a file move or to the formatter commit #2744 (AST-identical). The discipline held because of review. A substrate step that loads each contract at the merge-base and fails on any rise or new key, absent a named label, closes it.

6. **[LOW, NEW] The module-size counter skips every triple-quoted span, not only docstrings.** `tests/operator-module-size.test.ts:131-153`; a docstring-only AST count puts `adapter/voice/transform.py` at 744 against the ratchet's 669 and `send_as_acts.py` at 509 against 489. Triple-quoted SQL, prompts and templates are code the reader holds. Document the broader rule or restrict the exclusion to first-statement strings.

7. **[LOW, NEW] 19 non-test `try/except/pass` or `continue` sites remain, and one is behavioural.** `ruff --select S110,S112`: 16 and 3. `skills/deadline-miss-escalator/pre_run.py:925-926` swallows a failed degraded-suppress audit write and falls through to the stripped wake with nothing recording the write failure; the `ruff.toml` table still says "S110 14 / 14" dated 2026-08-24.

8. **[LOW, NEW] `scripts/migrate-to-entities.ts` is dead and cannot run.** It reads `lead_signals` and `parking_lot`, dropped by migrations 0080 and 0011; invisible to knip because `scripts/*.{ts,mjs}!` are entries and to lint because of `scripts/**`; its `migrateToEntities` is 288 lines at complexity 53. Delete it.

9. **[LOW, NEW] Stale justifications and directives.** `sections-persona-skills.ts:155-170` mirrors the restrictiveness table "so the validator layer stays free of portal imports", but `vertical-floors.ts` is no longer a portal module. `eslint.config.js:56-64` says the cap is 13. `src/env.d.ts:1`, `marketing/attribution.ts:53`, `rbac-audit.ts:162` disable a rule with no reason. `tests/operator-module-size.test.ts:16` says substrate is not required.

10. **[POSITIVE, verified] Every gate the 09-10 review asked for now runs inside a required check and fails in both directions.** Ruff with the three security rules, RUF100, noqa-reasons, `ruff format --check`, the pyright ratchet, the function-size ratchet, shellcheck, and the module-size and shell-size ratchets: each run by the orchestrator (vfy_01M3CMMNGE68X7K9C0P4KB7YZ9) and read for its failure mode. Each is guarded against passing on an empty census: pyright's shrink check, `test_the_counter_can_fail`, the vendored-symbols floor, the `vendored_sync` floors.

11. **[POSITIVE, verified] The vendored-copy system is discovered, not listed.** md5 over non-test Python finds 5 identical-file groups with 25 copies (`pre_run.py` stamp x8, `skill_helpers.py` x7, `escalation_ledger.py` x6, `broker_writer.py` x2, `routing.py` x2), every group glob-gated with a floor through `operator/tests/vendored_sync.py`, and `test_vendored_sync.py` plants temporary trees to prove a missing, drifted or edited copy fails. The count rose from 3 groups / 14 copies on 09-10 because more duplication moved into gated whole-file vendors.

12. **[POSITIVE, verified] The route error-body contract is enforced by three layers.** A catalog (`src/lib/api/errors.ts`), a membership test, and lint selectors rejecting hand-built `error` bodies in `src/pages/api/**`. What would show it false: any sentence-case first argument in the `errorResponse` grep, of which there are none.

**Grade: B- (up from C+).** Rubric: "C: `strict: false` OR 3+ `any` OR inconsistent error handling OR notable DRY OR 3+ dead exports." The 53 `broker: Any` parameters are the nearest thing to the second clause, and they are a regression a new gate cannot see. Both 09-10 HIGHs closed at the gate layer; error handling is consistent in shape and enforced in routes; dead exports and dead modules are zero; DRY is gated on the Python side. Not B: the `Any` gap, the money-path casts, the dark Stripe catches and a three-way security codec are more than "1-2 minor". Not C+: none of the C clauses holds on current evidence.

### 4. Testing

**Carried findings.** **Closed:** `tests/build-output.test.ts` (deleted by #2750); `src/lib/db/assessments.ts` (`tests/assessments-behaviour.test.ts`, 17 cases importing the real module against miniflare D1 with every migration applied and an org-isolation case per query); `src/lib/stripe/checkout.ts` (`tests/stripe-checkout.test.ts`, 16 cases importing the real module and asserting wire shapes including the fixed coupon id); the 17 source-text suites (14 now import their subject, #2773; the three that remain are a behavioural sibling's companion, a repo-wide policy scanner, and `clients.test.ts`); `test_packet.py` (`:290,295` `monkeypatch.delenv`; the orchestrator's run with the key empty passed 2,873); `operator/fixtures/**` in the substrate path list; `substrate` required (T1, holds: ruleset, HEAD check runs, vfy_01M3CMNCYWCN58G1QGVADMEF5Q). **Open:** `tests/sow-render.test.ts` `describe.skip` (`:31-36`); no coverage measurement in either language (deliberate, `vitest.config.ts:57-92`; `package.json:13` still carries a `test:coverage` script with no provider installed).

1. **[MEDIUM, NEW] `runtime-ac-proof.yml` is not a required check, so the Law 9 gate cannot block a merge.** Top action item 2. Orchestrator: the ruleset's required contexts are `Security Summary` and `substrate`; legacy is `Typecheck, Lint, Format, Test`; the job name at `runtime-ac-proof.yml:35` is in neither. `scripts/runtime-ac-proof.mjs:38` matches `\bvfy_[0-9A-Za-z]{20,}\b` and never resolves it. It also passes vacuously on a PR body with no `## Acceptance criteria status` section or no `(runtime)` tag. The security agent read the crane-console reusable workflow at its pinned SHA: `unmet-ac-on-close` returns without checking when the closer is a PR, when the issue has no "Acceptance" H2, or when `force-close` is present, deferring to this check by design.

2. **[LOW, NEW] Ten vitest tests run on developer machines and never in CI, four of them behavioural.** CI vitest on HEAD: 5,375 passed / 15 skipped (agent read of run 36154740893); local 5,385 / 5. `tests/provisioner-gates-parse-not-grep.test.ts:145` is `describe.skipIf(!hasUv())` and `verify.yml` installs no `uv` (orchestrator: zero hits), so the four behavioural cases of a provisioning gate that once failed silently on the law-firm seat are skipped on every PR; `heartbeat-field-parity` (3) needs an overlay checkout CI never has. Add `setup-uv` to `verify.yml`; fetch the overlay tarball at the pin for the parity block, as substrate already does for the hash gate.

3. **[LOW, carried] The SOW PDF render path has no executing test.** Unchanged; the one artifact a client signs is validated by `npm run build` compiling the template.

4. **[LOW, NEW] Nothing tests the approver and read-set overlap.** `test_msgraph_read_app_cannot_read_staff_probe.py` has seven single-approver cases; `test_staff_mailboxes.py` has nine refusals, none for an address that is also a send-as approver. The fix and its test belong together (Security 1).

5. **[LOW, carried residual] The `substrate` required context carries no `integration_id`.** Any app posting a check run named `substrate` satisfies the rule.

5a. **[LOW, NEW, fixed in this PR] `tests/obligation-reconcile.test.ts` went red on machine load alone.** Each of its 44 cases spawns `npx tsx scripts/ci-reconcile-obligations.ts` (a cold TypeScript compile per invocation) under vitest's default 5 s timeout. The orchestrator's first full run passed (load average 4); the pre-push `npm run verify` for this report failed 18 of them at 5.0 to 6.7 s with the load average at 14 (a peer session was running live seat proofs), every assertion true. The suite could not distinguish a slow instrument from a wrong answer. Fixed here with `vi.setConfig({ testTimeout: 60_000 })` and a comment sizing the timeout to the instrument. `git grep -l "execFileSync('npx'" tests` names only this file, so the class is closed with it; other spawning suites run prebuilt scripts.

6. **[POSITIVE, verified] Every large feature in the window ships a behavioural test against the real module.** Send-as: `workspace_broker/tests/test_send_as.py`, 33 tests driving `Broker` with gateway and agent peers (admin sending, forged Sent Items answer, tampered or foreign token, expired link, caller Bcc, digest over rendered HTML, double send). Staff mailbox reads: every refusal asserts zero Graph requests. Daily post filing: `test_letter_pages.py` 28, `test_letter_tools.py` 18. Medchron: 305 runner tests in CI's own venv. Obligation register: 112 tests across four files, including an ungrounded quote refused and `row.what` never in a public finding. Decommission: 59 tests across two files covering every 09-10 edge. Per-tenant machine credentials: a shared HMAC vector pinned on both sides (`tests/machine-credentials.test.ts` spawns the Python minter). Firm-config currency: the bash guard executed against real git fixtures.

7. **[POSITIVE, verified] Both suites are green, large and nearly skip-free, with honest shapes.** vitest 364 files, 5,385 passed, 0 failed, 112 s; Python 2,873 passed; zero snapshot tests; the only two `expect(true).toBe(true)` inside the documented `sow-render` skip; `doctrine-integrity` reports un-runnable cross-repo checks as skips with the reason in the test name rather than as passes.

**Grade: B (up from C+).** Rubric: "B: tests exist but minor gaps." Both named critical-path gaps closed behaviourally, fourteen suites were upgraded from source-text, both environment-dependent tests were fixed for the right reason, and every feature in a 170-commit window arrived with a test that runs the real code. Not A: the SOW render is still skipped, ten tests never run in CI, nothing measures coverage, and the AC gate the repo declares as a merge gate is advisory. Not B-: the medium is a ruleset entry, not a coverage gap, and no critical path either agent or the orchestrator probed is untested.

### 5. Dependencies

**Carried findings.** **Closed:** seat-image Python installs unpinned and unaudited (the three-review HIGH; `Dockerfile:1215-1216,1426-1428,1448-1449` install `--no-config --require-hashes -r /app/requirements/*.txt`, 435 hashes in `broker.txt`; `tests/operator-dockerfile.test.ts:1685` fails any unhashed `uv pip install`; `security.yml` `pip-audit` job runs `pip-audit==2.10.1` through `operator/bin/pip_audit_gate.py` over `operator/requirements/*.txt`, exits 2 when nothing is scanned, and `Security Summary` needs it; `dependabot.yml` carries two `pip` ecosystems; `.github/pip-audit-allowlist.json` exists with the npm allowlist's contract and `allow: {}`). **Changed:** zero audit findings (finding 1). **Open, deliberate:** `typescript` 6.0.3 against 7.0.2 at the root (the three workers are on 7). **Still true:** all 55 `uses:` lines SHA-pinned, zero `npm install` in workflows, three workers with zero production dependencies, no eval-style code reachable in the Worker bundle (the one `new Function(` in `dist/server` is zod's capability probe, false under Cloudflare), `npm ls ajv handlebars --omit=dev` empty.

1. **[MEDIUM, NEW] One moderate advisory on the root lockfile, fixable today, and the repo's automation will not fix it.** Top action item 7; orchestrator ran `npm audit --json` (root moderate 1, high 0, critical 0; three workers 0/0/0/0), `npm ls devalue` (`astro@7.3.1 -> devalue@5.8.1`), read astro's range (`^5.8.1`), `npm view devalue time` (5.9.1 on 2026-08-20, 5.9.2 on 08-27, 5.9.4 on 09-18), the advisory (`first_patched_version` 5.9.2, severity medium), and `automated-security-fixes` (`enabled: false`). The gate is green because `scripts/audit-allowlist.mjs` fails only on high and critical by design. Correction to the orchestrator's own first read: `.github/audit-allowlist.json` exists and is tracked with `allow: {}` and `expires: null` (vfy_01M3CN7ZAST0T8G93WWBAPSWB1); the script also treats an absent file as "no exceptions".

2. **[LOW, carried] The knip fix for the two false "unused dependency" warnings is written down and does not work.** `knip.jsonc:46-53` says declaring `astro.config.mjs!` as an entry is the fix; `npm run deadcode` at HEAD still prints `@astrojs/cloudflare` and `@astrojs/sitemap` (orchestrator ran it); the `project` array at `:88-95` omits the file. Add it there, or `ignoreDependencies` with the reason, and a test that runs knip and asserts zero dependency findings.

3. **[LOW, NEW] The seat image still resolves some third-party code at build time outside the lockfiles.** `Dockerfile:1181-1182` installs the overlay with dependency resolution (its `pyproject.toml` at the pin declares four ranges; the test at `operator-dockerfile.test.ts:1698` exempts this line by design); `:1233` pins `@oktopeak/clio-mcp@2.0.0` with floating transitives; `:1126` `npm install` in the Hermes tree. None is audited by `pip_audit_gate.py`. Far smaller than the 09-10 HIGH. Install the overlay `--no-deps` from a hashed `hermes-overlay.txt`, or record the residual in the Dockerfile comment.

4. **[LOW, NEW] Dependabot housekeeping.** Two `pip` entries cover the same four files and open byte-identical PRs (#2897 and #2898; orchestrator listed both). Six green Dependabot PRs sit unmerged (#2866, #2867, #2868, #2870, #2897, #2898); only two have auto-merge. `dependabot-auto-merge.yml`'s `version-update:semver-security` branch cannot match (fetch-metadata emits only major/minor/patch; security PRs carry `ghsa-id`), and its header at `:23-25` presumes the repo is private.

5. **[LOW, NEW] `prettier-plugin-astro` is newly one major behind** (0.14.1 against 1.0.1, published 09-17) with no Dependabot PR; `@formepdf/{core,react}` sit at 0.18.0 against 0.23.0 under a caret that holds 0.x minors. `eslint-plugin-astro` and `vitest` "outdated" rows are registry `latest`-tag artifacts.

6. **[POSITIVE, verified] `OVERLAY_REF` pin discipline.** 26 pin moves in the window; `contracts/overlay-pairs.json` carries exactly one `overlayRef` equal to `Dockerfile:983`; `verify-overlay-pairs.py` runs in substrate, fetches the overlay at that ref and hash-checks every twin, exit 1 on mismatch and 2 on fetch failure.

**Grade: C+ (down from B).** Rubric: "C: medium-severity audit findings." One moderate is live on the lockfile, so C is the literal band. The plus: it is unreachable on inspection, one lockfile refresh from closed, and the 09-10 independent gap (unpinned, unaudited seat Python) closed properly with hashes, a Dockerfile test, a fail-closed audit job and Dependabot coverage. Not B: the advisory is real and the repo's own automation is configured so that it never gets fixed without a human noticing. Not C: only one real major is behind, and it is deliberate.

### 6. Documentation

**Carried findings.** **Closed:** three numbers for one fact (`CLAUDE.md:309`, `decision-stack.md:26`, `adr/index.md:9` all say 38 active through #56, derived by `tests/docs-facts.test.ts`); the ADR range claim (CLAUDE.md now delegates to `docs/adr/index.md`); README TypeScript version; no API documentation (`docs/api/README.md` generated by `scripts/api-inventory.ts`, 95 rows for 95 route files, `tests/api-inventory.test.ts` fails on drift; an inventory, not a contract: path, methods, gate, purpose, file, with 4 rows reading "(no header comment)" and at least one purpose that is a stray comment); `workers/booking-cleanup/` false claims (zero hits); `data-model.md` nine migrations behind (cites 0114 through 0118; last changed 2026-09-17 in the same commit that added 0117 and 0118, orchestrator confirmed). `docs/reviews/index.md` lists every prior review.

1. **[MEDIUM, NEW] The handbook's ADR index claims to list every ADR and stops at 0065.** Top action item 8; orchestrator: highest number on the page 0065, 24 ADR files above it, last page change 2026-07-27, no test names the page. It renders in the admin console as the Reference map into the why-canon.

2. **[MEDIUM, NEW] The handbook maintenance contract is honoured for schema and admin work, not for connectors or new ADRs.** The deps agent classified the window's feature commits against the "if you change X, update Y" table: of six applicable, one fully complied (#2830: data-model, admin-console, the new obligation-register page), two partly, three missed. `docs/handbook/connectors-channels.md` was last changed 2026-07-03 and mentions neither the staff-mailbox reader nor the letter pipeline (orchestrator: zero hits for "staff mailbox" or "letter"). `npm run handbook:drift` flags 27 of 35 pages, advisory only.

3. **[LOW, NEW] `operator/CLAUDE.md`'s "load these ADRs" list stops at 0021** plus 0037 and 0053, omitting 0050, 0052, 0056, 0057, 0086, 0089. An agent following it loads a mid-2026 architecture.

4. **[LOW, NEW] `README.md:18` says Vitest 4; `package.json` declares `^5.0.0`.** `docs-facts.test.ts` pins the TypeScript major and not the Vitest major.

5. **[POSITIVE, verified] The window's largest new modules document themselves.** `register.mjs` opens with ADR 0088 and migration 0117 and why the grounding gate exists; `decommission.py` cites its issues and ADRs and states the data-plane doctrine; `send_as_acts.py` cites ADR 0089 and the four rules the broker enforces. `deployment-release.md:81-93` records why zero required reviews is a decision.

**`crane_docs_drift_audit`:** the deps agent ran it; it audits crane-console's docs tree, not this repo's. Same scope note as 09-10.

**Grade: B- (up from C+).** Rubric: "B: missing 1-2 sections." CLAUDE.md, README, schema and API inventory are present and test-pinned. Not B: the handbook's own ADR index is 24 records behind while claiming completeness, and the contract missed on three of six sampled commits. Not C+: the "no API documentation" clause that held the grade is no longer true.

### 7. Golden Path Compliance (Tier 2)

| Requirement                                                          | Status                        | Instrument                                                                                                                                                                   |
| -------------------------------------------------------------------- | ----------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Source control, CLAUDE.md, TypeScript + ESLint, no hardcoded secrets | Met                           | `verify.yml:49-63`; `tsconfig` strict; gitleaks full history green on HEAD and clean over 3,667 commits                                                                       |
| Error monitoring (Sentry)                                            | Improved, partial             | 37 of 181 catch-bearing non-test `src` files reach Sentry directly or through `api/failures` (orchestrator); every 5xx a route answers is captured, test-enforced; Stripe webhook catches dark |
| Full CI/CD                                                           | Met, one declared gate advisory | 21 workflows; deploy on merge; required checks `Typecheck, Lint, Format, Test` (legacy) plus `Security Summary` and `substrate` (ruleset); `Runtime ACs carry a verification ID` not required |
| Branch protection                                                    | Partial, documented decision  | ruleset active, `bypass_actors: []`, force-push and deletion blocked; `required_approving_review_count: 0`, `enforce_admins: false`; rationale at `deployment-release.md:81-93` |
| Uptime monitoring                                                    | Met                           | `workers/fleet-alerts/src/edge-poll.ts` polls `https://smd.services/api/health` on `crons = ["*/2 * * * *"]`; the poller has its own dead-man ping (secret name present, value not verified) |
| API docs                                                             | Met (inventory)               | `docs/api/README.md`, 95 of 95, test-pinned                                                                                                                                  |
| PWA                                                                  | Not applicable                | the dashboard's stated next action for SS                                                                                                                                    |
| Secrets scanning                                                     | Met                           | `security.yml:200` `gitleaks git` under `fetch-depth: 0`; binary fetched as `releases/latest` without a checksum (low)                                                        |

1. **[MEDIUM, counted once in Testing] The declared AC merge gate is not required.**
2. **[LOW, carried, documented] Zero required reviews and admin bypass permitted.** Unchanged; now a written decision.
3. **[LOW, NEW] The outside-in poller shares a failure domain with what it watches** (same Cloudflare account); the dead-man ping to healthchecks.io is what pages through a platform incident, and its value was not verified here.
4. **[LOW, NEW] The gitleaks binary is fetched unpinned and unverified** (`security.yml:167-188`: tag from `releases/latest`, tarball extracted without the checksums file), inside a required gate, on a repo that SHA-pins all 55 actions.

**Grade: B (up from B-).** Rubric: "B: 1-2 non-critical items missing." Uptime, secrets scanning and API docs moved from partial or missing to met. Not B+: one gate the repo itself declares as a merge gate is advisory, required reviews are zero, and Sentry reaches a fifth of catch-bearing files. Not B-: every item the 09-10 table scored partial or missing moved except the reviews line.

## Model Convergence

Four agents, disjoint dimensions, so convergence is between each agent and the orchestrator. Every graded claim was re-probed. Orchestrator numbers replaced agent numbers in four places, each recorded: ceiling violations in the ignored trees 20 (agent 23, which included `max-depth` and `max-params`); Sentry reach 37 of 181 (the src agent's 14 of 180 counted direct `captureError` importers only; the deps agent's 39 of 170 used a different catch-file census); non-test `sys.path` mutations 10 sites in 7 files (both agents agree on 10); `stripeHeaders` still x3, which the src agent found and wave 5a's title claims closed. One orchestrator claim was wrong and is corrected in the ledger: the npm audit allowlist file exists (the first `cat` ran from a shell whose working directory had drifted into `workers/fleet-alerts`). One agent finding was environment-specific and is recorded so it is not mistaken for a leak: the review worktree's `dist/` held an inlined Clerk key from the deps agent's local build; it was deleted before this report was written and never left the machine.

## Trend Analysis

| Dimension     | 08-23 | 09-09 | 09-10 | 09-25 |
| ------------- | ----- | ----- | ----- | ----- |
| Architecture  | D     | C     | C+    | B-    |
| Security      | D     | C     | C     | C+    |
| Code Quality  | C     | C     | C+    | B-    |
| Testing       | C     | C     | C+    | B     |
| Dependencies  | C     | D     | B     | C+    |
| Documentation | B     | C     | C+    | B-    |
| Golden Path   | C     | C     | B-    | B     |
| **Overall**   | D+    | C-    | C+    | B-    |

Six of seven dimensions moved up; Dependencies moved down one step on one moderate advisory that a single lockfile refresh closes. The 09-04 claims probe re-run at HEAD reports 30 closed, 4 still open, 2 changed and 2 "regressed": F7 (a stale threshold, see Architecture carried) and B3 (`broker dispatch rows never carry skill_name`, at-least 1 expected, got 0; the probe's instrument should be re-read before the next run, since the ledger schema it keys on moved in the window). Of the 48 `source:code-review` issues ever filed, all 48 are closed; no issue from the 09-10 review was filed because the Captain chose follow-through PRs, and those 26 PRs are most of what this review graded up.

What did not move is short for the first time: `stripeHeaders` x3, the SOW render skip, coverage measurement, zero required reviews, the `integration_id` pin. What is new is mostly where the gates do not look: two ignored lint trees, a Python `Any` gap the type ratchet cannot see, a stub step a refusal does not list, a probe that checks one of N, an advisory merge gate, a moderate advisory below the audit threshold. The pattern the last review named still holds: the mechanism lands, and its report is not yet a probe of what it did.

## File Manifest

Tracked at ceaca4c0: 824 `.ts`, 543 `.py`, 222 `.astro`, 175 `.sql`, 66 `.json`, 51 `.yaml` + 22 `.yml`, 39 `.sh`, 22 `.mjs`, 3 `.tsx`, 907 `.md`. Code lines by language (raw): ts 168,149; py 167,986; astro 35,532; sh 8,943; mjs 3,968; yaml 11,635. Top-level: `operator/` 1,229 files, `src/` 648, `docs/` 356, `tests/` 353, `migrations/` 167. Route files: 95 API (48 admin, 19 portal), 36 admin pages, 31 portal pages. Test files: 24 under `tests/` root plus 12 in `src/`, 208 Python, 7 in workers. Three tracked workers. 21 CI workflows. Six `pyproject.toml` (root `operator/` plus five leaves). Full manifest used by the agents lived in the session scratchpad and is not committed.

## Raw Model Outputs

The four agent reports are preserved verbatim in [code-review-2026-09-25-agent-reports.md](./code-review-2026-09-25-agent-reports.md): `src-ts` (Architecture + Code Quality, TypeScript half), `operator-py` (Architecture + Code Quality, Python half), `sec-test` (Security + Testing), `deps-docs-gp` (Dependencies + Documentation + Golden Path). Codex review: skipped (Phase 1). Gemini review: skipped (Phase 1).
