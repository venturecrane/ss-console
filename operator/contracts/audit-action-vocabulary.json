{
  "$comment": "ss#2320. The overlay's ACCEPTED_ACTION_TYPES, snapshotted at the overlay commit a customer Machine actually ships. WHY THIS FILE EXISTS: the runtime validates rows against the OVERLAY's constant, the portal renders from THIS repo's constant, and nothing pinned the two together. shared/audit_contract.py performs no validation at all (agent_event_params + a raw INSERT accepts any string), so the path 'overlay adds an emitter -> declares it overlay-side -> overlay CI passes -> rows land in client ledgers -> portal renders nothing' had no gate anywhere on it. A type the overlay knows and this repo does not is invisible on the client feed and indistinguishable from a deliberate suppression. HOW TO MAINTAIN: when OVERLAY_REF moves in operator/templates/Dockerfile, re-extract overlayTypes from plugins/hermes-smd-audit/schemas.py at the new ref and update overlayRef here in the same PR. tests/audit-vocabulary-parity.test.ts fails when the two refs disagree, so a bump cannot silently skip the refresh. 2026-08-19 re-stamp 0088352a -> eeeac283 (overlay#281, ss#2444 Hermes v0.20.4 promotion PR-1): the range touches bootstrap/translate.py + tests only; plugins/hermes-smd-audit/schemas.py ACCEPTED_ACTION_TYPES untouched, vocabulary re-read as identical. 2026-08-19 re-stamp eeeac283 -> ab352d5b (overlay#282+#283): neither commit touches plugins/hermes-smd-audit/schemas.py ACCEPTED_ACTION_TYPES; vocabulary re-read as identical. 2026-08-20 re-stamp ab352d5b -> 88f6daa (overlay#284+#285+#286, ss#2444 PR-2 + ss#2464): the range touches bootstrap/mcp_registry.py, contracts/consumes.yaml, tests/test_connector_platform.py, tests/test_matter_mixing_wire_names.py only; plugins/hermes-smd-audit/schemas.py ACCEPTED_ACTION_TYPES untouched, vocabulary re-read as identical. 2026-08-20 re-stamp 88f6daa -> 78c2544 (overlay#287, ss#2444): the range touches shared/inbound.py, plugins/hermes-smd-establishment/__init__.py, tests/test_inbound.py, tests/test_establishment_plugin.py only; plugins/hermes-smd-audit/schemas.py ACCEPTED_ACTION_TYPES untouched, vocabulary re-read as identical. 2026-08-20d: 78c2544 -> 16770d3 (overlay#288, ss#2489): the msgraph reply carries a rendered HTML body so the firm can read it. Graph's /reply composes in HTML and we passed a plain-text comment, so every newline collapsed; four replies reached the paying client as one unbroken block. Files: plugins/hermes-smd-reply, shared/msgraph_broker.py + tests - NOT tracked pairs; every overlaySha256 unchanged, only overlayRef. 2026-08-20e: 16770d3 -> 7a8ba42 (overlay#290, ss#2489 follow-on): report_render closes the list when a paragraph follows a list item, so a report stops swallowing itself. Pre-existing since 2026-07-16 and reaching AgentMail reports too. Files: shared/report_render.py, tests/test_report_render.py - NOT tracked pairs; every overlaySha256 unchanged, only overlayRef. 2026-08-21: d35e0b0 -> 4dbf415 (overlay#294 ss#2497, #292 ss#2498, #295 ss#2501, #297 ss#2499, #296 ss#2511): vocabulary re-extracted by AST from plugins/hermes-smd-audit/schemas.py at the new ref, 62 types; added ['ROUTINE_DISABLED', 'ROUTINE_ENABLED'], removed none (ROUTINE_ENABLED/ROUTINE_DISABLED come from overlay#292; the console side landed in ss#2507). #296 is the ss#2511 provenance fix: the seat's own text no longer seeds the identifier register, add_file/render_docx_draft are scanned report-only, and the enforce reason for an executed internal write no longer reads 'routed to draft folder'. The range touches plugins/hermes-smd-audit/emit.py, a tracked pair; re-recorded in overlay-pairs.json. 2026-08-21b: 4dbf415 -> 4ca6682 (overlay#298, ss#2511 flip): mcp_smokeball_render_docx_draft leaves the identifier gate's report-only carve and blocks like every other draft tool; add_file stays report-only. Captain decision after four pilot drafting lanes (0 false positives, 1 genuine catch: computed deadlines reached a filed Word draft). Files: plugins/hermes-smd-trust/outbound.py + tests/test_identifier_gate_carves.py, NOT tracked pairs; plugins/hermes-smd-audit/schemas.py untouched, vocabulary re-read as identical (62 types). 2026-08-21c: 4ca6682 -> 349d86b (overlay#299 + #300, ss welcome rehearsal): the range touches shared/citation_filter.py, shared/outbound_gate.py, shared/provenance.py and tests/test_citation_case_name_shapes.py only; plugins/hermes-smd-audit/schemas.py ACCEPTED_ACTION_TYPES untouched, re-extracted by AST at both refs and compared as sets: 62 types at each, none added, none removed, so overlayTypes is re-read as identical and only overlayRef moves. 2026-08-21d: 349d86b -> 991044a (overlay#301 + #302, ss-console#2529): the firm can teach the Operator by talking to it. #301 gives a spoken rule somewhere to live (the spec applier renders standing adjustments beside a distilled body; the intake installs one confirmed sentence with no corpus and no compilers). #302 is the seat behaviour: establish_propose reads the rule back with a tag, the reply must carry that block verbatim, the affirmative is read from the sender's own words with quoted history stripped, and a submit commits only the id the seat saw confirmed. VOCABULARY MOVED, and this is the delta this file exists for: re-extracted by AST from plugins/hermes-smd-audit/schemas.py at the new ref, 62 -> 65 types; added ['ESTABLISHMENT_RESULT', 'ESTABLISHMENT_SUBMITTED', 'RULE_PROPOSED'], removed none. Those three move OUT of consoleOnly and INTO overlayTypes here, exactly as the ss#2529 PR 1 note promised. Two of them are a CORRECTION rather than a new feature: ESTABLISHMENT_SUBMITTED and ESTABLISHMENT_RESULT have been reaching client ledgers since establishment shipped (ss#2161) while NEITHER vocabulary declared them, so the portal rendered them as nothing and the silence was indistinguishable from a deliberate suppression (ss#2316's third state). The console side (this file's AUDIT_ACTION_TYPES mirror, the producer manifest, and the client-language decision) landed in ss#2529 PR 1. NO TRACKED PAIR MOVED: the range touches establish_intake/intake.py, spec_applier/applier.py, shared/rule_confirm.py (new), shared/action_classes.py, plugins/hermes-smd-audit/schemas.py, plugins/hermes-smd-corrections/__init__.py, plugins/hermes-smd-establishment/__init__.py and tests; none of the nine overlayPaths in overlay-pairs.json is among them, so every overlaySha256 is unchanged and only overlayRef moves there. Verified by re-hashing all nine at the new ref, not assumed. 2026-08-21c: 991044a -> 07ed486 (overlay#303 + #305 operator-own-matter commitment confirm, #304 ss#2529 hotfix: a person's yes is read from the email body below the untrusted delimiter, not from the Operator's own prompt preamble; person possession note no longer claims firm-level authority): plugins/hermes-smd-audit/schemas.py untouched, vocabulary re-read as identical (65 types); no tracked pair moves. 2026-08-21 re-stamp 07ed486 -> b782926c (overlay#306, ss#2537 follow-up): the range touches bootstrap/validate.py and tests/test_customer_config.py only; plugins/hermes-smd-audit/schemas.py ACCEPTED_ACTION_TYPES untouched, vocabulary re-extracted at the new ref and read as identical (65 types). 2026-08-21d: b782926c -> e9d9fae1 (overlay#307, ss#2529 second hotfix from the pilot proof: a confirmed commit sends only the proposal id, and a reply may not claim a rule is in effect until the install is observed): schemas.py untouched, vocabulary re-read as identical (65 types); no tracked pair moves. 2026-08-22 re-stamp e9d9fae1 -> 2b0c786d (overlay#308, ss#2537 follow-up: the SOUL line for a commitment at the confirm ceiling tells the model to call the tool and relay the gate's act line, not to ask in prose): the range touches bootstrap/translate.py and tests/test_bootstrap_translate.py only; plugins/hermes-smd-audit/schemas.py ACCEPTED_ACTION_TYPES untouched, vocabulary re-extracted at the new ref and read as identical (65 types, no additions and no removals); no tracked pair moves. 2026-08-22b re-stamp 2b0c786d -> 241df3bc (overlay#309, ss#2547: a refusal that keeps a routine from reaching a human now reaches us; send_refusals heartbeat fields, pre-run handoff seeding, .smd fence, staff-class dash normalization): the range touches plugins/hermes-smd-trust/{__init__,enforce,outbound}.py, shared/{cron_attribution,heartbeat,pre_run_handoff}.py and tests only; plugins/hermes-smd-audit/schemas.py ACCEPTED_ACTION_TYPES untouched (git diff of the range on that file is empty), vocabulary read as identical (65 types); no tracked pair moves. 2026-08-22b: 241df3bc -> 119f6bf (overlay#311, ss#2546 PR 2: a paralegal's rule reaches the right administrator, and she hears what happened). ONE overlay merge in the range, checked rather than assumed: `git log 241df3bc..119f6bf` returns a single commit. VOCABULARY MOVED, and this is the delta this file exists for: re-extracted by AST from plugins/hermes-smd-audit/schemas.py at both refs and compared as sets, 65 -> 68 types; added ['RULE_DECLINED', 'RULE_LAPSED', 'RULE_REQUEST_NOTIFIED'], removed none. All three move OUT of consoleOnly and INTO overlayTypes here, exactly as the ss#2546 PR 1 note promised. Two of them (RULE_DECLINED, RULE_LAPSED) are still WRITTEN by the workspace broker; they are declared overlay-side for the same reason CORRECTION_PROPOSED is, so the vocabulary names every type a ledger can contain rather than only the ones that repo writes. RULE_REQUEST_NOTIFIED is genuinely overlay-produced from this ref: the establishment plugin emits it when a non-admin's rule has been emailed to the administrators the firm named on scope.rule_requests_to, and its producer-manifest entry moves from side: deferred to side: overlay in the same PR, which returns the deferred count to zero. NO TRACKED PAIR MOVED: the range touches plugins/hermes-smd-audit/schemas.py, plugins/hermes-smd-establishment/{__init__.py,plugin.yaml,sweeper.py}, plugins/hermes-smd-trust/{__init__.py,enforce.py}, shared/{action_classes,customer_config,operations_request,rule_confirm,rule_dispatch,send_dispatch,spec_gate}.py and three test files; none of the nine overlayPaths in overlay-pairs.json is among them, so every overlaySha256 is unchanged and only overlayRef moves there. Verified by re-hashing all nine at the new ref, not assumed. 2026-08-22c re-stamp 119f6bf2 -> 29536fcb (overlay#310, ss#2547 follow-up: the unsent-wake fact joins by skill_name + time, not session id): the range touches shared/heartbeat.py, tests/test_heartbeat.py, tests/tools/send_refusals_retro.py only; plugins/hermes-smd-audit/schemas.py ACCEPTED_ACTION_TYPES untouched, vocabulary read as identical (68 types); no tracked pair moves. 2026-08-22d: 29536fcb -> 87fa8501 (overlay#312 ss#2552 colleague preference wording; #313 ss#2546 fixes: a firm rule may attach only to the six registry classes, and the requester hears the install when it is observed): schemas.py untouched, vocabulary identical (68); no tracked pair moves. 2026-08-23: 87fa8501 -> cce3126e (overlay#314, ss#2546 reopened: a request only SMD can grant comes back answered). ONE overlay merge in the range (`git log 87fa8501..cce3126e` is a single commit). VOCABULARY MOVED: re-extracted by AST from plugins/hermes-smd-audit/schemas.py at both refs and compared as sets, 68 -> 71 types; added ['OPS_REQUEST_LAPSED', 'OPS_REQUEST_RECORDED', 'OPS_REQUEST_RESOLVED'], removed none. All three move OUT of consoleOnly and INTO overlayTypes here, as the ss#2546 PR 1 (#2561) note promised. All three are WRITTEN by the workspace broker (ops_propose / ops_resolve / lapse_notified on an ops row) and declared overlay-side so the vocabulary names every type a ledger can contain. NO TRACKED PAIR MOVED: the range touches plugins/hermes-smd-audit/schemas.py, plugins/hermes-smd-establishment/{__init__.py,sweeper.py}, shared/{customer_config,operations_request,rule_confirm,rule_dispatch}.py and tests; none of the nine overlayPaths is among them; re-hashed at the new ref by restamp, pairs moved: []. 2026-08-23b: cce3126e -> fc8f88c1 (overlay#315, ss#2546 fixes from the pilot proof: the requester's outcome letter is claimed once across the sweeper and the in-turn path, and a reply to someone who cannot answer an ops request may not claim it is closed). The range touches plugins/hermes-smd-establishment/__init__.py, sweeper.py and tests only; plugins/hermes-smd-audit/schemas.py ACCEPTED_ACTION_TYPES untouched, vocabulary re-extracted by AST at the new ref and read as identical (71 types); no tracked pair moves (9 re-hashed). 2026-08-23c: fc8f88c1 -> 2c4e8e92 (overlay#316, ss#2546: the requester's outcome letter is claimed in the broker before it is sent, because the seat runs the plugin in two processes and an in-process claim could not arbitrate). Range touches plugins/hermes-smd-establishment/__init__.py and tests only; schemas.py untouched, vocabulary identical (71); no tracked pair moves. 2026-08-23d: 2c4e8e92 -> 0f84a625 (overlay#317, ss#2546: inside a pass-on session, a future description of the requested routine is withheld even when the sentence names it only by pronoun, and contractions/typographic apostrophes no longer slip the patterns). Range touches plugins/hermes-smd-establishment/__init__.py and tests only; schemas.py untouched, vocabulary identical (71); no tracked pair moves. 2026-08-24: 0f84a625 -> f16d4920 (overlay#318, ss#2390 + the ss#2547 live defects: pre-run handoff binds in production, matter-number record seeding, empty-register refusal drops the removal hatch, heartbeat 'degraded' send-refusal kind). The range touches plugins/hermes-smd-trust/{__init__,outbound}.py, shared/{heartbeat,pre_run_handoff,provenance}.py and tests only; plugins/hermes-smd-audit/schemas.py ACCEPTED_ACTION_TYPES untouched (git diff of the range on that file is empty), vocabulary read as identical (71 types); no tracked pair moves. 2026-08-25 re-stamp f16d4920 -> 99c62699 (overlay#319, the runaway-loop arms): the range touches plugins/hermes-smd-audit/__init__.py + shared/cost_breaker.py + a new test. schemas.py is NOT among them. Vocabulary re-extracted from schemas.py ACCEPTED_ACTION_TYPES at the new ref and diffed against this list rather than transcribed: 71 types, none added, none removed. 2026-08-25 re-stamp 99c62699 -> 671507b7 (overlay#320-#325): carries the message-structure floor (#323, ss#2090), the pilot-law retirement (#324), the abandoned loop-arm self-check REMOVED (#325), and two guard fixes kept from #320-#322 (asyncio.wait_for + try/except around the COST breaker boot probe; a top-level guard on handle()). The runaway-loop BRAKE (_meter_loop_arms) is unchanged and already in the old pin. schemas.py is UNTOUCHED across this range (git diff --stat is empty), so ACCEPTED_ACTION_TYPES is byte-identical and overlayTypes is re-read as identical. The structure floor deliberately adds NO action type: it reuses SPEC_GATE_TRIGGERED with reason=structure_floor. 2026-08-28 re-stamp 671507b7 -> c2668ce2 (overlay#327 read-volume gate): the range touches shared/read_volume.py (new), plugins/hermes-smd-trust/enforce.py + __init__.py, plugins/hermes-smd-webhook-router/__init__.py, contracts/consumes.yaml, tests only; plugins/hermes-smd-audit/schemas.py ACCEPTED_ACTION_TYPES untouched, vocabulary re-read as identical. 2026-08-28b re-stamp c2668ce2 -> 3f86737d (overlay#328): shared/read_volume.py + tests only; ACCEPTED_ACTION_TYPES untouched, vocabulary re-read as identical. 2026-08-28c re-stamp 3f86737d -> 6e4223d1 (overlay#329): shared/read_volume.py + tests only; ACCEPTED_ACTION_TYPES untouched, vocabulary re-read as identical. 2026-08-28d re-stamp 6e4223d1 -> bc9285bf (overlay#330): read_volume trace journal only; vocabulary re-read as identical. 2026-08-28e re-stamp bc9285bf -> 29eac2c7 (overlay#331): read_volume unwrap fix only; vocabulary re-read as identical. 2026-08-29f: 29eac2c7 -> 3c1b8e98 (overlay#333, ss#2614 routine 11): hermes-smd-medchron plugin (submit/status/allowance), shared/medchron_client.py, medchron_jobs runtime-read kind, three action_classes rows. NOT tracked twins; every overlaySha256 unchanged, vocabulary identical (71 types); MEDCHRON_JOB_* are consoleOnly (broker-written). 2026-08-31: 3c1b8e98 -> c5846c68 (overlay#334 escalation ledger-integrity pair + overlay#335 identifier register-anchored bare digits; ss#2652/ss#2654). git diff of the range on plugins/hermes-smd-audit/schemas.py is EMPTY; vocabulary re-extracted by AST from schemas.py at the new ref and diffed as sets against this list rather than transcribed: 71 types, none added, none removed. TWO tracked pairs move in overlay-pairs.json (escalation_ledger byte-identical again at 912bac29...; identifier_filter re-recorded at 71e598a5...); the ledger plugin reuses the existing escalation_event_append verb and adds NO action type. 2026-08-31c: c5846c68 -> 45cc19e7 (overlay#336, ss#2616): the handoff route is materialized (translate.py) with its prompt, the gate retries any non-2xx forward, and medchron_job_submit gains selection.include_file_ids. Not tracked twins; every overlaySha256 unchanged, vocabulary re-read as identical. 2026-08-31 re-stamp 45cc19e7 -> 727f3f5b (overlay#337, ss#2664 WS-RENDER pair): the range touches the trust plugin, shared dispatch/render/broker-client modules and tests only; plugins/hermes-smd-audit/schemas.py ACCEPTED_ACTION_TYPES untouched, vocabulary re-extracted by AST at both refs and diffed as sets: 71 types, none added, none removed. 2026-09-01 re-stamp 727f3f5b -> 799647d1 (TWO merges: overlay#338 plain_body_sha256 stamp, console half ss#2674; overlay#339 supervisor states starting + never-healthy, console half ss#2677): the merge range -- read at the merge commit, since overlay#339 branched from 727f3f5b before #338 landed and a two-dot diff off its head hides #338 -- touches plugins/hermes-smd-trust/__init__.py, shared/gateway_loop_check.py, tests/contract/send-render.yaml, tests/test_dispatch_body_stamp.py and tests/test_gateway_loop_check.py only; plugins/hermes-smd-audit/schemas.py ACCEPTED_ACTION_TYPES untouched and absent from the range, vocabulary re-extracted by AST at both refs and diffed as sets: 71 types, none added, none removed, and the recorded overlayTypes list matches the extraction at the new ref exactly. Neither change adds an action type: #338's new hash rides on the existing CONFIRM_SEND_DISPATCHED row as an extra stamp field, and #339's two new words are values of the gateway_supervisor_state HEARTBEAT field, a different plane from the audit vocabulary entirely. 2026-09-04 re-stamp 9951fbf8 -> e0288582 (overlay#345+#346, console half ss#2697 B3): the range touches shared/prerendered_dispatch.py, tests/, and tests/contract/ only; plugins/hermes-smd-audit/schemas.py ACCEPTED_ACTION_TYPES untouched (empty range diff), vocabulary re-read as identical. #346 adds skill_name to audit_extra, a stamp field on the existing CONFIRM_SEND_* rows, not an action type. 2026-09-08 re-stamp e0288582 -> 5770b79f (overlay#348, ss voice-establishment): the range e0288582..5770b79f touches shared/matter_gate.py, plugins/hermes-smd-establishment/__init__.py, tests/test_matter_mixing.py (overlay#348) and tests/contract/send-render.yaml (overlay#347); plugins/hermes-smd-audit/schemas.py ACCEPTED_ACTION_TYPES is untouched (absent from the range), vocabulary re-read as identical (71 types, none added, none removed). The establishment read-exemption emits no audit action type -- it only marks a session exempt from the read-time matter-mixing fence.",
  "overlayRef": "5770b79fea3206f01df343489d232daf2fdc4a2b",
  "overlaySource": "plugins/hermes-smd-audit/schemas.py :: ACCEPTED_ACTION_TYPES",
  "overlayTypes": [
    "AGENT_RESUMED",
    "AGENT_SKILL_CREATED",
    "AGENT_STOPPED",
    "BROKER_DECISION_ALLOWED",
    "BROKER_EXECUTED",
    "COMPLIANCE_PACKET_EXPORTED",
    "CONFIG_WRITE",
    "CONFIRM_SEND_DISPATCHED",
    "CONFIRM_SEND_FAILED",
    "CONNECTOR_AUTH_EXPIRED",
    "CONNECTOR_AUTH_RESTORED",
    "CONNECTOR_BOUND",
    "CONNECTOR_HEALTH_PROBE_FAILED",
    "CONNECTOR_TOKEN_REFRESHED",
    "CONNECTOR_UNBOUND",
    "CORRECTION_PROPOSED",
    "CURATOR_DISMISSAL",
    "CURATOR_DRAFT",
    "CURATOR_PROMOTION",
    "DECOMMISSION_DRAIN_COMPLETE",
    "DECOMMISSION_FINAL",
    "DECOMMISSION_INITIATED",
    "DRAFT_APPROVED",
    "DRAFT_CREATED",
    "DRAFT_EXPIRED",
    "DRAFT_REJECTED",
    "ESCALATION_ACKNOWLEDGED",
    "ESCALATION_FIRED",
    "ESTABLISHMENT_RESULT",
    "ESTABLISHMENT_SUBMITTED",
    "FABRICATION_FILTER_TRIGGERED",
    "HONCHO_DISMISSAL",
    "HONCHO_OBSERVATION",
    "HONCHO_PROMOTION",
    "IDENTIFIER_UNVERIFIED",
    "INBOUND_RECEIVED",
    "INVARIANT_BOOT_CHECK_FAILED",
    "INVARIANT_VIOLATION",
    "LLM_TURN_COMPLETED",
    "MATTER_UNRESOLVED",
    "MEMORY_RULE_ADDED",
    "MEMORY_RULE_DELETED",
    "MEMORY_RULE_EDITED",
    "OPS_REQUEST_LAPSED",
    "OPS_REQUEST_RECORDED",
    "OPS_REQUEST_RESOLVED",
    "RBAC_EVENT",
    "REPLY_FAILED",
    "REPLY_HELD",
    "REPLY_SENT",
    "ROUTINE_DISABLED",
    "ROUTINE_ENABLED",
    "RULE_DECLINED",
    "RULE_LAPSED",
    "RULE_PROPOSED",
    "RULE_REQUEST_NOTIFIED",
    "SCOPE_CHANGED",
    "SKILL_DISABLED",
    "SKILL_ENABLED",
    "SPEC_GATE_TRIGGERED",
    "SUBAGENT_INCOMPLETE",
    "SUBAGENT_STOPPED",
    "TOOL_CALL_COMPLETED",
    "TRUST_DEMOTED",
    "TRUST_PROMOTED",
    "VOICE_GATE_FAILED",
    "VOICE_GATE_NEAR_PASS",
    "VOICE_GATE_PASSED",
    "VOICE_GATE_TRIGGERED",
    "WEBHOOK_ROUTED",
    "WEBHOOK_SUPPRESSED"
  ],
  "overlayOnlyNote": "Declared by the overlay, absent from this repo's AUDIT_ACTION_TYPES. Each needs a reason, because an overlay-only type that ever gains an emitter writes rows the portal renders as nothing. None of these has an emitter at the pinned ref (they appear only in the declaration), which is why they are tolerated rather than fixed. 2026-08-18 re-stamp 20518e86 -> 0716dc17: compare API shows zero action_type deltas in the range (only hermes-smd-reply internals, matter_binding, docs, tests); vocabulary re-derivation is the identity. 2026-08-18 re-stamp af0c8a0f -> 0088352a: plugins/hermes-smd-audit/schemas.py untouched in the range (single commit, file list is msgraph poller/replay/consumes/tests only); vocabulary re-derivation is the identity.",
  "overlayOnly": {
    "CURATOR_DRAFT": "Declared, no emitter at the pinned ref. Memory-curation subsystem, admin-plane if it ever ships.",
    "CURATOR_PROMOTION": "Declared, no emitter at the pinned ref.",
    "CURATOR_DISMISSAL": "Declared, no emitter at the pinned ref.",
    "HONCHO_OBSERVATION": "Declared, no emitter at the pinned ref. Honcho is not deployed (see the venture memory on Honcho).",
    "HONCHO_PROMOTION": "Declared, no emitter at the pinned ref.",
    "HONCHO_DISMISSAL": "Declared, no emitter at the pinned ref."
  },
  "consoleOnlyNote": "Declared here, absent from the overlay vocabulary. These are written by CONSOLE-side producers (the decommission pipeline, the wake path, the customer.yaml sync, the admin console), never by the seat runtime, so the overlay has no reason to know them. Harmless in the direction that matters: a console-written row is already rendered from this repo's own vocabulary. 2026-08-22 (ss#2546): RULE_REQUEST_NOTIFIED, RULE_DECLINED and RULE_LAPSED are declared here at PR 1 while the overlay half is still unmerged. Two of the three are genuinely console-side (the workspace broker writes them). RULE_REQUEST_NOTIFIED is not - it is written by the seat's establishment hook - and it sits here for exactly one release so the portal is never handed a row it cannot name. ALL THREE MOVE INTO overlayTypes AT THE OVERLAY_REF BUMP IN PR 3, re-extracted by AST at the new ref rather than transcribed. 2026-08-23 (ss#2546, the operations half): OPS_REQUEST_RECORDED, OPS_REQUEST_RESOLVED and OPS_REQUEST_LAPSED are declared here, and unlike RULE_REQUEST_NOTIFIED above they are genuinely console-side for good - all three are written by the workspace broker (operator/workspace_broker/establishment.py: ops_propose, ops_resolve, and establish_lapse_notified on a row of kind ops_request), which appends to the hash-chained ledger directly, so the overlay's hermes-smd-audit plugin neither emits nor validates them and its vocabulary does not gate the write. They are declared in the SAME PR as the broker that writes them, so there is no window in which a client ledger can hold a row this repo cannot name. IF the paired overlay PR chooses to declare them in plugins/hermes-smd-audit/schemas.py as well - which it may, for the reason CORRECTION_PROPOSED and RULE_DECLINED are declared there, so the overlay vocabulary names every type a ledger can contain - then they move out of consoleOnly and into overlayTypes at that OVERLAY_REF bump, re-extracted by AST at the new ref rather than transcribed. The parity gate that every console-only type is genuinely absent from the overlay is what makes forgetting that move loud.",
  "consoleOnly": {
    "AGENT_SKILL_REMOVED": "Console-side skill removal record.",
    "CUSTOMER_YAML_SYNCED": "Written by the git->D1 projection, not the seat.",
    "CUSTOMER_YAML_STRUCTURAL_CHANGE_DEFERRED": "Written by the projection when a structural change needs Captain review.",
    "DECOMMISSION_STEP_BEGIN": "Decommission pipeline runs from the console.",
    "DECOMMISSION_STEP_COMPLETE": "Decommission pipeline runs from the console.",
    "DECOMMISSION_STEP_FAILED": "Decommission pipeline runs from the console.",
    "EMITTED_WAKE": "Wake dispatch is console-side.",
    "SUPPRESSED_WAKE": "Wake dispatch is console-side.",
    "HONCHO_CONCLUSION_DISMISSED": "Captain action in the admin console (ADR 0016).",
    "ACT_PROPOSED": "ss#2536. Written by operator/workspace_broker/establishment.py under the broker uid, same reason RULE_PROPOSED carried before the 991044a8 bump: console-owned source running on the seat, not overlay runtime, so the overlay's hermes-smd-audit vocabulary does not gate the write. The overlay declares neither type at any ref yet; both move into overlayTypes at the bump that ships the ss#2536 confirm path.",
    "ACT_COMMITTED": "ss#2536. Broker-written, same reason as ACT_PROPOSED.",
    "MEDCHRON_JOB_SUBMITTED": "ss#2614 routine 11. Written by operator/workspace_broker/medchron_verbs.py under the broker uid when the Operator (or root on the box) submits a chronology-package job: job id, matter number, unit count, the allowance remainder. Console-owned source running on the seat, so the overlay vocabulary does not gate the write.",
    "MEDCHRON_JOB_RUNNING": "ss#2614. Broker-written on the runner daemon's medchron_job_record(running); also the release after a seat-pause hold.",
    "MEDCHRON_JOB_HELD": "ss#2614. Broker-written when the runner holds (a seat at HARD_STOP, an unexplained file, an unmatched folder) or refuses (the cents cap, the allowance); the reason travels in metadata as prose, never a document.",
    "MEDCHRON_JOB_DELIVERED": "ss#2614. Broker-written on delivery: documents read, pages, cents, the folder id, and the file names with sha256 and byte counts read back from the matter.",
    "MEDCHRON_JOB_FAILED": "ss#2614. Broker-written when the runner exits without a verdict or a stage fails; the reason is the runner's sentence."
  }
}
