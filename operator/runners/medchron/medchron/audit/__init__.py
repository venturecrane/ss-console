"""The citation audit, split on its real seams: `claims` (what is audited),
`render` (page images), `page_text` (the native text layer behind a page,
when there is one), `anchors` (the checkable tokens in a claim), `verify`
(one verdict, image or text, through the doorway), `run` (one audit round),
`coverage` (the final gate), `repair` (edits by removal or weakening), and
`diag` (the double-sweep guard's explanation and the strip rekey)."""
