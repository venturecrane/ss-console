/**
 * Shared constants used across the Pages app and Workers.
 *
 * Single source of truth for values that must be identical in both contexts.
 */

/** SMD Services organization ID (seeded in migration 0003). */
export const ORG_ID = '01JQFK0000SMDSERVICES000'

/** System entity ID for non-entity-specific context rows (seeded in migration 0013). */
export const SYSTEM_ENTITY_ID = '00000000-0000-0000-0000-000000000000'
