---
/**
 * /scorecard retired (ADR 0002 Phase 1 PR-C).
 *
 * Replaced by /outside-view as the unified diagnostic front door. The
 * middleware also serves a 301 from /scorecard → /outside-view; this
 * page is the belt-and-suspenders fallback in case the middleware
 * redirect regresses (per /critique 3 Devil's Advocate #3).
 *
 * Pre-existing scorecard JS (src/scripts/scorecard.ts), the submit
 * endpoint (/api/scorecard/submit), and the scoring utilities under
 * src/lib/scorecard/ all stay on disk for now — they're load-bearing
 * regression risk to remove in the same PR. The cleanup PR scheduled
 * one week after PR-C ships will remove them once we've confirmed zero
 * 404s on /scorecard or unexpected /api/scorecard/submit traffic.
 */
return Astro.redirect('/outside-view', 301)
---
