---
/**
 * /scan retired (ADR 0002 Phase 1 PR-C).
 *
 * Replaced by /outside-view as the unified diagnostic front door. The
 * middleware also serves a 301 from /scan → /outside-view; this page
 * is the belt-and-suspenders fallback in case the middleware redirect
 * regresses (per /critique 3 Devil's Advocate #3).
 *
 * The verify path (/scan/verify/[token]) is intentionally NOT redirected
 * — magic-link tokens issued before this PR shipped are still valid
 * and route to that landing page directly.
 *
 * This file is scheduled for deletion in a separate cleanup PR one week
 * after PR-C ships, after we've verified zero 404s on /scan in production
 * logs.
 */
return Astro.redirect('/outside-view', 301)
---
